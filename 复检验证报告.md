# 复检验证报告：修复后全仓再审计

- **背景**：用户依据 `审计报告.md` + `审计报告-复检.md` 修复了 8 条 medium + 36 条 low（约 54 个文件），本报告为**独立复检**——不采信修复声明，逐条回到文件原文核对证据，并重跑全部基线。
- **验证方式**：主线程逐文件核对（grep/read 到行级）+ 重跑仓库自带守卫（static-check/skill-numbering/doc-budget/shared-files/contracts/regex-sync/python-invocation/node 单测/test-scan-runtime）+ 全部改动脚本语法检查（node --check ×14、py_compile ×2）。
- **日期**：2026-08 · 快照（非 git 仓库，审计后临时 `git init` 已删除，工作区恢复原状）

## 一、基线重跑结果（全部通过）

| 检查 | 结果 |
|---|---|
| static-check.sh（13 skill） | **13/13 PASS，0 警告** |
| skill-numbering.py check | PASS（272 md） |
| check-doc-budget.sh | PASS：长篇日更 36169/36200、开书 25423/25500、正文 agent 38914/38960；改动后的 short-write SKILL.md 16734/16800、anti-ai-writing 13824/13900 均在预算内 |
| check-shared-files.sh | **OK：61 组引用 0 不一致；8 canonical 组 / 18 同步副本**（证明 anti-ai-writing×5、format-and-structure×3、check-outline-copy×3、cdp-utils×2、tracking-transaction×3、genre-writing-formulas 等共享副本全部字节一致） |
| check-current-skill-contracts.sh | 全部 PASS |
| check-hook-regex-sync.sh / check-python-invocation.sh | OK |
| node --test（dashboard 单测） | **21 通过 / 0 失败 / 6 跳过**（跳过为 Windows 平台限制） |
| node scripts/test-scan-runtime.js | **exit 0，全部 25 个测试函数执行**（含新增 dz e2e、fanqie 纯函数、ciweimao 参数校验） |
| node --check 全部改动 JS（14 个） | 全过 |
| py_compile 改动 py（2 个） | 全过 |

## 二、8 条 medium 逐条验证（全部属实已修）

| # | 修复声明 | 验证证据 |
|---|---|---|
| 1 | Dashboard GBK 有损写回 | `dashboard-server.mjs:598-601` 读侧检测 U+FFFD → 415 `unsupported_encoding`；`:643-647` 写侧拒绝含 U+FFFD 内容 → 400。单测 21/0/6 无回归 ✓ |
| 2 | analyze 失效章节正则引用 | `SKILL.md:131` 已内联 `第[一二三四五六七八九十百千零两0-9]+章`，:130 不再引用 style-profile-generator.md Step 4 ✓ |
| 3 | 扫榜文件头质量字段 | qidian `summarizeQuality`（:360）+ `[数据稀疏]`（:365）+ 数据质量/有效条目/问题摘要（:375-377）；fanqie `[OK]/[存在问题]`（:372-376）；jjwxc 同款（:282-290）✓ |
| 4 | check-outline-copy fail-open | 头注释更新退出码契约（:34 新增 exit 2）；`--outline` 缺参 stderr+return 2（:142-145）；不可读正文/细纲抛错（:163/:168）→ 顶层 catch stderr+exit 2（:244-246）；short-write SKILL.md:335/348/407 与 deslop SKILL.md:351/359/428 已接线 ✓ |
| 5 | anti-ai-writing 悬空 writing-craft.md | deslop 副本 grep 零命中；5 副本字节一致（shared-files 0 不一致）✓ |
| 6 | import format-and-structure 悬空引用 | import 副本 grep genre-writing-formulas/writing-craft 零命中；3 副本字节一致 ✓ |
| 7 | README demo 树含不存在 逐章记录/ | `README.md:240` 导入 demo 树已删 逐章记录/（通用项目结构 :313 保留为合法模板）✓ |
| 8 | README 100+ 口径 | `README.md:261`「部署包 agent-references 含 58 份方法论文件，全仓 references 共 200+ 份」✓ |

**附加 P0**（复检报告 #5）：`character-state-reverse.md:73` 已改为「逐章构造 append 事务（章号连续）并 commit，不要重跑 init（init 仅在追踪状态不存在时可用，绝不覆盖既有项目状态）」——与 tracking_commit.py 的 init 保护完全一致 ✓

## 三、P1 代码修复验证（全部属实）

| 声明 | 证据 |
|---|---|
| dz `--channel` 校验 | `dz-browse-scraper.js:213` 抛「未知 --channel: ...（可选 male/female/all）」；e2e 断言 `--channel 2` 报错 ✓ |
| dz 部分失败 exit 2 + 结构化 outcome | `:217-243` failed/partialReasons/partial；runCli partial→exit 2（test-scan-runtime:800）✓ |
| dz/heiyan 本地时间戳 | cdp-utils `localTimestamp()`（:198，导出 :254）；dz :176、heiyan :106 调用 ✓ |
| ciweimao `--type` 白名单 | `:126-127` 抛「未知 --type」；测试 :815 断言 ✓ |
| browser-cdp `--profile` 校验 | `setup-cdp-chrome.js:70-75` 缺参报错 + 拒绝 `..`/`/`/`\` ✓ |
| merge 脚本 UnicodeError | merge-claude-settings.py:100、merge-codex-hooks.py:109 `except (OSError, UnicodeError, json.JSONDecodeError)` ✓ |
| test-scan-runtime 新测试 | :756-776 fanqie 纯函数、:779-802 dz e2e（部分失败 exit 2 + 只写男频）、:815 ciweimao 校验；全部执行且 exit 0 ✓ |

## 四、P1 文档/元层验证（全部属实）

| 声明 | 证据 |
|---|---|
| marketplace 版本同步 | story-short-analyze 3.0.0（:66）、story-setup 1.2.7（:106），与 SKILL.md frontmatter 一致 ✓ |
| story/browser-cdp 补 version | story SKILL.md:3、browser-cdp SKILL.md:3 均 `version: 1.0.0` ✓ |
| story「修改时间」→sha256 | `story/SKILL.md:65`「内容版本校验（sha256）」✓ |
| browser-cdp wmic→PowerShell | `SKILL.md:124` Get-CimInstance；CHROME_INSTALLED=no 文档化（:45）✓ |
| workflow-daily 1500+→440 | `:40`「约 440 行」✓ |
| 受管清单措辞（7 处全链） | UPGRADING:113、import SKILL.md:58/388、tracking-transaction×3:36、workflow-daily:51 全部统一，无「按原样整体移入」残留 ✓ |
| pipeline-ops 跨 skill 软引用 | `:60`「见 story-long-write『对标节奏迁移』步骤 1」（只提 skill 名）✓ |
| story-review 8→10 种 | `:295`「10 种 AI 写作模式」✓ |
| 公式 21→19 | genre-writing-formulas ×4「19个题材」；README:366「19 大题材写作公式」；short-analyze SKILL.md 同步 ✓ |
| output-templates 断锚 | `:422/:558/:560`「structure_counts 数值校验」，「字段最小计数」零残留 ✓ |
| output-contract 拆文管道标注 | `:114`「material-decomposition.md，拆文管道适用」✓ |
| short-format 空行说明 | `:67`「模板示意里的段落间空行仅为 Markdown 排版展示」✓ |
| story-cover file 守卫 + base64 兼容 | `:193/:255` `command -v file && file "$OUT"`；`:187/:249` `(base64 -D 2>/dev/null \|\| base64 --decode)`；file 未入 requires.bins 与「可选」语义一致 ✓ |
| README 短篇结构图 | `:324-339` 项目根 `拆文库/` + `{短篇标题}/`（设定.md/小节大纲.md/正文.md/对标/）与 SKILL.md 一致 ✓ |
| demo 补李林.md | `demo/长篇/.../追踪/角色状态/李林.md` 存在 ✓ |

## 五、剩余待处理项核对（如实保留，未误删）

| 项 | 实测 |
|---|---|
| banned-words.md 7 处 check-ai-patterns.js 引用 | 仍存在（如 banned-words.md:24/147），共享 6 副本待全量同步 ✓ |
| story-cover `curl -f` 错误详情 | 未动（改变 fail-fast 语义，需实跑 API 验证）✓ |
| shared-assets.json 登记 banned-words/output-contract | grep 零命中，未登记 ✓ |
| .agents/skills 退化 | 仍为 9 字节文本文件（内容 `../skills`）✓ |

## 六、复检新观察（2 条，极轻微，不影响验收）

1. **check-outline-copy.js:149-151**：无正文参数（纯用法错误）时打印用法但 `return 0`——与 fail-loud 契约（exit 2）精神略不一致；因 stderr 有用法提示、可见，不构成静默漏检。建议顺手改 `return 2`。
2. **analyze 内联章节正则**：`第[一二三四五六七八九十百千零两0-9]+章` 未含「万」（10000+ 章场景）——与原始声称「覆盖 1000+ 章」一致，超出实际范围，仅记录。

## 七、结论

**修复质量高，全部 8 条 medium + 附加 P0 + P1 代码/文档修复经独立复核属实，基线全绿、共享副本字节一致、无回归。** 唯一未验证的是 story-cover 的 `curl -f` 与 banned-words 7 处引用（按声明保留为待处理，且均有明确理由）。复检未发现任何新的中危以上问题；2 条新观察均为信息级。

---

*复检验证：13/13 基线通过 · 8 medium + 1 附加 P0 全部属实已修 · 剩余 4 项待处理如实保留*
