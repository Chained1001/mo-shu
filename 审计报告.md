# oh-story-claudecode 全仓逐技能审计报告

- **审计对象**：`oh-story-claudecode` v0.7.6（agents_version 25）全部 13 个 skill + 仓库元层基础设施
- **审计方式**：13 个并行审计代理逐技能核查（frontmatter / 结构 / 引用完整性 / 跨 skill 约定 / agents_version / 文档一致性 / 脚本健壮性 / 安全）+ 主线程人工复核全部中危发现 + 实跑仓库自带守卫脚本
- **审计日期**：2026-08（快照：`oh-story-claudecode-main`，非 git 仓库）

## 〇、总体结论

**仓库工程化程度很高，13 个 skill 全部通过仓库自带静态守卫，未发现必然触发的功能级错误（无 high 发现）。** 问题集中在三类：文档/实现漂移、失效引用、少量健壮性边界。等级分布：**B × 9、C × 4**（无 A、无 D）。

| 等级 | 含义 | Skill |
|---|---|---|
| C | 有中危问题需修复 | `story`、`story-import`、`story-long-analyze`、`story-short-analyze` |
| B | 少量低危/观察级问题 | `browser-cdp`、`story-cover`、`story-deslop`、`story-long-scan`、`story-long-write`、`story-review`、`story-setup`、`story-short-scan`、`story-short-write` |

4 条中危发现已由主线程逐条复核证据，全部属实（见 §5）。

## 一、审计范围与方法

对每个 skill 核查：① frontmatter（name/description 单行/OpenClaw metadata/version）；② SKILL.md 工作流编号合法性（防 `Step N.M` 小数）；③ references/scripts 引用完整性（含 genre-prose-cards 索引↔单卡一一对应）；④ canonical 参考路径约定（`{项目根}/.claude/skills/story-setup/references/agent-references/`，禁止跨 skill 文件引用、browser-cdp 除外）；⑤ agents_version=25 一致性；⑥ 与 README/CHANGELOG 声称一致；⑦ 脚本 CLI 契约/错误处理/共享副本字节一致；⑧ 健壮性与安全（Windows 中文路径、GBK 区域、路径穿越、正则回溯、危险默认值）；⑨ 测试与契约有效性。

## 二、基线检查结果（实跑仓库自带守卫，全部通过）

| 检查 | 结果 |
|---|---|
| `scripts/static-check.sh`（13 skill 结构化校验） | **13/13 PASS，0 警告** |
| `python scripts/skill-numbering.py check` | **PASS**（272 个 Markdown 工作流编号规范） |
| `bash scripts/check-doc-budget.sh`（热路径预算） | **PASS**：长篇日更 36170/36200、开书 25423/25500、正文 agent 上下文 38944/38960，9 个热路径文件全部在预算内 |
| `bash scripts/check-shared-files.sh` | **OK**：61 组引用 0 不一致；8 个 canonical 组管理 18 份同步副本 |
| `bash scripts/check-current-skill-contracts.sh` | **全部 PASS**（manifest/schema/主产物回退语义/demo 产物） |
| `bash scripts/check-hook-regex-sync.sh` | **OK**（伏笔状态检测、毒句式正则 js↔py 逐字同步） |
| `bash scripts/check-python-invocation.sh` | **OK**（未发现裸调 python3；hook 内嵌 python stdout 编码 OK） |
| `node --test tests/dashboard-server.test.mjs tests/dashboard-trigger-contract.test.mjs` | **21 通过 / 0 失败 / 6 跳过**（跳过均为 Windows 平台限制：symlink 创建、不可读目录、POSIX 权限位） |

**环境说明**：
- 快照非 git 仓库；为运行依赖 `git rev-parse` 的守卫脚本，审计期间临时 `git init`（结束后已删除，见 §7）。
- 受限沙箱禁止 piped-stdio spawn 与 Git Bash signal pipe（EPERM / Win32 error 5），相关检查经权限升级后实跑，结果如上。
- Playwright e2e 未运行（需安装浏览器）；e2e spec 引用的 demo 文件路径已静态核对全部存在。

## 三、元层审计发现（跨 skill / 仓库级）

| # | 严重度 | 位置 | 发现 | 建议 |
|---|---|---|---|---|
| M1 | medium | `.agents/skills`（仓库根） | 该路径是 **9 字节普通文件、内容为 `../skills`**——Windows 检出时符号链接退化为文本文件。Codex 扫描 `.agents/skills` 会把它当文件而非目录，13 个 skill 发现失败。README 已警告需 `core.symlinks=true`，但未说明退化为文件时的表现。 | README 补一句「Windows 未开 symlink 时 `.agents/skills` 会退化为含 `../skills` 的文本文件，此时 Codex 走 `$story-setup` 部署」；或仓库改用 `.gitattributes` 强制 symlink。 |
| M2 | medium | `.claude-plugin/marketplace.json` vs SKILL.md | **插件清单版本与 SKILL.md 内部版本脱节**：`story-short-analyze` SKILL.md=3.0.0 vs marketplace=1.0.0；`story-setup` SKILL.md=1.2.7 vs marketplace=1.0.0（仅 `story-review` 1.1.1 恰好一致）。CHANGELOG 也未记录 3.0.0 的 bump。市场按清单版本做更新检测，清单停滞会影响更新提示。 | 统一版本同步规则：bump SKILL.md 时同步 marketplace.json 与 CHANGELOG（或明确清单版本独立语义）。 |
| M3 | low | `README.md` §短篇结构（L325-333） | README 画的短篇结构（`短篇/{标题}/` + 目录内 `拆文库/{书名}/`）与 SKILL.md 权威结构（项目根 `拆文库/` + `{标题}/` 含 `设定.md`/`对标/`）不一致；且 README 同页 L335 又称「拆文库默认输出到项目根目录」，自相矛盾；`短篇/` 前缀在 SKILL.md 中从未出现。 | 按 SKILL.md Phase 2 重画 README 短篇结构图并统一拆文库位置表述。 |
| M4 | low | `README.md` L262 | 声称「100+ 份方法论文件」，实际部署的 agent-references bundle 共 58 个 .md（26 顶层 + 32 题材卡）。 | 口径改为「数百份」或「仓库全量 references 200+ 份」。 |
| M5 | low | `README.md` L193 / `demo/拆文库/盘龙/_progress.md` | README 盘龙 demo 树声称「4剧情+2故事线 / 五个分场景剧情单元」，实际 demo 只有 4 个剧情单元文件、1 条主线；demo `_progress.md` 的「输出目录」写 `demo/拆文库-盘龙/`（该路径不存在）。 | 修正 README demo 树与 demo `_progress.md` 元数据。 |
| M6 | low | `.claude-plugin/marketplace.json` L113 | story-review 描述仍写「agents 过旧时自动降级 solo」，与 v0.7.4 起「版本不匹配照常并行、只报 Notice」的新策略矛盾。 | 删除「过旧」措辞。 |
| M7 | info | `skills/story/VERSION` | 仓库唯一 VERSION 文件 = 0.7.6，与 README、两个 marketplace.json、reasonix-plugin.json、.zcode-plugin/plugin.json、CHANGELOG 全链一致 ✓（正向确认）。 | — |

## 四、逐技能审计结果

### 4.1 story — C（中危 1 条）

整体非常健康：路由表 13 skill 一一对应、VERSION=0.7.6 全链一致、API 契约与前端/测试逐条对齐、`node --check` 通过、安全设计（回环监听、Host/Origin 校验、isPathInside、sha256 版本冲突 409、2MiB 上限、分页/搜索预算）严谨且单测覆盖 21 项。**唯一中危：Dashboard 对非 UTF-8 文件有损解码后仍可静默写回。**

- **[medium/robustness] `scripts/dashboard-server.mjs:597`**：`readWorkspaceFile` 以 `utf8` 读取（GBK 等编码被有损替换为 U+FFFD），`fileVersion`（L604）基于有损内容算哈希，`replaceFileAtomically`（L608）再以 utf8 写回——打开一个 GBK 的 `.txt/.md` 后直接保存，409 检查会通过并把原文件编码永久破坏，全程无警告。网文作者常有 GBK 旧稿，触发条件现实存在。**建议**：读取时检测 BOM / U+FFFD 或字节数异常，返回 4xx 提示先转码；保存路径同样拒绝含 U+FFFD 内容。
- [low/docs] `SKILL.md:64`：称用「修改时间」防外部更新，实现实际是 sha256 内容版本（更强），文档与实现口径不符。建议改文案。
- [low/robustness] `dashboard-server.mjs:643`：串行化锁 key 未 realpath，工作区内符号链接目录可经两条路径绕过串行化（sha256 仍保证只有一个成功，仅串行化失效）。建议锁 key 统一 realpath。
- [info] SKILL.md:41 「看项目/文件」间硬换行可能影响字面触发词匹配；dashboard 文件树显示无扩展名点文件（`.story-deployed` 等）属 UI 噪音。

### 4.2 browser-cdp — B

唯一允许被跨引用的基础组件，结构健康；setup-cdp-chrome.js 安全闸门完善（同意握手、端口闸门、进程树归属验证、孤儿回收），11 项 CDP 场景测试与实现吻合；CHANGELOG #295（禁止按可执行名批量清 Chrome）已落地。低危/观察：

- [low] `SKILL.md:121`：Windows 手动清理命令依赖已弃用的 `wmic`（Win11 24H2+ 已移除），脚本自身已用 Get-CimInstance 兜底，文档未同步。建议改 PowerShell 形式。
- [info] `setup-cdp-chrome.js:734`：detect-only 的 `CHROME_INSTALLED=no` 键未文档化。
- [info] `SKILL.md:98`：base64 示例在 GNU base64 按 76 列换行时长载荷可能带换行，建议 `tr -d '\n'`。
- [info] `setup-cdp-chrome.js:915`：`--remote-allow-origins=*` 允许任意本地页面驱动调试实例读登录态，建议文档提示。
- [info] `setup-cdp-chrome.js:68`：`--profile` 未校验路径分隔符可越出 profileDir；`setup-cdp-chrome.js:611` 首次整目录复制 profile 无进度提示。

### 4.3 story-cover — B

流程自洽（题材分析→视觉风格→提示词→出图→署名排版），内联 bash 错误处理与覆盖保护（`封面_vN` 自增）到位，无跨 skill 引用，CHANGELOG 声称全部落地。低危/观察：

- [low/docs] CHANGELOG 历史步骤号（Step 3.5/1.5）与现行编号（Step 5/2）漂移（历史记录按约定不回溯，知情即可）。
- [low/scripts] `SKILL.md:193/255`：文生图/图生图末尾 `file "$OUT"` 未声明在 `metadata.openclaw.requires.bins` 且未守卫，无 file 的最小化环境会误报失败。建议 `command -v file ... && file "$OUT"`。
- [info] 书名/作者名直入图像提示词文字层（功能必需，建议限长 20 字并注明「仅渲染文字」）；输出目录含中文，GBK 区域可能乱码（建议注明面向 UTF-8 环境）；默认模型名 `gpt-image-2` 与公开 API 现名（gpt-image-1 系列）需按实际 API 核对，已有 `GPT_IMAGE_MODEL` 可覆盖。

### 4.4 story-deslop — B

4 个共享脚本与兄弟 skill 逐字节一致、node --check 4/4、正则无灾难性回溯、blocking 7 类/advisory 13 类与文档逐项一致、v0.7.5「他说」误判修复已落地。低危/观察：

- [low/references] `references/anti-ai-writing.md:293`：「见 writing-craft.md『视角姿态：深度限知』」——本 skill 无此文件（5 份共享副本中只有 2 份所在目录有），用户按指引找不到。建议补副本入 shared-assets 或改内联表述。
- [low/docs] `SKILL.md:423`：`check-outline-copy.js` 随包分发但 SKILL.md 收尾序列未接线（long-write 侧已接线），属休眠资产。建议接线或从分发物移除并同步三端。
- [info] `check-ai-patterns.js:100` 用 lookbehind 正则（要求 Node ≥8.3），建议注明最低 Node 版本；SKILL.md:116 blocking 全归 Gate B 属宽松映射（章尾类应归 F、标点类应归 D）。

### 4.5 story-import — C（中危 1 条）

整体健康：v0.7.4「导入书不再登记成自身对标」已在核心原则/Step 9/题材定位/质量清单/短篇续写基线多处落地；tracking_commit.py 三份副本字节一致、Windows 中文路径处理到位；仓库守卫全过。**唯一中危：分批导入指引与工具 init 覆盖保护直接矛盾。**

- **[medium/robustness] `references/character-state-reverse.md:73`**：「扩大导入范围时重新执行一次完整导入」与 `tracking_commit.py:1018`（`init never overwrites project state`）及 SKILL.md「不要删 追踪/ 重来」矛盾——已 init 的项目无法再跑完整导入，agent 照做会被 init 拒绝。**建议**：改为对新增已写章节逐章构造 append 事务（章号连续），并说明 init 仅在 state 不存在时可用。
- [low] `SKILL.md:267`：目标目录已有 `正文/设定/大纲` 时覆盖/跳过/合并策略未定义，可能静默覆盖同名章节。建议先确认。
- [low] `SKILL.md:553`：story-explorer 交叉验证 spawn 未传模板要求的 `last_committed_chapter/state_revision`，全新导入项目大概率返回误导性的 `tracking_state_invalid`。建议先跑 `tracking_commit.py check` 再 spawn 或删该可选验证。
- [info] `tracking_commit.py:669` 错误消息说「exactly」实际是子集检查；SKILL.md:104 沿用「硬门禁」旧措辞；导入细纲模板缺新版蓝图的 阶段位置/单元ID/结构公式/禁止提前释放 四字段（日更首次需补建）。

### 4.6 story-long-analyze — C（中危 1 条）

管道（Stage 0-6）自洽完整，agents_version 25、demo 盘龙产物与 SKILL.md 输出结构一致、story-long-write 消费契约对齐、v0.7.4 Windows 文风统计修复已落地。**唯一中危：失效引用。**

- **[medium/references] `SKILL.md:130`**：声称「用 style-profile-generator.md Step 4 的章节正则（含 千/两）grep 出全部章节行号」，但该文件 Step 4 是「原文采样」（L49），且 L53 明确「Stage 6 只读 _progress.md 章节边界表，不重新搜索章节标题」；全 skill 不存在该正则（pipeline-ops.md L39 亦写「不再各自跑 regex」）。执行 Stage 0 的 agent 按引用查找会扑空。**建议**：内联给出具体章节正则或删除该引用并注明「边界表落盘后即唯一切片真值」。
- [low/docs] README 盘龙 demo 树（5 单元/2 故事线）与实际（4 单元/1 主线）不符；demo `_progress.md` 输出目录写 `拆文库-盘龙/`（不存在）。
- [low/docs] `references/output-templates.md:4` 写「Stage 0-5」实为 Stage 0-6。
- [info] SKILL.md version 1.0.0 与兄弟 skill（short-analyze 3.0.0 等）版本体系不统一（观察项）。

### 4.7 story-long-scan — B

6 个脚本 node --check 全过，CDP 采集健壮性（番茄字体反爬、超时/重试、gb18030 解码、Windows shell-free 调用）到位，CHANGELOG #339/#340 已落地。问题集中在文档/实现轻微漂移：

- [low/docs] `SKILL.md:153`：「采集质量检查」硬性要求的文件头三行（数据质量/有效条目/问题摘要）仅七猫完整落地，起点无质量行、番茄/晋江缺两行；`[数据稀疏]` 自动标注仅七猫实现（Phase 5 选题决策依赖它）。
- [low/docs] `SKILL.md:91`：番茄「评分」字段与 references（SSR 详情页无数字评分）及脚本实现自相矛盾，建议删「评分」二字。
- [low/scripts] `qidian-rank-scraper.js:56`：newsign 与 signnewbook 共用 mobilePath `/rank/sign/`，auto 模式两榜产出完全相同数据（批量采集默认组合会产生两份相同文件）。
- [low/references] `scan-output-format.md:21`：起点输出模板与脚本实际输出字段/格式不一致（标题、最新更新格式等）。
- [low/structure] `SKILL.md:331`：刺猬猫脚本存在但 SKILL.md 主体缺采集目标表与 CLI 示例。
- [info] fanqie/ciweimao 解析逻辑无单测；extractMobilePageContext 未导出无离线测试；批量采集表称起点「前20」但脚本无 `--top` 参数。

### 4.8 story-long-write — B

v0.7.6 声称的五项修复全部落地（narrative-writer 三端模板均有 Bash 白名单 + 可执行字数判据 + 句长分布报出 + 内容层/形状层双轨、check-outline-copy.js 与复沓锚句字段配套、Claude 写正文守卫覆盖 Bash 面与追踪检查点门）；SKILL.md 现 327 行/27KB（低于 v0.7.5 声称的 54KB）；genre-prose-cards 索引↔32 卡一一对应；4 个共享 JS + tracking_commit.py 跨 skill 字节一致；demo 项目 tracking_commit.py check 通过（last_committed_chapter=20）。低危/观察：

- [low/structure] `workflow-daily.md:25`、`workflow-setup.md:49/276`：story-explorer/story-architect 的可用性检查只认 `.claude/agents/`，与 SKILL.md 自定的 `.claude → .opencode → .codex` 三平台顺序不自洽，OpenCode/Codex 项目上会静默丢失并行捷径。建议统一查找顺序。
- [info] `check-outline-copy.js:237`：`catch { process.exit(0) }` 把内部异常也吞成「干净」（与兄弟检测器退 2 的契约不一致），建议区分「无法判定」与「内部异常」。
- [info] `quality-checklist.md`：长篇版与 agent-references 版双向漂移（IGNOre_NAMES 声明为有意分叉），但同一本长篇主会话与子代理会采用不同审查清单。建议至少让 agent 版补齐长篇专项。
- [info] `workflow-revision.md:36`：角色快照缺失时的措辞比主协议宽松，可能诱导手写派生快照。

### 4.9 story-review — B

多视角 spawn 降级链路、内置 rubric fallback、跨批落盘（`.story-review/state.md`）契约完整；18 项评分标准与 quality-rubric.md 逐项对齐且有 CI 断言防漂移；4 个 reviewer agent 模板一致。低危：

- [low/docs] `.claude-plugin/marketplace.json:113` 与 `SKILL.md:347`：「过旧降级」旧措辞残留（v0.7.4 后版本不匹配照常并行），建议删「stale」。
- [low/references] `SKILL.md:77`：规范路径表缺 check-degeneration.js、tracking_commit.py、tracking-transaction.md 三条被引用条目。
- [info] `SKILL.md:168`：check-degeneration.js 调用未带 `--fail-on=blocking`（默认 all 会 exit 1），与 advisory 只作提示的语义不完全一致。

### 4.10 story-setup — B

工程化程度最高的 skill：Step 1 部署矩阵 35 行与 146 个文件一一对应；三处 story_hook_core.js SHA256 字节一致；两个 merge 脚本幂等且经 v24→v25 迁移回归；Phase 1 检测逻辑与 v0.7.4/0.7.5/0.7.6 修复承诺全部落地；Windows 中文路径/GBK 区域、无 node 降级、symlink 失效边界均有明确处理。低危/观察：

- [low/structure] `SKILL.md:78`：矩阵 hooks 行 Validation check 枚举漏 pre-compact.sh / post-compact.sh（文件实际存在且注册了事件），按表机械核对会漏验。
- [low/docs] `references/opencode/plugin.ts:65`：头注释称「仅部署 tool.execute.before」，代码实际还注册了 tool.execute.after，过时注释。
- [info] `references/zcode/hooks/hooks.json` 实际存在但未列入矩阵（插件 manifest 专属来源，建议加说明）；`merge-claude-settings.py:44` 按路径段识别受管 hook 的判定边界未注明；README「100+ 份方法论」与部署包 58 份出入（见 M4）。

### 4.11 story-short-analyze — C（中危 1 条）

Stage 2-6 管道 + output-contract/output-templates/material-decomposition 三方契约严格对齐，20 个 references 全部存在，与 short-write 共享文件字节一致，demo 54 节点/11 手法/_meta.json 与契约字段完全对应。**唯一中危：验收门控自相矛盾。**

- **[medium/structure] `SKILL.md:180`**：AI 腔自检规定「跳过以 `>` 开头的引用行」，但本 skill 自己的模板（output-templates.md）与 demo `拆文报告.md`（如 L86/146/181 的「> 双线设计是这条曲线的关键…」）恰恰大量用 `>` 写分析师本人评述——按规则字面执行，报告主体相当一部分分析师措辞被整体豁免，守门员漏扫 AI 腔。**建议**：仅豁免明确标注为源文引用的行，或规定 `>` 块引用只能承载源文引用、评述一律用普通段落，并让 demo 与模板同步遵守。
- [low/version] SKILL.md version 3.0.0 vs marketplace 1.0.0（见 M2）。
- [low/docs] README「Phase 7 门控依据」为过时术语（现为 Stage 2-6 + 四步验收）。
- [low/references] output-templates.md:560 引用不存在的「字段最小计数」表（实际名「structure_counts 数值校验」）；SKILL.md:143 章节名「情节节点提取」与正式名「情节节点提取规则」不一致。
- [low/cross-skill] 共享 anti-ai-writing.md:293 提及本目录不存在的 writing-craft.md（与 deslop 同款问题）。
- [info] 兄弟 skill short-write 目录树示例缺 `原文/` 与 `_meta.json`；current-contract.json 未覆盖短篇 `_meta.json` schema 与 demo 产物（无机器级契约测试）。

### 4.12 story-short-scan — B

整体健康；CHANGELOG 黑岩频道文件名修复已落地。低危集中在点众采集脚本 CLI 契约：

- [low/scripts] `scripts/dz-browse-scraper.js:115`：`--channel` 无早期合法性校验，非法值只得到泛化报错（对比 heiyan 的明确报错）。
- [low/scripts] `dz-browse-scraper.js:228`：`--channel all` 单频道失败时 exit 0 掩盖部分失败（cdp-utils 已有 `{planned,written,failed,partial}` 契约未用），与 CHANGELOG「如实标注采集质量」承诺不完全一致。
- [low/docs] `SKILL.md:4`：平台清单表述不一致（description 为 知乎盐言/七猫/黑岩/点众，README/marketplace/Phase 1 提问含「番茄短篇」但本 skill 无番茄采集资产）。
- [info] 报告头「抓取时间」用 toISOString（UTC，展示早 8 小时）；SKILL.md 未说明产物输出目录约定；dz-browse-scraper 无 e2e 测试。

### 4.13 story-short-write — B

Phase 1-4 流程自洽，output-contract 与 short-analyze 字节一致，10 个 genre-styles 包齐备，共享资产字节同步正常，agents_version 统一 25。低危/观察：

- [low/docs] README 短篇项目结构缺 `设定.md`/`对标/`、拆文库位置画错（见 M3）。
- [low/scripts] `scripts/check-outline-copy.js` 支持短篇形态（正文.md↔小节大纲.md）但 SKILL.md 未接线，属随包分发的休眠资产（long-write 侧已接线）。建议在 Phase 4 接入或从分发物移除并同步三端。
- [info] 共享 output-contract.md:114 提及仅 analyze 侧存在的 material-decomposition.md（文档性说明引用）；demo `_meta.json` 的 genre_detected「追妻火葬场」与现行口径「追妻」字面不匹配（依赖前缀模糊匹配）；README「8 节结构」注释与 SKILL.md 无固定 8 节要求；normalize-punctuation 就地写回无备份（确定性工具，低风险）。

## 五、中高危发现 TOP（已人工复核证据）

| # | Skill | 严重度 | 一句话 | 证据 |
|---|---|---|---|---|
| 1 | story | medium | Dashboard 打开 GBK/非 UTF-8 文件后保存会静默损坏原文件编码 | `dashboard-server.mjs:597` utf8 有损解码 → `:604` 哈希 → `:608` utf8 写回，版本检查通过 |
| 2 | story-long-analyze | medium | SKILL.md 引用不存在的「Step 4 章节正则」，与「边界表为唯一真值」新设计矛盾 | `SKILL.md:130` vs `style-profile-generator.md:49/53` |
| 3 | story-import | medium | 分批导入指引「重跑完整导入」会被 init 覆盖保护拒绝，agent 照做死胡同 | `character-state-reverse.md:73` vs `tracking_commit.py:1018` |
| 4 | story-short-analyze | medium | AI 腔自检豁免 `>` 行，但模板与 demo 大量用 `>` 写分析师评述，守门员形同虚设 | `SKILL.md:180` vs `output-templates.md`/demo `拆文报告.md:86/146/181` |
| 5 | 元层 | medium | `.agents/skills` 在 Windows 上退化为 9 字节文本文件（内容 `../skills`），Codex skill 发现失败 | 实测文件 Length=9 |
| 6 | 元层 | medium | marketplace.json 插件版本与 SKILL.md 内部版本脱节（short-analyze 3.0.0 vs 1.0.0 等） | 实测对比 |

## 六、修复建议优先级

**P0（建议尽快）**
1. story Dashboard：非 UTF-8 文件保存前做编码健全性检查，拒绝有损写回（#1）。
2. story-long-analyze SKILL.md:130 失效引用修正（#2）。
3. story-import character-state-reverse.md 分批导入指引改为 append 事务（#3）。
4. story-short-analyze `>` 豁免范围收窄为「仅源文引用」（#4）。

**P1（低危、随下次版本清理）**
5. 统一 marketplace.json 与 SKILL.md 版本同步规则（M2）。
6. README 短篇结构图、盘龙 demo 树、`100+ 份` 口径修正（M3/M4/M5）。
7. `.agents/skills` Windows 退化行为写入 README（M1）。
8. story-review 删除「过旧」措辞（M6）；story-deslop/short-analyze 共享 anti-ai-writing.md 的 writing-craft.md 悬空引用。
9. story-long-write 三处 agent 可用性检查统一三平台查找顺序。
10. check-outline-copy.js 在 short-write/deslop 侧接线或移除。
11. 短篇扫榜 dz-browse-scraper.js 补 `--channel` 校验与部分失败退出码。
12. 起点/番茄/晋江采集质量头与 `[数据稀疏]` 标注落地（story-long-scan）。

## 七、附注

- 审计期间为运行仓库守卫脚本临时 `git init`，**已删除 `.git`**，工作区恢复为原快照状态（不含任何 git 元数据）。审计过程未修改任何 skill 文件；唯一新增文件为本报告。
- 本报告 findings 由 13 个并行审计代理产出、主线程对全部 medium 及以上条目逐条复核原文证据后采纳；low/info 条目为代理报告，个别行号可能因版本漂移有 ±几行误差。

---

*报告生成：oh-story-claudecode v0.7.6 快照 · 13/13 skill 审计 · 基线守卫全部通过*
