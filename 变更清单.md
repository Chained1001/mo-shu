# 变更清单

> oh-story-claudecode v0.7.6 快照 · 全仓审计与修复的全量改动记录（非 git 仓库，本清单为交付/回滚依据）。
> 对应报告：`审计报告-复检.md`。所有改动已通过 `static-check.py` 13/13、`node --check`、共享副本 SHA-256 字节一致校验。

## 一、审计产出（新增 2 文件）

| 文件 | 说明 |
|---|---|
| `审计报告-复检.md` | 14 单元独立复检报告（8 medium + 36 low + 修复进度追踪） |
| `demo/长篇/让你管账号，你高燃混剪炸全网/追踪/角色状态/李林.md` | 补 demo 角色状态快照（6 角色与设定一一对应） |

## 二、P0 中危修复（8 条）

| 文件 | 改动 |
|---|---|
| `skills/story/scripts/dashboard-server.mjs` | 读/写两侧加 U+FFFD 检测（GBK 有损写回 415/400）；写串行化锁 key 改 realpath |
| `skills/story-long-analyze/SKILL.md` | :130 内联章节正则、:322 删旧描述；原文备份补 GBK 转码检查 |
| `skills/story-{short-analyze,deslop,review,long-write}/references/anti-ai-writing.md` + `story-setup/.../agent-references/anti-ai-writing.md` | 5 副本：`writing-craft.md` 悬空引用改自包含表述 |
| `skills/story-import/references/format-and-structure.md` + `story-long-write/...` + `story-setup/.../agent-references/...` | 3 副本：删 `genre-writing-formulas.md`/`writing-craft.md` 跨 skill 文件名引用 |
| `skills/story-import/references/character-state-reverse.md` | 分批导入「重跑完整导入」→ append 事务 |

## 三、P1 代码修复

| 文件 | 改动 |
|---|---|
| `skills/story-long-scan/scripts/qidian-rank-scraper.js` | 新增 `summarizeQuality` + 数据质量/有效条目/问题摘要 + [数据稀疏] |
| `skills/story-long-scan/scripts/fanqie-rank-scraper.js` | 数据质量枚举归一 [OK/存在问题] + 有效条目/问题摘要 |
| `skills/story-long-scan/scripts/jjwxc-rank-scraper.js` | 数据质量枚举归一 + 有效条目/问题摘要 |
| `skills/story-long-scan/scripts/ciweimao-rank-scraper.js` | `--type` 白名单校验 |
| `skills/story-long-scan/SKILL.md` + `references/scan-output-format.md` | 文件头质量字段口径、番茄「评分」删除、四平台模板同步 |
| `skills/story-long-write/scripts/check-outline-copy.js`（源）+ deslop/short-write 副本 | fail-open 修复：不可读/异常/`--outline` 缺值均 exit 2 |
| `skills/story-short-write/SKILL.md` + `skills/story-deslop/SKILL.md` | check-outline-copy 接入 Phase 3/4 |
| `skills/story-short-scan/scripts/dz-browse-scraper.js` | `--channel` 校验 + 结构化 outcome（部分失败 exit 2）+ 本地时间戳 |
| `skills/story-short-scan/scripts/heiyan-booklist-scraper.js` | 本地时间戳 |
| `skills/story-{long,short}-scan/scripts/cdp-utils.js` | 新增 `localTimestamp()` |
| `scripts/test-scan-runtime.js` | 新增 fanqie 单测 + dz e2e + ciweimao 参数校验；同步 jjwxc 枚举断言 |

## 四、P1 文档/元层

| 文件 | 改动 |
|---|---|
| `README.md` | demo 树「逐章记录/」删除、「100+ 份」校准、短篇结构图、盘龙 demo 树、「21→19」等口径 |
| `.claude-plugin/marketplace.json` | story-setup 1.0.0→1.2.7、story-short-analyze 1.0.0→3.0.0 |
| `skills/story/SKILL.md` | 补 `version: 1.0.0`；「修改时间」→sha256 |
| `skills/browser-cdp/SKILL.md` | 补 `version`；wmic→PowerShell；`-y` 别名、`CHROME_INSTALLED=no` 文档化 |
| `skills/browser-cdp/scripts/setup-cdp-chrome.js` | `--profile` 分隔符/`..` 校验 |
| `skills/story-setup/scripts/merge-{claude-settings,codex-hooks}.py` | except 扩为含 UnicodeError |

## 五、low 清理（文档措辞）

| 文件 | 改动 |
|---|---|
| `skills/story-long-write/references/workflow-daily.md` | 「1500+ 行」→约 440 行；「整体移入」→「受管清单移入」 |
| `skills/story-long-analyze/references/pipeline-ops.md` | 跨 skill 软引用改只提 skill 名 |
| `skills/story-review/SKILL.md` | 「8 种」→「10 种」AI 模式 |
| `skills/story-short-analyze/references/output-templates.md` | 「字段最小计数」→「structure_counts 数值校验」断锚 |
| `skills/story-short-analyze/SKILL.md` | 「21 大」→「19 大」题材公式 |
| `skills/story-{short-write,short-analyze}/references/output-contract.md` | 「拆文管道适用」标注 |
| `skills/story-short-write/references/short-format.md` | 模板空行加「仅排版展示」说明 |
| `skills/story-cover/SKILL.md` | `file` 加守卫；`base64 --decode` 跨平台兼容（`-D \|\| --decode`） |
| `genre-writing-formulas.md` ×4（short-write/long-write/setup/short-analyze） | 「21个」→「19个」题材 |
| `tracking-transaction.md` ×3（long-write/import/review）+ `story-import/SKILL.md` + `story-setup/UPGRADING.md` | 「按原样整体移入」→「按受管清单移入」（7 处全链一致） |

## 六、剩余待处理（4 条 info/meta，建议随 CI 一起做）

| 项 | 说明 |
|---|---|
| `story-short-analyze` banned-words.md 7 处 check-ai-patterns.js 引用 | 共享文件 6 副本，改写需全量同步 |
| `story-cover` `curl -f` 错误详情 | 改错误处理改变 fail-fast 语义，需实际跑 API 验证 |
| `shared-assets.json` 登记 banned-words/output-contract | 需核对 sync 脚本 schema 与 check-shared-files 分工 |
| `.agents/skills` 静态检测 | 涉及 check-codex-adapter 等 CI 脚本 |

---

## 七、剩余项处理（复检后第二轮 · 3/4 完成）

| 项 | 状态 | 改动与验证 |
|---|---|---|
| 1. check-ai-patterns.js 悬空引用（7 处/副本） | ✅ 完成 | **banned-words.md** ×6 副本（每份 2 处）+ **anti-ai-writing.md** ×5 副本（每份 5 处）共 7 处/副本，全部改为「确定性 AI 句式检测」通用表述；规则名（`quote-emphasis-tic`/`abstract-summary-tic`/`micro-action-tic`/`action-list-tic`/`cliche-density-tic`）保留。全副本 SHA-256 字节一致；残留引用仅剩「有脚本的 skill」（long-write 3、short-write 1）与 narrative-writer agent 模板 2 处（`skill scripts/ 下` 相对引用，合法） |
| 2. story-cover `curl -f` 错误详情 | ⏳ 保留 | 需真实 `GPT_IMAGE_API_KEY` 实跑 API 验证，避免破坏 fail-fast 语义 |
| 3. shared-assets.json 登记 banned-words/output-contract | ✅ 完成 | 新增 2 组：`banned-words-reference`（deslop 源 → 其余 5 副本）、`short-analyze-output-contract`（analyze 源 → short-write）→ **10 组 / 24 份**；`sync-shared-assets.py check` 与 `check-shared-files.sh`（61 组 0 不一致）全过。注：`test-shared-assets.py` 的 mode-drift 用例在 Windows 上因 `chmod` 只切只读位而无法制造 mode 差异（平台限制，非回归；Linux CI 正常） |
| 4. `.agents/skills` 静态检测 | ✅ 核实为已实现 | `check-codex-adapter.sh:112-114` **已有** symlink 检测（`[ -L ]` + `readlink` 目标必须为 `../skills`），已接入 cross-platform.yml（ubuntu + macos，Windows 任务不跑此检查规避退化误报）。本机实跑验证：9 字节退化 stub 被正确 FAIL，报错含「on Windows enable git core.symlinks」——检测早已存在且生效，无需改码 |

**验证**：static-check 13/13 PASS · check-shared-files 10 组/24 份 + 61 组 0 不一致 · doc-budget 通过 · skill-numbering PASS。

*生成：oh-story-claudecode v0.7.6 快照 · 合计改动约 54 个文件 · 验证基线：static-check.py 13/13 · 共享副本 SHA-256 全一致*
*第二轮（剩余项）：3/4 完成 · banned-words ×6 + anti-ai-writing ×5 共享副本字节一致 · shared-assets 10 组/24 份*
