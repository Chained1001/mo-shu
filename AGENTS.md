# mo-shu 仓库 AI 工作约束（AGENTS.md · V2）

> 本文件约束在 mo-shu 仓库内工作的 AI 助手（含 DeepSeek V4 Flash 施工者与人类协作者的 AI 工具）。
> 本文件为唯一宪法（原则/术语/不学清单已自总纲收口，2026-08-22 批B7）。施工协议：`docs/治理/规格/README.md`；文档索引与状态总览：`docs/README.md`。
> 治理沿革：SparkArc AGENTS.md 移植 + 源码级审计教训 + 研究-v3 三参考项目交叉验证（2026-08-20 作者确认批次与不学清单）。
> **旧规划封存区已删除**（2026-08-20 建区、2026-08-28 作者指示删除——曾封存 V2 世代过程产物：总纲/规格-V2/V2.1/研究-v2~v4/审计-V3/V4 等，历史保留在 git）；现行施工依据以本文件与 `docs/治理/` 为准。
>
> **V2 落地说明（2026-08-20 收官，2026-08-21 修订）**：V2 批 0-9 已全部闭环；批 4「下一步判定」当时经总纲 §4.3 弹性决策跳过，后于审计-V3 整改 D5 补做（提交 `147d07f`）——现由 `skills/moshu/scripts/next_step.py` 承担 moshu 路由 S0-S6 只读判定，其余 skill 流程仍按文件证据判定。本文件引用的守卫与机制均已落地。规划冻结（§1.6）随 V2 收官解除；后续规划产出须由作者明确发起。

---

## 1. AI 权限红线（最高优先级，违反即停）

1. **git 只读默认**：未经用户明确语言要求，禁止 commit / push / reset / stash / 删除 tag 等任何 git 写操作。
2. **禁止把自动批准当用户意图**：即使用户环境开着自动批准请求，git 写入也必须经用户明确语言确认。
3. **参考项目绝对只读**：`otherMaterials/referProject/**` 不得修改/删除任何文件；不在其内安装依赖、跑构建、跑联网命令；其 git 仓库只读。
4. **临时产物不入库**：临时验证脚本与输出一律放 `/.tmp/tests/<任务>/`，用完即删；不以 `test_*` 伪装临时测试。
5. **封存区纪律**：旧规划封存区 `docs/归档/` 已删除（2026-08-28 作者指示，历史在 git）；若未来重建封存区，去留仍由作者决定，施工与文档默认不得读写。
6. **规划冻结**：V2 批 0 开工至批 9 收尾期间，不再产出任何新的规划/研究文档（施工日志与审核记录的追加条目除外）；规格修订仅限施工中发现的勘误，且须经作者明确确认。

## 2. 施工纪律（flash 按批规格干活）

0. **施工入口与协作协议**：`docs/治理/规格/README.md`（开工顺序/每批标准流程/状态机/待决问题/审核协议）；施工状态与双模型交接记录在 `docs/治理/施工日志.md`（append-only，状态总表唯一权威）；审核往来集中在 `docs/治理/审核记录.md`（施工模型只可写"整改回执"小节）。
1. 按当期批次规格施工（规格存放 `docs/治理/规格/<世代>/批N-*.md`，由作者发起新增；V2/V2.1 世代已收官归档），**一批一个提交**（V2 批 3 为三子步三提交先例）；严格按规格"文件级改动清单"执行，不扩大范围、不顺手改无关文件。
2. 规格的"**禁止事项**"是硬约束，提交前逐条核对。
3. **验收**：规格内验收命令全部跑绿才算完成；失败先判因再改代码——**禁止改断言变绿**。
4. 提交消息按规格"提交规范"格式。
5. 新增确定性脚本：按需进 `shared-assets.json`（跨 skill 副本）、进 `scripts/README.md` 索引、进 `.github/workflows/cross-platform.yml` 与 `CONTRIBUTING.md`（CI 三处同步）、配正式回归测试（`test_<能力>`）。
6. 只吃判定结果：写作类流程的状态判定来自文件系统证据（moshu 路由 S0-S6 由 `skills/moshu/scripts/next_step.py` 只读判定——审计-V3 整改 D5 实施；其余 skill 流程按文件存在性/非空判定），不得凭对话记忆猜。
7. **候选永不拦截**：机检/审查的候选（candidate）类发现只呈报作者，永不阻断流程、永不影响退出码语义。
8. **AI 产出先落文件**：结构化载荷（追踪事务/审查工单等）必须先写临时文件再以 `--input <文件>` 提交给确定性脚本，不进对话复述、不进 argv。

## 3. 提交前自检清单

1. 新脚本/模板是否接入现有机制（shared-assets / behavior-contracts / doc-budget / current-contract / CI 三处同步）？
2. 跨 skill 副本是否经 `sync-shared-assets.py` 同步（`check-shared-files.sh` 全绿）？
3. agent 模板变更是否 bump `agents_version` + 更新 `UPGRADING.md`？
4. schema 变更是否带 migrate + 备份 + 四副本同步（write/build/import/review），并全仓 grep 涟漪面（hook 拒旧常量与消息文本/契约断言文本/fixture/shell e2e/守卫 JSON 标记——含 .json，B58 两轮 CI 红判因链）？
5. 新增测试是正式回归（长期守护）还是临时验证（已删）？测试文件顶部是否带"守护对象"声明？
6. 文档数字与代码一致？（skill 数/脚本数/版本号——`check-story-numbers.sh` 守护；"清单类断言最易过期"）
7. 参考项目只读遵守了吗？旧规划目录零接触了吗？

## 4. 反模式清单（错误做法 → 正确方式）

| # | 错误做法 | 正确方式 |
|---|---|---|
| 1 | 为过检改断言/改守卫数字 | 先判因；文档数字与实测对齐 |
| 2 | 删代码留文档残留（或反之） | 删改机制当次全仓 grep 清理全部引用（DeterminFlow `052f264` 六处同步范本） |
| 3 | 模板里写"格式同上/参照上文"式互引 | 显式内嵌或指向真实文件路径（两段互斥时 LLM 看不到对方——SparkArc E2） |
| 4 | 同一规范复制多份维护 | 单副本 + 锚点引用 + 守卫验证挂载点存在（`check-agent-template-rules.sh`） |
| 5 | 自造数字参数（把无关数字当护栏） | 常量命名 + CLI 可覆盖 + 验收可测，并标注 mo-shu 自定 |
| 6 | 一次性大 schema 迁移 | 拆子步，每步 migrate+备份+测试绿（批 3 模式） |
| 7 | 静默降级（缺文件当没有数据） | 读失败三分类：缺文件/空文件/内容坏，各自明示（DeterminFlow `local_archive.py:77-88`） |
| 8 | 在 CI/守卫里跑 LLM 或联网 | 场景剧本人工走查 + 脚本校验断言；守卫零外部依赖 |
| 9 | 临时验证脚本留在仓库 | `/.tmp/tests/<任务>/` 用完即删；要防"再次坏掉"才写正式回归 |
| 10 | 新能力直接开平行管线 | 先过 §5 决策树 |

## 5. 新增能力决策树（新增流程推荐模板）

新增任何机制前，按序自问：
1. 能否**不新增**（既有 skill/references/脚本已覆盖或近似覆盖）？→ 用既有的。
2. 能否挂**现有收口**（shared-assets 同步、behavior-contracts 契约、既有守卫清单）？→ 挂上。
3. 必须新开文件/脚本时：是否带正式回归测试 + CI 三处同步 + `scripts/README.md` 索引？→ 缺一不合并。
4. 是否触碰不学清单（§6）？→ 触碰即停，先改本文件 §6 并经作者确认。

---

**Agent（子代理）使用判据**（新增/改造能力时与上树配套自问；模式自六个既有 agent 实践归纳，2026-08-24 作者确认成文）：
1. **批量并行**——数量级任务（如 analyze 逐章情节点提取）；
2. **上下文隔离的重型阅读**——原料几万 token 级、只交回蒸馏结论（如拆书语料/文风样本/审查多章）；
3. **审查隔离**——审查者与创作者分身防自审（review 四 reviewer，须配审稿令牌）；
4. **条件性重活**——轻量版主会话先做，复杂时才 spawn（build 的 architect 系列）。
四条全不沾 → 不开 agent（主会话/技能内完成）。任何新 agent 必须两条硬纪律：**fallback 主线程**（agent 不可用不阻断流程）；**部署物全套成本**（模板 + spawn 协议 + 契约摘要 + agents_version bump）。

## 6. 明确不做（不学清单，14 条，作者 2026-08-20 确认；每条含理由，本节自包含）

**维持不做（三参考项目多源收敛验证）**：
- RAG/向量检索——三参考项目多源收敛验证不做。
- LLM 导演黑盒自治（水平 Agent 通信）——同上。
- "文件即真相"整体重构——同上。
- 每章全量快照——同上。
- work_tracker 任务板——同上。
- 自动连写污染传播（无暂存无作者定稿的连写）——同上。
- 知识治理重三件套——同上。
- 多宿主适配——同上。

**本轮研究后新增不做**：
- 数据库后端（任何形式）——DeterminFlow 13413 行推倒教训。
- Dashboard 常驻服务化/产品化——v6 建了 v7 删；mo-shu 现有 dashboard-server.mjs 保持轻量本地浏览定位，不扩。
- 外部 AI 检测器依赖进主链路——DeterminFlow `052f264` 删除教训（不可复现/不可分发）。
- git 书仓托管与 git 隐身——v7 产品形态，mo-shu 无书仓管辖权。
- npx 安装器/插件市场分发改造——形态不匹配（mo-shu 已在 Claude marketplace）。
- PreToolUse hook 拦截式门禁——v7 #113 教训"hook 只能 ask 不能 deny"；mo-shu 现有 hooks 保持提醒语义。

> 均为显式决策，**勿静默引入**。理由与证据底稿：三参考项目已用真实代价多源验证（证据底稿已随研究-v3 归档，见头部归档清单）；本节自包含，此后不再外指。

## 7. 研究与规划文档纪律

1. 每条论断标证据（文件路径+行号）；严格区分"代码事实"与"（推断）"；拿不准标"存疑"。
2. 实测类数字必须可复现（可跑命令或留复现路径）——"N 测试全绿"类结论若无复现路径不得写入。
3. 引用参考项目机制必须标版本（如 v6/v7）与是否已废弃；README 自述数据降级为"文档宣称"。
4. "照抄参考项目"的设计必须能给出源码路径；"mo-shu 自定"必须写清验收标准。

## 8. 测试纪律（详见 `scripts/README.md` 测试纪律节）

- **测协议不测实现**：断言事件/状态机终态/注册表一致性/回放能力，不断言 prompt 全文或生成结果快照。
- **架构/契约类测试准入三条件**：守护跨模块稳定不变量；直接覆盖统一收口；普通迭代不频繁改断言。
- 失败先判因，禁止改断言变绿（与 §2.3 同源，SparkArc 移植）。

## 9. 宪法收口（术语表·原则正文，自旧总纲收口迁入；不学清单正文见 §6）

> 本节将术语表与宪法原则正文自旧总纲（V2 世代）收口迁入本宪法（历史注记：2026-08-22 批B7；旧总纲已随归档区删除，历史在 git）。规格与施工必须遵循本节与 §1-§8。

### 术语表（规格与施工必须使用下列叫法；首次出现即定义）

| 术语 | 定义 | 禁用的别称 |
|---|---|---|
| 续写状态卡 | `追踪/上下文.md`，7 栏固定结构的当前语义检查点（`tracking_commit.py:35-43`） | "上下文.md 检查点""状态卡文件"单独使用 |
| 单权威 state | `追踪/_tracking-state.json`，唯一提交点，最后原子写 | "追踪状态文件""state.json" |
| 追踪事务 | 每章一次的结构化 JSON 提交（init/commit/check 三子命令） | "tracking 提交""章节提交"混用 |
| 主对标 | 开书时登记的唯一对标书目录（`对标/`）。**泛指对标作品、非指代已登记那一本时，可用「对标书」** | "对标书""对标"单独指代主对标时 |
| 拆文库 | `拆文库/{书名}/`，moshu-analyze/import 的产物区 | "拆解库""模块库" |
| 机检 | 确定性检测器（check-*.js / tracking_commit.py），分**阻断（blocking）**与**候选（candidate）**两列 | "机器审查" |
| 候选 | 机检/审查的疑似发现，永不拦截，只呈报作者 | — |
| 工单 | moshu-review 产出的结构化修改单，落盘 `.moshu-review/tickets/tickets_{时间戳}_{起章}-{止章}.json`（批 6 引入） | "审查发现清单"自由文本 |
| 信息差 | "谁知道什么"维度：知情人×读者已知的登记（批 3a 引入，`追踪/信息差.md`） | 与伏笔"揭示状态"混用 |
| 悬置章距 | 伏笔自最近一次变动章（updated_chapter）至 last_committed_chapter 的差值 | — |
| 下一步判定 | next_step.py 只读推断的写作进度序（S0-S6）与建议动作 DTO（历史注记：批 4 原弹性跳过，后由审计-V3 整改 D5 实施，提交 `147d07f`） | 与 moshu"意图路由表"混用 |
| agents_version | 部署物版本号，权威在 `moshu-setup/UPGRADING.md`（不在本表重复数字，避免过期；当前值以权威文件为准） | — |
| schema 版本 | INPUT_SCHEMA_VERSION / TRACKING_SCHEMA_VERSION，权威在 tracking_commit.py:26-27 三副本（不在本表重复数字） | — |
| 守卫 | 开发仓库 `scripts/check-*`（护仓库结构），区别于机检（护作品） | 混称"检测器" |
| 共享资产 | shared-assets.json 登记的字节级同步副本（组数与副本数以 `scripts/shared-assets.json` 实测为准，不在本表重复数字，避免过期） | — |
| 批 | 批次规格（`docs/治理/规格/<世代>/批N-*.md`）定义的一次施工单元，一批一提交；V2/V2.1 世代规格已归档 | "单元 U""阶段" |
| 点名清单 | 续写状态卡内按下一章细纲出场顺序/涉及设定装配的列名级写作上下文清单（B58） | "注入清单"单独使用 |
| 伏笔剧透锁 | 信息差条目未到 reveal_chapter 时知情内容不渲染进写作上下文的物理隔离（B58b） | 与"禁止提前释放"纪律混称 |
| 语声锚 | 角色快照 voice_samples 滚动保留的最近 5 条代表作派台词（B59） | "台词库" |
| 双向称谓 | address_book 域登记的 A 称 B × B 称 A 结对事实（B59） | "称呼表" |
| 对话 DNA | 作派级五要素（口头禅功能位/句长习惯/语气词与标点/回避话题/情绪外显），拆文→建卡→写作全链传递（B59a） | 与"人设卡"混称 |
| 进度登记 | progression 域的数值指标登记制（direction up/down 声明，回退无归因出候选）（B62） | "数值表" |
| 防撞对照 | Stage 5/修订流对主对标的三维重合检查（人物功能位/桥段节拍/设定机制），单源两维高重合出候选、多源共性降噪（B65） | "抄袭检查" |
| 登记免责 | 防撞对照中卷纲对标坐标与剧情单元卡已登记的参照项免计渗透——照列标"免责"可审计，非免检（B65） | "白名单" |
| 场景表 | `大纲/场景表_单元{ID}.md`，单元→场景→章数分解表，预估章数=场景行加总（B68） | "场景分解表" |
| 一进一出 | 每章解决一个小问题/欠一个新问题的呼吸节律（Swain 场景-续景理论显式化）（B68） | 与"收支平衡"混称 |
| 写作浮现 | `大纲/写作浮现.md`，写作期涌现的 append-only 登记，补纲时收割（B68） | "灵感笔记" |
| 拆并章 | 未写细纲区间内的章界流动操作，锁定区间须作者确认（B68） | — |
| 推进度仪表 | pace_meter.py 的卷进度% vs 剧情进度%双百分比+背离候选（B68） | "进度条" |
| FINALIZE | next_step 的完结建议值（非新增状态、非拦截），末卷复盘+无下卷或完结宣告时触发（B70） | 与"完结状态"混称 |
| final-report | tracking_commit 全书终账子命令，纯派生渲染不写 state（B70） | — |
| 完结清账 | `追踪/完结清账.md`，跨卷伏笔/信息差/承诺/进度的终态报告（B70） | — |
| 有意留白 | 作者宣告文件专属的伏笔/信息差终态标注，final-report 读之降为已决（B70） | "开放式结局"混称 |
| 完结宣告 | `大纲/完结宣告.md`，作者显式完结意图+留白标注的唯一载体（B70） | — |
| detail-batch 评审 | evaluator 细纲批评审型，写正文前最后一道语义闸，报告经作者处置后才进正文（B69） | — |
| settings 评审 | evaluator 设定包评审型，含题材卡置信度复核与防撞三维独立复核（B69） | — |
| revision 评审 | evaluator 修订包评审型，裁决前独立视角，不构成裁决依据（B69） | — |
| 前置查库 | 采风 query 出网前先对 catalog 路由与题材卡索引匹配，命中收窄为特性层（B71） | — |
| 题材框架 | 题材定位.md 头部的框架路由结论行，供单元卡对标分流与防撞消费（B71） | — |

### 宪法原则（本节收三条核心原则；其余在 §1/§2/§4 已有对应，见节尾映射）

- **兼容四原则**（任何批次不得破坏）：新字段/新文件全可选增量；老数据零丢失；迁移带备份；渲染规则确定性可重放。
- **证据溯源原则**（与 §7 互补：§7 管研究文档，本条管规格与施工）：每条设计必须标注「移植（源码路径）」或「mo-shu 自定（含验收标准）」；无源码依据的参数删除或显式标注。规格引用的行号以实测为准。
- **三层分工宪法**：脚本做确定性、AI 做语义、作者做品味（`skills/moshu-write/SKILL.md` 为流程权威）。新增能力先问"能否挂现有 skill/references/脚本"，防平行管线。

> 原则映射：作者红线（§1）/ 一批一提交（§2.1）/ 临时产物不入库（§1.4）/ 删改同步（§4 反模式 #2）已在前文对应，不重复迁入。
