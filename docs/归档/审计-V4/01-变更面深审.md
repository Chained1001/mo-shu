# 01 变更面深审（审计-V4 第1部分）

- 审计基线：HEAD = `3391fa3ab4747b1b35136c5e358416ecfb42d6da`（`3391fa3`，分支 `main`）
- 变更面核对基准：`git diff 8f049da..HEAD`（B1a/b/c 三子步；93 文件 +8815/-395，与任务给定清单一致）
- 审计方式：只读（git log/show/grep/diff、read/grep、python 只读执行与临时 fixture 自删）；唯一写入 = 本文件
- 实测命令（可复现）：
  - `python scripts/test-next-step.py` → 12/12 OK
  - `python scripts/test-impact-scan.py` → FAILED（errors=3，UnicodeDecodeError）
  - next_step 手动 fixture 推演（`.tmp/audit-v4/fixtures/`，用后自删）→ S0-S6 全态输出见 §1.4

---

## 1.1 moshu-build 四件

### 1.1.1 SKILL.md — 【通过】

- **frontmatter 三方一致**：name=`moshu-build`、version=`1.0.0`（`skills/moshu-build/SKILL.md:2-3`）与 marketplace 条目（`.claude-plugin/marketplace.json:31,35`）一致；description 触发词 `/moshu-build、/建书、开书、建设定、写世界观、写大纲、搭大纲、卷纲、修订设定、改设定、改大纲、开新卷`（`SKILL.md:4`）覆盖路由表关键词 `开书、设定、世界观、写大纲、卷纲、修订设定`（`skills/moshu/SKILL.md:14`）；README 描述（`README.md:91`「开书构建 · 题材定位、世界观、人物、全书大纲、首卷卷纲、设定/大纲/卷纲修订、开新卷规划」）与 marketplace description（`marketplace.json:32`）语义同源。
- **入口表链接全部可达**：开书→`workflow-build.md`「开书构建」节、修订→`revision-workflow.md`、开新卷→`workflow-build.md`「开新卷」节（`SKILL.md:19-23`），三文件均真实存在（`skills/moshu-build/references/workflow-build.md`、`revision-workflow.md`）。
- **细纲归 write 边界声明存在**：`SKILL.md:9`「细纲与正文归执行环（moshu-write），本技能不建细纲、不写正文」、`SKILL.md:13`「细纲归 write」、`SKILL.md:4`「细纲与正文写作归 /moshu-write」。

### 1.1.2 references/workflow-build.md — 【通过】

- **内部链接/反引号路径全部可达**（对照 `skills/moshu-build/references/` 目录清单）：`idea-seed.md`(:21)、`genre-prose-cards.md`(:105)、`references/genre-prose-cards/` 目录(:105)、`style-genre-modules.md`(:105)、`character-relations.md`(:103)、`genre-core-mechanics.md`(:104)、`reader-contract-and-progression.md`(:156,173)、`emotional-methods.md`(:175)、`outline-methods.md`(:181)、`emotional-arc-design.md`(:181)、`reversal-toolkit.md`(:181)、`tracking-transaction.md`(:182)、`scripts/tracking_commit.py`(:182)——均存在。
- **Phase 结构完整**：Phase 1 确认选题方向(:13)、Phase 2 核心设定(:54)、Phase 3 卷级大纲(:115)三节齐备。
- **对标书路径查找规则内联**：`workflow-build.md:5`「优先 `{项目}/对标/{书名}/`，不存在则回退 `拆文库/{书名}/`；排除同名或来源指向当前正文的 `对标/{当前书}/`」。
- **开新卷消费卷复盘方向候选语义正确**：`workflow-build.md:196`「读 `大纲/卷复盘_第X卷.md` 的『下卷方向候选』（1-3 个，含各候选的主推线/战果线、与终局储备的衔接）」，与 write 侧 `volume-review.md` 第 3 步「产出下卷方向候选 1-3 个（含各候选的主推线/战果线、与终局储备的衔接）」口径一致（`skills/moshu-write/references/volume-review.md:16`）。

### 1.1.3 references/revision-workflow.md — 【通过】

- **五步流程可执行**：①影响分析(:9)→②作者裁决(:21)→③留痕落盘(:27)→④级联标记(:38)→⑤结束与回流提示(:50)，每步指令具体、可落地。
- **变更日志追加一行格式**：`revision-workflow.md:33` `- YYYY-MM-DD | {对象} | {变更一句话} | {影响范围摘要}`，append-only（:30「不覆盖不改历史」）。
- **stale 标记格式**：`revision-workflow.md:43` `<!-- stale: 设定修订 YYYY-MM-DD {对象} -->`，与任务要求格式逐字一致。
- **回流入口引用真实存在**：`review_tickets.py list --project {项目根} --status open`(:58)——`skills/moshu-review/scripts/review_tickets.py` 确有 `list` 子命令且支持 `--project`（必填）+ `--status` 过滤（`review_tickets.py:265-267`），findings 含 `dimension` 字段可做设定类过滤（`review_tickets.py:105`）；`追踪/上下文.md` 的 `## 连贯性风险` 与 `追踪/信息差.md`(:59)为运行时产物路径，与 write 侧追踪事务渲染一致（`skills/moshu-write/scripts/tracking_commit.py:701,1164`）。

### 1.1.4 scripts/impact_scan.py — 【发现（阻断）】

**边界语义与退出码、零写入、JSON 字段均正确**：
- 未写细纲 = 章号 > boundary（读 `大纲/细纲_第*章.md`）：`impact_scan.py:57-68`（`chapter <= boundary: continue`）；已写正文 = 章号 ≤ boundary（读 `正文/第*章*.md`）：`impact_scan.py:71-82`（`chapter > boundary: continue`）。
- 追踪四域：`impact_scan.py:22` `("characters","foreshadow","timeline","information_gaps")`，与 tracking_commit.py 的 state 键名逐字一致（`tracking_commit.py:870-871,905-908`）。
- 退出码：0=分析完成含无命中（`impact_scan.py:142`），2=未初始化/读失败/参数错（`impact_scan.py:126-127`），读失败三分类「缺文件/内容坏/类型错」（`impact_scan.py:29-37`）。
- 零写入：grep `open(|write_text|.write|unlink|mkdir` 于 impact_scan.py 无命中；仅 `read_text`(:31,48) + `json.dumps`/`print`。
- 输出 JSON 字段与断言逐字段一致：`keywords/{kw}/unwritten_outlines|written_chapters|tracking_hits` + `boundary_chapter`（`impact_scan.py:129-141`）↔ `scripts/test-impact-scan.py:126-148`；命中对象 `{file,line}`（:53）与 `{domain,key}`（:97）↔ 断言 `unwritten[0]["file"]`/`["line"]`/`tracking hit domain/key`（`test-impact-scan.py:131-139`）。

**阻断发现 F1.1**：`impact_scan.py` 缺 UTF-8 stdout/stderr 重配，中文 Windows 下 `test-impact-scan.py` 全量失败。证据：
- `impact_scan.py` 全文无 `sys.stdout.reconfigure(encoding="utf-8")`，而 `skills/moshu/scripts/next_step.py:32-36` 有同款重配（grep `reconfigure` 于 `skills/**/*.py` 仅命中 `deploy.py`/`next_step.py`/`check_chapter_summary.py`/`chapter_boundary.py`，impact_scan.py 与 tracking_commit.py 均缺）。
- 实测 `python scripts/test-impact-scan.py` → `FAILED (errors=3)`，根因 `_readerthread` `UnicodeDecodeError`（字节 `0xb2/0xc3/0xb6` = GBK）：子进程 stdout/stderr 用 locale 编码（GBK）输出中文 JSON/报错，与测试端 `encoding="utf-8"`（`test-impact-scan.py:119,154`）不匹配。
- 对照实验：同 harness、同 `encoding="utf-8"` 的 `test-next-step.py` 因 next_step.py 有重配而 12/12 通过——证明是 impact_scan.py 缺重配所致，非环境问题。
- 修复：向 `impact_scan.py` 头部补 `sys.stdout/stderr.reconfigure(encoding="utf-8")`（对齐 next_step.py:32-36）。

---

## 1.2 outline-workflow.md 完整性

旧文件 `git show 8f049da:skills/moshu-write/references/workflow-setup.md` 与 `skills/moshu-write/references/outline-workflow.md`（112 行）逐段对照：

| # | F1 清单段（旧 workflow-setup.md 行） | 新 outline-workflow.md 落点 | 结论 |
|---|---|---|---|
| ① | 细纲节（旧 :184-250：大纲安全七检/安全审查/每章必建/分批建纲/细纲模板代码块） | 七检 :11、安全审查 :13、每章必建 :15、分批建纲 :17-26、模板代码块 :27-75 | ✅ 等量 |
| ② | 大纲锁定写作侧语义（旧 :252） | :77（前 10 章细纲锁定 + 卷纲锁定定义全文） | ✅ |
| ③ | 中途补纲/扩纲小流程（旧 :254-259） | :79-84（5 步，第 2 步新增「扩卷/开新卷走 /moshu-build」） | ✅ |
| ④ | 细纲质量要求（旧 :261） | :86 | ✅ |
| ⑤ | 细纲钩子来源（旧 :263） | :88 | ✅ |
| ⑥ | 章节标题规则（旧 :265） | :90 | ✅ |
| ⑦ | 细纲后设定补全（旧 :267-272） | :92-97（角色/势力/世界观三规则 + 增量补充不覆盖 + Q7 边界） | ✅ |
| ⑧ | 前 3 章 opening 加载（旧 :279） | :99 | ✅ |
| ⑨ | architect 细纲批段（旧 :281-292） | :101-112（含「契约摘要必须原样附带」） | ✅ |

- **等量迁移核对**：七检全文（:11）、细纲模板字段清单（:30-74）、补全三规则（角色→`设定/角色/`、势力→`设定/势力/`、世界观规则→`设定/世界观/`，:93-95）、锁定定义（:77）逐条存在，无丢内容。
- **残留构建内容检查结论：无残留**。outline-workflow.md 全文不含 Phase 1 选题/对标发现、Phase 2 核心设定表、卷级大纲模板、「对标节奏回流」（这些已正确留/移入 `workflow-build.md`）；细纲节里也不含旧 Phase 3 的「大纲完成后 artifact 创建（大纲.md/卷纲/追踪 init）」段。
- **头注存在**：`outline-workflow.md:3-5`「开书构建已移 /moshu-build + 本文件只管细纲 + Q7 增量建档边界（只新建不修改，改既有→/moshu-build 修订）」。
- **链接自查**：`outline-workflow.md` 内引用的 `opening-design.md`(:99)、`outline-methods.md`(:17)、`outline-structure-theory.md`(:11,38,81,86)、`beat-cards.md`(:39)、`character-basics.md`(:93)、`reader-contract-and-progression.md`(:11,110)、`emotional-methods.md`(:13)、`workflow-daily.md`(:80,93) 均在 `skills/moshu-write/references/` 下真实存在。

---

## 1.3 副本新鲜度

- **shared-assets.json 组数**：实测 71 组，其中 `genre-prose-card-*` 32 组，与任务「71 组（含 32 个 genre-prose-card-*）」一致（`scripts/shared-assets.json`）。
- **build 侧副本字节一致抽查（MD5，source=write 侧）**：

| 文件 | 字节一致 | 时效结论 |
|---|---|---|
| outline-methods.md | ✅ | 无 workflow-setup 引用；「开书 Phase 1」指 build 仍保留的 Phase 1，非过期 |
| style-genre-modules.md | ✅ | 干净 |
| tracking-transaction.md | ✅ | 干净 |
| character-relations.md | ✅ | 干净 |
| reader-contract-and-progression.md | ✅ | 干净 |
| beat-cards.md / emotional-arc-design.md / emotional-methods.md / genre-core-mechanics.md / genre-prose-cards.md / idea-seed.md / reversal-toolkit.md | ✅ | 干净 |
| scripts/tracking_commit.py | ✅ | — |
| genre-prose-cards/（东方仙侠、都市高武、青春甜宠、豪门总裁、科幻末世 抽样 5） | ✅ | — |

- **时效 grep 结论**：build 侧副本 grep `开书在本 skill|在本 skill|Phase 1-3|workflow-setup|开书 Phase`——仅命中 `idea-seed.md:3,11,45` 与 `outline-methods.md:11` 的「开书 Phase 1/2」**单相**表述（指 build 侧 workflow-build.md 仍存在的 Phase 1/Phase 2，语义仍成立，非「Phase 1-3 在本 skill」类过期）；**零命中** `workflow-setup.md`、`开书在本 skill`、`Phase 1-3`。write 侧已删 workflow-setup 的引用在 `chapter-core.md`/`artifact-protocols.md`/`outline-methods.md`/`outline-structure-theory.md`/`volume-review.md` 中已全部改写为 `outline-workflow.md` 或 `/moshu-build`（git diff 8f049da..HEAD 核对，无悬挂引用）。
- **moshu-setup/agent-references/ 副本新鲜度**：抽查 outline-methods / style-genre-modules / character-relations / reader-contract-and-progression / beat-cards / emotional-methods / reversal-toolkit / genre-core-mechanics / opening-design 共 9 份与 write 源字节一致；`tracking-transaction.md` 在 setup agent-references 下不存在（shared-assets.json 中该组不含 setup target，属预期，非缺失）。

---

## 1.4 路由五方 + next_step 判定序

### 1.4.1 五方逐方证据

| 方 | 证据 | 开书语义 |
|---|---|---|
| moshu/SKILL.md 路由表+判定表 | 路由表 :14 `开书/建设定→/moshu-build`；判定表 :50 序3「有书但无正文→开书 /moshu-build」 | 指向 build ✅（但序3 见 F1.2） |
| next_step.py | S1→`moshu-build`(:139)、S6→`moshu-build`(:234)、S3→`moshu-write`(:186)、S5→`moshu-write`(:226) | S1/S6 build、S3/S5 write ✅（S2 见 F1.2） |
| scripts/test-next-step.py | test_s1 断言 `moshu-build`(:69)、test_s6 断言 `moshu-build`(:121-122) | 断言与代码一致 ✅（覆盖缺口 F1.5） |
| README.md | :91 moshu-build 行、:103「帮我开书→moshu-build（细纲与正文→moshu-write）」 | ✅ |
| README_EN.md | :91 moshu-build 行、:102「帮我开书→**moshu-write**」 | ❌ 滞后（F1.3） |
| .claude-plugin/marketplace.json | :30-50 moshu-build 条目（name/version/description/skills 指向 `./skills/moshu-build`） | ✅ |

grep 结论：开书语义在 moshu 路由表、next_step、test-next-step、README.md 四方均不指向 write；仅 README_EN.md:102 残留「帮我开书→moshu-write」，另 moshu-write 自身 frontmatter 触发词仍列「帮我开书」「写大纲」（F1.4，正文场景表已正确转 build）。

### 1.4.2 判定序推演（读代码 + 手动 fixture 实测，fixture 自删）

| 态 | 期望（代码/判定表） | 实测 step/skill | 一致 |
|---|---|---|---|
| P1（.story-deployed 缺） | S0 → setup | S0 / moshu-setup | ✅ |
| P2（拆文 _progress 未 completed） | INTERRUPT analyze | （test-next-step.py:146-155 覆盖）| ✅ |
| P3（.moshu-review/state.md 非空） | INTERRUPT review | （test-next-step.py:157-166 覆盖）| ✅ |
| S1（无 正文/大纲/追踪） | 开书 → build | S1 / moshu-build | ✅（对应判定表 序2） |
| S2（有书但无正文/全空） | 写第 1 章 → **write**（next_step.py:158-167） | S2 / moshu-write | ⚠️ 判定表 序3 写「build」→ 矛盾（F1.2） |
| S3（下一章无细纲） | 补纲 → write | S3 / moshu-write | ✅（对应判定表 序4，语义一致） |
| S4（下一章有细纲） | 日更 → write | S4 / moshu-write | ✅（对应判定表 序5） |
| S5（卷末、无卷复盘） | 卷复盘 → write | S5 / moshu-write | ✅（对应判定表 序6 前半） |
| S6（卷末、有卷复盘） | 下卷规划 → build | S6 / moshu-build | ✅（对应判定表 序6「下卷规划转 build」） |

- 判定表「有正文但无细纲→补纲 write」与 next_step S3 一致（`skills/moshu/SKILL.md:51` ↔ `next_step.py:179-188`），无矛盾；唯「有书但无正文」一态存在脚本/判定表分叉（F1.2）。

---

## 发现汇总

| 编号 | 级别 | 证据 | 影响一句话 |
|---|---|---|---|
| F1.1 | **阻断** | `skills/moshu-build/scripts/impact_scan.py`（全文无 stdout/stderr UTF-8 重配，:126/:141 print）；对照 `skills/moshu/scripts/next_step.py:32-36`；实测 `python scripts/test-impact-scan.py` → errors=3（UnicodeDecodeError，GBK 字节）；`scripts/test-impact-scan.py:119,154` 用 utf-8 | 中文 Windows（GBK 区域）下 impact_scan 回归测试必挂，且 UTF-8 型 harness/agent 解析其输出会失败，冲击 B1a 新能力；需补重配三行（对齐 next_step.py） |
| F1.2 | 需修（低） | `skills/moshu/SKILL.md:50`（序3「有书但无正文→/moshu-build」）vs `skills/moshu/scripts/next_step.py:158-167`（last==0→S2→moshu-write）；实测 S2 fixture→moshu-write | 脚本优先故影响小，但 `moshu/SKILL.md:42` 宣称「判定语义与脚本一致」不成立，Python 缺失回退判定表时会误路由回 build |
| F1.3 | 需修（低） | `README_EN.md:102`「帮我开书→moshu-write」vs `README.md:103`「→moshu-build」 | 英文 README 开书路由滞后，与自身 :21「five-way consistency」自述矛盾 |
| F1.4 | 候选 | `skills/moshu-write/SKILL.md:4` 触发词仍列「帮我开书」「写大纲」（正文场景表 :45 已正确转 build） | write frontmatter 与路由语义措辞歧义，建议清掉或标注「开书转 build」 |
| F1.5 | 候选 | `scripts/test-next-step.py:62,69,121` 仅断言 S0/S1/S6 的 suggested_skill，S2/S3/S5（:83-109）未断言 skill | 回归覆盖缺口：S2/S3/S5 的 write 语义无断言守护，建议补 3 条断言 |

---

### 分级统计
- **阻断 1**（F1.1）
- **需修 2**（F1.2、F1.3，均低优先）
- **候选 2**（F1.4、F1.5）
- 存疑 0

### 是否有阻断级
**有 1 条阻断级（F1.1）**：`impact_scan.py` 缺 UTF-8 stdout/stderr 重配，导致 `scripts/test-impact-scan.py` 在中文 Windows（本仓库主目标平台）全量失败，建议 v1.5.0 发布前修复（补 3 行重配即可，与 `next_step.py:32-36` 对齐）。其余发现均不阻断。
