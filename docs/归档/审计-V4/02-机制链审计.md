# 02 机制链审计（审计-V4 第2部分）

- 审计基线：HEAD hash = `3391fa3ab4747b1b35136c5e358416ecfb42d6da`
- 审计范围：B1 引入/影响的六条机制链（文档指示 ↔ 脚本能力双向核对）
- 审计方式：只读；实测命令在 `.tmp/audit-v4/` 临时验证区执行，用后已自删
- 铁律遵守：未修改任何仓库文件、未读 `docs/归档/` 与 `otherMaterials/`、未执行 git 写操作；本文件为唯一写入产物

---

## 链①：修订工作流链（build）

**判定：闭合**

### 文档端证据（skills/moshu-build/references/revision-workflow.md）
| 步骤 | 执行入口 | 证据 |
|---|---|---|
| ① 影响分析 | 运行 `impact_scan.py` | revision-workflow.md:13-14（命令 `{PYTHON} {build skill 根}/scripts/impact_scan.py --project ... --keyword ...`）；:17-19（三清单 + 退出码 0/2 语义） |
| ② 作者裁决 | AskUserQuestion（不拦截） | revision-workflow.md:23-25 |
| ③ 留痕落盘 | 追加 `大纲/变更日志.md` 一行 | revision-workflow.md:29-36 |
| ④ 级联标记 | 文件头追加 `<!-- stale: 设定修订 YYYY-MM-DD {对象} -->` | revision-workflow.md:40-48 |
| ⑤ 结束与回流 | 报告留痕 + 回流入口读工单/风险 | revision-workflow.md:50-61 |

### 脚本端证据
- `skills/moshu-build/scripts/impact_scan.py` 存在（146 行），argparse 定义 `--project`（required）+ `--keyword`（append, required）——impact_scan.py:101-110；退出码 0=分析完成、2=追踪未初始化/参数/读文件错误——impact_scan.py:125-127、142。

### 双向核对
1. **命令路径一致**：文档 `{PYTHON} {build skill 根}/scripts/impact_scan.py` ↔ 真实路径 `skills/moshu-build/scripts/impact_scan.py`，一致 ✓
2. **变更日志路径**：`大纲/变更日志.md` 是项目产物（revision-workflow.md:30），文档说明即可，非仓库文件 ✓
3. **stale 标记格式**：`<!-- stale: 设定修订 YYYY-MM-DD {对象} -->`（revision-workflow.md:43）与链⑥ write 消费前缀一致 ✓
4. **build 执行 / write 不执行**：revision-workflow.md:11-14 指示 build 侧跑 impact_scan；outline-workflow.md 全文无 impact_scan 命令，仅在 outline-workflow.md:5 声明"修改既有构建资产 → 转 /moshu-build 修订工作流（构建侧 impact_scan 影响分析）"，write 不修订、不调用 impact_scan ✓

### 实测（已自删）
构造临时项目（`last_committed_chapter=3`，细纲第4/5章 + 第2章，正文第2/3/4章，追踪 characters 域含关键词），运行 `python skills/moshu-build/scripts/impact_scan.py --project <临时项目> --keyword 玄清`：
- `exit=0`，输出 `unwritten_outlines` 仅命中 细纲_第4章（章号 4>3），未命中 细纲_第5章（无关键词）、细纲_第2章（2≤3 排除）；`written_chapters` 仅命中 正文第2/3章（≤3），未命中 正文第4章（>3）；`tracking_hits` 命中 characters/C1；`boundary_chapter=3` —— 三清单边界正确 ✓
- 无命中关键词 → `exit=0`（空数组，分析工具非判定器）✓
- 追踪 `_tracking-state.json` 缺失 → `exit=2` + 明示报错 ✓
- `last_committed_chapter` 非整数 → `exit=2` ✓

---

## 链②：开书→细纲接力链（build→write）

**判定：断裂**（字段级：`单元ID` build 未产出但 write 必读）

### 双向核对表
| 字段/产物 | build 产出证据 | write 消费证据 | 判定 |
|---|---|---|---|
| 设定/关系.md | workflow-build.md:103 | （结构档案，write 按需读） | ✓ |
| 设定/题材定位.md（终局底牌/升级台阶） | workflow-build.md:104 + reader-contract-and-progression.md:47-48（开书列进题材定位「终局底牌与升级台阶」小节） | outline-workflow.md:80（读题材定位「终局底牌与升级台阶」） | ✓ |
| 设定/题材正文提示卡.md | workflow-build.md:105 | （正文层提示卡） | ✓ |
| 大纲/大纲.md | workflow-build.md:180 | outline-workflow.md:80（定位当前卷） | ✓ |
| 大纲/卷纲_第X卷.md · 剧情单元卡 | workflow-build.md:181 | outline-workflow.md:20、34 | 部分 ✓（见下） |
| —— 对标剧情参照 | workflow-build.md:156 | outline-workflow.md:20、88 | ✓ |
| —— 结构分布/情节点索引 | workflow-build.md:156 | outline-workflow.md:20 | ✓ |
| —— 单元节拍/章功能分配 | workflow-build.md:156 | （细纲字段「单元内第几拍/承担功能」outline-workflow.md:34） | ✓ |
| —— **单元ID** | **未出现**（build 全部文件 grep `单元ID` 零命中；workflow-build.md:156 字段模板未列） | **必读**：outline-workflow.md:34「单元ID/位置：{卷纲剧情单元ID}」、chapter-core.md:11、workflow-daily.md:44 | ✗ **断裂** |
| 追踪/_tracking-state.json · last_committed_chapter | workflow-build.md:182（tracking init） | chapter-core.md:15、20；workflow-daily.md:19 | ✓ |
| 追踪/上下文.md 续写状态卡 7 栏（当前位置/长期约束/核心角色状态/活跃伏笔/近三章速记/下一章承诺/连贯性风险） | workflow-build.md:182 + tracking_commit.py:36-42（固定 7 栏） | outline-workflow.md:80；workflow-daily.md:34 | ✓ |
| 伏笔/时间线视图 | workflow-build.md:182（空的伏笔/时间线视图） | write 侧事务维护 | ✓ |

### 关键证据（单元ID 缺口）
- build 卷纲剧情单元卡字段模板仅列「单元节拍/章功能分配、对标剧情参照、结构分布/情节点索引」——workflow-build.md:156；全 build 目录 `grep 单元ID` 零命中。
- write 细纲模板把「单元ID/位置」列为每章必填字段并指向「卷纲剧情单元ID」——outline-workflow.md:34；写前从卷纲读「单元ID/位置」——chapter-core.md:11、workflow-daily.md:44。
- 完整剧情单元卡模板（含「单元ID：{L卷号-序号}」，卡片头 `### 剧情单元 {L卷号-序号}`）只在 write 侧 `artifact-protocols.md:294-295`，build 侧 workflow-build.md 未引用该模板（:156 仅指向 reader-contract-and-progression.md 的契约/推进规则）。

> 影响一句话：B1b 把卷纲构建移交给 build 后，build 的卷纲字段模板漏了 `单元ID`，导致 write 建细纲时可能拿不到「单元ID/位置」锚点。write 侧 artifact-protocols.md 仍保留完整模板可自补，故实际不阻断，但属 build 产出物与 write 消费物之间的字段级断裂。

---

## 链③：审稿令牌链（v1.4.0 修复后）

**判定：闭合**

### 四环核对
| 环 | 证据 | 判定 |
|---|---|---|
| ① 生成令牌 | review-workflow.md:180「spawn 前先生成本轮唯一的 8 位令牌 `{token}`（大写字母+数字）…写入 `审稿令牌：{token}`；同一批次四个 Agent 共用」 | ✓ |
| ② 四 spawn 注入 | review-workflow.md 四个 spawn prompt 首行均 `审稿令牌：{token}`：Agent1=:187、Agent2=:223、Agent3=:253、Agent4=:285；4 个 reviewer 模板均含「## 审稿令牌」节且措辞一致：moshu-architect.md:23-25、moshu-character-designer.md:21-23、moshu-narrative-writer.md:26-28、moshu-consistency-checker.md:24-26 | ✓ |
| ③ 采纳前比对 | review-workflow.md:329「采纳前逐字比对令牌…取每份 reviewer 报告第一行与本轮 `{token}` 逐字比对，不等或首行为 `审稿令牌：缺失` → 不采纳，按原 prompt 重跑一次」 | ✓ |
| ④ 落盘 verify-token | review-workflow.md:332「`review_tickets.py verify-token --ticket <落盘工单> --token {token}` 必须输出 `token ok`（退出 0）」；脚本端 verify-token 子命令存在——review_tickets.py:240-247、269-271 | ✓ |

### 实测
- `review_tickets.py --help` → `exit=0`，子命令 `{write,resolve,list,verify-token}`。
- `verify-token --ticket <token=A7K2M9QX 的工单> --token A7K2M9QX` → `exit=0`，stdout `token ok`。
- `verify-token ... --token XXXXXXXX` → `exit=2`，stderr `ERROR: token mismatch`。

> 结论：四环（生成→注入→采纳前比对→落盘校验）文档指示与脚本能力全部闭环。

---

## 链④：schema 消费方链

**判定：闭合**（无消费者钉死旧版本）

### 枚举结果
| 消费者 | 版本常量/断言 | 证据 | 与当前是否一致 |
|---|---|---|---|
| tracking_commit.py（4 副本：write/import/review/build） | `INPUT_SCHEMA_VERSION = 2`、`TRACKING_SCHEMA_VERSION = 5` | 各副本 :26-27 | 一致（4 副本 SHA256 全同） |
| hooks story_hook_core.js | 钉 `document.schema_version !== 5` | story_hook_core.js:118-119 | 一致（=5，当前值） |
| evals/scenarios/开书/README.md | 断言 `schema_version: 5` | README.md:22 | 一致（=5） |
| current-contract.json | `progress_schema_version: 2` | current-contract.json:6 | 一致（progress 续跑契约，对应 analyze/import 管线 `_progress.md`，非追踪 schema） |
| tracking-transaction.md（4 副本） | 事务/init 输入 `schema_version: 2` | 各副本 :44、:72 | 一致（=INPUT_SCHEMA_VERSION=2） |
| analyze 管线 `_progress.md` | `schema_version: 2` | chapter_boundary.py:235、pipeline-ops.md:15/47/53、analyze-workflow.md:117/119、import-workflow.md:104/173 | 一致（=progress_schema_version=2） |
| review_tickets.py | `SCHEMA_VERSION = 1`（工单 schema，独立域） | review_tickets.py:27、:140 | 独立域，自洽 |
| capability-wiring.json（守卫） | 断言 hook `must_contain "schema_version !== 5"` | capability-wiring.json:95 | 一致 |

### 实测
- 4 个 `tracking_commit.py` 副本 `Get-FileHash -Algorithm SHA256` 结果全部 = `B30572ABF0AE27DC4DD3FCEAEC07111DFDDC4A1F049C1E4D51204E2AD2C3F5E6`（字节一致）。

> 结论：追踪 schema 当前值 = 5（TRACKING_SCHEMA_VERSION），hook/evals 断言均为 5；progress schema = 2（analyze 续跑契约）；工单 schema = 1（独立）。无任何消费者钉死旧追踪版本。

---

## 链⑤：工单链（write 大修）

**判定：闭合**

### 文档端（skills/moshu-write/references/workflow-revision.md）
| 指示 | 证据 |
|---|---|
| 读 open 工单 | workflow-revision.md:81「`review_tickets.py list --project {项目根} --status open`——待办唯一读点」 |
| 处置 resolve（fixed/dismissed + 一句证据） | workflow-revision.md:82「`review_tickets.py resolve --project {项目根} --ticket <文件> --id T001 --status fixed|dismissed --note "<证据一句>"`；不得绕过 resolve 手工编辑」 |
| 复审闭环 | workflow-revision.md:83「全部 open 项处置后重跑 review，只验证 open→fixed 项是否真的修好」 |

### 脚本端（skills/moshu-review/scripts/review_tickets.py）
| 子命令 | 证据 |
|---|---|
| list | review_tickets.py:265-267（`--project` + `--status` 过滤） |
| resolve | review_tickets.py:258-263（`--project/--ticket/--id/--status/--note`，open→fixed/dismissed 单向） |
| write | review_tickets.py:254-256（`--project/--input`，schema 校验） |
| verify-token | review_tickets.py:269-271（`--ticket/--token`） |

### 双向
- 文档指示的 `list`/`resolve` 与脚本子命令名一致；`write`/`verify-token` 由 review-workflow.md Phase 4（:331-332）指示，脚本均有对应子命令。`--help` 实测子命令集 `{write,resolve,list,verify-token}` 与文档完全对应 ✓

> 附带观察（非断裂，标【存疑】）：`review_tickets.py` 单一副本位于 `skills/moshu-review/scripts/`（glob 全仓仅此一份，且 `scripts/shared-assets.json` 未将其列为共享副本），而 write 侧 workflow-revision.md 以无路径前缀 `review_tickets.py` 引用——跨 skill 定位依赖运行时 skill 搜索路径，文档未显式标注来源路径。

---

## 链⑥：stale 链（build 写 → write 消费 → 移除）

**判定：闭合**

### 三方一致核对
| 方 | 格式字符串 | 证据 |
|---|---|---|
| ① build 写格式 | `<!-- stale: 设定修订 YYYY-MM-DD {对象} -->` | revision-workflow.md:43 |
| ② write 消费格式 | 识别标记前缀 `<!-- stale:`（「写前若细纲文件头含 `<!-- stale:` 标记」） | chapter-core.md:159 |
| ③ 移除指示 | 「处理后在文件头移除该标记」 | chapter-core.md:159 |

- 前缀 `<!-- stale:` 在 build 写格式与 write 消费格式中完全一致 ✓
- 移除指示与消费句同句（chapter-core.md:159）✓

> 附带说明（非断裂）：chapter-core.md:159 同段还存在 write 侧独立的失效标记 `<!-- 受影响: {角色/对象}（正文第{N}章变化） -->`，与 build 的 `<!-- stale:` 标记并存。stale 消费句明确只认 `<!-- stale:` 前缀，两种标记职责不同（前者=正文变化导致细纲失效，后者=构建环修订留痕），不构成前缀不一致。

---

## 发现汇总

| 编号 | 级别 | 链 | 证据 | 影响一句话 |
|---|---|---|---|---|
| F2.1 | 需修 | 链② | build 卷纲剧情单元卡字段模板（workflow-build.md:156）未列 `单元ID`；write 细纲必读该字段（outline-workflow.md:34、chapter-core.md:11、workflow-daily.md:44）；build 全目录 `grep 单元ID` 零命中 | B1b 卷纲移交 build 后，build 产出物漏了 write 必读的 `单元ID` 字段，细纲「单元ID/位置」锚点可能悬空 |
| F2.2 | 候选 | 链⑤ | `review_tickets.py` 单副本仅存 `skills/moshu-review/scripts/`（glob 唯一命中，shared-assets.json 未列为共享副本）；write 侧 workflow-revision.md:81-82 以无路径前缀引用 | write 大修引用工单脚本时跨 skill 定位无显式路径标注，依赖运行时搜索路径 |

### 发现分级统计
- 阻断：0
- 需修：1（F2.1）
- 候选：1（F2.2）
- 存疑：0（链⑤ 的跨 skill 定位观察已并入 F2.2，作候选级）
