# 批 6 规格：U6 文风负向约束 + U7 体验层

> 状态：待实施 | 前置：批 3（U6/U7 与 schema 无强依赖，批 3 完成后可与批 5 并行） | 预计量：文风模板 + 备料结构 + 收尾规范 | 风险：低
> 依据：SparkArc `style_analysis.yaml`（作者回避内联抽取/语体档位/脱敏短例）；writing-dna `SKILL.md`（L1 口径/什么不写/≤4000 字）；v7 `prep/index.js` + `v7-prd.md:200`（备料七组件）、`knowledge/index.js`（候选 ≤3）、`util/degradation.js`；v6 skills 收尾规范（三段式）
> **v2 修正**：五段任务书/BM25 不再作为 v7 依据（备料结构 + 关键词打分替代）；语体档位并入本批。

## 1. 目标（两个无依赖单元同批）

- **U6**：文风库补负向约束（作者回避）+ 稳态/高潮语体档位 + L1 统计口径扩充。
- **U7**：写前备料结构化组装；建议候选制；失败显式降级；agent 边界写死。

## 2. 现状事实

- `skills/moshu-style/SKILL.md` + `references/style-learn-sop.md`：文风产出规范（整体语感/对话技法/锚点/分层建议），**无回避节/无语体档位**；锚点 4 段可回查（mo-shu 优势保留）。
- `文风库/文风.md` 无字数上限。
- `workflow-chapter.md` 步骤 3 指令确认 = 现有任务书形态（非五段）。
- explorer/consistency-checker 模板边界靠 prompt（P7）。
- 拆书模板选择/题材卡/对标推荐现状=单模板复用（无候选制）。

## 3. 文件级改动清单

### U6（文风）
| # | 文件 | 改什么 |
|---|---|---|
| 1 | `skills/moshu-style/references/style-learn-sop.md` | 产出规范补三节：①「作者回避」——分析时内联抽取，每条必须有原文锚点行号（≥3 条，mo-shu 自定；记录"有例证才记"纪律）②「稳态 vs 高潮态语体」——高潮态特征（句子骤短/变长、修饰骤增/脱落）+ 档位切换指引 ③ L1 统计口径扩充：破折号 vs 括号比、引号使用场景、小标题字数、中英混用、段落平均句数 |
| 2 | 同上 | 副词噪声过滤（"过度副词=噪声"，阈值 mo-shu 自定：样本词频前 20% 副词 + 每千字限次，验收时实测校准）+ "什么不写"反向归纳（从语料缺失推排斥规则，改造为写法/语气排斥） |
| 3 | `skills/moshu-style/SKILL.md` | 蒸馏预算：`文风库/文风.md` ≤4000 字（"过长=没蒸馏干净"），超限判定=产出打回 |
| 4 | `skills/moshu-setup/references/templates/agents/moshu-narrative-writer.md` | 写前召回补：回避约束 + 语体档位（按本章情绪强度选择） |
| 5 | `scripts/test-style-structure.py`（新建） | 文风.md 结构校验：回避节每条有锚点、语体节存在、字数上限 |

### U7（体验层）
| # | 文件 | 改什么 |
|---|---|---|
| 6 | `skills/moshu-write/references/workflow-chapter.md` | 步骤 3 升级为**备料七组件**（v7 式，脚本组装 `写作材料.md` ≤12KB）：全书近况/要写到的事/事实切片/信息差边界/近章结尾/反复读清单/文风锚点；五段式描述仅保留为写作 agent 的任务输入结构 |
| 7 | `skills/moshu-write/scripts/`（或复用 prep 逻辑新建 `assemble_materials.py`） | 备料组装脚本（确定性拼接，对照 v7 prep/index.js） |
| 8 | `skills/moshu-setup/references/templates/agents/moshu-explorer.md` | 边界写死段："你只做检索，不做推理核对"；检索增强=关键词打分（名称精确 +100/含名称 +30/词项 +5~20 + 近期软降权）优先，BM25-lite 可选 |
| 9 | `skills/moshu-setup/references/templates/agents/moshu-consistency-checker.md` | 边界写死段："你只做推理核对，不做自由检索" |
| 10 | `skills/moshu-analyze/references/*workflow.md` + 题材卡/对标推荐相关引用 | 候选制 ≤3 条 + 未命中降级声明（"无匹配模板，走通用框架"） |
| 11 | 各 skill 收尾规范 | 三段式报告（总状态/产生的文件/问题与下一步）——**v6 收尾规范延续，标注 mo-shu 自定**；**degradation 三分类**（对照 v7 `util/degradation.js`：有损/良性/合理吞错）——有损事件进 `degraded` 数组，消费方（写作/审查 agent）必须区分"没有数据"与"读取失败后的残缺数据"：degraded 涉及的板块一律视为"无法核实"而非"确认无问题"，不得据此断言一致（v7 `roles/事实审查.md:10` 纪律原文移植） |
| 12 | `skills/moshu-review/SKILL.md` + `skills/moshu-deslop/SKILL.md` | B5 执行方案命名：lean/full、检查/修复显式化为命名方案 |

## 4. 验收

```bash
python scripts/test-style-structure.py && bash scripts/static-check.sh && bash scripts/check-behavior-contracts.sh
# U6 冒烟：/moshu-style 生成 → 文风.md 含回避节（每条带锚点行号）+ 语体档位节 + ≤4000 字
# U7 冒烟：写一章 → 写作材料.md 七组件齐全且 ≤12KB；无匹配题材 → 明确降级声明；读文件失败 → degraded 标记
```

## 5. 回滚

模板/规范 revert；存量项目重部署即回。

## 6. 禁止事项

- 文风回避条目不写"无锚点"的条目（防幻觉，SparkArc validator 已删除的前车之鉴）
- 备料不得进 doc-budget 热路径超支（≤12KB 显式预算）
- explorer/checker 边界声明之外不引入新职责

## 7. 提交规范

```
feat(U6): 文风负向约束——作者回避（锚点例证≥3）+ 稳态/高潮语体档位 + L1 统计口径扩充 + ≤4000 字蒸馏预算
feat(U7): 体验层——v7 式备料七组件 + 候选制≤3/降级声明 + degraded 标记 + 三段式收尾 + explorer/checker 边界写死
```
