# 批B98：审计四期整改批（N1-N3 修复+两立案收口+PRD 轻量 lint）

> **目的**：整改审计-四期 v1 的三处需修（N1-N3，全文档级）与两条立案（本地/CI 验收面漂移→一键回归脚本；grep 假绿机制→防假绿规则），顺带以轻量 lint 收口三次辗转的 C4/C6（PRD 漂移守卫化，作者裁定降级）。**零阻断审计后的标准整改小批。**
>
> **审计出处**：docs/治理/审计-四期-v1.md（2026-09-01，零阻断/需修 3/候选 6）。

## 现状事实（施工前逐条复核）

1. **N1**：.github/ISSUE_TEMPLATE/feature_request.md:15 与 bug_report.md:15 技能枚举含 `moshu-deslop`（B95 已删技能）——B95/B96 验收 grep 域名漏 ISSUE_TEMPLATE，审计 v2.1「独立重跑」规则首用抓到。
2. **N2**：README.md:164 与 README_EN.md:161 产物树仍列 `理想书评.md`（B94 删 ideal-review-template.md 后未同步）；同树题材定位行无标尺节表述。
3. **N3**：skills/moshu-write/SKILL.md:10 正文段落残留「单章写作、日更续写、大修」旧三车道措辞（description 已 B95 更新，正文漏改）。
4. **test 套件形态（实测）**：scripts/ 下 39 个 test 文件——`test-*.py`/`test_*.py` 30 个（抽查三个均带 `__main__` 可直跑）、`test-*.sh` 7 个（bash）、`test-*.js` 5 个（node）。CI 枚举执行，本地无一键全跑（立案一证据）。
5. **已知 Windows 假红**：test-shared-assets.py 的 mode-drift（chmod no-op 类，审计法 v1.3 平台假红清单第二类，CI Linux 为准）。
6. 盲区清单 §1 现为 14 行，无防假绿条目。
7. 规格 README 无「验收须含回归套件」条款（7f99404 教训：B94-B96 仅守卫绿即闭环）。

## 文件级改动清单

### A. N1-N3 修复（4 文件 5 处）

**A1.** `.github/ISSUE_TEMPLATE/feature_request.md:15` 与 `bug_report.md:15`：技能枚举删 `moshu-deslop`（改 `[moshu-write / moshu-review / moshu-import / 其他]`）。
**A2.** `README.md:164`+`README_EN.md:161`：产物树删「理想书评.md」行；`设定/题材定位.md` 行注（含成品标尺节，B94 起）——英文版同义。
**A3.** `skills/moshu-write/SKILL.md:10`：正文段落术语对齐——「细纲补建/滚动、单章写作、日更续写、大修与卷复盘执行」→「细纲补建/滚动、逐章写作（K 参数）、改稿与卷复盘执行」；改后全文件再 grep「单章写作\|大修」确认无第二处残留（「日更」作触发词保留）。

### B. 立案一收口：本地一键回归（run-regressions.sh）

**B1. 新增 `scripts/run-regressions.sh`**：
- 跑法：三类 glob——`scripts/test-*.py`+`scripts/test_*.py`（python 探测链 python3→python→py，`-X utf8`+PYTHONIOENCODING）、`scripts/test-*.sh`（bash）、`scripts/test-*.js`（node，node 缺失时显式报缺不静默跳过）；
- **防假绿内建**：逐项打印「[i/N] 文件 … exit=码」，**不得 `2>/dev/null`**，stderr 透传；
- **SKIP 清单**：文件顶部显式数组+每项一行原因注释（首项=`test-shared-assets.py`，注「mode-drift Windows chmod 假红，审计法 v1.3 第二类，CI Linux 为准」）——跳过项在汇总显式列出，禁止静默扩；
- 汇总：总数/通过/失败/跳过各计数，任一失败 exit 1；
- **step 0 内嵌 PRD 轻量 lint（C4/C6 降级版，作者裁定）**：grep PRD 退役词（理想书评/虚拟对标/moshu-deslop/moshu dashboard），排除历史版本行（`v2.` 前缀行）与迁移注记行（含「收口/退役/已删」），命中即报——文件内注释声明「轻量 lint 非独立守卫（C4 三次辗转降级裁定 2026-09-01）」。
- **定位声明**（文件头注释+scripts/README）：本地全跑 CI 回归面的一次性入口，**不进 CI**（CI 保持枚举步骤不变——改 CI 为 wrapper 属另一批决策）。
**B2. `scripts/README.md`** 增索引行（含定位与用法）。
**B3. 规格 README 验收纪律补条款**：批次验收命令原则上须含 test-* 回归（或跑 `run-regressions.sh`），仅守卫（check-*）绿不算全绿——7f99404 教训（落点：验收相关节，施工时定位于 §7 或等效节）。

### C. 立案二收口：盲区清单防假绿条目

**C1.** 盲区清单 §1 表新增第 15 行：**残留/零引用类 grep 防假绿**——①不得 `2>/dev/null` 吞 stderr（ugrep 报错静默=假零，B96 施工+审核双假零实证）；②退出码显式判定（grep 无命中 exit 1≠零残留，须 `|| echo` 哨兵区分）；③目标名连字符/下划线双形态都查（`next_step` 匹配不到 `test-next-step.py`，7f99404 实证）；④域名含 `.github/ISSUE_TEMPLATE/`（审计四期 N1 实证）。

### D. 收尾

**D1.** 施工日志 B98 条目（含审计四期整改对照+SKIP 清单初始项声明）；审核记录待旗舰审核后追加。

## 禁止事项

1. 不改任何测试断言；SKIP 清单初始仅 test-shared-assets.py 一项，新增须带原因。
2. run-regressions.sh 不进 CI workflow（CI 枚举步骤零触碰）；不配自举回归测试（它本身是回归运行器，自举无意义——盲区 #12 过闸声明，验证=本批验收全跑）。
3. 不动 10 技能流程文件（A3 仅 SKILL:10 一行 prose）。
4. N1-N3 修复不得扩散为全文重写（最小 diff）。
5. 禁止 git add -A。

## 验收命令（Windows 探测链；零网络）

```bash
# 1. N1-N3 独立复跑（v2.1 规则——审核侧亦须亲跑）
grep -rn "moshu-deslop" .github/ ; echo "exit=$?（1=零命中）"
grep -n "理想书评" README.md README_EN.md | grep -v "收口\|退役"; echo "exit=$?"
grep -n "单章写作\|大修" skills/moshu-write/SKILL.md; echo "exit=$?（触发词「日更」不在检查列）"

# 2. 一键回归全跑（含 PRD lint）
bash scripts/run-regressions.sh; echo "exit=$?（0=全绿）"

# 3. 盲区 #15 在位
grep -c "防假绿" docs/治理/盲区清单.md

# 4. 索引与条款
grep -c "run-regressions" scripts/README.md
grep -c "run-regressions\|回归套件" docs/治理/规格/README.md

# 5. 固定守卫全套 + 第七查零 diff
bash scripts/check-story-numbers.sh && bash scripts/check-doc-budget.sh && bash scripts/check-shared-files.sh
git diff --stat -- skills/moshu-setup/references/templates/agents/
```

## 提交规范

单提交：`b98: 审计四期整改——N1 ISSUE_TEMPLATE×2 删 deslop 枚举+N2 README×2 产物树同步+N3 write SKILL:10 术语对齐+立案一 run-regressions.sh（本地一键回归+PRD 轻量 lint[C4 降级]）+立案二盲区 #15 防假绿四条+规格 README 验收纪律条款`

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（审计四期整改+两立案收口+C4 降级裁定） |
| 2 | 预算余量实测 | 通过——零热路径触碰（write SKILL:10 一行，+30 字内；flash 实测回填） |
| 3 | 涟漪面 | 通过——本批即涟漪面修复（ISSUE_TEMPLATE 域名正是盲区 #15④ 补的域）；run-regressions.sh 为 repo 级脚本无跨技能副本 |
| 4 | 共享副本声明 | 通过——零 shared-assets 组触碰 |
| 5 | 跨技能接口 | 通过——A3 仅 prose 术语，无语义接口变化 |
| 6 | 存量兼容 | 通过——纯增量+文档修 |
| 7 | 端到端走查 | 通过——验收 2 即全链走查（39 测试+lint） |
| 8 | 输出幂等性 | 通过——无时间戳工件（run-regressions.sh 输出进对话不落盘） |
| 9 | 验收可复现 | 通过——Windows 探测链零网络；**本批验收自身示范防假绿**（每条 grep 带 exit 哨兵） |
| 10 | 热路径字数报告 | 通过——零热路径 |
| 11 | token 成本核算 | 通过——零 spawn |
| 12 | 决策树过闸 | 条件——新脚本 1 个（run-regressions.sh）：repo 级非 CI 执行→不进 CI 三处同步（理由在文件头声明）；自举回归豁免（运行器自举无意义）；其余三问通过 |
| 13 | 部署面改动联动 | 通过——零 agent 模板变更（第七查） |
| 14 | 概念分离后术语考古 | 通过——N1-N3 即术语考古修复；盲区 #15 把形态差陷阱永久化 |
