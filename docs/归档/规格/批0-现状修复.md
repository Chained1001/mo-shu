# 批 0 规格：现状修复（文档事实对齐）

> 状态：待实施 | 前置：无 | 预计量：~0.5h | 风险：零（纯文档/数字修正）
> 依据：`docs/研究-v2/mo-shu-现状盘点.md` §8 P0/P2 清单

## 1. 目标

把仓库文档中的过时数字与事实漂移修正到与代码一致，跑通 flash 施工→守卫→提交的完整流程。**不改任何脚本逻辑、不改 marketplace、不碰 CHANGELOG 语义。**

## 2. 现状事实（盘点已核实，无需重摸）

- `skills/` 实际 **10 个** skill（含 moshu-style）；`.claude-plugin/marketplace.json` 已 10 plugin。
- `skills/moshu-scan/scripts/` 实际 **4 个** scraper（qidian/fanqie/jjwxc/qimao），刺猬猫已删（CHANGELOG v1.1.2）。
- 全仓 `references/*.md` 实测 **187** 个；`shared-assets.json` 实测 **36 组**（51 target + 36 source）。
- `scripts/doc-budget.json` 实测组上限与成员和偏差：正文 agent 组 43600 < 成员和 43650；开书组 22750 < 成员和 22800。

## 3. 文件级改动清单

| # | 文件 | 改什么 | 怎么改 |
|---|---|---|---|
| 1 | `README.md` | Skills 表补 `moshu-style` 行（10 行）；"适用平台"删刺猬猫（4 平台）；"189 份 references"→187 | 按现表格式补一行：`moshu-style /moshu-style 文风学习 · 文风库/文风.md 生成（句长/标点/对话技法/锚点）`；平台列表 `起点中文网 · 番茄小说 · 晋江文学城 · 七猫小说` |
| 2 | `README_EN.md` | 与 README 同三处修正 | 对齐英文表述 |
| 3 | `docs/architecture.md` | "9 个 Skill 入口"→10；§4 分层图补 moshu-style | 图内 Skills 节点与说明文字同步 |
| 4 | `scripts/README.md` | "9 个 skill"→10；"5 个 scraper"→4；"64 组共享 reference"→36 组/87 副本 | 数字全部对齐实测 |
| 5 | `scripts/doc-budget.json` | 正文 agent 组 43600→43650；开书组 22750→22800 | 保持成员不变，只改组上限；`_comment` 追加一行"批0 对齐实测（组上限必须 ≥ 成员和，防 CI 假失败）" |
| 6 | `skills/moshu-setup/references/templates/hooks/guard-outline-before-prose.sh` | 顶部注释如实声明 fail-open 语义 | 补注释块："阻断依赖 node（追踪检查点/毒句式/Bash 识别）；node 缺席时仅细纲门可拦，其余放行——这是已知降级语义，部署文档如实说明" |
| 7 | `README.md`（或 setup 部署说明处） | guard hook 的 node 依赖说明 | 在 hooks 表 `guard-outline-before-prose.sh` 行加注：阻断强度依赖 node，无 node 环境仅细纲门生效 |
| 8 | `docs/moshu-style-plan.md` | "学任意书/自己的风格"口径矛盾修复 | 找到"自己的风格"表述（约 419 行处），改为"以对标书文风为主；学自己作品文风列为可选观察项，暂不排期"——与《实施总纲》附录 C 不学清单对齐（审计 C11 裁决） |
| 9 | `CONTRIBUTING.md` | 仓库结构树补 moshu-style | §仓库结构 代码块内 skills/ 列表补 `moshu-style/ # 文风学习` 一行（当前 9 个，实际 10 个——同款 9vs10 漂移） |

## 4. 验收（flash 自查，全部通过才算完成）

```bash
# 1. 守卫全绿（仓库根）
bash scripts/static-check.sh
bash scripts/check-doc-budget.sh
bash scripts/check-shared-files.sh
bash scripts/check-claude-adapter.sh

# 2. 数字对齐抽查
grep -c "moshu-style" README.md          # ≥1（Skills 表行）
grep -n "刺猬猫" README.md || echo OK     # 应为 OK（0 命中；CHANGELOG 历史除外——只查 README）
grep -n "9 个" docs/architecture.md || echo OK
# 3. 预算上限 ≥ 成员和
python - <<'EOF'
import json
d = json.load(open('scripts/doc-budget.json', encoding='utf-8'))
# 断言每个组上限 >= 成员去空白字数之和（实现时按 doc-budget.json 实际结构写校验）
print('check ok')
EOF
```

## 5. 回滚

单提交，`git revert <commit>` 即完整回滚。

## 6. 禁止事项

- 不改 `skills/**` 下任何脚本/模板逻辑（除 #6 注释）
- 不改 `scripts/*.json` 的成员数字与 `_comment` 历史
- 不更新版本号、不动 CHANGELOG
- 不跑 `npm install` / 不碰 node_modules

## 7. 提交规范

```
docs: 批0 现状修复——9→10 skill 表、平台删刺猬猫、references 187、shared-assets 36 组、doc-budget 组上限对齐实测
```
