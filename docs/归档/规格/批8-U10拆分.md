# 批 8 规格：U10 团队化拆分（moshu-plan + moshu-write）

> 状态：待实施（独立立项，先出拆分设计文档） | 前置：批 5 稳定后 | 预计量：结构性变更（新 skill + 路由 + 守卫 + 预算重排） | 风险：高（拆最大 skill，须设计先行）
> 依据：`docs/研究-v2/mo-shu-现状盘点.md` §2.2/§9（moshu-write 81 references 最大、Phase1-5 耦合）；SparkArc `agent_lorebook.py:34`（输出纯度）、AGENTS.md lorebook tool_rules（关系纪律）；总纲附录 D 拆分评估

## 0. 前置要求

**本批必须先产出《拆分设计文档》**（`docs/研究-v2/02-拆分设计.md`），经作者确认后才动代码。设计文档内容：

1. skill 边界表：moshu-plan（Phase 1 选题确认/Phase 2 核心设定/Phase 3 卷纲+细纲/中途补纲；消费 outline-*/genre-*/character-* ~15 文件；产出 设定/大纲/追踪初始化）vs moshu-write（Phase 4 单章/日更/修订；消费 workflow-chapter/daily + writing-* ~10 文件；产出 正文/追踪）。
2. 文件搬迁清单：references 逐个归属（81 文件 → 两 skill），标"移动/复制/shared-assets"三类。
3. 路由改动：moshu 路由表 10→11 行；`/moshu-plan` + `/moshu-write`。
4. 衔接规则：日更读细纲按"对标书路径查找"同款回退读取，不跨 skill 复制。
5. 设定守护职责（归 plan）：输出纯度校验（拒收网页/代码误输出，对照 `_is_invalid_worldview_document` 思路）/关系只写长期稳定+作者确认（临时互动禁止写入）/每批细纲后设定补全+与追踪转正联动。
6. doc-budget 重排方案（两 skill 热路径预算重新分配，附 why）。
7. 契约守卫与测试迁移清单；marketplace 10→11。

## 1. 目标

创作规划与正文执笔分离：开书不背执笔方法论、日更不背规划方法论；设定守护职责明确归属。

## 2. 现状事实

- `skills/moshu-write/`：SKILL.md 221 行 + references 81 文件（含 Phase1-3 规划方法论 + Phase4-5 执笔方法论）；热路径 workflow-setup/chapter/daily。
- marketplace 10 plugin；moshu 路由表 11 行意图表。
- architect agent 过宽（题材+世界观+大纲+钩子+弧线）——拆分候选列观察项。

## 3. 文件级改动清单（设计文档确认后执行）

| # | 文件 | 改什么 |
|---|---|---|
| 1 | **新建** `skills/moshu-plan/` | SKILL.md（Phase1-3 + 设定守护职责段）+ references（outline-*/genre-*/character-* + workflow-setup）+ 路由 `/moshu-plan` |
| 2 | `skills/moshu-write/` | 保留 Phase4-5；删除/移交规划侧 references；SKILL.md 瘦身 + 入口提示读 plan 产物 |
| 3 | `skills/moshu/SKILL.md` | 路由表 10→11 行（/moshu-plan 插入） |
| 4 | `.claude-plugin/marketplace.json` | plugin 10→11 |
| 5 | `scripts/doc-budget.json` | 热路径预算重排（plan 路径新预算 + write 路径削减，附 why） |
| 6 | `scripts/behavior-contracts.json` / `check-claude-adapter.sh` | 契约守卫与 marketplace 映射同步 |
| 7 | `skills/moshu-setup/references/templates/CLAUDE.md.tmpl` | 路由表补 /moshu-plan |
| 8 | `scripts/test-*` | 路由/契约/预算回归更新 |
| 9 | architect 拆分（planner/lorekeeper） | **列可选观察项，本批不做**（使用频率数据积累后再评估） |

## 4. 验收

```bash
bash scripts/static-check.sh && bash scripts/check-doc-budget.sh && bash scripts/check-claude-adapter.sh
# 冒烟：/moshu-plan 开书全程不加载执笔方法论（grep 验证 references 无 writing-* 引用）；/moshu-write 日更不加载规划方法论；存量项目重部署后路径不变
```

## 5. 回滚

合并回（文件契约不变，skill 列表回 10）。

## 6. 禁止事项

- 设计文档未经作者确认不动代码
- 不复制 references 内容跨 skill（用回退读取规则，防双源漂移）
- 本批不新增 architect 拆分 agent

## 7. 提交规范

```
feat(U10): 团队化拆分——moshu-plan（Phase1-3+设定守护）+ moshu-write（Phase4-5）+ 路由/守卫/预算重排（拆分设计 docs/研究-v2/02-拆分设计.md）
```
