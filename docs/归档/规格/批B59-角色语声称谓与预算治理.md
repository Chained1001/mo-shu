# 规格 · 批 B59：角色语声样本与双向称谓 + 预算治理

- 版本：v1.0（2026-08-28，旗舰起草；来源：[06-强化方案草案](../../研究/开源强化/06-强化方案草案.md) B59 定稿 + 作者并入裁定⑤⑥⑦。裁决点 D2 在此定值：**载体=单权威 state**——B58b 已建 schema 升级全套模式，无需两步走；派生视图只读）
- 性质：write 追踪语声/称谓域增强（schema 6→7）+ analyze/build 角色资产五要素 + 预算治理（作者 2026-08-27 裁定并入）。**脚本变更**（tracking_commit.py×4）+ 文档变更（chapter-core/analyze/build 模板）+ agent 模板变更（narrative-writer，agents_version 38→39）。分两子步两提交（批 3/B58 先例）。

## 一、设计依据

| 改进 | 外部来源（研究文档） | mo-shu 现状（实证） |
|---|---|---|
| 台词样本滚动 5 条（语声锚） | lnw（04）——统计画像管不到「说话不像他」层 | 角色快照键集 `{identity,location,goal,state,abilities_resources,relationships,knowledge,open_threads}`（tracking_commit.py:266-280）无语声槽；style 管篇章级统计 |
| 双向称谓登记（A 称 B） | NovelForge 关系边称谓+证据队列（04，语义移植不用图谱） | characters.relationships 是单侧自由文本列表，无结对结构；称谓漂移（突然改叫全名）无防线 |
| 对话 DNA 五要素 | ans 三层角色资产（01·M10） | 拆文产物有「主角塑造{直接描写/对话/行为/他人评价}」（analyze output-templates.md:140），无作派级五要素；build 角色卡无对应节 |
| ⑤ doc-budget 覆盖面 | 09 审计 §2.2 + 文件盘点 | review-workflow **19045**、analyze-workflow **14712**、setup-workflow **3345** 未登记（守卫白名单制，未登记=不管）；moshu-research SKILL 亦未登（flash 复核实际路径） |
| ⑥⑦ 余量治理 | 09 审计 §2.2/2.3 | chapter-core 基线 **20871**（B58 关闭时）；贴顶文件余量普遍 <100（narrative-writer 16/anti-ai-writing 36 等） |
| B 段判据「台词换人不变味」 | ans（01·M10） | chapter-core B 段第二遍四查 d=角色辨识度，未挂语声对照 |

**定值说明**：衔接双锚（上一章结尾/本章细纲）经 B58 前置走查确认已覆盖，本批零改动；卷末快照（NovelForge 思想）不进本批，留 write 完整走查定案。

## 二、现状事实（flash 开工前逐条复核；行号可能漂移，以 grep 为准）

1. `skills/moshu-write/scripts/tracking_commit.py`（×4 副本：write/build/import/review）：characters 快照键集约 :266-280、`normalize_snapshots` 约 :285、输入键 `delta/context/character_snapshots` 约于 :1228/:1289、视图落盘约 :1226（含「角色状态.md」约 :81 的派生清单）、`render_views`/`apply_transaction` 在 B58 已带 injection_budget 参数。TRACKING_SCHEMA_VERSION=6、INPUT_SCHEMA_VERSION=2（:26-27）。
2. 涟漪面（宪法 §3.4 七类，B58 判因链）：`story_hook_core.js:118-119`（`!==6`+消息文本）；`scripts/test-tracking-workflow-contracts.py`（断言文本 `schema_version=6`）；`scripts/capability-wiring.json:95`（标记 `schema_version !== 6`）；fixture 带 `"schema_version":6`：test-story-continuity（主 5 处+⑦ 用例 1 处为 v5 旧版语义）、test-hook-encoding-portable×3、check-moshu-setup-deployment×3、test-next-step×1；`test-writing-pipeline.sh` 已版本无关（无需动）。
3. `skills/moshu-write/references/chapter-core.md` 实测 **20871**（预算 21500；本批硬目标净零增，见改动 59b-4）；B 段四查 d（角色辨识度）与 D 段事务指引在位。
4. `skills/moshu-analyze/references/output-templates.md`（人物出场行约 :140）与 `analyze-workflow.md`（14712）；build 侧角色卡模板 `character-basics.md`（write/build 共享副本，flash 定位）。
5. `skills/moshu-setup/references/templates/agents/moshu-narrative-writer.md` 实测 13284（预算 13300，贴顶）。
6. 产物契约/行为契约守卫（check-behavior-contracts/check-current-skill-contracts/check-capability-wiring）可能枚举模板节名与产物字段——改动 59a 后必须复跑。

## 三、文件级改动清单

### 子步 59a：对话 DNA 五要素 + 预算治理（零 schema）

**1a. analyze 侧**：output-templates.md 角色卡模板加「对话 DNA」行：`| 对话DNA | 口头禅功能位{句} | 句长习惯{短促/绵长/混用} | 语气词与标点{特征} | 回避话题{什么绝口不提} | 情绪外显{方式} |`（01·M10 五要素转译，学结构不抄血肉）；analyze-workflow.md 角色提取步加一句提取指令。预算随动（analyze-workflow 本批新登后按实测调）。

**1b. build/write 侧**：character-basics.md 主角卡/配角卡加「对话 DNA」节（同五要素；有对标→建卡时从拆文库角色卡带入；无对标→留占位待填，不杜撰）；workflow-build Stage 3 人物步加一句消费口径（有对标带入源）。

**1c. doc-budget 覆盖面补全（⑤）**：files 层登记 `skills/moshu-review/references/review-workflow.md`（19045→**20000**，×1.05 取整百）、`skills/moshu-analyze/references/analyze-workflow.md`（14712→**15500**）、`skills/moshu-setup/references/setup-workflow.md`（3345→**3500**）、moshu-research SKILL（flash 复核路径与实测后登记）；**枚举规则**：每 skill 的 SKILL.md 与被其无条件引用的首层主 workflow 必登——flash 用 `ls skills/*/references/` 对照 SKILL.md 引用逐个排查补全，冷路径按需文件（如 cold-path.md）不登。组层：正文 agent 上下文组不动；新增组或随动按现状口径。

**1d. 锁定注记（⑥）**：未触碰的贴顶文件（anti-ai-writing/banned-words/cdp/deslop/analyze SKILL 等）why 追加「B59 锁定：增长须先压缩或下沉」；数值不动。本批触碰文件（chapter-core/narrative-writer/analyze 模板）执行净零增（见 59b-4/5）。

**1e. 契约复核**：改动 1a/1b 后复跑 behavior/current-skill/capability 三守卫，红则按注册表同步（产物契约注册表条目如枚举角色卡节名须补「对话 DNA」）。

### 子步 59b：语声样本与双向称谓（schema 6→7）

**2a. tracking_commit.py（×4）——state 扩展**：
- characters 快照键集加 `voice_samples`（list[str]，滚动 ≤5 条、每条 ≤120 字节，可选默认 `[]`；输入侧接受 `voice_samples: [{character, quote}]`——AI 挑本章代表作派台词 ≤2 句/主要角色，脚本只做追加/精确去重/滚动截断）；
- 新顶级可选域 `address_book`（称谓登记，仿 information_gaps）：条目 `{a, b, a_calls_b, b_calls_a, first_recorded_chapter, updated_chapter}`，pair 键=两名排序后 `|` 连接；输入 `address_book_updates: [{action: register|update|retire, a, b, a_calls_b, b_calls_a}]`，称谓各 ≤24 字节；retire 只带 a/b；
- TRACKING_SCHEMA_VERSION 6→**7**，migrate 链 v6→v7（characters 逐条补 `voice_samples: []`、顶层补 `address_book: {}`，一次备份覆盖全链，老键零丢失）；INPUT_SCHEMA_VERSION 保持 2（可选键不拒老输入，B58b 先例）。

**2b. 渲染**：
- 角色状态派生文件（`追踪/角色状态/{名}.md`）加「## 近作台词（语声锚）」节（≤5 行原文）；
- 新派生视图 `追踪/称谓.md`（全量表格：A｜B｜A 称 B｜B 称 A｜登记章）；
- 点名清单节加「出场称谓」行：**两端均出场**的登记对逐对一行，**内嵌上限 8 对**（mo-shu 自定，超出行改「另有 N 对见 追踪/称谓.md」）；上下文卡字节数仍受 B58 双闸兜底。

**2c. chapter-core 消费（⑦ 净零增，批末实测 ≤20871）**：
- D 段事务指引加：提交载荷含语声/称谓提取（AI 语义层，≤2 句/角色、只记新建立或变化的称谓）；
- A 段点名清单消费加一句：出场角色语声锚读 `追踪/角色状态/{名}.md` 近作台词节、称谓照点名清单出场对行；
- B 段四查 d 挂判据：对照语声锚与称谓——「这句台词换个人说成立吗？A 对 B 的称呼与登记一致吗？」；
- **对冲删并**：A3 中 B57「分层读取」长句与点名清单句合并精简、第二遍四查 d 原文与新判据合并——新增必须在批内找到等量删减，实测超标即继续压缩。

**2d. narrative-writer 模板**：spawn 材料清单加两项引用（出场角色语声锚文件路径+点名清单称谓行；不整文件复制进模板）——**agents_version 38→39 bump**（preview→confirm 全套）+ UPGRADING v39 条目 + 指纹重登记；模板自身净零增（压缩对冲，批末实测 ≤13284）。

**2e. schema 涟漪面（宪法 §3.4 七类全仓 grep，含 .json）**：story_hook_core `!==6`→`!==7` 与消息文本；test-tracking-workflow-contracts 断言文本→7；capability-wiring.json 标记→7；全部 `"schema_version":6` fixture 升 7（continuity ⑦ 旧版拒绝用例的 v5 fixture **保持不动**——数字无关断言永久有效）；pipeline e2e 已版本无关零改动；守卫 JSON 复扫。

**2f. 回归扩用例**：test-tracking-commit 加 3 类（voice_samples 滚动截断与去重 / 称谓 register-update-retire 全周期 / migrate v6→v7 键零丢失）。

## 四、验收命令（全部退出码 0；fixture 放 `/.tmp/tests/B59/` 用完即删）

1. 59a：check-doc-budget 输出含 review-workflow/analyze-workflow/setup-workflow（可数断言）且绿；grep output-templates/character-basics 含「对话DNA」；behavior/current/capability 三守卫绿。
2. 59b migrate：v6 fixture → v7（voice_samples 空列表、address_book 空域、备份存在、老键零丢失）。
3. 59b 滚动：同角色连续两次 commit 各带 3 句 → 槽内 5 条、重复句去重。
4. 59b 称谓：register A|B → 称谓.md 与点名清单出场对行在位；retire → 两处消失；构造 10 对出场 → 点名清单内嵌 8 对+「另有 2 对」行。
5. 59b 语声渲染：角色状态/{名}.md 含「近作台词」≤5 行；上下文卡 CONTEXT_MAX_BYTES 不破（B58 双闸回归同跑）。
6. 59b 涟漪面：§三.2e 清单逐条 grep 复核（hook/契约文本/capability 标记/fixture）；test-tracking-workflow-contracts、test-story-continuity（含⑦）、test-hook-encoding-portable、check-moshu-setup-deployment、test-next-step、test-writing-pipeline 六套件全绿。
7. 净零增：node 实测 chapter-core ≤20871、narrative-writer ≤13284。
8. 全套：15 项守卫矩阵 + test-tracking-commit（含新增 3 类）+ agents_version 39 一致+指纹一致 + sync 四副本 shared-files 绿。

## 五、禁止事项

1. 语声/称谓的**挑选是 AI 语义层**——脚本只做槽位管理（去重/截断/校验），禁止脚本做语义判断。
2. schema 只加可选键+可选域，老数据零丢失，migrate 带备份；INPUT_SCHEMA_VERSION 不动。
3. 宪法 §3.4 七类涟漪面漏检即返工（B58 两轮红的教训已入册）。
4. chapter-core/narrative-writer 净零增是硬目标；其他文件不顺手改。
5. 候选不拦截语义不涉及；不改 B58 已验收机制（点名清单结构只增「出场称谓」行）。
6. 台词样本进派生视图与 state，不进对话复述（宪法 §2.8 载荷先落文件再 --input）。

## 六、预算耦合（files 层，node 计数）

- chapter-core ≤20871（预算 21500 不动）；narrative-writer ≤13284（预算 13300 不动）。
- 新登文件：review-workflow 20000 / analyze-workflow 15500 / setup-workflow 3500（实测×1.05 取整百，实测以 flash 复核为准，why 记判因链）；analyze output-templates/character-basics 增量若超现值随动调高并记因。
- 组层随动：正文 agent 上下文组（narrative-writer 不增则不动）、新增组按 1c 枚举结果。

## 七、提交规范（两子步两提交）

- 59a：`feat(analyze+build): B59a 对话DNA五要素与预算治理——拆文角色卡/build 角色卡五要素节 + doc-budget 覆盖面补全（review-workflow/analyze-workflow/setup-workflow 等，实测×1.05）+ 贴顶文件锁定注记`
- 59b：`feat(write): B59b 语声样本与双向称谓——characters.voice_samples 滚动5条 + address_book 域（schema 6→7 migrate带备份）+ 称谓.md 派生/点名清单出场对≤8 + 角色状态近作台词节 + chapter-core 消费净零增 + agents_version 39`
- 提交前作者明确说「提交」；每子步验收全绿后停下请求确认。

## 八、风险与回滚

- 风险 1：schema 连续 bump（6→7）——B58 已走通全套涟漪纪律且宪法 §3.4 刚入册，本批即其首次完整应用；七类面清单写进验收，漏检概率已压。
- 风险 2：语声提取质量依赖 AI 判别——≤2 句/角色约束+滚动窗口自洁（差样本会被后续覆盖）；实测门评估。
- 风险 3：称谓对膨胀——state 无硬上限但渲染仅出场对+8 对内嵌上限，卡体积受双闸兜底；若实测发现登记滥用再议上限。
- 回滚：59a 纯文档/守卫配置，revert 即恢复；59b 新键全可选、旧代码读到空列表等价未登记，revert 后 migrate 备份可恢复。
