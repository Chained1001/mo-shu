# 批 7 规格：U8 进度持久化 + U9 模板字段

> 状态：待实施 | 前置：批 3 | 预计量：小文件 + 模板 + 脚本扩展 | 风险：低-中（stale 传播触 workflow-setup 模板）
> 依据：SparkArc `auto_write_state.py:100-146`（多游标）、`structure_state.py:57-135`（stale）、`outline_parser.py:132-135`（契约字段）、`story/text_metrics.py:31`（最终可见字符）；笔枢 `parse_intent.py`（意图两路）
> **v2 修正**：knowledge_before/after 等由现有字段覆盖不重复；新字段全设默认值。

## 1. 目标

- **U8**：日更中断后可断点续写（进度持久化）。
- **U9**：细纲补机器可检查的伏笔引用与因果依赖；结构 stale 传播；字数口径统一；意图两栏。

## 2. 现状事实

- 日更流程 `workflow-daily.md` Step 2 无进度文件；中断靠人工读追踪判断。
- 细纲模板在 `workflow-setup.md`（或细纲产出规范）：已有视角/信息差/信息留白/禁止提前释放/INF 登记，**无 setup_refs/payoff_refs/causal_dependencies**。
- `check-outline-copy.js` 只查正文与细纲重合；无 stale 传播。
- 字数统计：`count_story_body_chars`（各脚本口径待统一）；CRLF 归一化已在 v1.1.2 修复。

## 3. 文件级改动清单

| # | 文件 | 改什么 |
|---|---|---|
| 1 | **新建** `追踪/_write-progress.json` 协议（写入 `workflow-daily.md` + tracking-transaction.md 说明） | 结构 `{current_chapter, completed[], next_batch_start, last_error, updated_at}`；日更每章落盘后更新；中断重开读断点；批末清空。**流程态原子写，不进 delta 重放** |
| 2 | `skills/moshu-write/references/workflow-daily.md` | Step 2 升级：读 `_write-progress.json` 断点 → 写章 → 每章更新 |
| 3 | `skills/moshu-write/references/workflow-setup.md`（细纲模板处） | 细纲补 `setup_refs` / `payoff_refs`（伏笔引用，机器可检查）+ `causal_dependencies`（本章事件依赖的前置条件）；knowledge_before/after、forbidden_setup 由现有"视角/信息差/信息留白/禁止提前释放"覆盖（不重复加字段）；**全部新字段设默认空值** |
| 4 | `skills/moshu-write/scripts/check-outline-copy.js` | 扩展：正文兑现未登记伏笔复扫（setup_refs 未兑现 → 提示）+ 细纲字段存在性校验 |
| 5 | `skills/moshu-write/references/artifact-protocols.md`（或细纲产出规范处） | 结构 stale：卷纲/细纲带 `derived_from`；改卷纲 → 相关未写细纲标 stale + 写前警告（"过期产物只能作历史参考，不得覆盖较新的上游事实"） |
| 6 | 字数口径统一 | `count_story_body_chars` 语义对齐"最终可见字符"（排除标记/构思块）；软目标不硬切（对照 text_metrics.py 与 context_builder.py:101-103 的"严禁凑字数注水"） |
| 7 | `skills/moshu-write/references/workflow-chapter.md`（意图确认处） | B6 意图两栏：日更意图确认显式拆"情节指令 OD / 写法指令 SE"（对照笔枢 parse_intent 只归类不判断） |
| 8 | **新建** `scripts/test-progress-stale.py` | 进度断点 + stale 传播 + 细纲新字段复扫回归 |

## 4. 验收

```bash
python scripts/test-progress-stale.py && bash scripts/check-behavior-contracts.sh && bash scripts/static-check.sh
# 冒烟：日更中断 → 重开从断点继续；改卷纲 → 相关细纲标 stale 且写前警告；正文兑现未登记伏笔 → 复扫提示
```

## 5. 回滚

模板 revert；`_write-progress.json` 删除即回（纯新增流程态）。

## 6. 禁止事项

- `_write-progress.json` 不得进追踪事务 delta（流程态独立）
- 新细纲字段必须默认空值（防膨胀拖慢细纲产出）
- stale 传播只警告不阻断（上游更新是常态）

## 7. 提交规范

```
feat(U8+U9): 日更进度持久化断点续写 + 细纲 setup_refs/payoff_refs/causal_dependencies + 结构 stale 传播 + 字数口径统一 + 意图两栏
```
