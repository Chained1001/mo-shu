# 批B93：采风接线与竞态修复批（v2，含源优先级表实测修订）

> **目的**：治实测三期两个流血伤口——①spawn prompt 断链（B90 源优先级表住在 caifeng-methods 但从未到达 researcher agent，三期 3/3 agent 全部撞 Bing/Google 墙浪费回合）；②竞态条件（采风后台跑、主会话不等就进轮 3 生成理想书评——采风产物从未被消费）。同步全仓扫描同类问题（spawn 协议面断链+后台并行无同步点）一并修复。**v2 修订**：源优先级表经旗舰 2026-09-01 全量实测更新——搜狗 web 已被 IP 限流（CAPTCHA），新增搜狗微信/360 搜索两个发现层，优先级表从「四层栈」扩为「三引擎 fallback + 六内容源」。
>
> 规划模型：旗舰｜依据：实测三期 test1-2 首战 + 作者指示「探测国内哪些网站能用」+ 源可达性全量探测（2026-09-01 旗舰 15 站实测）｜施工顺序：B92 之后，v2.5.3 小版本先行。

## 现状事实（施工前逐条 grep 复核；行号为起草时实测，以 grep 为准）

1. **spawn prompt 断链实证（三期）**：caifeng-methods.md:39-45 有「国内可达源优先级表」——但 workflow-outline.md:124 的 spawn 指令只说「spawn moshu-researcher（结构采风，CF 票据登记）」，spawn prompt 由主会话摘要式构造，**优先级表从未被搬入**。三期 3/3 agent 撞墙：Bing 分词坏、Google 超时×5、DuckDuckGo 超时×3。
2. **竞态实证（三期）**：三路采风后台 spawn 后主会话推进轮 2→轮 3→理想书评已产出——采风 agent 仍在挣扎。理想书评纯信号推演，**未消费任何采风产物**。workflow-outline.md 轮 3 步无前置检查。
3. **源可达性全量探测（2026-09-01 旗舰 15 站实测）**——**B90 四层栈的地基动摇**：
   - 搜狗 web（B90 主力）：**CAPTCHA**（antispider+验证码壳，三种 UA+Referer 全灭）——IP 限流，不再可靠
   - 搜狗微信 `weixin.sogou.com/weixin?type=2`：**200+35K，高价值新源**（公众号文章=网文方法论主要发表阵地，返回《跟着宅猪学网文写作》《卖报小郎君创作之路》等）
   - 360 搜索 `so.com/s?q=`：**200+427K**（备用发现引擎，中文正常）
   - 豆瓣书评页/萌娘百科/百度百科 API/晋江/豆瓣小组/搜狐号/网易号/潇湘问答：全部 200+正常内容
   - Bing cn（分词坏）/Google/DuckDuckGo（超时）/知乎专栏（403）/贴吧（403）/起点（反爬）/番茄（404）/龙空/飞卢/纵横：全部不可用
4. **全仓同类扫描**：outline 四处 spawn（结构采风 :124/深度融合 :127/人物评审 :204/大纲终审 :206）+ volume 四处（打磨环 evaluator :245/防撞复核 :197/情节情绪采风/融合 researcher）——八处 spawn prompt 均由主会话摘要式构造，无「必含条款」清单。
5. caifeng-methods.md:33 已有 CDP 预检——但三期主会话也没执行（同优先级表：住在文件里但 workflow 侧无引用触发）。
6. 检查采风的正确机制（作者 2026-09-01 裁定）：**文件系统证据**（`ls 设定/采风-CF*.md`），检查须绑定到理想书评步（必经步）而非独立检查步（可跳过步）。

## 文件级改动清单

### 1. `skills/moshu-volume/references/caifeng-methods.md`——源优先级表 v2 + spawn 必含条款

**1a. 替换「国内可达源优先级表」为 v2 版**（~15 行替换旧 4 层栈）：

```
**国内可达源优先级表 v2（B93 实测修订，2026-09-01 旗舰 15 站全量探测）**

发现层（按序 fallback，不并行尝试）：
  ① 搜狗 web `www.sogou.com/web?query=`（主力——但 IP 限流风险高，CAPTCHA 壳即切下一层）
  ② 搜狗微信 `weixin.sogou.com/weixin?type=2&query=`（公众号文章=网文方法论重要来源；限流独立于 web 版）
  ③ 360 搜索 `www.so.com/s?q=`（备用——搜狗全灭时用；中文分词正常）

内容层（直接抓取，不走发现引擎）：
  豆瓣读书书评页 book.douban.com/review/{id}
  豆瓣小组 douban.com/group/topic/{id}
  萌娘百科 mzh.moegirl.org.cn（ACG 元数据最强源）
  百度百科开放 API baike.baidu.com/api/openapi/BaikeLemmaCardApi?bk_key=
  晋江文学城 jjwxc.net（作品页元数据，**GBK 编码**）
  搜狐号/网易号（server-rendered，注意 AI 软文识别）

禁用名单（白探即弃，勿浪费回合）：
  google / wikipedia / duckduckgo（国内不可达）
  Bing cn（中文书名分词失效——拆成首字返回字典结果，加引号不救）
  起点 / 知乎专栏 / 百度贴吧 / 番茄小说 / 龙空 / 飞卢 / 纵横（反爬/403/404/500）
```

**1b. 新增「spawn prompt 必含条款（B93）」小节**（~20 行）：

```
主会话构造 researcher spawn prompt 时，以下条款**逐条搬入，不得摘要省略**：

① 源优先级表 v2（全文引用上方发现层+内容层+禁用名单）
② 发现层按序 fallback 逻辑：搜狗 web 试 1 次→CAPTCHA 即切搜狗微信→再切 360——不并行尝试、不反复重试
③ 搜狗 web 可能限流（IP 频率）——CAPTCHA/验证码壳 = 立即切下一层，不硬闯
④ Bing 禁用：cn.bing 对中文书名分词失效（三期实测），禁用 Bing
⑤ 内容层直接抓取（不走搜索引擎）：已知目标 URL 直接 curl，只有发现层需要引擎
⑥ 反爬名单：禁用名单里的站点白探即弃
⑦ CDP 状态（主会话预检结果传入：ready/不可用——不可用则 curl 栈）
⑧ 效率纪律（检索≤20 回合/先写骨架/超限降级收尾）
⑨ 临时目录约定+Windows 三坑（.caifeng-tmp/PYTHONIOENCODING/GBK）
```

每条带一句「为什么」（②③⑤带三期实测证据或设计理由）。

### 2. `skills/moshu-outline/references/workflow-outline.md`——轮 3 前置检查+spawn 引用修复

**2a. 轮 3 理想书评步加前置检查**（~5 行，插在「4. 理想书评」行之前）：

```
→ **前置检查（B93）**：跑 `ls 设定/采风-CF*.md 2>/dev/null`——
   有产物 → 展示要点表 → 融合五步 → 进理想书评（织入采风信号）
   无产物 → 告知作者：「采风还在跑（CF-{N} 路）——建议等或说跳过」
              作者说等 → 暂停，agent 返回通知到达后再继续
              作者说跳过 → 理想书评头部标注「未消费采风·按信号推演」继续
```
（检查绑定到理想书评步=必经步——作者裁定的「不靠 AI 记忆」原则：AI 可以忘掉独立检查步，不会忘掉写理想书评这个主任务）

**2b. 采风 spawn 指令（:124 附近）补两行**：
- 「spawn prompt 构造按 caifeng-methods『spawn prompt 必含条款』逐条搬入（含源优先级表 v2/发现层 fallback 逻辑/Bing 禁用/CDP 状态）——不得摘要省略」
- 「spawn 前主会话跑 CDP 预检（caifeng-methods『CDP 预检』节）——结果传入 spawn prompt 条款⑦」

### 3. `skills/moshu-volume/references/volume-workflow.md`——volume 侧同款修复

1. 采风补强（🔄 选项，:296 附近）加前置检查（同 outline 款：检查 CF 产物是否回→有→消费/无→告知作者）。
2. 采风 spawn 指令处补「spawn prompt 按 caifeng-methods 必含条款构造」引用。
3. 打磨环 evaluator spawn 不影响——evaluator 是只读 agent 不联网，无源优先级问题。

### 4. `docs/治理/开发标准.md`——spawn 协议纪律

补一条「**spawn prompt 构造纪律（B93）**」：
> 方法论文件定义的操作约束（源优先级/工具路径/环境限制/fallback 逻辑）必须在 spawn prompt 中**逐条搬入**，不得摘要省略——「住在方法论文件里但 spawn 时没搬」= 接线断链（B93 教训：B90 的优先级表落了文件但三个 agent 全不知道）。新方法论文件带操作约束的，同批定义 spawn 搬入清单。

### 5. 全仓 spawn 点排查表（施工时产出）

flash 执行：grep 全部 workflow/SKILL 中的 spawn 指令（约 8 处——outline 四处+volume 四处），逐处标注：
- spawn 类型（researcher/evaluator/其他）
- 是否有方法论文件定义的操作约束需搬入（researcher=源优先级；evaluator=无联网需求）
- 是否已加搬入指令（本批改 2b/3 补的）
- 排查表写进施工日志（新增 spawn 点的未来防漏参考）。

## 禁止事项

1. **零 agent 变更**（templates/agents/ 与 agent-references 不动——本批全改主会话侧，不改 agent 定义）。
2. deploy.py / merge-claude-settings.py 零改动。
3. **不新建检查脚本**——前置检查是 `ls` 命令+流程规则（AI 必经步），不是新守卫。
4. **不删除 Bing/搜狗 web 的历史提及**——caifeng-methods 的禁用名单/限流注记更新即可，历史施工日志不动。
5. spawn prompt 必含条款**不改 agent 定义文件**——agent 模板改动触发 agents_version bump（盲区 #13）。
6. 前置检查**不拦截**（作者说跳过就跳过，标注「未消费采风」继续——提醒不拦截语义）。
7. **不试图绕过搜狗限流**（不加代理/IP 轮换——外裁定）；限流=切下一层 fallback，不硬闯。
8. **不把搜狗微信/360 加入 CI 或守卫**（它们是 agent 运行时的源，不是 CI 可测的——CI 零网络）。

## 验收命令（Windows 探测链；零网络）

```bash
# 1. 源优先级表 v2 + spawn 必含条款
grep -n "源优先级表 v2\|搜狗微信\|360 搜索" skills/moshu-volume/references/caifeng-methods.md
grep -n "spawn prompt 必含条款" skills/moshu-volume/references/caifeng-methods.md
grep -n "Bing 禁用\|Bing.*分词" skills/moshu-volume/references/caifeng-methods.md
grep -n "CAPTCHA.*切\|限流.*fallback" skills/moshu-volume/references/caifeng-methods.md
# 2. 轮 3 前置检查
grep -n "前置检查" skills/moshu-outline/references/workflow-outline.md
grep -n "未消费采风" skills/moshu-outline/references/workflow-outline.md
# 3. spawn 引用修复
grep -n "必含条款" skills/moshu-outline/references/workflow-outline.md skills/moshu-volume/references/volume-workflow.md
grep -n "CDP 预检" skills/moshu-outline/references/workflow-outline.md
# 4. volume 侧同款
grep -n "前置检查" skills/moshu-volume/references/volume-workflow.md
# 5. 开发标准
grep -n "spawn prompt 构造纪律" docs/治理/开发标准.md
# 6. 固定守卫
bash scripts/check-doc-budget.sh   # 整段读含组级
# 7. shared 副本核查（workflow-outline/volume-workflow 若在组 → sync+指纹）
# 8. 第七查：templates/agents/ 零 diff
```

全部 grep 命中、守卫退出码 0 方算完成。

## 提交规范

一条提交：`fix(B93): 采风接线与竞态修复批——源优先级表 v2（搜狗 web 限流→微信+360 fallback/六内容源/禁用名单更新）、spawn prompt 必含条款（九条逐条搬入，治三期 3/3 agent 撞墙）、轮 3+volume 采风前置检查（文件系统证据绑定必经步）、全仓 spawn 点排查、spawn 构造纪律入开发标准`。只含本批清单文件。

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（治三期两个流血伤口+源表实测修订） |
| 2 | 预算余量实测 | 条件——workflow-outline/volume-workflow 已五批连压（9800/14600），本批增量 ~15 行（两 workflow 各）+~35 行（caifeng-methods 不在 doc-budget），须精写；超限压缩对冲优先 |
| 3 | 涟漪面（含 .json/互引） | 通过——无版本常量/路径改动；新增互引 2 处（outline→caifeng-methods/volume→caifeng-methods——侧名先例在）；不涉 .json |
| 4 | 共享副本声明 | 条件——workflow-outline/volume-workflow 按归属为各技能独有，flash 复核，在组则 sync+指纹 |
| 5 | 跨技能接口 | 通过——caifeng-methods 被 outline 跨技能消费（侧名引用），改后 grep 引用仍可解析 |
| 6 | 存量兼容 | 通过——纯增量；B90 版优先级表被 v2 替换（旧表内容全保留在禁用名单中，只是发现层重构）；老书零影响 |
| 7 | 端到端走查 | 不适用——三期实测正在走查中（本批修复后下一轮即验证） |
| 8 | 输出幂等性 | 通过——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫，Windows 零网络；源可达性不在 CI 中（禁事项 8） |
| 10 | 热路径字数报告 | 通过——两 workflow 在 doc-budget 在册，flash 回填前后值；caifeng-methods 不在册（按需加载冷路径） |
| 11 | token 成本核算 | 通过——零 agent 模板改动；前置检查 +1 条 ls/理想书评步（可忽略）；spawn prompt 增量 ~250 字/次（优先级表搬入——换 agent 不浪费 10+ 回合，净收益正） |
| 12 | 决策树过闸 | 通过——零新文件/新脚本/新 agent（全挂既有文件节）；源表 v2 替换旧表=既有节的内容修订 |
| 13 | 部署面改动联动 | 通过——零 agent 模板/agent-references/deploy.py/merge/CLAUDE.md.tmpl 变更（第七查 diff 对照） |
| 14 | 概念分离后术语考古 | 通过——「spawn prompt 必含条款」「发现层 fallback」为新词；B90 的「四层栈」被「三引擎 fallback+六内容源」替换——旧术语在 caifeng-methods 中应随批更新（grep「四层栈」零残留） |
