# 规格 · 批 B45：审核闭环与称谓收尾

- 版本：v1.1（2026-08-25 修订：旗舰侧已直接落档审核记录与日志翻转（§一.4），原 v1.0 清单 6/7 由「施工执行」改为「核对+原样入库」；提交规范补作者确认门槛与 hash 回填；推送移出本批。施工要点不变）
- 依据：旗舰审核 B32-B44 十三批结论=全部通过（结论已由旗舰侧写入 `docs/审核记录.md`「批B32-B44 审核」节，随本批入库）；本批落地 F1/F2/F3 修复。F4（B42 backlog 两项）挂起不施工。
- 性质：称谓残留收尾 + 去编号化消歧 + 审核落档入库。零行为变更（不碰任何脚本逻辑与部署物）。

## 一、现状事实（grep 实测；行号可能漂移，以文字描述 + grep 为准）

1. **F1 残留**（B33/B34 漏网，B26 映射：步 N→Stage N+1）：`outline-methods.md:327`「步 1…步 2 人物步精化回填」（→Stage 2/Stage 3）；`outline-workflow.md:7` 两处「步 5」（→Stage 6）、`:21`「步 4」（→Stage 5）。genre-writing-formulas.md:4 的「步 1」**不在本批**（与 /moshu-research 措辞同行，并入批B46 一次改完）。
2. **F3 残留**（「Stage 4-6 大纲」语义错误——write 顶层 Stage 1-5（大纲搭建=Stage 3）、build 骨架=Stage 2，两套编号体系均不成立；裁决去编号化）：`outline-methods.md:3`、`outline-conflict.md:3`、`idea-seed.md:43`、`character-design-methods.md:396` 四处加载提示。
3. 共享副本面：outline-methods（build+setup 副本）、outline-conflict（build+setup）、character-design-methods（build+setup）、idea-seed（build）、outline-workflow（build）——全部经 sync-shared-assets 同步，check-shared-files 绿为准。
4. **旗舰侧落档已在工作区（未提交，随本批 docs 提交原样入库）**：`docs/审核记录.md` 尾部「批B32-B44 审核」节（十三批结论+F1-F4 裁决）；`docs/施工日志.md` 状态总表 B32-B43 翻「已关闭」12 行 + B44 行补记。
5. 非残留豁免（不碰）：emotional-arc-design.md:69「起步 10%」为 grep 误命中；workflow-build.md:5 的 Phase 字样为 v3.0 重构历史注记（B33 豁免类）。

## 二、文件级改动清单

1. `skills/moshu-write/references/outline-methods.md`（+副本同步）：:327 按 F1 修正（步 1→Stage 2、步 2→Stage 3）；:3 按 F3 去编号化「Stage 4-6 建大纲时加载」→「建大纲/卷纲时加载」。
2. `skills/moshu-write/references/outline-workflow.md`（+build 副本）：:7 两处「步 5」→「Stage 6」；:21「步 4」→「Stage 5」。
3. `outline-conflict.md:3` →「设计矛盾与主线结构时加载」（+副本同步）。
4. `idea-seed.md:43` →「那些是核心设定与大纲阶段的事」（+build 副本同步）。
5. `character-design-methods.md:396` →「卷纲定人物弧线时」（+副本同步）。
6. `docs/施工日志.md`：追加本批 B45 行（按协议状态机：施工中→待提交确认→已提交/待审核，**不预置已关闭**）。旗舰侧已做的 B32-B44 翻转与 B44 行**不得改动**。

## 三、禁止事项

- 不碰 genre-writing-formulas.md、workflow-build.md:5 历史注记、历史文件（旧规格/审计报告/施工日志历史条目/CHANGELOG）。
- 不改任何可执行逻辑；规格没写的不做；失败先判因禁止改断言变绿。
- `docs/审核记录.md` 与施工日志的旗舰侧内容**原样入库、一字不增删**（AGENTS.md：施工模型不得修改审核记录除「整改回执」小节外内容；docs 提交前 `git diff docs/审核记录.md docs/施工日志.md` 确认仅含旗舰已写内容与本批清单内改动）。
- 归档目录 `docs/归档/**` 零接触；`otherMaterials/` 只读；临时脚本放 `/.tmp/tests/<批次>/` 用完即删。
- 与规格不符走 README §5 待决问题协议。

## 四、验收命令

1. `grep -rn "侧步 [0-9]\|步 [1-5] 用本节\|完成步 [1-5]\|步 [1-5] 精化" skills/ --include="*.md" | grep -v genre-writing-formulas` → 零命中（「起步 10%」类误命中不算残留）
2. `grep -n "Stage 4-6"` 上述四个 write references 文件 → 零命中
3. `bash scripts/check-shared-files.sh` → 绿（全部共享副本同步）
4. 守卫/回归矩阵全绿（Windows 已知假红按假红清单注记）
5. `grep -n "批B32-B44 审核" docs/审核记录.md` → 1 处且含 F1-F4；施工日志 B32-B44 全「已关闭」且 B45 行在位
6. 本批两笔提交后 `git status --porcelain` → 工作区干净（旗舰落档文件全部随批入库）

## 五、提交规范

提交须先向作者请求确认，作者明确说「提交」后执行（AGENTS.md 红线 1/2；README §2.6）：

1. `fix: B33 称谓残留收尾（outline-methods 步1/步2→Stage 2/3、outline-workflow 步5/步4→Stage 6/5、共享副本同步）+ Stage 4-6 加载提示去编号化（审核 F3 裁决：双编号体系均不成立，场景描述消歧）+ 规格批B45 入库`
2. `docs: B32-B44 审核闭环落档入库——审核记录「批B32-B44 审核」节（旗舰侧结论十三批通过+F1-F4 裁决）+ 施工日志 B32-B43 翻已关闭与 B44 行补记（旗舰侧）+ B45 行`
3. hash 回填：`docs(施工日志): B45 提交 hash 回填`（先例 359469b）

推送**不在本批范围**——待 B45 审核通过、作者明示后再推（开发标准 §8.3）。
