# 规格 · 批 B29：性能优化批（审计提案 P1+P2 落地）

- 版本：v1.0（2026-08-24）
- 前置依赖：B28 已合入。
- 依据：`docs/审计-moshu-build-v1.5.md` §十 性能提案 P1（workflow-build 冷热分离）+ P2（方法论大文件去重）。

---

## 一、现状事实（施工前复核）

1. **workflow-build.md 瘦身紧迫性**：非空白 24196 / 预算 24700，余量仅 **504**——B29 后再增长就会撞红。
2. **冷热分离候选 4 节**（审计 2a 逐节引用计数）：
   - 自动步的打断与恢复（:653-660）——主流程 1 次显式引用
   - 停靠选项的级联说明（:662-673）——仅 :319 显式引用
   - 与现有机制的关系（:730-740）——0 显式引用
   - 方法论副本索引（:79-96）——0 显式引用但**承载 static-check 链接可达图 Warn 0**（B4 决策，下沉需维持可达性）
3. **方法论跨文件重复 2 组**（审计 2b 发现）：
   - 六种爽点类型：outline-methods:298 vs plot-emotion-system:130-139
   - 倒推法：outline-methods:294-298 vs plot-emotion-system:141-146
4. **plot-frameworks.md 711 行**（>700 候选瘦身）。

## 二、设计总纲

### 产出①：workflow-build.md 冷热分离下沉（P1）

**下沉 3 节到 cold-path.md**（方法论副本索引不下沉——守卫耦合）：

| 节 | 行范围 | 下沉理由 | 预期收益 |
|---|---|---|---|
| 自动步的打断与恢复 | :653-660 | 1 次引用，冷知识 | ~8 行 |
| 停靠选项的级联说明 | :662-673 | 1 次引用，停靠时才需 | ~12 行 |
| 与现有机制的关系 | :730-740 | 0 引用，纯背景文档 | ~11 行 |

**workflow-build 原位处理**：每节替换为一行指针：
```
**打断与恢复**：见 [cold-path.md](cold-path.md)「自动步的打断与恢复」
```

**cold-path.md 追加**：三节完整内容 + 头注更新（加载时机声明）。

**方法论副本索引不下沉**：保留 workflow-build 原位——static-check 链接可达图依赖此节维持 Warn 0；但在节头加一行注记：
```
> 本节承载 static-check 链接可达性检查（B4 决策）——不下沉冷路径。
```

**预期收益**：下沉 ~31 行 ≈ 约 800-1200 非空白字符 → 预算余量从 504 回升至 ~1300-1700。

### 产出②：方法论跨文件去重（P2）

**六种爽点类型去重**：
- outline-methods:298 删除重复段，替换为锚点引用：`六种爽点类型详参 [plot-emotion-system.md](plot-emotion-system.md)「爽点类型」`
- plot-emotion-system:130-39 保留为权威源

**倒推法去重**：
- outline-methods:294-298 压缩为一句话+锚点：`倒推法（逆向提纲）详参 [plot-emotion-system.md](plot-emotion-system.md)「倒推法」`
- plot-emotion-system:141-146 保留为权威源

**注意**：改的是 write 源 → sync → check-shared-files 全绿。

**预期收益**：outline-methods 减 ~10 行 ≈ ~300 非空白字符。

### 产出③：plot-frameworks.md 瘦身评估（只评估不实施——超出本批）

`grep -c "^## \|^### " plot-frameworks.md` 列出全部节名+行数，标记引用计数 <2 的节为候选——产出瘦身评估表附施工日志。**本批不实施**——改动量大需独立批。

## 三、文件级改动清单

1. `skills/moshu-build/references/workflow-build.md`：
   - 删 3 节（:653-660/:662-673/:730-740），各替换为一行指针
   - 方法论副本索引节头加守卫耦合注记
   - 预算变更后在 doc-budget.json 调低 workflow-build 预算并 _comment 说明
2. `skills/moshu-build/references/cold-path.md`：
   - 追加 3 节完整内容（打断恢复/级联说明/机制关系）
   - 头注更新加载时机声明
3. `skills/moshu-write/references/outline-methods.md`：
   - 六种爽点 + 倒推法两处替换为锚点引用
4. sync → check-shared-files 全绿
5. `docs/施工日志.md` B29 条目 + plot-frameworks 瘦身评估表

## 四、禁止事项

1. **方法论副本索引不下沉**——守卫耦合（B4 决策），只加注记
2. **plot-frameworks 本批只评估不实施**
3. 下沉的 3 节内容**一字不差搬移**到 cold-path（不重写不压缩——是搬不是改）
4. workflow-build 删节后指针行必须保留链接（维持 reference-closure 可达）
5. doc-budget 调低 workflow-build 预算时 **_comment 写明理由**（"B29 冷热分离下沉 3 节"）

## 五、验收命令

```bash
# 产出①：下沉验证
grep -c "cold-path.md.*打断与恢复\|cold-path.md.*级联\|cold-path.md.*机制" skills/moshu-build/references/workflow-build.md  # ≥3（指针行）
grep -c "自动步的打断与恢复\|停靠选项的级联说明\|与现有机制的关系" skills/moshu-build/references/cold-path.md  # ≥3（节头在位）
# 守卫耦合注记
grep -c "static-check.*可达" skills/moshu-build/references/workflow-build.md  # ≥1
# 产出②：去重验证
grep -c "六种爽点类型详参\|倒推法.*详参" skills/moshu-write/references/outline-methods.md  # ≥2
# 体积验证
python -c "import pathlib; s=pathlib.Path('skills/moshu-build/references/workflow-build.md').read_text(encoding='utf-8'); print(f'workflow-build: {sum(1 for c in s if not c.isspace())} 非空白')"
# 期望 < 23500（从 24196 降约 700+）
# 全量守卫
bash scripts/static-check.sh && python scripts/check-reference-closure.py && bash scripts/check-shared-files.sh && bash scripts/check-doc-budget.sh
```

## 六、提交规范

`perf(moshu-build): 审计提案 P1+P2 落地——workflow-build 冷热分离下沉 3 节（打断恢复/级联/机制关系→cold-path，指针行维持可达）、方法论跨文件去重（六种爽点+倒推法 outline→plot-emotion 锚点引用）、方法论副本索引守卫耦合注记、plot-frameworks 瘦身评估表（B27 提案 P1/P2；B29）`
