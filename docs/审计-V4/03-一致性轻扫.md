# 03 一致性轻扫（审计-V4 第3部分）

- 审计基线：HEAD = 3391fa3ab4747b1b35136c5e358416ecfb42d6da（与 `00-基线-守卫全量.md:3` 记录的 3391fa3 一致）
- 范围：版本四轨 / 术语表 / 悬空引用 / 数字口径 / doc-budget / V3 已修项回归抽查
- 方法：只读。守卫脚本（`scripts/check-*.sh`）全部 exit 0；grep/glob/python 只读核数。
- 铁律遵守：未修改任何文件；未读 `docs/归档/` 与 `otherMaterials/`；无 git 写操作。

---

## 3.1 版本四轨

| 轨道 | 实测值 | 期望值 | 结论 |
|---|---|---|---|
| `skills/moshu/VERSION` | `1.5.0`（`skills/moshu/VERSION:1`） | 1.5.0 | ✅ |
| marketplace metadata.version | `1.5.0`（`.claude-plugin/marketplace.json:8`） | 1.5.0 | ✅ |
| README 最近更新 | `最近更新（v1.5.0）`（`README.md:19`） | 含 v1.5.0 | ✅ |
| README_EN Latest update | `Latest update (v1.5.0)`（`README_EN.md:21`） | 含 v1.5.0 | ✅ |
| CHANGELOG 首条目 | `## v1.5.0（2026-08-21）`（`CHANGELOG.md:5`） | v1.5.0 | ✅ |

**marketplace 11 个 plugin version ↔ 对应 SKILL.md frontmatter version（逐一对）：**

| plugin | marketplace version | SKILL.md frontmatter | 结论 |
|---|---|---|---|
| moshu | 1.3.0（`marketplace.json:16`） | 1.3.0（`skills/moshu/SKILL.md:3`） | ✅ |
| moshu-build | 1.0.0（`:35`） | 1.0.0（`skills/moshu-build/SKILL.md:3`） | ✅ |
| moshu-scan | 1.2.0（`:56`） | 1.2.0（`skills/moshu-scan/SKILL.md:3`） | ✅ |
| moshu-analyze | 1.1.1（`:77`） | 1.1.1（`skills/moshu-analyze/SKILL.md:3`） | ✅ |
| moshu-write | 1.4.0（`:96`） | 1.4.0（`skills/moshu-write/SKILL.md:3`） | ✅ |
| moshu-deslop | 1.1.1（`:115`） | 1.1.1（`skills/moshu-deslop/SKILL.md:3`） | ✅ |
| moshu-cdp | 1.1.1（`:135`） | 1.1.1（`skills/moshu-cdp/SKILL.md:3`） | ✅ |
| moshu-setup | 1.4.0（`:155`） | 1.4.0（`skills/moshu-setup/SKILL.md:3`） | ✅ |
| moshu-review | 1.2.0（`:176`） | 1.2.0（`skills/moshu-review/SKILL.md:3`） | ✅ |
| moshu-import | 1.1.1（`:198`） | 1.1.1（`skills/moshu-import/SKILL.md:3`） | ✅ |
| moshu-style | 1.0.0（`:217`） | 1.0.0（`skills/moshu-style/SKILL.md:3`） | ✅ |

**3.1 结论：✅ 一致**。四轨均对齐 v1.5.0；11 个 plugin 与 frontmatter 全量一致。moshu-build 1.0.0、moshu-style 1.0.0（新建）、moshu 1.3.0、moshu-write 1.4.0（B1c bump）均为设计值，按口径不判值本身（`CHANGELOG.md:26` 印证 bump 链）。

---

## 3.2 术语表违例

判据：`docs/执行总纲V2.md:35-54` 术语表（续写状态卡/主对标/追踪/机检…）；批 1 结论（`docs/规格-V2/施工日志.md:87-89`、`批1-术语统一.md:65` 禁止事项 6）。

| 检查项 | 扫描范围 | 实测 | 结论 |
|---|---|---|---|
| `检查点卡` | skills/ scripts/ README.md docs/architecture.md | 0 命中 | ✅ |
| `当前语义检查点` | 同上 | 0 命中（原 workflow-daily.md:46 定义句已改为纯「续写状态卡」，见 `workflow-daily.md:26/34`） | ✅ |
| `当前检查点`（排除语义串） | 同上 | 0 命中 | ✅ |
| `workflow-setup` | skills/ scripts/ docs/architecture.md README.md README_EN.md CONTRIBUTING.md | 0 命中（B1b 硬验收口径） | ✅ |
| `story-long-write` | 同上 | 0 命中 | ✅ |
| `跟踪`（应为「追踪」） | skills/ scripts/ README.md README_EN.md CONTRIBUTING.md docs/architecture.md | 仅 1 命中 `skills/moshu-write/references/plot-emotion-system.md:234`「追逐/追踪/跟踪三种形式」——剧情技法词（追击场面形式），非机制术语，不构成违例 | ✅ |

**合规保留的「检查点」**（批 1 禁止事项 6 明确不动，逐个确认均属允许域）：
- `tracking-checkpoint` CLI 命令面/「追踪检查点门」：`story_hook_cli.js:8/13/128`、`guard-outline-before-prose.sh:7/128/150`、`UPGRADING.md:73`、`check-moshu-setup-deployment.sh:560`、`test-hook-encoding-portable.sh:51/163`。
- moshu-analyze「检查点间隔/机制」（另一机制）：`analyze-workflow.md:129/131/214/225`。

**3.2 结论：✅ 基本一致**。硬违例（检查点卡/当前语义检查点/当前检查点/workflow-setup/story-long-write/跟踪）全部零命中。2 处「检查点」松散措辞归为存疑（见 F3.4/F3.5），非硬违例。

---

## 3.3 悬空引用

**守卫实测（全部 exit 0）：**
- `bash scripts/static-check.sh` → 11/11 skill PASS（frontmatter + links + anchors + agents + references + dead-reference），0 warn（`static-check.py` 覆盖 `](...)` 链接、`references/`/`scripts/` 反引号路径、锚点、跨 skill、孤儿 reference）。
- `bash scripts/check-eval-scenarios.sh` → 3 剧本 OK（脚本路径存在性）。
- 自写只读链接扫描（skills/**/*.md + README×2 + docs/architecture.md + evals/**/*.md，`](...)` 相对路径解析）：**271 处链接，0 处真悬空**（5 处为模板占位符 `{URL}`：`scan-output-format.md`、`moshu-researcher.md`，属有意占位）。
- 脚本名存在性扫描（skills/**/*.md + README×2 + architecture + CONTRIBUTING，提取 `*.py/*.js/*.sh/*.mjs/*.cjs`，对照 skills/*/scripts/ 与 scripts/ 及 templates/）：**0 处悬空**。`backup_chapter.py` 当前范围零引用（仅见于 `docs/归档/` 与 `docs/研究-v3/` 档案，非运行时依赖）。

**重点抽查：**
- moshu-build 全目录：static-check PASS（含 workflow-build.md / revision-workflow.md / SKILL.md 链接、锚点、references 可达）。
- moshu-write references：static-check PASS（outline-workflow/chapter-core/workflow-daily/workflow-chapter 等零死链零孤儿）。
- `moshu/references/dashboard-guide.md`：全文无链接，无可悬空。
- `evals/scenarios/开书/README.md`：引用 `skills/moshu-build/SKILL.md`、`skills/moshu-build/references/workflow-build.md`、`skills/moshu-write/references/outline-workflow.md`、`tracking_commit.py check`（`:10-29`）均存在 ✅。
- 脚本实体核对：tracking_commit.py（build/import/review/write 4 副本）、impact_scan.py（build）、review_tickets.py（review）、check-ai-patterns.js / check-outline-copy.js / check-degeneration.js / normalize-punctuation.js（deslop/review/write 共享副本）、check-prose-candidates.js（write）、chapter_boundary.py（analyze）、volume-report（tracking_commit.py 子命令，4 副本均含 `volume_report()`）——全部存在。

**3.3 结论：✅ 一致**。零悬空链接、零悬空脚本引用、零孤儿 reference。

---

## 3.4 数字口径

| 口径 | 实测 | 期望 | 结论 |
|---|---|---|---|
| skill 数 | skills/ 含 SKILL.md 目录=11；README Skills 表 11 行（`README.md:88-98`）；README_EN 11 行（`README_EN.md:88-98`）；marketplace plugins=11；`check-story-numbers` → "11 skills" | 11 | ✅ |
| 契约数 | `behavior-contracts.json` contracts=14（实测 len=14）；`check-behavior-contracts` → "14 契约就位"；CONTRIBUTING 未硬编码条数（`CONTRIBUTING.md:104` 写"条数以 behavior-contracts.json 为准"） | 14 | ✅ |
| 组数/副本数 | `shared-assets.json` groups=71、copies=97（实测）；`check-shared-files` → "71 canonical groups manage 97 synchronized copies" | 71/97 | ✅ |
| capability-wiring | `capability-wiring.json` capabilities=12；`check-capability-wiring` → "12 能力 33 消费点全部就位"（33=按 (能力,消费文件) 去重后的消费点，见 `check-capability-wiring.py:43-56`） | 12/33 | ✅ |
| agents_version | 权威 `moshu-setup/UPGRADING.md:6` = `agents_version: 30`；`check-agents-version-sync` → "30 在全部 skills/**/*.md 一致"（grep `agents_version: 30` 共 13 行，全为 30） | 30 | ✅ |
| doc-budget 登记文件数 | `doc-budget.json` files=20；`check-doc-budget` 输出 20 行文件全部 `[ok]` | 20 | ✅ |

**旧数字残留（README/scripts-README/CONTRIBUTING）：** `10 个 skill` / `9 条` / `36 组` 在扫描范围内零命中。但发现 2 处相关过期数字（见 F3.2、F3.3）。

**3.4 结论：✅ 一致**（六口径全对）。附带发现 F3.1（双语路由漂移，README_EN 自然语言示例）、F3.2（README_EN 31/189 过期）、F3.3（总纲 36 组过期）。

---

## 3.5 doc-budget 未登记热路径

**B1 三文件登记核对（`doc-budget.json` + `check-doc-budget` 实测）：**

| 热路径文件 | 已登记 | 实测用量/预算 | 结论 |
|---|---|---|---|
| `skills/moshu-build/SKILL.md` | ✅ | 1486/1560 | ✅ |
| `skills/moshu-build/references/workflow-build.md` | ✅ | 7677/8055 | ✅ |
| `skills/moshu-build/references/revision-workflow.md` | ✅ | 1879/1972 | ✅ |
| `skills/moshu-write/references/outline-workflow.md` | ✅ | 6641/6986 | ✅ |
| `skills/moshu-write/references/chapter-core.md` | ✅ | 16978/17100 | ✅ |

路径组合计（`check-doc-budget`）：长篇日更主会话 17858/36200、长篇开书 15668/16186（= moshu-write/SKILL 9027 + outline-workflow 6641，已接替被删的 workflow-setup 13600 登记）、正文 agent 上下文 43603/43700、构建路径 11042/11587（= build 三文件 1486+7677+1879=11042）。

**抽查未登记新热路径：** 无发现。`tracking-transaction.md` / `recovery-protocol.md` / `workflow-revision.md` 等按需加载（references 副本），按既有口径不登记；脚本（impact_scan.py 等）不登记——均符合口径。

**3.5 结论：✅ 一致**。B1 三 build 文件 + outline-workflow + chapter-core 全部在表；无新热路径漏登记。

---

## 3.6 V3 已修项回归抽查（10 项）

| # | 项 | 实测 | 结论 |
|---|---|---|---|
| 1 | RB1 审稿令牌链 | `review-workflow.md:180`（令牌生成+首行注入）、`:187/:223/:253/:285`（4 spawn prompt 首行 `审稿令牌：{token}`）、`:329`（采纳前逐字比对）、`:332`（`verify-token` 确定性复核） | ✅ 仍绿 |
| 2 | S1/S2 文风「平均句长」 | `moshu-style/SKILL.md:98`「平均句长 N 字」；`style-learn-sop.md:60` 强制字段名逐字「平均句长」；`test-prose-candidates.js:89/107` 契约用例锁住 | ✅ 仍绿 |
| 3 | W1 information_gap_changes | `tracking_commit.py:734/775/802/1122`（事务字段）；`chapter-core.md:149/153`、`workflow-chapter.md:18`、`workflow-daily.md:54` 写入指引 | ✅ 仍绿 |
| 4 | W2 候选机检接入单章收尾 | `chapter-core.md:118`「候选机检（可选，永不拦截）…check-prose-candidates.js」；`workflow-daily.md:83` 批末复扫链 | ✅ 仍绿 |
| 5 | W3 批前查 open 工单 | `workflow-daily.md:17`「查 `review_tickets.py list --status open`…先闭环再开写」；`workflow-revision.md:81-82` 处置读点 | ✅ 仍绿 |
| 6 | AM1 chapter_boundary.py --renumber-volumes | `chapter_boundary.py:80`（参数）、`:130-152`（重编逻辑）、`:152`（提示） | ✅ 仍绿 |
| 7 | M1 判定序优先中断 | `next_step.py:13-14/89-102`（P2 analyze INTERRUPT）、`:110-120`（P3 review INTERRUPT） | ✅ 仍绿 |
| 8 | PM1 deploy.py verify 非零退出 | `deploy.py:222-223`（settings FAIL 计入 fatal）、`:253`（verify）、`:365-366`（"PM1：验证失败必须以非零退出码暴露" `sys.exit(1)`） | ✅ 仍绿 |
| 9 | G1 marketplace 版本断言 | `check-claude-adapter.sh:54/70-80`（version 必须等于 SKILL.md frontmatter，不等 `raise SystemExit`） | ✅ 仍绿 |
| 10 | PM4 hook 头注 | 各 hook 头部注释齐备：`guard-outline-before-prose.sh:2-3`（PreToolUse + exit 2 BLOCKING）、`check-prose-after-write.sh:2-20`、`validate-story-commit.sh:2`（WARNING no BLOCKING）、`session-start.sh:2-3` 等 | ✅ 仍绿 |

**3.6 结论：✅ 10/10 仍绿**，无回退。

---

## 发现汇总

| 编号 | 级别 | 证据 | 影响一句话 |
|---|---|---|---|
| F3.1 | 需修 | `README_EN.md:102`：自然语言示例「帮我开书 → `moshu-write`」，中文 `README.md:103` 已改「→ `moshu-build`」 | 英文 README 未随 B1 路由切换，开书入口双语漂移，违背「README 五方一致」 |
| F3.2 | 候选 | `README_EN.md:125`：硬编码「31 methodology files / 189 references」，实测 agent-references 64 文件、全仓 references 239 份；中文 `README.md:129` 已脱敏为「数量随版本增长/数百份」 | 英文侧漏同步 G7 脱敏，硬编码数字过期（清单类断言最易过期） |
| F3.3 | 候选 | `docs/执行总纲V2.md:53` 术语表「共享资产（36 组）」，实测 shared-assets.json=71 组 | 权威术语表数字过期；总纲为判定依据，是否随 V4 勘误需作者裁定 |
| F3.4 | 存疑 | `skills/moshu-build/references/workflow-build.md:182`「生成检查点、续写状态卡（固定 7 栏）」把「检查点」与「续写状态卡」并列 | 术语表定义「续写状态卡=当前语义检查点」，二者同物，此处措辞轻微冗余（非硬违例） |
| F3.5 | 存疑 | `skills/moshu-setup/references/templates/rules/story-consistency.md:44`「检查点却无法知道哪些派生文件已经失效」 | 「检查点」裸用指代追踪状态，语义松散（机制名「追踪检查点」合法，但此处语义指追踪状态） |

**统计：需修 1、候选 2、存疑 2，共 5 项。**
