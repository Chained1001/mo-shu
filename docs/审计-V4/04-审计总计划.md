# 04 审计总计划（审计-V4 第4部分）

- 审计基线：HEAD `3391fa3`（V2.1 战役 B1a-c 之后、v1.5.0 发布之前）
- 日期：2026-08-21 ｜ 角色：审计员（只查不改不修）｜ 唯一写入：docs/审计-V4/
- 关联产出：[00-基线-守卫全量.md](00-基线-守卫全量.md) ｜ [01-变更面深审.md](01-变更面深审.md) ｜ [02-机制链审计.md](02-机制链审计.md) ｜ [03-一致性轻扫.md](03-一致性轻扫.md)

---

## 1. 审计方式与复核纪律

- **第 0 部分**（守卫矩阵）：本人直接执行，41 项全量。
- **第 1/2/3 部分**（深审）：三个并行子代理深审产出 01/02/03，**本人对全部关键结论独立复核**（每项标注 ✅本人复核成立 / 🔄定性修正）。
- **自我推翻记录**（V3 纪律延续，不信任转述）：
  1. **F1.1 定级推翻（阻断 → 需修-高）**：子代理判"阻断"（中文 Windows 下 test-impact-scan 全挂）。本人复现确认技术事实成立（无 `PYTHONIOENCODING` 时 errors=3，`impact_scan.py` 确无 reconfigure），但对照 **V3 审计-跨skill与仓库级.md:91 记载的同类平台限制**（test-static-check/test-story-numbers/test-behavior-contracts 等 6 个 test-* 在中文 Windows 直跑必红、设 PYTHONIOENCODING=utf-8 后全绿，V3 整改 A→H 闭环时此问题仍存在并被接受）+ 实测对照 test-static-check.py 同样红 + README §7 明确 Windows 需设 PYTHONIOENCODING + CI（Linux UTF-8）必绿 → **定性修正为需修（高优先，建议发布前补 3 行对齐 next_step.py:32-36），非阻断**。技术事实不推翻，仅降级。
  2. **F2.1 复核成立（需修）**：本人独立 grep 确认 build 侧 `单元ID` 零命中、write 侧 4 处必读 → 子代理判定属实。
  3. **F1.2 复核成立（需修-低）**：本人实测 S2 fixture → moshu-write，判定表序3 写 build → 矛盾属实；脚本（真源）语义正确。
  4. **README_EN:102 双语漂移**：第 1/3 部分子代理独立发现同一问题（F1.3=F3.1），本人复核确认，合并为一条。

## 2. 基线结果摘要（详见 00）

- 守卫矩阵 41 项 = **40 绿 + 1 平台限制红**（test-shared-assets mode-drift，Windows chmod no-op，批9 已知平台限制，CI Linux 绿，不计为发现）。
- 全部业务守卫与测试零红。

## 3. 六链判定（详见 02，✅本人复核）

| 链 | 判定 | 证据核心 |
|---|---|---|
| ① 修订工作流链（build） | **闭合** | 五步执行入口全在；impact_scan 实测三清单边界/退出码正确 |
| ② 开书→细纲接力链 | **断裂（字段级）** | 单元ID build 未产出、write 必读（F2.1） |
| ③ 审稿令牌链 | **闭合** | 生成→4 spawn 注入→采纳前比对→verify-token 四环齐全，实测 exit 0/2 |
| ④ schema 消费方链 | **闭合** | 4 副本 tracking_commit 字节一致、TRACKING=5/INPUT=2、hook/evals 钉当前值 |
| ⑤ 工单链 | **闭合** | list/resolve 指示 ↔ 脚本子命令一致（附带跨 skill 定位观察 F2.2） |
| ⑥ stale 链 | **闭合** | 三方前缀 `<!-- stale:` 一致 + 移除指示同句 |

## 4. 发现分级汇总（去重后）

### 阻断级：0

（F1.1 经自我推翻降级，见上；六链无阻断；基线无红。）

### 需修级：6

| 编号 | 证据（路径:行号） | 影响一句话 | 建议修法（最小） |
|---|---|---|---|
| A1 | `skills/moshu-build/scripts/impact_scan.py`（全文无 UTF-8 重配）vs `skills/moshu/scripts/next_step.py:32-36` | 中文 Windows 无 PYTHONIOENCODING 时 test-impact-scan 全挂；UTF-8 harness 解析其输出失败 | 头部补 `sys.stdout/stderr.reconfigure(encoding="utf-8")` 三行（对齐 next_step.py） |
| A2 | `skills/moshu-build/references/workflow-build.md:156`（剧情单元卡字段模板未列单元ID）vs `outline-workflow.md:34`、`chapter-core.md:11`、`workflow-daily.md:44` 必读 | build 卷纲产出漏 write 必读字段，细纲「单元ID/位置」锚点悬空 | workflow-build.md:156 字段模板补「单元ID」 |
| A3 | `skills/moshu/SKILL.md:50`（判定表序3「有书但无正文→开书 /moshu-build」）vs `next_step.py:158-167`（S2→写第1章→moshu-write） | 判定表与脚本矛盾；:42 宣称「判定语义与脚本一致」不成立，Python 缺失回退判定表时误路由 | 判定表序3 改为「写第 1 章（/moshu-write，先补第 1 章细纲）」 |
| A4 | `README_EN.md:102`「帮我开书→moshu-write」vs `README.md:103`「→moshu-build」 | 英文 README 开书路由滞后，违背「五方一致」自述 | README_EN:102 改为 → moshu-build |
| A5 | `skills/moshu-import/SKILL.md:74`「从零开新书→/moshu-write+开书」；`skills/moshu-setup/SKILL.md:65/:92`「直接开书→/moshu-write」；`skills/moshu-setup/references/deploy-manual.md:90`「开书（moshu-write Phase 2）」 | 跨 skill 路由语义残留：用户会被引导到 write 开书（write 内部虽会转 build，但入口语义错误） | 4 处改为 /moshu-build（命令文本） |
| A6 | `skills/moshu-write/SKILL.md:4`（frontmatter 触发词仍列「帮我开书」「写大纲」） | write 入口触发词与「开书→build」路由语义歧义 | 触发词清理或标注「开书转 /moshu-build」 |

### 候选级：5

| 编号 | 证据 | 影响一句话 |
|---|---|---|
| B1 | `skills/moshu-scan/references/topic-decision.md:4`（「写作（moshu-write）开书时直接用」）、`:83`（「/moshu-write 会自动找到」） | 描述性文本过时（:77 已改、:83 漏改），误导用户 |
| B2 | `scripts/test-next-step.py:62,69,121`（仅断言 S0/S1/S6 suggested_skill，S2/S3/S5 未断言） | 回归覆盖缺口：S2/S3/S5 的 write 语义无断言守护 |
| B3 | `README_EN.md:125`（硬编码 31 files/189 references，实测 64/239） | 英文侧漏同步 G7 脱敏，清单类断言过期 |
| B4 | `docs/执行总纲V2.md:53`（术语表「共享资产 36 组」实测 71 组） | 权威术语表数字过期，是否勘误需作者裁定 |
| B5 | `skills/moshu-review/scripts/review_tickets.py`（单副本）vs `workflow-revision.md:81-82` 无路径前缀引用 | 跨 skill 定位依赖运行时搜索路径，无显式标注（B1a 已记录开放点） |

### 存疑级：2

| 编号 | 证据 | 说明 |
|---|---|---|
| C1 | `skills/moshu-build/references/workflow-build.md:182`「生成检查点、续写状态卡（固定 7 栏）」 | 「检查点」与「续写状态卡」并列，术语表定义二者同物，措辞轻微冗余（非硬违例） |
| C2 | `skills/moshu-setup/references/templates/rules/story-consistency.md:44`「检查点却无法知道哪些派生文件已经失效」 | 「检查点」裸用指代追踪状态，语义松散（机制名「追踪检查点」合法） |

### 分级计数

- **阻断 0 ｜ 需修 6 ｜ 候选 5 ｜ 存疑 2**（去重后共 13 项；F1.3 并入 A4、F3.1 同源）

## 5. 建议整改批次编排

- **A 批（阻断→需修-高）**：A1（impact_scan UTF-8 重配，3 行）+ A2（单元ID 补字段，1 行）——两处均为 B1 新能力的最小缺口，建议 v1.5.0 发布前/紧随修复。
- **B 批（需修）**：A3（判定表序3）、A4（README_EN:102）、A5（4 处跨 skill 路由残留）、A6（write 触发词）——路由一致性收口，与 B1b/B1c 的"五方一致"目标同批。
- **C 批（候选）**：B1-B5——呈报作者勾选；B4 涉总纲勘误需作者裁定。
- **D 批（存疑）**：C1-C2——仅记录，作者决定是否动。

## 6. 结论

V2.1 B1 战役（moshu-build 新建 + write 拆分 + 契约锁定）整体质量良好：**六链五闭合一字段级断裂、基线 40 绿、无阻断级**。主要问题集中在**路由切换的跨 skill 遗漏面**（英文 README、import/setup/scan 侧共 6 处"开书→write"残留）与 **2 处 B1 新能力最小缺口**（impact_scan 重配、单元ID 字段）。审计-V3"守卫锁话不锁链"根因在本轮六条链上基本修复到位（五条闭合，唯一断裂为字段级非脚本能力缺失）。
