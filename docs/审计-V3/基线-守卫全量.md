# 守卫全量基线（整改前）

- 采集日期：2026-08-21
- 用途：整改前的**绿基线快照**。整改后逐项复跑对比；任何由整改引入的新红必须判因修代码，**禁止改断言变绿**（`AGENTS.md` §2.3）。
- 采集环境：Windows 11 + Git Bash（MINGW64）+ Python 3.12.8 + Node v25；**无 `python3`**，用临时 shim `.tmp/bin/python3`（`exec python "$@"`）——与 `docs/规格-V2/README.md` §7 的 Windows 惯例一致。
- 复跑命令：`bash .tmp/audit/run-guards.sh`（临时脚本，用完即删；等价于遍历 `scripts/check-*.sh`、`test-*.{sh,py,js}` + `eval-prose-quality.sh` + `skill-numbering.py check`）。

## 结果总览

| 组 | 数量 | 结果 |
|---|---|---|
| `scripts/check-*.sh` | 13 | **全绿**（含最慢的 `check-moshu-setup-deployment.sh` 与 `check-claude-adapter.sh`） |
| `scripts/test-*.sh` | 9 | **全绿**（含 `test-ai-patterns.sh`、`test-writing-pipeline.sh`、`test-skill-numbering.sh`） |
| `scripts/test-*.py` | 10 | 4 绿 + 5 绿（需 `PYTHONIOENCODING=utf-8`）+ 1 已知平台限制 |
| `scripts/test-*.js` | 5 | **全绿** |
| 其他 | 2 | **全绿**（`eval-prose-quality.sh`、`skill-numbering.py check`） |

**结论：仓库本体在等价条件下 38/38 绿，无实质失败。** 首轮 6 项红全部判因为环境，非缺陷：

## 判因记录（首轮 6 红）

| 脚本 | 首轮现象 | 判因 | 证据 |
|---|---|---|---|
| `test-agent-template-rules.py` | `UnicodeDecodeError` → `result.stderr` 为 None → `TypeError` | 中文 Windows 子进程以 GBK(cp936) 输出，父测试按 utf-8 解码 | 加 `PYTHONIOENCODING=utf-8` 后 **exit 0** |
| `test-agents-version-sync.py` | 同上（`:73`） | 同上 | 同上 |
| `test-behavior-contracts.py` | 同上（`:61`） | 同上 | 同上 |
| `test-static-check.py` | 同上（`:103`） | 同上 | 同上 |
| `test-story-numbers.py` | 同上（`:64`） | 同上 | 同上 |
| `test-shared-assets.py` | `:82` mode_drift 断言失败 | **Windows `chmod` 是 no-op**，无法制造 mode 漂移 | `:64-65/:80-82` 依赖 `chmod 0o755/0o644`；`docs/规格-V2/施工日志.md:304` 批 9 已记录为已知平台限制，CI Linux 绿 |

## CI 落位事实（用于判断上述红的影响面）

`.github/workflows/cross-platform.yml`：
- **static-guards job（ubuntu-latest）**跑上述 5 个 `test-*.py`（`:24/:26/:42/:46/:50/:56`）——它们**只在 Linux 跑**，windows job 不含；
- **windows-latest job**（`:137`起）只跑检测器/追踪/编号/共享文件/标点/scan runtime/CDP/字符统计/hook 编码可移植性；
- 因此这 5 项的中文 Windows 不可移植性**在 CI 视野之外**，属本地开发体验问题（见总计划 G4）。

## 与 V2 收官记录的一致性

`docs/规格-V2/施工日志.md:304`（批 9 C3）称「CI 清单全量 39 项逐条跑，全部退出 0」，并同样记录了两条平台说明（`test-shared-assets` mode-drift 必失败、三个脚本内部硬编码 `python3` 需 shim）。本次基线**独立复现了同一结论**，V2 收官状态可信。
