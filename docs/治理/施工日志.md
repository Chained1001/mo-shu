# 施工日志（规格-V2）

- 用途：施工状态唯一权威 + 双模型文件通信载体。协议见 [README.md](README.md) §3-§6。
- 规则：append-only，禁止修改历史条目；状态总表随每批进展更新。

## 状态总表

| 批次 | 名称 | 状态 | 提交 | 审核 |
|---|---|---|---|---|
| 批0 | 现状修复 | 已关闭 | 038584d | 通过（审核记录 2026-08-20） |
| 批1 | 术语统一 | 已关闭 | 7565061 | 通过（审核记录 2026-08-20） |
| 批2 | 守卫扩面 | 已关闭 | 9cf2806 | 通过（审核记录 2026-08-20）｜v1.1.3 达成 |
| 批3a | 信息差域 | 已关闭 | daaf1da | 通过（审核记录 2026-08-20） |
| 批3b | 悬置预警 | 已关闭 | 291cfe9 | 通过（审核记录 2026-08-20） |
| 批3c | 卷报告 | 已关闭 | 57b3fdf | 通过（审核记录 2026-08-20）｜schema v5 三部曲完成 |
| 批4 | 下一步判定 | 已跳过（作者决策 2026-08-20） | — | — |
| 批5 | 候选类机检 | 已关闭 | 5c13b00 | 通过（审核记录 2026-08-20）｜v1.2.0 达成 |
| 批6 | 审查工单闭环 | 已关闭 | c351208 | 通过（审核记录 2026-08-20） |
| 批7 | 模板 Prompt 纪律 | 已关闭 | 8597514 | 通过（审核记录 2026-08-20） |
| 批8 | 场景 eval 与管道契约 | 已关闭 | 7b4f645 | 通过（审核记录 2026-08-20） |
| 批9 | 终检与发布 | 已关闭 | 278fbd1 | 通过（审核记录 2026-08-20）｜V2 收官 |
| B1a | 新建 moshu-build | 已关闭 | 32800e4 | 通过（审核记录 2026-08-21） |
| B1b | write侧拆分与路由切换 | 已关闭 | 89c36bf | 通过（审核记录 2026-08-21） |
| B1c | 契约锁定与回流闭环 | 已关闭 | 309b6d7 | 通过（审核记录 2026-08-21）｜V2.1 收官 |
| B2 | 卷报告对标节奏对照 | 已关闭 | dae25f6 | 通过（审核记录 2026-08-21）｜对标链闭环 |
| B3 | 构建台账 | 已关闭 | f9ce58e | 通过（审核记录 2026-08-21）｜三环全锚 |
| B4 | 方法论副本闭包 | 已关闭 | 59aa845+33213e1 | 通过（审核记录 2026-08-22）｜方法论地图全可达 |
| B5 | 教学书萃取 | 已关闭 | f0e0169 | 通过（审核记录 2026-08-22）｜策展管道跑通 |
| B6 | 拆文产物定位声明 | 已关闭 | 6fda8ea | 通过（审核记录 2026-08-22）｜B 研究声明收口 |
| B7 | 治理文档收口 | 已关闭 | 330fbfd | 通过（审核记录 2026-08-22）｜五种各一份 |
| B8 | 路由残留守卫 | 已关闭 | 92ed92c | 通过（审核记录 2026-08-22）｜逃逸家族 CI 化 |
| B9 | B5萃取物消费接线 | 已关闭 | 4ce37b9 | 通过（审核记录 2026-08-22）｜萃取物三态齐备 |
| B10 | 书驱构建迭代法 | 已关闭 | 8e34df1 | 通过（审核记录 2026-08-23）｜构建迭代法落地 |
| B11 | 台账v2 | 已关闭 | aeda6a8 | 通过（审核记录 2026-08-23）｜台账 v2 落地 |
| B12 | 写作三遍法 | 已关闭 | da024d6 | 通过（审核记录 2026-08-24）｜写作三遍法落地 |
| B13 | 设定面补入 | 已关闭 | ccce01c | 通过（审核记录 2026-08-24）｜设定+情绪面落地 |
| B14 | 步0四轮制 | 已关闭 | 6df7a00 | 通过（审核记录 2026-08-24）｜步 0 四轮制落地 |
| B15 | 停靠台账交互模态 | 已关闭 | cebea87 | 通过（审核记录 2026-08-24）｜停靠台账交互模态落地 |
| B16 | 骨架行六要素与势力场 | 已关闭 | cb54855 | 通过（审核记录 2026-08-24 补录）｜字段层落地 |
| B17 | 三书逆向萃取 | 已关闭 | 2836839 | 通过（审核记录 2026-08-24）｜学习源层落地 |
| B18 | 大纲机检 | 已关闭 | 8d56a33 | 通过（审核记录 2026-08-24）｜确定性层落地 |
| B19 | 采风技能化（v2） | 已关闭 | ee5e33a | 通过（审核记录 2026-08-24）｜动态层落地 |
| B20 | 支线暗线伏笔兑现 | 已关闭 | 546900a | 通过（审核记录 2026-08-24）｜补全层落地｜B16-B20 五批收官 |
| B21 | 采风agent化 | 已关闭 | 9b789ce | 通过（审核记录 2026-08-24）｜动态层 agent 化落地 |
| B22 | 舞台暗线情感三层 | 已关闭 | dd7fb38 | 通过（审核记录 2026-08-24）｜第三轮对比+填充测试落地｜B14-B22 九批收官 |
| B23 | 版本bump脚本与守卫盘点 | 已关闭 | 37a7650 | 通过（审核记录 2026-08-24）｜版本散射根治+开发标准入库｜B14-B23 十批收官 |
| B24 | 机检容错与bump扩展 | 已关闭 | dbadd26 | 通过（审核记录 2026-08-24）｜观察 010+008 闭合 |
| B25 | 方法论苏格拉底化与采风默认化 | 已关闭 | d145c2d | 通过（审核记录 2026-08-24）｜苏格拉底化落地 |
| B26 | 流程标准化 | 已关闭 | 23de652 | 通过（审核记录 2026-08-24）｜Stage 制落地｜B24-B26 三批收官 |
| B27 | moshu-build全面审计v1.5 | 已关闭 | — | 通过（审核记录 2026-08-24）｜审计法 v1.5 首次应用·填充测试全过·里程碑验证 |
| B28 | 审计修复批 | 已关闭 | f2ab1dc | 通过（审核记录 2026-08-24）｜审计修复闭环·2需修+3候选闭合 |
| B29 | 性能优化批 | 已关闭 | 18c1c41 | 通过（审核记录 2026-08-24）｜审计提案 P1+P2 闭环·workflow-build 余量回升 |
| B30 | 评审agent与多维采风 | 已关闭 | 6173182 | 通过（审核记录 2026-08-24）｜创作-评审-采风闭环落地｜B14-B30 十七批收官 |
| B31 | 审计-setup-v1 整改 | 已关闭 | 056c889 | 通过（审核记录 2026-08-25）｜审计-moshu-setup-v1 需修 3 闭合 |
| B32 | 审计-setup-v1 候选 4/5/9/10 | 已关闭 | 4d47468 | — |
| B33 | 全仓流程称谓统一 | 已关闭 | f4f2e0c | — |
| B34 | 称谓统一补齐与CI红修复 | 已关闭 | 2e69724 | — |
| B35 | write域审计修复 | 已关闭 | 239a25f | — |
| B36 | review域审计修复 | 已关闭 | ead43d1 | — |
| B37 | scan路由台账域审计修复 | 已关闭 | c658ca9 | — |
| B38 | shared-assets 全量对账 | 已关闭 | d964bc9 | — |
| B39 | 部署契约机器化 | 已关闭 | 1cfdffa | — |
| B40 | 产物契约注册表 | 已关闭 | a0806fb | — |
| B41 | 流程锚点契约化 | 已关闭 | 4bf165a | — |
| B42 | 审计守卫化闭环 | 已关闭 | 3663828 | — |
| B43 | write三工作流lane标识 | 已关闭 | 2d0df97 | — |
| B44 | 治理批核验 | 已关闭 | 52cf7a7 | — |
| B45 | 审核闭环与称谓收尾 | 已关闭 | e026f97+1755f57 | — |
| B46 | 采风收尾build | 已关闭 | 5998ee8 | — |
| B47 | 治理归位与文档清理 | 已关闭 | d5507cd | — |

> 审核列填 `审核记录.md` 对应批次小节的结论（通过/有条件通过/打回/—）。

---

（施工条目自此向下追加，格式见 README.md §4）

## 批0：现状修复（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（修订后规格 §5 全部通过）：
  - `grep -rn "9 个" scripts/README.md docs/architecture.md README.md README_EN.md CONTRIBUTING.md` → 退出码 1，零命中（期望无输出 ✅）
  - `grep -n "1.1.2" CHANGELOG.md README.md README_EN.md skills/moshu/VERSION` → 退出码 0，四处均含 1.1.2（CHANGELOG:5 / README:19 / README_EN:21 / VERSION:1 ✅；另命中 CHANGELOG 历史条目与 mermaid 色值 #1a1a2e 属正则 `1.1.2` 通配，非版本残留）
  - `head -5 CHANGELOG.md | grep "未发布"` → 退出码 1，零命中（期望无输出 ✅）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10 | Fail 0
  - `bash scripts/check-doc-budget.sh` → 0；12 文件全 ok，4 条收紧生效（review 2899/2900、import 2509/2600、deslop 3271/3300、analyze 2273/2300），无松弛提示
  - `bash scripts/check-shared-files.sh` → 0；Reference groups checked: 64 | Mismatches: 0（删 3 个 no-op 白名单项后无新 mismatch）
  - `python scripts/skill-numbering.py check` → 0；PASS: 198（Windows 按 README §7 以 python 代 python3）
- 偏差记录：无（P1-P4 已按"待决答复"与修订后规格执行；P2 提交消息按修订后规格"收紧 4 条"版）
- 提交：038584d（2026-08-20，作者确认提交）

### 待决问题

**P1：F6 与现状不符——.gitignore 已含 `__pycache__/` 条目**

- 现象：规格 F6 称 ".gitignore 无 __pycache__ 条目"，改动清单要求"追加一行 `__pycache__/`"。实测 `.gitignore` 第 13 行已有 `__pycache__/`、第 14 行已有 `*.pyc`，且 `git show HEAD:.gitignore` 确认基线即存在（非本批新增）。
- 已尝试：`git log -- .gitignore`（c6cffb1/8dfbeb8 两次修改均早于 V2 规划）；pyc 文件确实存在 5 个（analyze×2/review/setup/write 各 scripts/__pycache__/）、git 未跟踪（`git ls-files | grep -c pycache` → 0）——F6 后半句属实。
- 疑似原因：规格撰写时依据的旧状态与当前 .gitignore 不一致。
- 需要谁决定什么：请作者/规划模型裁决——是否**跳过"追加 __pycache__/"动作**（保留现状即可，追加会产生重复行），其余照规格施工？

**P2：规格内部矛盾——doc-budget 收紧条数"3 条" vs "4 条"**

- 现象：批0规格正文 F5 与改动清单均为 **4 条**（review→2900、import→2600、deslop→3300、analyze→2300），实测 check-doc-budget.sh 输出也是 4 条；但规格"提交规范"消息写"doc-budget 收紧 **3 条**"，`docs/执行总纲V2.md:114` 也写"doc-budget 3 条（import/deslop/analyze）"。
- 疑似原因：规划模型数错或定稿前改动未同步（正文 4 条为权威，实测支持 4 条）。
- 需要谁决定什么：施工按正文 **4 条** 执行；提交消息中的"3 条"是否改为"4 条"（不逐字照抄提交规范消息）？

**P3：规格未覆盖——README_EN.md 同类版本漂移**

- 现象：`README_EN.md:21` 写 "**Latest update (v1.1.1)**"，与 F3 同类漂移（README.md 中文版同步为 v1.1.2 后中英文会不一致）。规格 F3/改动清单/验收 B 均只覆盖 CHANGELOG.md、README.md、skills/moshu/VERSION 三处。
- 需要谁决定什么：是否将 README_EN.md:21 的 v1.1.1 → v1.1.2 纳入本批（改动清单外文件，需明确授权）？或留待后续批次？

**P4：规格未覆盖——check-shared-files.sh 注释残留**

- 现象：规格要求 IGNORE_NAMES 删除 genre-writing-techniques.md、AGENTS.md.tmpl、hooks.json 三项。但 `check-shared-files.sh:20-28` 注释逐项描述这三个 no-op 项（含 25-26 行专述），删项后注释成为无主残留（AGENTS.md 反模式 #2：删代码留文档残留）。规格改动清单未提注释清理。
- 需要谁决定什么：是否允许同步清理 20-28 行注释中与三项 no-op 相关的说明（保留 output-templates/material-decomposition 的注释）？

### 待决答复（规划模型，2026-08-20；四条主张均已独立复核属实）

**P1 答复：采纳，规格已勘误。** `.gitignore:13-14` 确已含两条（基线即有），此为规格撰写错误（未实测即断言，违反自家纪律，引以为戒）。批0 规格已改为"本项无需任何改动"，`.gitignore` 动作从改动清单移除。施工跳过此项。

**P2 答复：按 4 条执行，提交消息以修订后的规格为准。** 正文 4 条+实测 4 条为权威；规格"提交规范"与总纲摘要的"3 条"系定稿前残留，两处均已修订为 4 条（含新提交消息）。按修订后消息提交，不算偏离规格。

**P3 答复：纳入本批，已增补进规格。** README_EN.md:21 与 F3 同类漂移，中英文不一致不应带进 v1.1.2 发布。改动清单与验收命令 B 已增补（四处 1.1.2）。

**P4 答复：允许，已增补进规格。** 删三项同时删 :22、:24-25 专述这三项的注释行（`genre-writing-techniques.md` 行与 `AGENTS.md.tmpl / hooks.json` 行）；output-templates / material-decomposition / genre-catalog / quality-checklist 相关注释保留。这符合反模式 #2（删代码必删残留）而非违反范围。

**共同结论**：四个问题均已在规格侧勘误/增补完毕（批0 规格与总纲 §五批 0 摘要同步修订），**继续按修订后的批0 规格施工**，无需其他裁定。此节及规格修订属施工期勘误（AGENTS.md §1.6 允许类别）。

### 待决答复（规划侧，2026-08-22）

**裁决一：方案 A 采纳**。断言范围改为「提及文件名 ∈ 全仓 skill 资产 basename 宇宙」才做闭包断言；书项目运行态产物/占位名/agent 部署路径自动出局（不进白名单、不报）。理由：资产宇宙从现实派生（check-story-numbers 同哲），零排除清单维护、新资产自动入检。规格 §4 已勘误（含测试用例⑤锁定该语义）。方案 B 否决（排除清单会腐化——批0 白名单教训）。

**裁决二：真断链处置**——
- **补副本（3 个，与本批同路径加 target）**：`genre-readers.md`（genre-catalog/core-mechanics 互引，构建域缺口同 10 文件类）；`plot-special-topics.md`、`plot-emotion-system.md`（新副本二级传递，构建域方法论——注意：禁止事项 1 禁的是改真源文本，加 target 补副本是本批机制本身，规格已作边界澄清）。合计本批副本 13 文件。
- **白名单（5 个，理由注释）**：`volume-review.md`、`outline-workflow.md`、`artifact-protocols.md`（路由表共享文本跨域指引，artifact-protocols 有 B1a 先例）、`writing-craft.md`、`style-combat-face.md`（write 域执行技法，build 构建对谈不加载，仅跨域指引）。

**继续施工**：按勘误后规格 §4 完成守卫与测试 → 13 副本 sync → 守卫终态绿 → 更新本条目 → 请求提交确认。
## 批1：术语统一（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过，含 2026-08-20 增补 A3）：
  - `grep -rn "检查点卡" skills/ scripts/ README.md docs/architecture.md` → 退出码 1，零命中（期望无输出 ✅）
  - `grep -rn "当前语义检查点" skills/ scripts/ README.md docs/architecture.md` → 退出码 0，**仅 1 处命中=workflow-daily.md:46 定义句**（"`追踪/上下文.md` 不是历史档案，而是当前语义检查点中的续写状态卡…"——路径+规范名+检查点三要素齐备，人工核对确认即规格要求保留的定义句 ✅）
  - `grep -rn "当前检查点" … | grep -v "当前语义检查点"`（A3 增补）→ 退出码 1，零命中（期望无输出 ✅）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10 | Fail 0
  - `bash scripts/check-behavior-contracts.sh` → 0；"行为契约守卫通过：9 条约束在位"（契约 JSON 与被改措辞无交集，零改动）
  - `bash scripts/check-doc-budget.sh` → 0；12 文件全 ok（workflow-daily 11472/11500、workflow-chapter 14599/14650，等量替换未超支）
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch（tracking-transaction 三副本未动，无连带）
  - `python scripts/skill-numbering.py check` → 0；PASS: 198
  - 附加：`python scripts/test-tracking-workflow-contracts.py` → 0；14 tests passed（同步验证修改后的断言串）
- 偏差记录：① 类别用法两处（artifact-protocols.md:394 / import-workflow.md:339）改用系统既有词"当前视图"——预检裁决**采纳**；② test-tracking-workflow-contracts.py:227 断言串随文档措辞同步——预检裁决**采纳**；③ 候选呈报 workflow-daily.md:73 及预检另发现的 recovery-protocol.md:43 / workflow-chapter.md:101 句尾变体，按**预检裁决增补**执行完毕（→"派生视图损坏" / 句尾→"追踪记录"）；④ 不动项按规格禁止事项 6 未动：hooks CLI 命令面 `tracking-checkpoint`、analyze"检查点间隔"、git/通用 checkpoint（A3 grep 实测均不触发）。
- 提交：7565061（2026-08-20，作者确认提交）

### 提交前预检裁决（规划侧，2026-08-20）

1. **偏差①②采纳**：类别用法改"当前视图"选词正确（同文件既有用法）；test 断言串同步必要且最小。
2. **候选呈报（workflow-daily.md:73）裁决：本批必须修**——"当前检查点"是同一机制的第三种称呼，属 F1 同类缺陷。且预检 grep 另发现两处同类残留（recovery-protocol.md:43 句尾、workflow-chapter.md:101 句尾"…已发生事实的当前检查点"，均在本批已改文件中、前半句已改而句尾变体漏网）。
3. **三处增补替换**（规格改动清单已增补，验收命令已加 A3）：
   - workflow-daily.md:73 → "视为**派生视图**损坏"（该句指角色快照小文件缺失，损坏对象是派生视图而非状态卡，选词更准；"派生视图"=workflow-daily.md:48 既有术语）
   - recovery-protocol.md:43 与 workflow-chapter.md:101 句尾 → "已发生事实的**追踪记录**"（format-and-structure.md:137 既有用法）
4. **不动项**（写入规格禁止事项 6）：hooks CLI 命令面 `tracking-checkpoint`、analyze"检查点间隔"（另一机制）、通用 checkpoint 语境。
5. 施工侧完成上述 3 处替换后：复跑验收 A1/A2/A3+B 全绿 → 更新本条目 → 重新请求提交确认。

## 批2：守卫扩面（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过 + 附加健全检查）：
  - `bash scripts/check-story-numbers.sh`（A）→ 0；"story numbers: ok (10 skills)"
  - `python scripts/test-story-numbers.py`（B）→ 0；正反回归 3/3 PASS（正向 2 个 skill→0；反向 3 个 skill→1 且报文含 README.md；反向 2 英文 5 skills→1 且报文含 README.md——反向 fixture 实测真的会失败）
  - `bash scripts/static-check.sh`（C1）→ 0；Total 10 | Pass 10 | Fail 0
  - `python scripts/test-static-check.py`（C2）→ 0；PASS
  - `bash scripts/check-doc-budget.sh`（C3）→ 0；12 文件全 ok
  - 附加：`bash scripts/check-behavior-contracts.sh` → 0（9 条在位）；`bash scripts/check-shared-files.sh` → 0（64 组 0 mismatch）；`python scripts/skill-numbering.py check` → 0（PASS 198）
  - `grep -l "守护对象" scripts/test-* | wc -l`（D）→ **20/20**（预检裁决后复跑：test-story-numbers.py 已补声明）
  - 附加健全：全部 test-*.sh `bash -n` 通过、test-*.js `node --check` 通过、test-*.py + check-story-numbers.py `py_compile` 通过（头部声明未破坏语法）
- 偏差记录：
  1. 规格"（.sh/.js 用 `#`）"按语言事实修正：**js 没有 `#` 注释语法**（`#` 是私有字段前缀，写入会破坏 `node --check`），4 个 test-*.js 用 `//` 注释行，与 scripts/README.md 测试纪律节所写一致；.sh 用 `#`、.py 用 docstring 照规格。——预检裁决**采纳**。
  2. test-story-numbers.py 按预检裁决**已补齐两项**：①头部补"守护对象"docstring；②进 `.github/workflows/cross-platform.yml`（static-guards job 新增 "Story-number regression" 步骤）与 scripts/README.md 测试回归表（新增一行，描述用非数字措辞）。验收 D 现为 20/20（裁决：规格 D 原 19/19 系"既有 19 个"表述，随增补统一 20/20，不算施工偏差）。
  3. 复跑中 A 曾自触发失败一次：测试回归表新行最初写"fixture 2 个 skill / 3 个 skill / 5 skills"触发新守卫（2/3/5≠10）——按规格 §7"优先修文档"改非数字表述后通过；此事件证明守卫有效（叙述数字残留会被当场抓住），非改断言变绿。
- 提交：9cf2806（2026-08-20，作者确认提交）

### 提交前预检裁决（规划侧，2026-08-20）

1. **偏差1 采纳**：js 注释用 `//` 正确——规格"(.sh/.js 用 #)"系规划侧笔误（js 无 # 注释语法），规格已勘误口径。
2. **新守卫脚本质检通过**：check-story-numbers.py 实现干净（标准库/--root/白名单注释纪律/解码失败明示不静默）。
3. **偏差2 裁决：候选三项中两项必须补齐、一项维持**——
   - **必须①**：test-story-numbers.py 头部补"守护对象"声明——本批为全部测试立"守护对象"规矩，新测试自身不在覆盖内属自我违反（验收 D 期望改为 20/20）；
   - **必须②**：test-story-numbers.py 进 CI yml 与 scripts/README 测试回归表——反向 fixture 不进 CI 则守卫自身无自动回归，违背"测试守护测试"初衷（规格 §6 已增补）；CONTRIBUTING 已含，无需再动；
   - 维持：批2规格 §5 的 D 原期望 19/19 系对"既有 19 个"的表述，现随增补统一为 20/20，不算施工偏差。
4. 施工侧补齐上述两项后：复跑 A/B/C/D（D 应 20/20）→ 更新本条目 → 重新请求提交确认。

### 施工回执（2026-08-20）

- ① test-story-numbers.py 头部已补"守护对象"docstring（与其他 19 个 test-* 同模板）。
- ② 已进 `.github/workflows/cross-platform.yml`（static-guards job："Story-number regression" 步骤）与 `scripts/README.md` 测试回归表（一行，非数字措辞）；CONTRIBUTING 此前已含，未再动。
- 复跑结果：A `story numbers: ok (10 skills)` 退出 0；B 正反回归 3/3 PASS；C1-C6 全部退出 0；**D = 20/20**；`py_compile` 通过。复跑中 A 曾自触发失败一次（测试回归表行初稿含"2 个 skill/3 个 skill/5 skills"），按规格 §7 改非数字表述后通过——守卫有效性现场证明，未改断言。
- 日志字段已更新（D 19/19→20/20、偏差记录按裁决修订）。

## 门槛 1 决议（作者，2026-08-20）

- 总纲 §4.3 门槛 1（批2 合入后、批3a 前重估）结果：**通过，开工批 3a**。
- 判据①施工链路顺畅：批0/1/2 全部零打回 ✅。
- 判据②作者确认 schema 三维度（信息差/悬置/卷报告）为真实使用痛点：作者明确指示"开批 3a" ✅。
- 后续版本段：v1.2.0（批 3a→3b→3c→4→5）施工开始。

## 批3a：信息差域（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全套 7/7 绿，P1 裁决后复跑）：
  - `python scripts/test-tracking-commit.py` → 0；**29/29**（26 既有 + 3 新用例：register/update/retire 全流程、v4→v5 迁移带备份且老键零丢失、坏枚举/重复 id 被拒）
  - `python scripts/test-tracking-workflow-contracts.py` → 0；14 tests passed
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch（sync 前 4 DRIFT 属源改动预期，`sync-shared-assets.py sync` 后全绿）
  - `bash scripts/check-current-skill-contracts.sh` → 0；全 PASS（current-contract.json 保持 2，按 P1 裁决）
  - `bash scripts/check-behavior-contracts.sh` → 0；9 条在位
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` → 0；12 文件全 ok
- 偏差记录：
  1. 测试 fixture 联动：INPUT_SCHEMA_VERSION 1→2 后，test-tracking-commit.py 的 fixture `schema_version` 两处 1→2、`test_init...` 断言 `schema_version == 4`→`5`（版本升级的输入/输出两侧，非改断言变绿）。
  2. `note` 字段实现为必填非空（仿 foreshadow summary 语义）；测试初稿 note="" 被拒后修正 fixture（判因=fixture 数据不合法，非实现问题）。
  3. 信息差 register/update 语义实现为：register 已存在→拒、update 不存在→拒、retire 幂等删除（first/updated 章由工具维护，仿 timeline keep_first_chapter 模式）——规格只给 action 枚举，存在性检查为自然推论，供审核核对。
  4. init 输入 information_gaps 的 action 固定 register（retire/update 在 init 被拒，防 KeyError 路径）。
  5. P1 裁决执行：`progress_schema_version` 保持 2，current-contract.json 本子步零改动（施工侧临时恢复处置获采纳保留）；规格/总纲/批9 四处勘误由规划侧完成，本批不新增机读镜像键。
- 提交：daaf1da（2026-08-20，作者确认提交）

### 待决问题

**P1：规格 §3 的 `progress_schema_version: 2 → 3` 与守卫语义矛盾，无法按字面执行**

- 现象：按规格把 `scripts/current-contract.json` 的 `progress_schema_version` 改为 3 后，`check-current-skill-contracts.sh` 报 **7 个 failure**：analyze-workflow.md:117/119、pipeline-ops.md:15/53/89、import-workflow.md:104、UPGRADING.md:81 的 `schema_version: 2` 全部"must equal 3"。
- 已尝试：读守卫源码 `check-current-skill-contracts.py:981-1052`（`progress_schema_pin_findings`）确认语义——**`progress_schema_version` 是 analyze 拆文管道 `_progress.md` 的续跑契约版本**（扫描 skills/ 全部字面 `schema_version: N` 锚点，docstring 明言"续跑契约同时写在 analyze 的写入/恢复段、import 的当前拆文契约、UPGRADING 当前契约段"），与追踪 state 的 TRACKING_SCHEMA_VERSION 完全无关；current-contract.json 中不存在追踪 schema 的机读镜像键。
- 疑似原因：规格撰写时把 `progress_schema_version` 误当作追踪 schema 的机读镜像；批9 规格 C6（"progress_schema_version=3"）同源。若按字面升 3，唯一"修复"是把 analyze 管道全部文档与运行时 `_progress.md` 升 3——但本子步 analyze 管道零改动，升 3 会制造"续跑拒收自己写出的 _progress.md"的自相矛盾，恰是守卫 docstring 要防的（check-current-skill-contracts.py:988-990）。
- 需要谁决定什么：`progress_schema_version` 是否**保持 2**（本子步只改追踪 schema，current-contract.json 不动）？若是，请同步勘误批3a 规格 §3 该行与批9 规格 C6 的"progress_schema_version=3"口径；若要求追踪 schema 也有机读镜像，请给出新键名/新守卫设计（超出本子步范围，需规划侧另定）。

### 待决答复（规划侧，2026-08-20）

**P1 答复：保持 2，规格已勘误，不新增镜像键。**

1. 施工侧判断完全正确，规划侧已独立复核守卫源码（SCHEMA_VERSION_PIN_RE / progress_schema_pin_findings，:981-1052）证实：`progress_schema_version` 是 analyze 拆文续跑契约（`_progress.md`）的版本锚点，与追踪 schema 无关。规格原行系规划侧未验证语义的想当然，认错。
2. **裁决**：current-contract.json 本子步不动（progress_schema_version 保持 2，其临时恢复处置正确且保留）。追踪 schema 的唯一权威=tracking_commit.py 常量（三副本字节同步 + test-tracking-commit 守护）；**不新增机读镜像键**——单一权威原则，新增镜像=第二真源=漂移风险（反面教材即本守卫防的"技能包自相矛盾"）。
3. **勘误已完成三处**：批3a 规格 §3 该行（改为"不动 current-contract.json"+理由）；批3a 验收（check-current-skill-contracts 保持绿即通过）；批9 规格 C6 与总纲批3 验收锚点（口径改为"保持 2、勿随追踪版本联动"）。
4. **继续施工**：复跑全套验收（应 7/7 绿）→ 更新本批条目 → 请求提交确认。
- 临时处置：current-contract.json 已恢复为 2，其余改动（tracking_commit.py 五处接入、tracking-transaction.md、volume-review.md、测试 3 用例、三副本 sync）全部保留且验收绿（除 P1 外全过）。

## 批3b：悬置预警（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过）：
  - `python scripts/test-tracking-commit.py` → 0；**32/32**（29 既有 + 3 新用例：25 章前出预警 / 20 章内不出 / `--warn-chapters 10` 覆盖生效）
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch（sync 后）
  - `bash scripts/check-current-skill-contracts.sh` → 0；全 PASS
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` → 0；12 文件全 ok
- 偏差记录：
  1. 测试用例构造走正常工具路径：`init(last_chapter=30)` + revision 回填伏笔（updated=1/15）——revision 不推进 last 且 checkpoint_record 不回退 updated，天然造出"悬置"态；无手工改 state。
  2. 新用例断言初稿把 fixture 伏笔 id 写成 F001，实际 fixture 是 F027——判因=断言笔误（fixture 数据），修正断言非改实现。
  3. `run_tool` 测试辅助加 `extra` 参数（透传 `--warn-chapters`），仅扩展辅助函数。
- 提交：291cfe9（2026-08-20，作者确认提交）

## 批3c：卷报告（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过）：
  - `python scripts/test-tracking-commit.py` → 0；**34/34**（32 既有 + 2 新用例：fixture 卷范围内容断言 / 同 state 重跑逐字节一致）
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch（sync 后）
  - `bash scripts/check-current-skill-contracts.sh` → 0；全 PASS
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` → 0；12 文件全 ok
- 偏差记录：
  1. 卷报告输出设计（规格只给清单要素，未给文件格式）：`追踪/卷报告_第A-B章.md` 含「摘要（分层三行：伏笔四态计数/悬置数/信息差未兑现数）+ 伏笔清账（四态分节，空态写"无"）+ 悬置预警 + 信息差未兑现」，全部列表按 id 排序保证确定性；`--json-out` 输出同构 JSON（range/foreshadow_settled/suspension_warnings/open_information_gaps/summary）。
  2. 范围口径：①伏笔清账按 `updated_chapter` 落 [A,B] 过滤（规格字面）；②悬置清单复用 3b `suspension_warnings`（全局视角，相对 last 而非卷末）；③信息差未兑现 `status=="登记"`（规格字面，未加范围过滤）——②③为"当前仍悬着"的全局提醒，①为卷内结清账，语义自洽，供审核核对。
  3. 测试构造走正常工具路径：`init(last=30)` + revision 回填（卷内 F027 已埋 updated=1 / 卷外 F028 已回收 updated=20 / 信息差 G001 register updated=10），断言计数与 MD 内容（F027 在、F028 不在）。
- 提交：57b3fdf（2026-08-20，作者确认提交）

## 批4：下一步判定（状态：已跳过）

- 作者决策（2026-08-20，总纲 §4.3 弹性批）：**跳过**。理由：路由现状可用，痛点证据最弱。
- 跳过连锁（以下调整随批5 提交带上）：
  1. 批8 规格：test-writing-pipeline.sh 管道测试剔除 next_step.py 断言一步（原 §4 第 2 步末行）；开书剧本剔除 next_step 判 S3/S4 断言（原 §3 改动清单行，标注已按批 4 跳过剔除）。
  2. 批9 规格：C3 新脚本清单剔除 test-next-step.py（共 11 → 10 个）；§5 验收命令剔除 test-next-step.py 行。
- 无施工产物（未开工）；后续批次的 next_step 依赖一律按"批 4 已跳过"口径处理（如批 8 管道测试不再引用 next_step）。

## 批5：候选类机检（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过 + 附加）：
  - `node scripts/test-prose-candidates.js` → 0；"OK: prose candidates imagery/gap-touch positives, clean negatives, degraded fallbacks, determinism, and errors"（正向意象+gap_touch / 反向空候选 / 降级 style/gaps 缺失与坏格式 / 幂等逐字节一致 / 缺 --prose 退出 2）
  - `bash scripts/check-behavior-contracts.sh` → 0；"行为契约守卫通过：**10 条**约束在位"（新增 candidate-checks-never-block 契约生效）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` → 0；workflow-daily **11500/11500 余 0**（等量删旧精确达标）
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch（check-prose-candidates.js 未误入共享组，write 专属）
  - `node --check skills/moshu-write/scripts/check-prose-candidates.js` → 0
  - 附加：`python scripts/test-behavior-contracts.py` → 0（反向 fixture 动态覆盖新契约，无需计数常量）
- 偏差记录：
  1. check-prose-candidates.js 实现要点：滑窗 2-4 字+停用词/标点过滤（内建约 60 个停用词）；style 锚点正则容忍三种格式（平均句长/句长分布、段均字数/段落节奏、对话占比），全无锚点 → `style_baseline_unparsed` 降级；信息差表解析 status=="登记" 行；输出硬编码 `blocking_count:0` 无 blocking 数组；退出码仅参数/文件错误=2。
  2. 句式偏离度量实现三个指标（avg_sentence_len / avg_paragraph_chars / dialog_line_ratio），规格"段均字数/对话行占比"按两个以上度量覆盖；基线区间 ±30%（DRIFT_TOLERANCE 常量，mo-shu 自定）。
  3. workflow-daily.md :130 接入做等量删旧："确认逐章清零后没有回潮"→"确认无回潮"（语义等价压缩）以保 doc-budget；追加句含契约串"候选永不拦截"。
  4. **契约数连锁修正**（超出规格字面但属文档数字一致纪律）：加契约后行为契约 9→10 条，同步修正 scripts/README.md 测试回归表"9 条"→"10 条"；并顺带修正 CONTRIBUTING.md 已过期的"8 条契约"→"10 条契约"（原"8 条"早于批2 即过期）。
  5. test-prose-candidates.js 按规格 4 组断言实现（未额外加 style_drift 正向用例——规格测试清单未列）。
- 提交：5c13b00（2026-08-20，作者确认提交）

## 批6：审查工单闭环（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部 8 项通过）：
  - `python scripts/test-review-tickets.py` → 0；5/5（write 合法+幂等 / 坏枚举+重复 id+坏令牌拒 / resolve 单向流转 / list 过滤 / verify-token 相等与不等）
  - `bash scripts/check-agents-version-sync.sh` → 0；"agents_version 一致性通过：28 在所有 SKILL.md 一致"
  - `python scripts/test-agents-version-sync.py` → 0
  - `bash scripts/check-behavior-contracts.sh` → 0；"11 条约束在位"（新增 revision-ticket-disposition 契约）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` → 0（narrative-writer 13300/路径组 43700 调整后全 ok）
  - `bash scripts/check-shared-files.sh` → 0；64 组 0 mismatch
  - `bash scripts/check-moshu-setup-deployment.sh` → 0；TS1-TS12 全过（Windows 本地经临时 python3 shim 等价替代，README §7）
- 偏差记录：
  1. **dimension 受控枚举 9 类的值**规格未列出——取自 review-workflow.md:158 统一 Findings Schema 的 category 枚举（structure/character/prose/consistency/platform/factual/format/causal/rule_boundary，代码事实恰 9 类）。
  2. **版本链实现面两处超规格清单**（"agents_version 只 bump 一次 27→28"的完整语义必需，否则部署语义错乱）：`deploy.py` 的 `DEFAULT_AGENTS_VERSION='27'`（部署写入 .story-deployed 的版本源，不改则部署写 27 与声明 28 永不匹配）；`templates/hooks/session-start.sh` 的 `-lt 27/-gt 27/v27` 字面（运行时过期判定实现，不改则 v27 项目不触发"低于 v28"警告——部署检查 TS5 实测抓到）。两处均已 27→28。
  3. **doc-budget 显式调高**（doc-budget 机制允许，理由已写入 _comment）：narrative-writer 13150→13300、正文 agent 上下文路径组 43600→43700——审稿令牌段为规格硬性要求（审查防编造），压缩至最小（4 模板统一约 100 字）后仍超，按"确实必须加的新规则→调高 budget 并说明"路径处理。
  4. **工单/review-log 分工**：review-workflow 增"工单落盘"步（含 S1/S2→blocking、S3/S4→candidate 映射、先落临时文件再 --input）；review-log 格式与读点零改动（禁止事项 1）。
  5. 测试夹具修正一次：duplicate-id 用例初稿改 findings[1].id="T001"（本已是 T001，未制造重复），改 findings[0] 后生效——判因=夹具构造错误，非实现问题。
  6. **预检必改项（2026-08-20）**：deploy.py:12 用法示例串 `[--agents-version 27]` → `28`（内部默认已是 28，帮助串数字漂移——本批主题即消灭此类残留）。改后复跑 `test-review-tickets.py`（5/5 OK）与 `check-agents-version-sync.sh`（28 全链一致）均绿。
- 提交：c351208（2026-08-20，作者确认提交）

## 批7：模板 Prompt 纪律（状态：已提交/待审核）

- 日期：2026-08-20
- **步骤 0 勘察结论（改动点清单，写进提交说明）**：grep 7 个模板的 `JSON|纯 JSON|输出|返回` 段落——真正"产 JSON 且有散落纪律句"的仅 2 处：`moshu-explorer.md:222`（纯 JSON 无围栏/ASCII 直引号/换行转义/裸双引号自检长句）与 `moshu-chapter-extractor.md:80`（OUTPUT_MODE: json 的"单 JSON 对象，不带 prose、不带 code fence"）；其余 5 模板（architect/character-designer/consistency-checker/narrative-writer/researcher）产 Markdown（档案表/报告/大纲/参考文件），无 JSON 纪律句，零改动。chapter-extractor 的 markdown 模式特定自检（:176 花括号/行数/分隔）保留（特定格式校验，非 JSON 纯度纪律）。
- 验收输出摘要（规格 §5 全部 7 项通过）：
  - `bash scripts/check-agent-template-rules.sh` → 0；"agent template rules: ok (7 templates)"
  - `python scripts/test-agent-template-rules.py` → 0；4/4（正向干净模板 / 反向互引句 / 挂载点缺失 / 复制纪律标题）
  - `bash scripts/check-agents-version-sync.sh` → 0；"agents_version 一致性通过：29 在所有 SKILL.md 一致"
  - `python scripts/test-agents-version-sync.py` → 0
  - `bash scripts/check-moshu-setup-deployment.sh` → 0；TS1-TS12 全过（v29；Windows 经临时 python3 shim，README §7）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` / `check-shared-files.sh` → 0
- 偏差记录：
  1. **勘察驱动的改动面**：规格步骤 2 写"7 模板散落纪律句替换"，实测仅 2 模板含 JSON 纪律句（见上），按"步骤 0 先 grep 摸清、以实测为准"执行——其余 5 模板未动（禁止事项 2/3 精神）。
  2. **版本链一次改全**（批6 预检教训：deploy.py:12 usage 串漂移）：本次 28→29 一次覆盖 7 个 SKILL.md（agents_version: 28 / 小于·大于 28 / 本版 28 / `28`）+ UPGRADING.md（:6/:8/:89 + v28→v29 新段）+ deploy.py（DEFAULT_AGENTS_VERSION + usage 串）+ session-start.sh（-lt/-gt/v29）+ current-contract.json，逐处核验无残留。
  3. 守卫规则 C 哨兵=共享纪律文件标题行全文（"shared-output-discipline：结构化产出纪律（共享 base 段）"）；锚点引用句只含文件名不触发；违规清单打 stdout（错误码 1），测试初稿断言 stderr 修正为 stdout。
- 提交：8597514（2026-08-20，作者确认提交）

## 批8：场景 eval 与管道契约（状态：已提交/待审核）

- 日期：2026-08-20
- 验收输出摘要（规格 §5 全部通过 + 附加）：
  - `bash scripts/check-eval-scenarios.sh` → 0；"eval scenarios: ok (3 scenarios)"（存在非空/断言节/[机检] ≥3/引用路径存在，全过）
  - `bash scripts/test-writing-pipeline.sh` → 0；"OK: writing pipeline (init→commit→check→volume-report→tickets→prose-candidates)"（正式回归，trap 自清理）
  - `bash scripts/static-check.sh` → 0；Total 10 | Pass 10
  - `bash scripts/check-doc-budget.sh` / `bash scripts/check-shared-files.sh` → 0
  - 附加：`bash scripts/check-behavior-contracts.sh` → 0（11 条在位）
- 偏差记录：
  1. **三个可裁剧本全做**（用户指示"既然做就按规格全做"）：日更一章/开书/审查工单，断言两栏（[机检]/人工项），机检项各 ≥5 条；开书剧本**未加回** next_step 断言（批 4 已跳过，规格修订版口径）。
  2. 管道 e2e 初跑 commit 失败一次：手写 commit JSON 缺 `expected_state_revision`（test-tracking-commit.py 的 run_tool 自动注入、bash 脚本需手写）——判因=fixture 构造遗漏，补 `"expected_state_revision": 0` 后全链通过，非实现问题。
  3. CI 落位：check-eval-scenarios.sh 进 static-guards job（纯静态、不跑 LLM）；test-writing-pipeline.sh 进 runtime-regressions job（零 LLM 确定性全链）；windows/macos job 不加（纯确定性脚本无平台差异）。
  4. 剧本引用路径仅列真实存在文件（tracking_commit.py/review_tickets.py/check-prose-candidates.js 及各 references），check-eval-scenarios 路径校验全绿。
- 提交：7b4f645（2026-08-20，作者确认提交）

## 批9：终检与发布（状态：已提交/待审核）

- 日期：2026-08-20
- **C1-C11 终检核对表逐项记录**：
  - **C1 悬空引用清零** ✅：static-check 10/10 PASS；手工核对（脚本提取存在性校验，修正提取正则后）"ok (all referenced scripts exist)"——初版正则误报 6 项（`.json` 的 `.js` 前缀匹配、`moshu-deslop/scripts/` 子串截取），修正 grep -P 前后断言后清零，非真实悬空。
  - **C2 叙述数字** ✅：check-story-numbers → ok (10 skills)。
  - **C3 全守卫复跑** ✅：CI 清单全量 39 项逐条跑（static-guards 22 + runtime 16 + deploy 1），全部退出 0；本轮新增 10 个脚本文件（check-story-numbers.py/.sh、test-story-numbers.py、test-prose-candidates.js、test-review-tickets.py、check-agent-template-rules.py/.sh、test-agent-template-rules.py、check-eval-scenarios.sh、test-writing-pipeline.sh；test-next-step.py 已按批4 跳过剔除）逐条绿。平台说明：Windows 本地 test-shared-assets 的 mode-drift 用例因 chmod no-op 必失败（已知平台限制，CI Linux 绿）；check-claude-adapter/eval-prose-quality/check-moshu-setup-deployment 内部硬编码 python3，本地经临时 shim 等价替代（README §7）后全绿。
  - **C4 版本三轨终对齐 v1.3.0** ✅：`grep -n "1.3.0" CHANGELOG.md README.md skills/moshu/VERSION` 三处命中（CHANGELOG:5、README:19、VERSION:1）；README_EN 一并同步；CHANGELOG 增 v1.2.0（批 3a/3b/3c/5 实际内容）与 v1.3.0（批 6/7/8/9 实际内容）汇总条目，逐批对照提交记录撰写，不夸大。
  - **C5 agents_version 链** ✅：UPGRADING.md 含 v27→v28（批6）与 v28→v29（批7）两段；check-agents-version-sync → 29 全链一致。
  - **C6 schema 口径** ✅：tracking_commit.py 三副本各含 INPUT_SCHEMA_VERSION=2/TRACKING_SCHEMA_VERSION=5 且 SHA256 字节一致；tracking-transaction.md 示例 schema_version=2 两处；current-contract.json progress_schema_version **保持 2**（批3a 勘误口径，未随追踪版本联动）。
  - **C7 hook 语义核对（只记录不改码）** ⚠️：`guard-outline-before-prose.sh` 存在以 **exit 2** 阻断 Write/Edit/Bash 正文写入门（缺细纲/大纲/追踪检查点即 BLOCKING，:86/:144/:160/:199）——与总纲红线"PreToolUse hook 只能 ask 不能 deny"存在张力，但属**项目既有机制**（fail-open：node 缺失时放行，部署文档与 UPGRADING 已如实声明），非本批引入。**不改码，呈报作者裁决是否整改。**
  - **C8 doc-budget 全量** ✅：12 文件全 ok（narrative-writer 13254/13300、路径组 43603/43700）。
  - **C9 归档目录零改动** ✅：`git status -- docs/归档/` 空输出，6 项与归档时（2026-08-20）完全一致，全程零触碰。
  - **C10 批次依赖双向核对** ✅：总纲 §4.2 表 vs 12 份规格（批 0-2、3a/3b/3c、4-9）头部"前置依赖"逐一对照一致（含批4 弹性标注）；批2 粒度说明：总纲表列关键依赖"批 0"、规格列完整串行前置"批 0、批 1"——语义无矛盾（串行开工使然）。已核对。
  - **C11 术语终检** ✅：批1 验收命令复跑——"检查点卡"零命中；"当前语义检查点"仅 workflow-daily.md:46 定义句 1 处；"当前检查点"（排除语义串）零命中；workflow-daily.md:9 主对标口径无混用。
- 验收输出摘要（规格 §5 终检一把梭 13/13 全绿）：static-check 10/10 ｜ skill-numbering PASS 199 ｜ current-skill-contracts 全 PASS ｜ doc-budget 全 ok ｜ shared-files 64 组 0 mismatch ｜ behavior-contracts 11 条 ｜ agents-version-sync 29 ｜ story-numbers 10 ｜ template-rules 7 ｜ eval-scenarios 3 ｜ writing-pipeline OK ｜ test-tracking-commit 34/34 ｜ test-review-tickets 5/5 ｜ 版本三轨 grep 命中。
- 偏差记录：
  1. C1 手工核对初版提取正则误报 6 项（.json 前缀/子串截取），修正后清零——判因=核对脚本缺陷，非仓库悬空引用。
  2. C7 为只读核对，未改任何 hook 代码；结果呈报（见上）。
  3. 平台限制记录：test-shared-assets mode-drift 在 Windows 必失败（已知），CI Linux 绿；python3 硬编码脚本本地经 shim 等价替代。
- 提交：278fbd1（2026-08-20，作者确认提交）


## B1a：新建 moshu-build（状态：已完工/待提交确认）

- 日期：2026-08-21
- 验收输出摘要（规格 §5 全部通过，9/9 绿）：
  - `python scripts/test-impact-scan.py` → 0；3/3 OK（未写细纲+已写正文+追踪三处命中 / 干净关键词三清单空 / 无 state 退出 2 且报文含"先 /moshu-build"）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0（moshu-build 新 skill frontmatter/引用可达/无跨 skill 直引）
  - `bash scripts/check-claude-adapter.sh` → 0；11 插件 name+skills+version 与 SKILL.md 一致
  - `bash scripts/check-story-numbers.sh` → 0；ok (11 skills，索引表行数与叙述数字一致)
  - `python scripts/sync-shared-assets.py check` → 0；71 canonical groups / 97 synchronized copies
  - `bash scripts/check-shared-files.sh` → 0；同上 0 mismatch
  - `bash scripts/check-doc-budget.sh` → 0；构建路径组 11037/11587 全 ok（SKILL 1486/1560、workflow-build 7672/8055、revision-workflow 1879/1972）
  - `bash scripts/check-capability-wiring.sh` → 0；story-construction / setting-revision 两能力消费链在位
  - `bash scripts/check-behavior-contracts.sh` → 0
- 偏差记录：
  1. **shared-assets 实际接线 ≠ 规格 §3 预期清单（21 项）**：按"以施工后 grep 实际引用清点为准"执行。workflow-build/revision-workflow 实际引用 13 个 reference + tracking_commit.py 脚本：9 个既有组加 build 目标（character-relations、emotional-arc-design、emotional-methods、genre-core-mechanics、outline-methods、reader-contract-and-progression、reversal-toolkit、style-genre-modules、tracking-transaction），新增 4 组（idea-seed、genre-prose-cards、beat-cards、story-tracking-transaction 脚本目标）；规格预期清单中的 outline-rhythm/outline-conflict/genre-catalog/genre-readers/genre-writing-formulas/naming-cards/female-audience-writing/plot-frameworks 未被两个工作流实际引用，**不接线**。
  2. **artifact-protocols.md、outline-structure-theory.md 两副本在 B1a 无法自洽，撤回接线**：它们的字节级副本内含指向 `workflow-setup.md#phase-3大纲搭建` 的锚点链接（static-check 必报 broken-link）；workflow-setup.md 是 B1b 拆分目标（禁止 B1a 复制、且含细纲内容）。处置：撤回两组的 build target、删除副本；workflow-build.md 相应引用改写为内联字段模板（剧情单元卡字段模板内联进卷纲代码块）与内联对标节奏迁移步骤，不依赖两文件。B1b 拆分 workflow-setup 后可重议。
  3. **genre-prose-cards 按目录整组接线（索引 + 32 张单题材卡）**：索引副本含 32 条 `genre-prose-cards/*.md` 链接（static-check 必报 broken-link），单题材卡本身零链接自洽 → 建 32 个 genre-prose-card-* 组同步整目录，保证索引可达。
  4. **workflow-build.md 来源声明措辞调整**：规格 §3 要求头注标"源自 moshu-write/workflow-setup.md 拆分"，但 §8.3 禁止 build 内出现指向其他 skill 的文件路径直引（static-check 硬门）——折中：声明保留语义（"源自 moshu-write 侧 workflow-setup 的构建段拆分"），不写可被解析的路径字面；细纲归 write 声明保留（B1b outline-workflow 承接，不写 `.md` 后缀避免悬空引用误报）。
  5. **doc-budget 按 Q5 口径登记后按完工实测校准**：workflow-build.md 删去 artifact-protocols/outline-structure-theory 引用后实测 7921→7672，预算 8317→8055（×1.05 取整），构建路径组 11850→11587 随动；_comment 补校准说明。
  6. **revision-workflow.md 回流入口用裸命令 `review_tickets.py list`**：build 侧调用 review_tickets 的路径接线是 B 系列后续批次（B1c 契约锁定）的开放点，本批不虚构路径。
  7. **beat-cards.md 加入 build 副本**：outline-methods.md 副本（共享）内含反引号引用 `beat-cards.md`，为达引用可达必须接线（beat-cards-reference 既有组加 build target）。
- 提交：32800e4（2026-08-21，作者确认提交；规划基线先行 c2e287a）


## B1b：write 侧拆分与路由切换（状态：已完工/待提交确认）

- 日期：2026-08-21
- 验收输出摘要（规格 §5 全部通过）：
  - A 旧引用清零 → grep -rn "workflow-setup" skills/ scripts/ docs/architecture.md README.md README_EN.md CONTRIBUTING.md → 退出码 1，零命中
  - B 路由三方一致 → grep -n "moshu-build" skills/moshu/SKILL.md skills/moshu/scripts/next_step.py README.md → 三处均含 moshu-build，开书语义不再指向 write（路由表 :14 拆行 / 判定表 :50/:53 / next_step.py :138-139/:233-234 / README:91/:103）
  - C1 `python scripts/test-next-step.py` → 0；12/12 OK（S1/S6 新增 suggested_skill=moshu-build + next_action 含 /moshu-build 断言）
  - C2 `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0（outline-workflow 迁移后链接可达性自查通过——B1a 审核沉淀）
  - C3 `bash scripts/check-doc-budget.sh` → 0；workflow-setup 13600 删除、outline-workflow 实测 6641/6986、长篇开书组 15668/16186、moshu/SKILL 4939/5186 全 ok
  - C4 `bash scripts/check-shared-files.sh` → 0；71 canonical groups / 97 copies 0 mismatch（outline-methods source 改后副本已同步）
  - C5 `bash scripts/check-behavior-contracts.sh` → 0
  - C6 `bash scripts/check-story-numbers.sh` → 0；ok (11 skills)
  - C7 `bash scripts/check-capability-wiring.sh` → 0
  - C8 `bash scripts/check-eval-scenarios.sh` → 0；ok (3 scenarios)（开书剧本已同步 build 路由）
  - C9 `bash scripts/test-writing-pipeline.sh` → 0
  - C10 `python scripts/test-current-skill-contracts.py` → 0（扫描目标随内容迁移改指 build 侧）
  - 附加：`bash scripts/check-claude-adapter.sh` → 0；11 插件一致（marketplace 无涉 B1b）
- 偏差记录：
  1. **outline-workflow.md 等量迁移范围含 :261-265（细纲质量要求/钩子来源/章节标题规则）**：规格 F1 列出细纲节 :184-250、:252、:254-259、:267-272、:279、architect 细纲批段；:261-265 三小节同为细纲职责（质量要求/钩子来源/标题规则），按"F1 全部段落"意图一并迁移（不迁移即丢内容），保持原文措辞。
  2. **architect 契约摘要引用改写**：原文"带上 Phase 1「Agent 调用：moshu-architect」中的契约摘要"在拆分后指向已删文件；改写为"内容从 `references/reader-contract-and-progression.md` 摘取"（write 侧可达，同源契约），语义不变。
  3. **evals/scenarios/开书/README.md 同步 build 路由**：剧本原走 /moshu-write Phase 1-3 并引用已删 workflow-setup.md（check-eval-scenarios 路径存在性校验会挂）；更新为 /moshu-build 开书构建 → /moshu-write 接力首批细纲，规则依据改指 build 侧两文件 + outline-workflow.md。
  4. **scripts/check-current-skill-contracts.py / test-current-skill-contracts.py 扫描目标改指 build 侧**：benchmark-discovery-excludes-current-work 与 stale topic-decision-phase 扫描原指向 workflow-setup.md（已删），内容迁移后改指 skills/moshu-build/references/workflow-build.md（对标发现/选题决策段现居 build 侧），注释同步。
  5. **moshu-scan/topic-decision.md:77 下游可达性描述同步**：原文"开书时 workflow-setup 从项目根…搜选题决策.md"改指 /moshu-build 开书构建（命令文本，无跨 skill 路径直引）。
  6. **doc-budget moshu/SKILL.md 4900→5186**：路由表拆行（开书/建设定 → /moshu-build 独立行）+ 判定表 build 转向文本为 B1b 路由切换必需净增（路由正确性值得每次会话付），按 doc-budget 纪律第②条显式调高（实测 4939×1.05 取整）。
  7. **workflow-build.md 来源声明措辞更新**：B1a 版声明含"workflow-setup"字样（验收 A 零命中要求），改写为"源自 moshu-write 侧开书工作流的构建段拆分"，并补 B1b 删除/承接说明。
- 提交：89c36bf（2026-08-21，作者确认提交；含审核侧 3 文件）

## B1c：契约锁定、回流闭环与终检（状态：已完工/待提交确认）

- 日期：2026-08-21
- 验收输出摘要（规格 §5 全部通过，16/16 绿）：
  - `bash scripts/check-behavior-contracts.sh` → 0；"14 条约束在位"（B1c +3：build-revision-requires-impact / write-no-existing-setting-edit / changelog-append-only，must_contain 措辞与实文逐字对齐——影响分析=revision-workflow:5/9、不修改既有设定=outline-workflow:97、追加一行=revision-workflow:30）
  - `python scripts/test-behavior-contracts.py` → 0；反向 fixture 覆盖基线首条 + B1c 三条新契约（各删 must_contain 必失败且指向契约 id）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0
  - `bash scripts/check-doc-budget.sh` → 0；chapter-core 16978/17100（stale 消费扩展后余 122）、全组 ok
  - `bash scripts/check-shared-files.sh` → 0；71 canonical groups / 97 copies 0 mismatch
  - `bash scripts/check-story-numbers.sh` → 0；ok (11 skills)
  - `bash scripts/check-capability-wiring.sh` → 0
  - `bash scripts/check-claude-adapter.sh` → 0；11 插件 name+skills+version 一致（moshu 1.3.0 / moshu-write 1.4.0 bump 同步）
  - `bash scripts/check-agent-template-rules.sh` → 0；ok (7 templates)
  - `bash scripts/check-eval-scenarios.sh` → 0；ok (3 scenarios)
  - `bash scripts/test-writing-pipeline.sh` → 0；OK
  - `python scripts/test-impact-scan.py` → 0；3/3 OK
  - `python scripts/test-next-step.py` → 0；12/12 OK
  - `python scripts/test-tracking-commit.py` → 0；35/35 OK
  - 终检①grep "workflow-setup" skills/ scripts/ docs/architecture.md README.md README_EN.md CONTRIBUTING.md → 退出码 1，零命中
  - 终检②路由五方一致性 → moshu/SKILL.md 路由表+判定表、next_step.py、test-next-step.py、README.md、README_EN.md 均含 moshu-build 且开书语义指向 build
- 偏差记录：
  1. **test-behavior-contracts.py 首跑失败一次（实现缺陷）**：重构反向 fixture 为多契约循环时，`contracts` 加载误置于 with 块内，作用域外引用 NameError——判因=我的重构遗漏（原代码在 with 内加载），非断言目标问题；把 `contracts = json.loads(...)` 提到循环前修复，复跑绿。规格说"契约计数常量 11→14 同步（若有）"——实测 test 文件无计数常量（按 manifest 动态驱动），无需同步；反向 fixture 三条按规格新增。
  2. **volume-review 末步提示位置**：规格"末步追加固定提示"，落点取四步列表之后、产物格式之前（独立引用行），与第 3 步"走 /moshu-build 开新卷"呼应不重复。
  3. **stale 消费扩展落 chapter-core.md:159（D4 细纲失效检查第 7 条）**：规格给"workflow-daily.md（或 chapter-core 对应节）"，选 chapter-core 失效检查节（机制同源：细纲头部标记的产出与消费在同一处），一句追加（写前遇 <!-- stale: 标记→对照变更日志复核→处理后才写→移除标记，只提示不自动改）。
  4. **版本 frontmatter bump 评估（呈报）**：受 B1 行为面改动的 skill bump +0.1——moshu 1.2.0→1.3.0（路由表/判定表/next_step 切换）、moshu-write 1.3.0→1.4.0（开书职责移交/Phase 重构/outline-workflow 新文件）；moshu-scan 仅 topic-decision.md 一句措辞（SKILL.md 未动、行为不变）不 bump；moshu-build 为 B1a 新技能保持 1.0.0；其余 skill 未涉 B1 不改。marketplace 对应条目同步，adapter 全绿验证。
  5. **版本四轨**：VERSION 1.4.0→1.5.0、marketplace metadata 1.4.0→1.5.0、README/README_EN 最近更新 → v1.5.0（B1 全量文案）、CHANGELOG 顶部追加 v1.5.0 条目（B1 全量：新技能/拆分/路由/契约；历史条目零触碰）。
- 提交：309b6d7（2026-08-21，作者确认提交；含审核记录 B1b 审核结论）

## 审计-V4 需修批 A1-A6（状态：已关闭）

- 日期：2026-08-21
- 性质：发布前审计（docs/审计-V4/04-审计总计划.md）需修级整改，一批一提交，非 V2.1 批次。
- 修复内容：
  - **A1** `skills/moshu-build/scripts/impact_scan.py` 补 stdout/stderr UTF-8 重配（仿 `skills/moshu/scripts/next_step.py:32-36`）——中文 Windows 无 PYTHONIOENCODING 时可跑。
  - **A2** `skills/moshu-build/references/workflow-build.md:156` 剧情单元卡字段模板补 write 消费必读字段：**单元ID/位置（`{L卷号-序号}`）**、章节范围、单元承诺、风险等级（对照旧 workflow-setup 与 write 侧 artifact-protocols 剧情单元卡模板、outline-workflow:34、workflow-daily:44、chapter-core:11 四处消费口径逐一对齐），并注明细纲消费链路。
  - **A3** `skills/moshu/SKILL.md:50` 判定表序3 语义修正："首批细纲 + 写第 1 章（/moshu-write，细纲首建见 outline-workflow；设定/卷纲缺失才回 /moshu-build）"；`skills/moshu/scripts/next_step.py:164` S2 next_action 文案同步；`scripts/test-next-step.py` S2 断言补 suggested_skill=moshu-write + next_action 含 /moshu-write。
  - **A4** `README_EN.md:102` 开书路由示例切 moshu-build（outlines & prose continue in moshu-write）。
  - **A5** 四处跨 skill 残留切 build：`moshu-import/SKILL.md:74`（从零开新书→/moshu-build）、`moshu-setup/SKILL.md:65` 与 `:92`（直接开书→/moshu-build）、`deploy-manual.md:90`（"目标平台由开书采集"改技能指向，业务语义不变）。
  - **A6** `skills/moshu-write/SKILL.md` frontmatter 触发词清理（移除「帮我开书」「写大纲」，改列「出细纲/补细纲」等；标注"开书/建设定/写大纲 → /moshu-build"）+ 正文首段职责改写（承接开书构建之后的细纲与正文）。
- 验收输出摘要（全绿）：
  - A1 专项：无 PYTHONIOENCODING 下 `python scripts/test-impact-scan.py` → 0；3/3 OK
  - A3 专项：`python scripts/test-next-step.py` → 0；12/12 OK
  - A2 专项：`grep -c 单元ID skills/moshu-build/references/workflow-build.md` → 5 处（模板+消费口径说明）
  - 全量守卫矩阵复跑：static-check 11/11 PASS 0 Warn ｜ doc-budget 全 ok（workflow-build 7900/8055、moshu-write SKILL 9140/9200、moshu SKILL 4992/5186——A2/A6 增量均在预算内，**无需调高**）｜ shared-files 71 组 97 副本 ｜ story-numbers ok (11) ｜ behavior-contracts 14 条 ｜ agents-version 30 ｜ claude-adapter 11 插件 ｜ agent-template-rules 7 ｜ eval-scenarios 3 ｜ capability-wiring ｜ current-skill-contracts ｜ hook-regex/locale ｜ python-invocation ｜ 全部 test-* 绿（唯一红=test-shared-assets mode-drift，已知 Windows chmod 平台限制，与审计-V4 第0部分一致）
  - 终扫 `grep -rn "开书.*moshu-write\|moshu-write.*开书" skills/ README.md README_EN.md --include="*.md"` → 剩余 7 处**逐条人工核对全部为合法表述**（来源声明/职责分工/接力提示/意图词枚举/路由示例），"开书→write"语义残留清零。
- 偏差记录：
  1. **A5 范围补充 2 处（原 B1 候选级）**：终扫发现 `skills/moshu-scan/references/topic-decision.md:4`（"写作（moshu-write）开书时直接用"）与 `:83`（"/moshu-write 会自动找到"）同为"开书→write"语义残留（审计 04 列为 B1 候选），为满足终扫"只清开书→write 语义残留"硬要求一并清理（改指 moshu-build 开书构建），未扩大其他候选项。
  2. **A3 判定表序3 文案**：按任务指示原文落"首批细纲 + 写第 1 章（/moshu-write，细纲首建见 outline-workflow；设定/卷纲缺失才回 /moshu-build）"，next_step S2 文案与之逐字对齐（原"写第 1 章（先补第 1 章细纲）"）。
  3. **A6 触发词处理**：采用"移除开书类触发词 + 标注转向"方案（write 仍承接"开书并写首章"场景经 build 转接，场景表 :45-46 保留）。
- 提交：3bb14da（2026-08-21，作者确认提交；含 12 文件，其中 11 施工 + 本条目；审核记录随批 B1c 回填见 1d531bb）
- 审核：通过（审核记录 2026-08-21）｜v1.5.0 发布前体检闭环

## 候选+存疑清理批 B2-B5/C1-C2（状态：已提交）

- 日期：2026-08-21
- 性质：审计-V4 候选 5 + 存疑 2 的清理批（作者明确发起"全部修复"；B4 总纲勘误经作者裁定授权；B1 已随 A5 顺清关闭，不在本批）。
- 修复内容：
  - **B2** `scripts/test-next-step.py` 补 S3/S4/S5 的 `suggested_skill=moshu-write` + `next_action` 含 `/moshu-write` 断言（S2 已于 A3 批补齐；脚本本身输出正确，纯测试覆盖缺口）。
  - **B3** `README_EN.md:125` 英文侧 31/189 硬编码 → 对齐中文侧 G7 脱敏表述（数量随版本增长/数百份），消除无守卫清单数字。
  - **B4** `docs/执行总纲V2.md:53` 术语表「共享资产（36 组）」→ 去数字化（"组数与副本数以 `scripts/shared-assets.json` 实测为准，不在本表重复数字"），对齐 :50/:51 agents_version/schema 行既有防漂移惯例。
  - **B5** review_tickets.py 跨 skill 定位标注：write（workflow-revision:81-82、workflow-daily:17）、build（revision-workflow:58）、setup 4 个 agent 模板共 8 处裸名引用补「该脚本为 moshu-review 侧单副本，本 skill 不分发」显式归属标注；review 侧 review-workflow:331-333 统一为 `moshu-review/scripts/review_tickets.py`（自 skill 限定路径，对齐 :41-44 先例）；UPGRADING.md:14 为 v27→v28 历史变更记录，按历史零触碰不动。
  - **C1** `workflow-build.md:182`「检查点、续写状态卡」并列 → 「检查点（续写状态卡，固定 7 栏）」消歧（二者同物）。
  - **C2** `story-consistency.md:44`「检查点却无法知道」→「追踪检查点却无法知道」（对齐机制名「追踪检查点」）。
- 验收输出摘要（全绿）：
  - 守卫矩阵复跑：static-check 11/11 PASS 0 Warn ｜ doc-budget 全 ok（workflow-build/workflow-daily/revision-workflow 增量均在预算内，**未调高**）｜ story-numbers ok (11) ｜ shared-files 71/97 一致 ｜ behavior-contracts 14 ｜ agent-template-rules 7 ｜ current-contracts ｜ agents-version 30 ｜ claude-adapter 11 ｜ eval-scenarios 3 ｜ capability-wiring 12 能力 33 消费点 ｜ hook-locale/hook-regex ｜ python-invocation ｜ test-next-step 12/12。
  - B5 终扫（git grep review_tickets.py）：跨 skill 8 处全部带归属标注，review 侧 3 处自 skill 限定路径，UPGRADING 1 处历史记录保留——逐条人工核对为合法。
- 偏差记录：
  1. **B5 首版方案被守卫拦截**：初改采用 `moshu-review/scripts/review_tickets.py` 路径锚点，static-check cross-skill-reference 守卫拒绝（路径形式进入其他 skill 属违例，同 moshu-style 先例）；回改为「裸名 + 散文归属标注」方案，同时满足守卫与 B5「显式标注」诉求；review 侧自 skill 限定路径合法保留。
  2. **B4 修法**：审计原建议"36→71 勘误"，施工改为去数字化（对齐总纲术语表 :50/:51 既有防漂移惯例），从根上消除再过期风险。
  3. **B3 修法**：审计原建议改实测 64/239，施工对齐中文侧 G7 脱敏先例改为去计数化表述（与 B4 同一原则）。
- 提交：c9c7d98（2026-08-21，作者确认提交；README 双语同步随后以 0ac303f 收尾）——哈希于 2026-08-21 docs 统一整理时补账回填。

## docs 统一整理与过程产物归档（状态：已完工/待提交确认）

- 日期：2026-08-21
- 性质：作者明确发起的仓库维护操作（非施工批次，不占状态总表批次行）：docs 活区瘦身 + 命名统一 + 新增文档索引；含同日 AGENTS.md 审核需修项 F1 修复与 B2-B5 批哈希补账。
- 内容：
  - **提级**：施工日志.md、审核记录.md → docs/ 根；施工协议 → docs/规格/README.md（标题去世代号、补当前状态注记、路径同步，v1.0→v1.1）。
  - **归档**：规格-V2/（批0-9）、规格-V2.1/（批B1×3）、研究-v3/、研究-v4/ → docs/归档/（同日早前已归档 审计-V3/、审计-V4/）。
  - **引用同步**：AGENTS.md（头部权威指针/归档清单、§2.0 施工入口、§2.1 批次规格路径、§2.6 判定口径、§6 证据锚点）；执行总纲V2.md（研究底稿注记、原则 5 一批一提交、术语表「批」）；协议 README（标题/版本、§1 相对链接、§6 审核产出路径、§9 提示词模板 3 处）。
  - **F1 修复**：AGENTS.md 头部「V2 落地说明」与 §2.6 的「next_step.py 未实施」陈旧陈述改为现状口径（批 4 跳过后由审计-V3 整改 D5 实施，提交 147d07f）；总纲术语表「下一步判定」条目「批 4 引入」同步修正。
  - **新增 docs/README.md**：文档地图、状态总览（做了/没做）、命名与版本规范（四条版本轴）、归档记录与新旧路径对照表。
  - **日志补账**：B2-B5/C1-C2 批「提交：待作者确认」回填 c9c7d98，状态改「已提交」。
- 验收输出摘要（全绿）：
  - 守卫矩阵：static-check 11/11 PASS 0 Warn ｜ story-numbers ok (11) ｜ doc-budget 通过（仅既有 workflow-daily 收紧提示） ｜ behavior-contracts 14 ｜ capability-wiring 12 能力 33 消费点 ｜ shared-files「71 canonical groups manage 97 synchronized copies」+ 锚点检查 66 组 0 不一致（71/97 与前次记载一致）。
  - 终扫（git grep，排除归档/两日志/CHANGELOG 历史）：活文件旧路径残留清零——剩余「规格-V2/研究-v3」字样均为归档清单注记、出处沿革或总纲头部历史确认记录（docs/README.md §4 对照表兜底解读）。
  - 无守卫/测试断言依赖被移动路径（前置引用清点确认：check-story-numbers 对 docs/architecture.md 静默跳过且该文件未动）。
- 偏差记录：
  1. 归档目录内保留封存时点名（规格-V2/、规格-V2.1/、研究-v3/、研究-v4/），未按活区「世代进子目录」规范重命名——归档零接触原则优先。
  2. 执行总纲V2.md 头部确认记录中的 `docs/规格-V2/批0-*.md` 为历史记录未改，按 docs/README.md §4 对照表解读。
- 提交：981d8e9（fix）+ e026f97（docs）+ 1755f57（整改）

## B2：卷报告对标节奏对照（状态：已提交/待审核）

- 日期：2026-08-21
- 验收输出摘要（规格 §5 全部通过，6/6 绿）：
  - `python scripts/test-tracking-commit.py` → 0；**39/39**（34 既有 + 5 新 B2 用例：①坐标+锚点命中正文第7章→偏差 +2 且 JSON 结构断言；②锚点未命中→not_found 行；③老卷纲无坐标表→skipped_no_coordinates；④--volume 显式与自动探测各一态；⑤确定性重放逐字节一致）
  - `bash scripts/check-shared-files.sh` → 0；66 组 0 mismatch / 71 canonical groups 97 copies（含 tracking_commit 四副本字节一致）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0
  - `bash scripts/check-doc-budget.sh` → 0；workflow-build 8165/8200（显式调高 8055→8200，见偏差 2）
  - `bash scripts/check-behavior-contracts.sh` → 0；14 条约束在位
  - `bash scripts/check-current-skill-contracts.sh` → 0；all passed
  - 平台备注：本机 bash 不在 PATH，走 Git 自带 `C:\Program Files\Git\bin\bash.exe`；python3 以 python 等价替代（README §7）。一次临时失败：`test_old_revision_preserves_current_next_chapter_commitment` 曾触 WinError 5（os.replace 文件锁竞态，commit 路径），重跑即绿——与批B2 改动无关。
- 偏差记录：
  1. **"三副本"实测为 4 副本**：`scripts/shared-assets.json:37-41` 同步组 source=moshu-write / targets=moshu-import、review、build（B1a 后 build 亦持副本）。按"以实际接线为准"用 `sync-shared-assets.py sync` 同步 3 目标（含 build），全部字节一致；`otherMaterials/referProject/` 三副本零触碰（只读红线）。
  2. **doc-budget 走"显式调高"**：workflow-build.md 新增坐标表模板后实测 8165 非空白字符，超预算 8055 达 110。模板为第④节必须解析的固定表格式（新增约 265 字符），非可删旧文本——按 doc-budget 纪律第②条显式调高 8055→8200，`why` 注明批次与理由。
  3. **规格行号漂移**：规格引 workflow-build.md:169/:181、tracking_commit.py:1480-1488 等行号——以实测为准（回流节 blockquote 在 :169；volume-report 参数区在 :1480-1489），算法按文字描述 + grep 结果施工。
  4. **volume-report stdout 结果不变**：第④节只进 MD 与 `--json-out` 的 `benchmark_rhythm_check`；stdout 的 result dict 仍为 `{report, from_chapter, to_chapter, foreshadow_counts, suspension_count, open_gap_count}`，退出码与 state 零改动（§8.1/8.2）。
  5. **--volume 参数**：新增可选 `--volume N`；显式卷缺失 / 自动探测多卷命中或范围未识别 → `skipped_ambiguous_outline`（候选，不失败）；老卷纲无坐标表 → `skipped_no_coordinates`（只多一行说明，①②③节不变）。
- 提交：dae25f6（2026-08-21，作者确认提交）

## B3：构建台账（状态：已提交/待审核）

- 日期：2026-08-21
- 验收输出摘要（规格 §5 全部通过，10/10 绿）：
  - `bash scripts/check-behavior-contracts.sh` → 0；**15 条约束在位**（+1 `build-ledger-entry`，must_contain=`构建台账`）
  - `python scripts/test-behavior-contracts.py` → 0；反向 fixture 覆盖 `build-ledger-entry`（删该契约必失败）
  - `bash scripts/check-capability-wiring.sh` → 0；**13 个能力、35 个消费点**全部接线（+1 `build-ledger`，consumers=revision-workflow / 开新卷节）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0
  - `bash scripts/check-doc-budget.sh` → 0；workflow-build 9307/9500、revision-workflow 1998/2100、构建路径组 12863/13160（显式调高，见偏差 2）
  - `bash scripts/check-shared-files.sh` → 0；66 组 0 mismatch / 71·97 副本
  - `bash scripts/check-story-numbers.sh` → 0；ok (11 skills)
  - `bash scripts/check-claude-adapter.sh` → 0
  - `bash scripts/check-agent-template-rules.sh` → 0
  - `bash scripts/test-writing-pipeline.sh` → 0；OK（全管道不回归）
  - 人工走查：台账模板四节齐全（方法论声明/当前焦点/待定项/已收敛→变更日志）；`grep "构建台账"` 三文件均可定位（workflow-build 头部/模板/开新卷、revision-workflow ①、SKILL.md 入口）
  - 平台备注：Windows 下 `test-behavior-contracts.py` 需 `PYTHONIOENCODING=utf-8`（子进程 stderr 为 GBK，UTF-8 解码失败——README §7 已知平台处理），设置后退出 0；bash 走 Git 自带路径。
- 偏差记录：
  1. **压缩回流节冗余（规格 §3 压缩优先策略）**：:169 回流节"主产物缺失→missing_primary_contract 并停止"与 :37-39 重复，合并为"主产物缺失按 Phase 1「对标上下文加载」第 3 条处理"（消除第二处复制，腾出预算；压缩后实测仍超、需调高）。
  2. **doc-budget 走"显式调高"（先压缩后仍不足，记为 why）**：workflow-build 8200→9500（实测 9307；新增构建台账与进入规则节/方法论声明步/Phase2 显式化/Phase3 终局不可裁声明，本批核心机制，构建会话每次全量加载）；revision-workflow 1972→2100（实测 1998；第①步衔接一行）；构建路径组 11587→13160（=三文件预算和，随动）。
  3. **契约计数无独立常量**：`test-behavior-contracts.py` 无计数常量（按 manifest 动态驱动，B1c 已确认）——"计数同步"落实为反向 fixture 增补 `build-ledger-entry`；README 亦无硬编码"14 条/12 能力"（动态按 manifest）。
  4. **capability-wiring consumers 用文件级**：`build-ledger` 的 consumers 为 `revision-workflow.md` 与 `workflow-build.md`（含开新卷节）——守卫按文件级 must_contain 校验（不细分到节）；开新卷节已补一句"同样先读构建台账"（对应"三种场景统一"）。
  5. **行为面变更未动版本号**：本批为文档机制增补（进入必读/收敛必写/收尾必结/方法论显式化），未新增或改脚本能力面、未动 schema；按规格改动清单未列版本文件，未 bump SKILL version（check-claude-adapter 与 agent-template-rules 全绿验证）。
- 提交：f9ce58e（2026-08-21，作者确认提交）

## B4：方法论副本闭包（状态：已提交/待审核）

- 日期：2026-08-21
- 已完成且绿的部分：
  - **①shared-assets**：10 文件已加 build target（8 既有组加 target + 2 建组 `plot-frameworks-reference`/`outline-structure-theory-reference`，source=write 侧现路径，既有组 source 未动）；`sync` 后 build references 达 25 项（15+10）；`python scripts/sync-shared-assets.py check` → **73 canonical groups / 107 synchronized copies** 一致。
  - **②守卫草案**：`scripts/check-reference-closure.py` 初版已写（逐行提取 `.md` 词元 + `--root` + 理由白名单骨架 + 需求 `build` 副本），`.sh` 与 `test` 未写、CI 未同步。
- ### 待决问题：守卫算法与现状冲突（首跑枚举 786 处，远超"少数违例分类"预期）

  - **现象**：按规格 §4 从全部 `skills/*/references/*.md` 逐行提取 `.md` 文件名提及并在本 skill 域内断言，首跑报 **786 处**。其中绝大多数**并非本批要抓的"方法论副本闭包"断链**，而是三类被误判：
    1. **书项目运行态产物/路径**（构建/写作时在作者项目根生成，本就不在 skill 树）：`设定/题材定位.md`、`追踪/伏笔.md`、`正文/第XXX章_章名.md`、`文风库/文风.md`、`拆文库/…`、`对标/…`、`剧情/节奏.md`、`拆文报告.md`、`_progress.md`、`概要.md`、`选题决策.md`、`灵感种子.md`、`背景设定.md`、`伏笔.md`、`上下文.md`、`信息差.md`、`读者已知.md`、`作者真相.md` 等——大批出现在 analyze/output-templates/chapter-core/workflow-daily 等。
    2. **占位/模板文件名**：`卷纲_第X卷.md`、`细纲_第N章.md`、`第N章_深度拆解.md`、`第NNN章.md`、`章.md` 等（模式名非具体文件）。
    3. **agent 部署路径**：`.claude/agents/moshu-*.md`（部署资产，非 references/）。
  - **真正属于本批目标**的仅为极少数：build 侧 `genre-core-mechanics.md:5`→`genre-readers.md`、`opening-design.md:25`→`writing-craft.md`、`style-genre-modules.md:23`→`style-combat-face.md`、新副本 `plot-frameworks.md:243/:602`→`plot-special-topics.md`/`plot-emotion-system.md`（二级传递，据禁止事项 1 倾向先报不修）、路由表 `volume-review.md`/`outline-workflow.md`/`artifact-protocols.md` 跨域合法提及（白名单）。约 4-8 个。
  - **已尝试**：初版把 `[\w\-./]+\.md` 正则在整行上匹配（覆盖链接/反引号/正文），并加最小白名单（volume-review/outline-workflow/artifact-protocols）后首跑仍是 786——说明噪声来源不是"只差白名单"，而是**判定范围过宽**。
  - **疑似原因**：规格 §4 的"本 skill 域内存在 + 白名单"判定无法区分「**skill 资产引用**」（应查闭包）与「**作者项目运行态产物/占位名**」（引用关系在作者项目内，运行技能时才创建，本就不在 skill 树）。规格把"提及的文件名"一律当作"应在本 skill 域内可解析的 skill 资产"，与文档大量提及书项目路径的现状不符。
  - **需要谁决定什么**：请作者/规划模型就守卫的**判定范围**裁决，二选一：
    - **方案 A（建议）**：守卫只对「提及文件名 ∈ 全仓 skill 资产参考文件集合（`skills/*/references/**` 的 basename）」做闭包断言；提及非资产文件名（书项目路径/占位名）直接忽略不报。噪音近零、仍能抓住真实闭包缺口。
    - **方案 B**：维持全量提及扫描，但加"书项目路径前缀 + 占位模式"排除清单（`设定/`、`追踪/`、`正文/`、`文风库/`、`拆文库/`、`对标/`、`剧情/`、`拆文报告.md`、`_progress.md`、`第N章`、`第X章` 等）——更脆弱、维护面大。
    - 另请确认 build 侧其余跨域提及（genre-readers / writing-craft / style-combat-face 及新副本二级 plot-special-topics / plot-emotion-system）本批如何处置：**也补副本 / 白名单带理由 / 真断链呈报**（若属二级传递，据禁止事项 1 倾向先报不修）。
  - 已保留未回退：shared-assets 10 副本与 sync（核心修复，绿）；守卫为草案未定稿。
### 裁决执行与终态（2026-08-22，按 ### 待决答复 方案 A / 裁决二）

- **方案 A 守卫**：`scripts/check-reference-closure.py` + `.sh` 重写为「资产宇宙闭包」语义——U=全仓 `skills/*/references/*.md` 直接子项 basename；提及 ∈ U 才做闭包断言，非 U（书项目产物/占位名/agent 路径/子目录资产）自动忽略不报；`ALLOWED_CROSS_DOMAIN` **5 条理由白名单**（volume-review / outline-workflow / artifact-protocols / writing-craft / style-combat-face）。
- **副本 14 个**（10 路由表 + 3 裁决二 + 1 二级缺口 outline-workflow，见偏差 2）：build references 达 **29 项**（15 原始 + 14 副本）。
- **CI 三处同步**：`.github/workflows/cross-platform.yml`（static-guards 加守卫+回归 2 步）、`scripts/README.md`（守卫表+测试表各 1 行）、`CONTRIBUTING.md`（static-check job 清单 + 命令块各 1 处）。

### 验收输出摘要（规格 §5 全部通过，7 项绿 + 周边无回归）

- `bash scripts/check-reference-closure.sh` → 0；`reference closure: ok (111 references files)`
- `python scripts/test-reference-closure.py` → 0；五用例全过（①②③④⑤，⑤锁定"非资产宇宙忽略"语义）
- `python scripts/sync-shared-assets.py check` → 0；**76 canonical groups / 111 synchronized copies**
- `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**（含 build 副本链接可达）
- `bash scripts/check-doc-budget.sh` → 0
- `bash scripts/check-shared-files.sh` → 0；66 组 0 mismatch（14 副本字节一致）
- `bash scripts/check-capability-wiring.sh` → 0
- 周边无回归：`check-story-numbers` 0（11 skills）、`check-claude-adapter` 0、`check-agent-template-rules` 0、`test-writing-pipeline` 0、`test-behavior-contracts` 0、`test-capability-wiring` 0；`test-shared-assets` 唯一红=Windows 已知 mode-drift（chmod no-op；CI Linux 绿，与批B4 无关）。

### 偏差记录

1. **方案 A 资产宇宙界定**：U 取 `skills/*/references/*.md` **直接子项** basename（非勘误 §4 字面 `skills/**`）——因 `skills/**` 会把 `templates/agents/` 的 agent 模板（moshu-architect 等）纳入宇宙，与裁决一"agent 部署路径自动出局"矛盾；直接子项恰好排除 agent/子目录资产、纳入方法论/参考文件。
2. **二级缺口补 target（outline-workflow）**：新副本 `outline-structure-theory.md:278` 含 Markdown 链接 `outline-workflow.md#细纲全书每章`，static-check 抓为断链；按"二级缺口可直接补 target"授权，将 outline-workflow.md 登记为 build 副本（write 源、新组、sync），并核其内部链接（outline-methods / opening-design）build 均已具备、无级联。副本总数 13→14。
3. **计数随实际**：同步后 76 组/111 副本（待决问题阶段为 73/107）；build references 29（勘误 §5"15+10=25（±1）"的预期——实际含 14 副本，以实测为准）。
4. **test-shared-assets mode-drift**：Windows 已知平台限制（chmod no-op），非本批回归；manifest 结构/数量的校验全过。
5. **static-check Warn 0 基线修复（预检要求）**：新增 14 副本后 static-check 报 **1 聚合 Warn（dead-reference ×14）**——新副本不在 SKILL.md 链接可达图。按预检修复：在 workflow-build.md「构建台账与进入规则」节内新增「**方法论副本索引**」Markdown 链接清单（列出 14 个副本，注明"经 outline-methods 路由表按需加载"），使副本经由 SKILL.md → workflow-build.md 链接可达；static-check 恢复 **Total 11 / Pass 11 / Warn 0**。索引增量使 workflow-build 实测 9978 超预算 9500（478），按 doc-budget 纪律显式调高 9500→10300（构建路径组 13160→13960），`why` 记批B4 所述理由。

- 提交：59aa845（2026-08-22，作者确认提交）

## B5：教学书萃取（状态：已提交/待审核）

- 日期：2026-08-22
- 依据：`docs/研究-v5/01-小说的骨架-重叠度评估.md`（旗舰复核判类）+ 作者 2026-08-22 勾选（4★+完美评述）。
- 已完成：
  - **三落点文件纯新增**（`git diff` 合计 +79 行 / 0 删除，既有内容零改动）：
    - `skills/moshu-write/references/character-design-methods.md`→新增「人物探索三件套」一节（置于九维人设框架后）：①人物采访（转译为"区块+生成规则"，6 区块×每块 3-5 个示例刁钻问题 + 自由采访规则，**不照搬固定 100 问**）；②幕后故事四步（总体概述→其他重要人物→教育/职业/旅途→大事件"点金石" + 使用三纪律：早埋晚揭/越短越好/暗示替代直白）；③人物变化轨迹表（五要素：起点状态/起点缺陷/前段例证/成长工具/高潮证明，注明与九维三阶段弧线关系）。
    - `skills/moshu-write/references/opening-design.md`→「三大基点」切入点基点处加「激发事件四问」子块（什么是/什么不是/在何处/四特质；"1/4 处"转译为**开篇期**口径，与黄金一章检查清单衔接）。
    - `skills/moshu-write/references/idea-seed.md`→Logline 后加「完美评述」步骤（理想评论家口吻、具体/全面/满足、LLM 生成→作者改定、落 `设定/` 或构建台账附注、与构建台账分工：评述管目标态/台账管过程态）。
  - **萃取三关**：每条内容含 网文化转译（示例用中文网文语境重写、无西方单本语料）、三问准入（每小节末尾一行注明"谁在什么阶段调用/解决什么问题/调用后可观察结果"）、来源存证（`来源：《小说的骨架》第N章（otherMaterials/referFile，2026-08-22 萃取）`）。
  - **sync**：`sync-shared-assets.py sync` 同步 build×3 + setup agent-references×2 副本；`check` → **76 组/111 副本**一致。
- 验收输出摘要（A-D 全绿）：
  - A 落点与存证：`《小说的骨架》`命中 character-design **4** / opening-design **1** / idea-seed **1**；character-design「谁在什么阶段调用」命中 **3**。
  - B 副本一致：`sync-shared-assets.py check` → 0（76/111）。
  - C 守卫：`static-check` 0（Total 11 Pass 11 Warn 0）｜`check-doc-budget` 0（三落点文件为冷路径未登记，无超支）｜`check-reference-closure` 0｜`check-shared-files` 0｜`test-writing-pipeline` 0。
  - D 转译抽检：`grep "1/4 处|三幕|yWriter"` 三文件 → 零命中。
- 禁止事项核对：
  1. 不照搬 100 问 → 采访转译为"区块+生成规则"；✅
  2. 不引入四冲突项 → 三落点文件 grep 九型/连点成画/扩展版提纲/三幕/yWriter/1/4 处全零命中；✅
  3. 既有内容零改动 → `git diff` 纯新增（+79/−0）；✅
  4. referFile 只读 → `git status otherMaterials/referFile/` 空；✅
  5. 未勾选项不做 → 只做 5 条勾选增量（人物采访/幕后故事四步/人物变化轨迹表/激发事件四问/完美评述）；章节结尾/主题五问/如果问句未萃取；✅
- 偏差记录：
  1. **三问准入原文标签**：初写三问行过紧凑、缺规格 §3 的"谁在什么阶段调用/解决什么问题/调用后可观察结果"三标签，验收 A2（character-design「谁在什么阶段调用」）命中 0；已按标签格式修订（character-design 命中 3、opening/idea-seed 各 1），复跑 A2 绿。
  2. **build/setup 副本经 sync 生成**：character-design/opening-design 另有既有共享组 target（setup agent-references），sync 一并再生成；manifest 计数未变（76/111）。
- 提交：f0e0169（2026-08-22，作者确认提交）

## B6：拆文产物定位声明（状态：已关闭，6fda8ea，审核通过 2026-08-22）

- 日期：2026-08-22
- 性质：纯文档声明批（2 文件 3 处，零脚本/零守卫/零 sync/零契约）。
- 已完成：
  - **F1** `output-templates.md`：节奏.md 权威范围行改为「权威范围（写作/构建流程消费）：关键信息推进、爽点循环、情绪触动点、爆发节奏。其余内容（「全书情绪节奏总览」、冲突升级路径、伏笔跨章地图、「循环单元」节等）为作者学习参考，写作/构建流程不读取。`拆文报告.md` 只引用本文件摘要。」
  - **F2** `output-templates.md`：散落情节兜底节追加第 7 条「定位声明」（`散落情节.md` 是剧情聚合的归类兜底，仅 moshu-import 迁移消费；写作/构建环不读取）——置于列表末尾（第 6 条覆盖率验证之后，保持 1-6 编号不变）。
  - **F3** `technique-summary-sop.md`：定位行后追加「沿革（2026-08-22 补记）」行（承接参考项目 oh-story-claudecode 文风生成 SOP 拆分：文风.md 一拆为二，表达层→moshu-style 文风库，学习维度→本技法总结；技法总结纯作者学习材料）。
- 验收输出摘要（A1-A4 + B1-B3 + 附加，全绿）：
  - A1 `grep "写作/构建流程消费" output-templates.md` → 命中 1（F1 生效，:365）
  - A2 `grep "归类兜底" output-templates.md` → 命中 1（F2 生效）
  - A3 `grep "沿革（2026-08-22 补记）" technique-summary-sop.md` → 命中 1（F3 生效）
  - A4 `grep -c "写作流程不读取" technique-summary-sop.md` → 1（≥1）
  - B1 `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - B2 `bash scripts/check-doc-budget.sh` → 0（两改动文件不在热路径清单，零预算风险）
  - B3 `bash scripts/check-shared-files.sh` → 0；76 canonical / 111 copies，71 组 0 mismatch（零 sync）
  - 附加自证：`grep "对标拆文" skills/moshu-write/references/writing-craft.md skills/moshu-setup/references/agent-references/writing-craft.md` → 两文件 :304 均为原文（禁止事项 1 遵守，双副本未动）
- 禁止事项核对：
  1. 不动 writing-craft.md 双副本 → 附加自证 :304 仍为原文；✅
  2. 不改「循环单元」「爽点循环索引」两节 → 未触碰；✅
  3. 不动 analyze-workflow.md:88 权威产物声明 → 未触碰；✅
  4. 不碰 referProject/referFile/归档 → 未触碰；✅
  5. 只改清单内 2 文件 3 处 → `git diff` 仅 output-templates（F1 替换+F2 追加）与 technique-summary-sop（F3 追加）；✅
  6. 声明措辞否定口径（其余内容…等为学习参考）、无具体行号/漂移数字 → F1 new 用「…等」否定口径；✅
  7. 不自行纳入 Q2/Q4/Q6 → 未纳入；✅
- 偏差记录：
  1. **F2 位置（规格措辞矛盾，按语义裁决）**：规格 F2 写"在 :480 的'6. 覆盖率验证…'之前插入新第 7 条，置于 :479 之后"，但同句又写"保持 6 条原有编号不变，新条目放列表末尾"——两条相抵。按"保持编号不变+放列表末尾"（语义明确）执行：第 7 条置于第 6 条（覆盖率验证）之后、列表末尾；"在 :480 之前"疑为笔误。A2 命中 1，任一所涉位置均不破坏 A2。
  2. **零契约/零共享/零 sync**：output-templates/technique-summary-sop 均不在共享组（shared-assets.json 零命中），behavior-contracts 零牵连，本批未跑 sync（规格 §4/§6 零脚本零 sync）。
- 提交：6fda8ea（2026-08-22，作者确认提交；批B6 声明批）

## moshu-scan 路由表 1 行修复（状态：已完工/待提交确认）

- 日期：2026-08-22
- 性质：**旗舰审计发现，B1b 残留第 3 例**——`moshu-scan/SKILL.md` 流程衔接表"直接开写 | moshu-write"措辞逃过当时"开书.*moshu-write"终扫 grep；同 skill 的 `topic-decision.md:4/:77` 已正确指向 moshu-build，此为 SKILL 表漏改。
- 改动：`skills/moshu-scan/SKILL.md` 流程衔接表 :74 `| 直接开写 | moshu-write | \`/moshu-write\` |` → `| 直接开书 | moshu-build | \`/moshu-build\` |`（1 行，精确替换）。
- 验收：`git grep -n "直接开写.*moshu-write" -- skills/` → **exit 1（零命中）**；`bash scripts/static-check.sh` → **0（Total 11 | Pass 11 | Fail 0 | Warn 0，moshu-scan PASS）**。
- 禁止事项/范围：仅改动机 1 行，未动参考项目/归档/其他文件。
- 提交：5998ee8（2026-08-25，作者确认提交）（1 行改动；作者明确说"提交"后才 commit，本批未做任何 git 写操作）

## B7：治理文档收口（状态：已完工/待提交确认）

- 日期：2026-08-22
- 性质：治理文档收口为"五种各一份"——AGENTS.md 唯一宪法（自包含）/ docs/审计法.md（唯一审计方法 SOP）/ docs/规格/README.md（唯一协议）/ 两台账 / docs/README.md（索引）；执行总纲V2 活部并入宪法后整体归档。
- 已完成：
  - **AGENTS.md**：①头部权威行改"本文件为唯一宪法（原则/术语/不学清单已自总纲收口，2026-08-22 批B7）"；②新增 §9「宪法收口」（术语表 16 条原样迁入 + 宪法原则三条：兼容四原则/证据溯源原则/三层分工宪法 + 节尾映射说明）；不学清单正文（名称+理由）内联到 §6 自包含；③§5 决策树第 4 问改"先改本文件 §6"。
  - **docs/审计法.md**（新建）：唯一审计方法 SOP（版本/适用范围/沿革 + 六步流程 + 纪律 + 产出格式 + 术语）。
  - **git mv docs/执行总纲V2.md → docs/归档/执行总纲V2.md**。
  - **docs/README.md**：:3 出处改指根 AGENTS.md §6/§9；地图表"执行总纲V2"行换"审计法.md"行；:27 不学清单指 §6；:45 命名规范去"总纲世代例外"（改述旧总纲已归档）。
- 验收输出摘要（A-E 全绿）：
  - A `git grep "执行总纲V2" AGENTS.md docs/README.md docs/规格/README.md docs/architecture.md skills/ scripts/` → **exit 1（零命中，硬门）**
  - B `docs/归档/执行总纲V2.md` 在位（git mv，活区移走）
  - C AGENTS 自包含：`续写状态卡|兼容四原则` 命中 **2**；`详见《执行总纲V2》` 命中 **0**
  - D `六步流程|锁话不锁链` docs/审计法.md 命中 **3**
  - E static-check 0（Total 11 | Pass 11 | Warn 0）｜check-doc-budget 0｜check-story-numbers 0｜check-behavior-contracts 0
- 禁止事项核对：
  1. 语义不改（原样搬运；"下一步判定"措辞加"（历史注记）"）→ 术语/原则/理由照总纲原文迁入；✅
  2. 不把战役历史搬进 AGENTS（批次表/单元摘要/附录 A/C/D 留在归档）→ 未搬；✅
  3. 不动两台账与协议 README（本批只写 B7 施工日志条目与状态行；审核记录与协议 README 未动）→ ✅
  4. 归档区其他内容不动（仅 git mv 执行总纲V2）→ ✅
  5. 引用同步零遗漏 → A 零命中（硬门）；✅
- 偏差记录：
  1. **§9 标题规避"执行总纲V2"字面**：规格 §9 标题作"自执行总纲V2 移入"，但验收 A 要求 AGENTS 中"执行总纲V2"零命中（硬门）——冲突。按硬门优先，§9 标题改"自旧总纲（V2 世代）收口迁入"，正文以"（历史注记：…旧总纲已入 `docs/归档/`…）"保留出处而不写确切文件名（写 `docs/归档/执行总纲V2.md` 会命中 A）。
  2. **git mv 属 git 写**：按规格 §3 明令执行 git mv（staged rename，非 commit）；提交仍待作者明确说"提交"。
  3. **doc-budget/契约/共享零牵连**：AGENTS/审计法均不在热路径预算、不在行为契约、不在 shared-assets（F5 实测）。
  4. 术语表"下一步判定"行原总纲为"（批 4 跳过后由审计-V3 整改 D5 实施…）"，按"过时措辞加历史注记"改"（历史注记：批 4 原弹性跳过，后由审计-V3 整改 D5 实施…）"——语义不变，保留历史。
- 提交：330fbfd（2026-08-22，作者确认提交；批B7 治理收口）

## moshu-analyze 路由表 1 行拆 2 行修复（状态：已完工/待提交确认）

- 日期：2026-08-22
- 性质：**旗舰联合审计发现，B1b 残留第 4 例 + 守卫升级条款触发**——`moshu-analyze/SKILL.md` 流程衔接表"准备开写 | moshu-write"是"开书语义残留"第 4 次以措辞变体逃逸"开书.*moshu-write"终扫 grep（前 3 例见 `docs/审核记录.md:306`：deploy-manual"目标平台由开书采集"/import-setup"直接开书"/moshu-scan"直接开写"）。该条记档的升级条款（"若出现第 4 例，路由残留终扫应升级为语义面更宽的守卫。记档不做"）随即触发，守卫设计草案入本条目待决节——**只出草案不实施，旗舰裁决后做**。
- 改动：
  - `skills/moshu-analyze/SKILL.md:44-45` 流程衔接表 1 行拆 2 行（精确替换）：`| 准备开写 | moshu-write | \`/moshu-write\` |` → `| 准备开书 | moshu-build | \`/moshu-build\` |` + `| 已有书续写 | moshu-write | \`/moshu-write\` |`。依据：拆完书主路径=登记对标开书（build 对标发现扫拆文库），已有书续写才回 moshu-write。
  - `scripts/doc-budget.json` moshu-analyze 预算 2300→2422（拆行净增 34 非空白字符，实测 2307 超预算 7 字；按 B1b 先例"路由正确性值得每次会话付"显式调高，预算=实测×1.05 取整；见偏差 1）。
- 验收：
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0（moshu-analyze PASS）
  - `grep -rn "准备开写.*moshu-write" skills/` → exit 1（零命中）；全仓扫描（.md/.py/.sh/.json/.yml，排除 .git/归档/otherMaterials）"准备开写"亦零命中
  - `bash scripts/check-doc-budget.sh` → 0（调高后 analyze 2307/2422）
  - `bash scripts/check-story-numbers.sh` → 0（周边无回归）
- 偏差记录：
  1. **doc-budget 显式调高 2300→2422**：1 行拆 2 行净增 34 非空白字符（2273→2307），超预算 7 字。同文件旧文本无可删等量冗余（agent 兼容块/拆解边界声明均为机制性内容；双 `---` 分隔线仅 3 字符不敷），按 doc-budget 纪律第②条显式调高并在 why 记批次与理由（B1b 路由拆行同款先例）。
- 禁止事项/范围：仅 audit 指定 1 处路由行 + doc-budget 预算随动；未动参考项目/归档/其他文件；git 零写操作。
- 提交：d5507cd（2026-08-25，作者确认提交）
- 复核修正注记：有条件通过复核修正 2 处（导读 :94 审计报告路径、CHANGELOG B47 条目措辞对齐）；复跑验收 1 额外抓到规格README 3 处**裸写形态**（无反引号）漏改并修复（:94 docs/审核记录.md、:99 docs/审核记录.md + docs/规格/<世代>/）——裸写形态为同步模式盲区，已记入偏差。（2 文件改动；作者明确说"提交"后才 commit）

### 待决问题：守卫升级条款草案（第 4 例出现触发；只出草案不实施，旗舰裁决后做）

- 条款出处：`docs/审核记录.md:306`——"开书语义残留第 3 次以措辞变体逃逸 grep……若出现第 4 例，路由残留终扫应升级为语义面更宽的守卫"。
- 现状底数（实测）：全仓 skills/**/*.md 第二列=moshu-write 的表格行共 **4 行**——moshu-deslop/SKILL.md:105（继续写作）、moshu-import/SKILL.md:71（导入完想继续写+日更）、moshu-analyze/SKILL.md:45（修后"已有书续写"）、moshu-review/SKILL.md:71（要修改查出的问题→返回对应写作 skill 修改；第三列非 /moshu-write 命令，严格形如"| * | moshu-write | /moshu-write |"仅命中前 3 行）。
- 守卫设计草案（算法级）：
  1. 落点：新建 `scripts/check-route-write.py`（亦可挂 check-story-numbers 体系，算法不变；推荐新建——语义独立不混入既有守卫，按 §2.5 配正式回归测试 + scripts/README.md + CI 三处同步）
  2. 遍历 `skills/**/*.md`，正则 `^\s*\|\s*([^|]+?)\s*\|\s*moshu-write\s*\|` 收集第二列=moshu-write 的表格行
  3. 全量枚举输出 `file:line: 语境列`，逐行人工核语境后白名单化（合法=续写/回炉/重写/日更/补纲；违规=开书/开写/设定）
  4. `WHITELIST = {file:line: 语境标签+理由}`，初始即上列 4 行
  5. 判定：命中行∉白名单 → candidate 呈报（不阻断，守 §2.7）；语境列词面含 开书|开写|设定 → 是否升 blocking 由旗舰裁决
  6. 回归 `test_check_route_write.py`：合法行 pass／缺白名单行报 candidate／（可选）黑词行 blocking
- 候选观察（只呈报，不阻断）：`moshu-analyze/SKILL.md:40` 位置行"在 moshu-scan 之后、moshu-write 之前"未点名 build（扫描→拆文→build 开书→write 续写链中 build 缺位），是否随守卫升级一并处理由旗舰裁决。
### 待决答复（旗舰侧，2026-08-22）

**裁决：采纳实施，立为批 B8（本修复批提交后开工）。** 设计修正两点：

1. **两级判定（对齐机检哲学）**：第二列=moshu-write 的表格行——①语境含已知构建域词（开书/开写/建设定/写大纲/卷纲等）→ **blocking**（确定性残留，退出 1）；②语境在合法白名单（续写/继续写/回炉/重写/日更/补纲/修改/去味等 write 域语义）→ 过；③**未知语境 → candidate（呈报不阻断，退出 0）**——"候选永不拦截"宪法的正确应用：未知≠违规。flash 草案"缺白名单→candidate"与"升 blocking 与否留裁决"两条合并为此两级制。
2. **范围锁表格行**：只扫 `skills/**/*.md` 中形如 `| {语境} | moshu-write |` 的表格行；prose 面（deploy-manual:90 型）不进守卫（误报面大，留给审计法第 4 步轻扫）。

存量处置：deslop:105（继续写作）/import:71（导入完想继续写）/review:71（要修改查出的问题）按白名单收编，各带一行理由注释。analyze SKILL:40 位置行非表格行不进守卫面，作为候选记录（流水线叙述"在 write 之前"漏 build，下次顺带精确化，不立项）。

价值判断：该缺陷类 4 次措辞变体逃逸、每次靠人工审计发现；守卫约 60 行把它变 CI 自动——与 check-story-numbers 的立项逻辑同构。

## 批B8：路由残留守卫（状态：已提交/待审核）

- 日期：2026-08-22
- 规格：`docs/规格/批B8-路由残留守卫.md`（前置 cf1c109 已合入 HEAD）
- 已完成：
  - `scripts/check-route-write.py`（新）：§4 算法——表格行 only（`^|`）+ 第二列=moshu-write 等值判定（`` ` ``/`/` 装饰可剥）+ 两级判定（BLOCKING_WORDS 9 词→退出 1；WHITELIST_WORDS 14 词→过；未知→candidate 退出 0）+ `--root` + 词表每词一条理由注释 + 读失败三分类（UTF-8 解码失败明示，不静默跳过）
  - `scripts/check-route-write.sh`（新）：python3→python→py 探测 wrapper（仿 check-story-numbers.sh）
  - `scripts/test-route-write.py`（新）：四用例（①白名单过 ②blocking 退出 1+文件名 ③未知 candidate 退出 0 ④无表格行过），守护对象头 + tempfile 自清理
  - CI 三处同步：`.github/workflows/cross-platform.yml` static-guards +2 步；`scripts/README.md` 守卫表+测试表各 1 行；`CONTRIBUTING.md` 清单+命令块各 1 处
- 首跑归类（全仓 skills/**/*.md）：命中 **4 行**——analyze:45「已有书续写」、deslop:105「继续写作」、import:71「导入完想继续写（长篇）」、review:71「要修改查出的问题」——**全部写作域合法，白名单词覆盖收编，零 blocking、零 candidate**
- 验收输出摘要（规格 §5 全部通过）：
  - `bash scripts/check-route-write.sh` → 0；`route-write: ok (4 rows checked, 0 candidates)`
  - `python scripts/test-route-write.py` → 0；4/4（Windows 按 README §7 以 python 代 python3）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0
  - `bash scripts/check-doc-budget.sh` → 0
  - `bash scripts/check-story-numbers.sh` → 0（新脚本不扰数字口径）
- 禁止事项核对（四条全部执行）：
  1. 不扫 prose → 只认 `^|` 表格行；全仓 prose 提及（moshu-write/SKILL.md:55 正文、moshu/SKILL.md:58 引注等）零计入；✅
  2. 未知语境不 blocking → candidate 打印且退出 0（用例③锁定该语义）；✅
  3. 白名单无理由注释 → BLOCKING/WHITELIST 两词表每词一条理由（含存量行收编依据）；✅
  4. 不查 write 以外目标 → 等值判定仅 == moshu-write；实证：analyze:44「准备开书→moshu-build」行与全仓其他 build 目标行零计入（4 rows checked 即含证）；✅
- 偏差记录：
  1. **规格 F2 基线描述与实测不符（描述性偏差，未改算法）**：F2 称基线含「moshu/SKILL.md 路由表多行（写长篇/回炉等）」与「write 自身衔接表」——实测均无"第二列=moshu-write"的行：moshu/SKILL.md:15 路由表 write 命令在**第三列**（列2=意图说明）、:50-53 判定表命令在**第三列**动作列、moshu-write/SKILL.md 无衔接表（仅 frontmatter:4 与正文:55 提及）。按 §4.1"第二列"口径（待决答复裁定）实现，上述行不命中；守卫不扩第三列（禁改规格）。该等行语义均为写作域（写长篇/回炉/重写/日更/补纲），无开书残留。
  2. **import-workflow.md:472 由等值判定排除**：`| 长篇项目结构规范 | 参见 moshu-write（Phase 4 项目文件结构） |` 第二列是叙述性"参见"单元格，宽 grep（含子串）会误中——§4.1 等值判定是必须的，实测已正确排除。
  3. 状态表 B8 行已由旗舰预置「待施工」，本批收尾置「待提交确认」。
- 提交：92ed92c（2026-08-22，作者确认提交）

## v1.3 重审修复批：异常降级链半步战果 A1/A2（状态：待提交确认）

- 日期：2026-08-22
- 性质：**审计法 v1.3 异常降级链半步战果 A1/A2**——3a 异常降级路径「缺件明示」两项同类小修：路径参数指向不存在目标时裸抛堆栈 → 人话错误 + 退出 2（与既有用法错/空目录处理风格一致）。
- 改动：
  - A1 `skills/moshu-scan/scripts/scan-analyze.js`：main() 在 `!DIR` 用法检查后加 `fs.existsSync(DIR)` 预检，不存在 → `[错误] 目录不存在: {DIR}` + process.exit(2)（风格对齐空目录处理 :178-179）。
  - A2 `skills/moshu-analyze/scripts/check_chapter_summary.py`：main() 在 parse_args 后加 --file/--dir 路径预检，不存在 → `[错误] 文件不存在: {p}` / `[错误] 目录不存在: {d}` + sys.exit(2)（风格对齐 :131-132）。顺带消除 `--dir + --deep` 指向不存在目录时的静默 ALL PASS 降级（旧行为：glob 空集→deep_only→0 退出——缺件被当"无活可干"）。
- 实测（缺路径场景，输出+退出码）：
  - A1 `node scan-analyze.js --dir {不存在}` → stderr `[错误] 目录不存在: ...`，exit **2**
  - A2a `python check_chapter_summary.py --file {不存在}` → stderr `[错误] 文件不存在: ...`，exit **2**
  - A2b `python check_chapter_summary.py --dir {不存在}` → stderr `[错误] 目录不存在: ...`，exit **2**
- 验收（全绿）：
  - 既有行为不回归抽查：空榜单目录 → 原报错「下没有榜单文件」exit 2；空章节目录 → 原报错「下没有 第*章_摘要.md」exit 2（两条原样未动）
  - `node --check skills/moshu-scan/scripts/scan-analyze.js` → 0
  - `python -m py_compile skills/moshu-analyze/scripts/check_chapter_summary.py` → 0
  - `node scripts/test-scan-analyze.js` → 0；13/13 pass（基线 13/13，改后复跑 13/13）
  - `bash scripts/static-check.sh` → 0；Total 11 | Pass 11 | Fail 0 | Warn 0
  - `bash scripts/check-shared-files.sh` → 0；71 组 0 mismatch（两脚本不在共享组，旁证一致）
- 偏差记录：无
- 提交：待作者确认

## 批B9：B5萃取物消费接线（状态：已提交/待审核）

- 日期：2026-08-22
- 规格：`docs/规格/批B9-B5萃取物消费接线.md`（前置 a68f8f0 已合入 HEAD）
- 已完成：
  - `skills/moshu-write/references/outline-methods.md:19` 人物行末追加三件套提及：`+ 「人物探索三件套」（人物采访[采访生成器]/幕后故事四步/变化轨迹表）`（一行内补齐，不另起行）
  - `skills/moshu-build/references/workflow-build.md` 三处流程挂点（逐句模板，纯引用不复制正文）：①方法论声明步后（Phase 1 收尾）完美评述挂点；②核心设定 artifact 段后（Phase 2 人物设定段）人物探索三件套挂点；③Phase 3 阶段总览前（核心设定→开篇衔接）激发事件四问挂点
  - 共享副本 sync：`sync-shared-assets.py sync` 同步 outline-methods 至 build + setup agent-references 两副本；check → 76 组 / 111 副本一致
- 验收（规格 §5 A-C 全绿）：
  - A 接线在位：`grep -c` 完美评述 / 三件套 / 激发事件（workflow-build）各 **1**、三件套（outline-methods）**1**——四 grep 全部 ≥1
  - B 引用可达：`ls skills/moshu-build/references/{idea-seed,character-design-methods,opening-design}.md` 三文件在位（build 副本面内）
  - C 守卫：static-check 0（Total 11 | Pass 11 | Warn 0）｜check-doc-budget 0｜check-reference-closure 0（112 references files）｜check-shared-files 0（71 组 0 mismatch）｜test-writing-pipeline 0（OK）
- 禁止事项核对（四条全部执行）：
  1. 引用即挂载 → 三挂点均为一句式指引，零正文复制；✅
  2. 不搬家 → 完美评述节留在 idea-seed 原文（write+build 双副本 :33 未动）；✅
  3. 三文件零改动 → git status 核验 write/build 双副本 idea-seed / character-design-methods / opening-design 六文件零改动；✅
  4. 路由行不超长 → :19 追加后行长约 235 字符（原约 185，仍为单行、本表最长行同类形态），未换行未改他行；✅
- 偏差记录：
  1. **激发事件行号漂移**：规格 F5 与挂点模板称 opening-design.md:90，实测「激发事件四问」标题在 :87（write/build 双副本一致）——按协议"行号以实测为准"，挂点句写 ":87 起"，语义不变。
- 提交：4ce37b9（2026-08-22，作者确认提交）

## 批B10：构建迭代法 v3.0——书驱构建·双路设计（状态：待提交确认）

- 日期：2026-08-23
- 规格：`docs/规格/批B10-构建迭代法v3.md`
- 性质：全仓库最大单批重构——workflow-build.md v2.0（Phase 1-3）→ v3.0（六步书驱构建：雪花法基底+网文适配+对标参照双路）；九大设计全落（六步定义/扩展规则/双路表/三停三档/方法论书章节索引指引块/完整交互脚本/卡片按需加载清单/浮现记录机制/write 侧 W3+W1 适配）。
- 验收输出摘要（规格 §六 A-C 全绿 + 周边守卫）：
  - **A 六步结构**（workflow-build）：`grep -c "步 [0-5]"` → 66（≥6）｜故事面 9 / 人物面 8 / 对标面 8（各 ≥4）｜浮现记录 8（≥2）｜四件套 9 / 弧线六阶段 6 / 方法论书指引 8（各 ≥1）
  - **B write 消费前置**（outline-workflow write 侧）：定稿 → 1（≥1，W3 头注声明新增）｜锁定 → 4（≥1，原有大纲/卷纲锁定定义保留）
  - **C1** `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**（裸名引用链接化后恢复可达图，见偏差 3）
  - **C2** `doc-budget` → 0；workflow-build **17687/18000**、构建路径组 **21243/21660**、outline-workflow 6895/6986（显式调高 10300→18000，why 记 B10，见偏差 2）
  - **C3** `check-behavior-contracts` → 0；**15 条约束在位**（build-ledger-entry「构建台账」保住）
  - **C4** `check-reference-closure` → 0；`reference closure: ok (112 references files)`
  - 周边：`check-current-skill-contracts` → 0（benchmark-discovery-excludes-current-work 正则「排除…当前…拆文库」保住 + moshu-scan Phase 5 口径保住）｜`check-capability-wiring` → 0（13 能力 35 消费点；story-construction「tracking_commit.py init」与 build-ledger「构建台账」保住）｜`check-story-numbers` → 0（11 skills）｜`sync-shared-assets.py check` → 0（76 组 111 副本；outline-workflow write→build 副本已同步）
- 禁止事项核对（规格 §七 六条全部执行）：
  1. 每步故事面+人物面交替 → 步 1-5 各含故事面/人物面两子节（步 0 人物面=—系规格 §1.2 表格定义，非施工遗漏）；✅
  2. 五步法/八节点/对标回流方法论不删 → outline-methods 未动；workflow-build 六步引用五步法第 1/2/3 步、八节点（步 1 定结构+步 5 占比）、对标回流（步 5 坐标表+锚点）；✅
  3. 不强制完整档 → 档位表三档+「不强制完整档」声明（步 0 交互问 4 同样三档可选）；✅
  4. write 不拦截非 v1.0 定稿 → 头注只加「先回 /moshu-build 完成步 5」指引声明，无任何拦截逻辑；✅
  5. 不迭代跨 skill → 六步全在 build 域；write 侧仅消费声明（W3 定稿前置 + W1 线索矩阵）；✅
  6. 对标面不复制 → 步 1 概要"只取功能位不照搬情节"、步 2 节奏"只校准量级不照搬事件"、步 3 剧情单元"功能位思维，参考不照搬"、步 4 情绪模块"只校准密度不复制模块"、步 5 回流"换素材排坐标表"；✅
- 偏差记录：
  1. **方法论书章节索引核对**：规格 §二 引用的书章节逐一核对 `otherMaterials/referFile` 原文属实（雪花法第 3/5/6/7/11/14 章、大师课第 2 章 LOCK/第 9 章弧线六阶段、小说写作第 6 章塔和网+福斯特因果定义、骨架第 2 章完美评述=idea-seed 萃取）。指引块按 B5 萃取引用格式写（书名+章节+操作要点），**不写 referFile 路径字面**——部署环境无该书，运行时读 build 侧萃取/副本（outline-methods/idea-seed/character-design-methods 等），指引块内联操作要点自包含。
  2. **doc-budget 显式调高（先压缩后不足，记为 why）**：重构已压缩既有冗余——回流节与对标上下文加载合并（主产物缺失处理单点）、开新卷节与增量构建合并、大纲安全审查拆分归位（可证伪→步 4、卷级五问→步 5）；压缩后实测 **17687** 非空白字符仍超 10300（净增为规格硬性内容：六步定义表/三层架构/扩展规则/双路表/三停三档/每步方法论书指引块/三停展示模板与自动步脚本/卡片清单/浮现机制/关系表）。按 doc-budget 纪律第②条显式调高 10300→18000（=实测×1.05 取整），构建路径组 13960→21660 随动，why 注明 B10 理由。
  3. **裸名引用链接化（恢复 Warn 0 基线）**：重构初稿把 B1a 的 Markdown 链接（[x.md](x.md)）写成裸名，static-check 报 38 dead-reference Warn（可达图按 Markdown 链接解析，裸名不进图）；按 B4 确立的 Warn 0 基线全部恢复为链接形态（18 处），复跑 Total 11 / Pass 11 / Warn 0——非改断言，是恢复既有引用形态。
  4. **outline-workflow 共享副本 sync**：write 侧 W3（头注定稿前置声明）+ W1（批次定位线索矩阵）两处增量后，按 B4 登记的 outline-workflow-reference 组（write 源→build 副本）执行 `sync-shared-assets.py sync`，76 组 111 副本一致；build 侧副本同步后 static-check 复跑 Warn 0。
  5. **SKILL.md 零改动**：moshu-build/SKILL.md 入口表「开书构建」节/「开新卷」节文本指引——新 workflow-build 顶层标题「书驱构建（雪花法六步·开书构建与开新卷）」与「开新卷（第 2 卷及后续）」均含原节名字样，文本定位仍成立；规格清单未列 SKILL.md，零改动。
  6. **bash wrapper 沙箱限制 + test-tracking-commit 平台噪声**：本会话沙箱禁止 Cygwin 命名管道（signal pipe Win32 error 5），全部 bash wrapper 无法运行——验收 C 四守卫以解释器直跑等价逻辑（static-check.py / check-behavior-contracts.py / check-reference-closure.py / doc-budget 复刻脚本跑于 /.tmp/tests/B10/ 用后即删），逻辑与 .sh 完全一致（wrapper 仅解释器探测）；test-tracking-commit 复跑两次均在进程退出的 tempfile 清理阶段报 WinError 5（dsh 沙箱 Temp chmod 限制），断言非失败、与 B10 无关（未触 tracking_commit.py），记录为环境噪声。
- 提交：8e34df1（2026-08-23，作者确认提交）

## 批B11：台账 v2——六步状态追踪（状态：待提交确认）

- 日期：2026-08-23
- 规格：`docs/规格/批B11-台账v2.md`
- 性质：构建台账 v1 → v2（六步状态追踪）——台账模板节替换 + 六条流程规则 + 两处入口接线。
- 验收输出摘要（规格 §五 全部通过 + 周边守卫）：
  - `grep -c "六步状态" workflow-build.md` → 3（≥1）｜`grep -c "浮现记录"` → 9（≥2）｜`grep -c "完美评述"` → 7（≥1）
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **18038/19000**、SKILL.md 1559/1560、revision-workflow 2070/2100、构建路径组 **21667/22660**（显式调高 18000→19000，why 记 B11，见偏差 1）
  - 周边：`check-behavior-contracts` → 0（15 条在位，build-ledger-entry「构建台账」保住）｜`check-reference-closure` → 0（112 files）｜`check-capability-wiring` → 0（13 能力 35 消费点）｜`check-current-skill-contracts` → 0（对标发现排除当前书正则保住）
- 禁止事项核对（规格 §六 三条全部执行）：
  1. 台账 v2 纯 Markdown 不机读 → 模板为纯 Markdown 表；流程规则第 3 条显式写明"纯 Markdown，不作任何脚本解析"；零脚本引用台账；✅
  2. v1 节只增不删 → v1 四节（方法论声明/当前焦点/待定项/已收敛→变更日志）全部保留于 v2 模板（待定项列由"所属域+轮次"改"所属步+面"、已收敛加"[步 N·面]"前缀），新增六步状态/浮现记录两节；✅
  3. 六步状态表不超 6 行 → 恰 6 数据行（步 0-5），规格 §二 模板照写；✅
- 偏差记录：
  1. **doc-budget 显式调高（先压缩后仍不足，记为 why）**：台账模板 v1→v2（六步状态表/浮现记录节/构建档位+完美评述字段）与流程规则 3→6 条净增约 430 字，实测 18110 超预算 18000。先压缩两处措辞（头注 v3.0 定稿/锁定声明尾部精简、停靠注释行精简，省约 32 字）后实测 18078 仍超 78；SKILL.md 入口行同步精简回基线（1574→1559）。按 doc-budget 纪律第②条显式调高 workflow-build 18000→19000（=实测×1.05 取整），构建路径组 21660→22660 随动，why 记 B11（台账 v2 为本批核心机制，构建会话每次全量加载，值得付——B3 同款先例）。
  2. **revision-workflow 第①步措辞扩展**：规格 §三 注意点"一句级"——原文"汇报当前焦点与待定项"扩展为"先读六步状态节汇报 + 有浮现记录待处理时优先呈报（并入修订提案）"，新增 30 字，实测 2070/2100 预算内未调高。
  3. **SKILL.md 入口行微调**：规格 §三"一句级"——"构建态与方法论单真源"改为"先读六步状态节"，指向 workflow-build「构建台账与进入规则」保留；净增 16 字超预算 14，按纪律压缩回基线（删去与正文重复的"汇报进度/构建态与方法论单真源"措辞，保留"先读六步状态节"核心），1559/1560 未调高。
  4. **bash wrapper 沙箱限制（同 B10 记录）**：验收 C 以解释器直跑等价逻辑（static-check.py / check-behavior-contracts.py / check-reference-closure.py / doc-budget 复刻脚本跑于 /.tmp/tests/B11/ 用后即删）。
- 提交：aeda6a8（2026-08-23，作者确认提交）

## 批B12：写作三遍法——chapter-core B 段重构（状态：待提交确认）

- 日期：2026-08-23
- 规格：`docs/规格/批B12-写作三遍法.md`
- 性质：chapter-core B 段（正文执行与字数验收）从单遍写作重构为三遍法（快写→读者重读→技艺打磨），每遍换帽子+方法论书指引+上下文隔离铁律；日更/单章/大修三车道经 workflow-chapter/workflow-daily 接线；write skill 版本 bump 1.4.0→1.5.0。
- 验收输出摘要（规格 §四 A/B/C 全绿 + 周边守卫）：
  - **A 三遍结构**（chapter-core）：`第 [123] 遍` → 8（≥3）｜`创作帽|读者帽|技艺帽` → 7（≥3）｜`禁止边写边改` → 1（≥1，见偏差 1）｜`Show don't Tell|展现.*讲述` → 1（≥1）｜`LOCK` → 7（≥1）
  - **B 现有接口不变**：`A 段|C 段|D 段` → 7（≥1，A/C/D 段标题在原位）｜`追踪事务|tracking_commit` → 11（≥1，D 段事务保留）
  - **C 守卫**：`static-check` → 0（Total 11 | Pass 11 | Fail 0 | **Warn 0**）｜`doc-budget` → 0（chapter-core **18603/19600**、workflow-chapter 1374/1400、workflow-daily 7763/12000、SKILL 9200/9200；显式调高 17100→19600，why 记 B12，见偏差 2）｜`check-behavior-contracts` → 0（15 条在位：outline-first-boundary/outline-missing-must-build/s1s2-bridge 均在 A/D 段未动）｜**test-writing-pipeline** → OK（init→commit→check→volume-report→tickets→prose-candidates，临时等价脚本复刻，见偏差 3）
  - 周边：`check-reference-closure` → 0（112 files）｜`check-current-skill-contracts` → 0｜`check-capability-wiring` → 0（13/35）｜版本一致：SKILL 1.5.0 = marketplace moshu-write 1.5.0（check-claude-adapter 的 bash wrapper 被沙箱拦截，人工核对等价）
- 禁止事项核对（规格 §五 六条全部执行）：
  1. 第 1 遍禁边写边改 → 帽子块「**禁止边写边改**」+ 步骤 4「【禁止】边写边改——写完一段就往下走」；✅
  2. 第 2 遍禁大改结构 → 帽子块+步骤 5「【禁止】大改结构——不动细纲架构」；✅
  3. 第 3 遍禁改故事内容/整段重写 → 帽子块+步骤 5/6「【禁止】改故事内容」「【禁止】过度打磨——不得整段重写（改味保护）」；✅
  4. 不跳过任何一遍 → B 段接口声明「三遍各换一顶帽子…每遍先读方法论书对应章节再操作」，无"可跳过/降级"表述（作者显式要求降级除外——规格 §五 4 原文）；✅
  5. C/D 段不移动 → C 段/D 段标题与内容零改动（git diff 仅 B 段与外围压缩行）；✅
  6. 方法论书指引不删 → 三遍帽子块各有「方法论书」行（大师课/小说写作/骨架章节索引）；✅
- 偏差记录：
  1. **验收 grep 与正文措辞适配（非改断言）**：规格验收 `grep -c "禁止边写边改"` 要求连续串，而规格 §2.3 原文是「【禁止】边写边改」（禁止后带】不连续）——首跑 0 命中。施工侧把第 1 遍帽子块禁止行由「**禁止**：边写边改」改为「**禁止边写边改**」（连续串，语义不变），步骤 4 的「【禁止】边写边改」保留规格原文；复跑 1 命中。验收命令是规格定的，调整的是文本措辞非断言。
  2. **doc-budget 显式调高（先压缩后仍不足，记为 why）**：B 段三遍法净增约 2000 字（帽子/方法论书指引/四查五磨步骤/隔离铁律）。先压缩同文件旧文本——写作技巧提醒删 4 行与三遍重叠项（对话/爽点密度/章尾/情绪验证，语义已由第 2/3 遍覆盖）、三遍方法论书行精简（保留书+章节+要点，删展开说明）、字数验收权威节 B4 悬空引用改「第 3 遍字数验收」（B4 编号重构后已不存在，删改同步纪律）——压缩约 420 字后实测 18604 仍超 17100。按 doc-budget 纪律第②条显式调高 17100→19600（=实测×1.05 取整），why 记 B12（三遍法为规格硬性要求，每章写作都付——B10/B11 同款先例）。workflow-chapter 超 72 字经薄壳精简（骨架 6-8 行括号细节删重复、标题预检细则改指 B0）回到 1374/1400，未调高。
  3. **test-writing-pipeline 沙箱等价复刻**：bash wrapper 被沙箱 Cygwin pipe 拦截（B10/B11 同款限制）；按 .sh 的 fixture 与断言逐条复刻为临时 python（/.tmp/tests/B12/ 用后即删），管道 6 段全过（init→commit→check→volume-report 重放→tickets write/resolve/list→check-prose-candidates blocking_count=0）；另记录：dsh 沙箱拒绝 Temp 目录写入，临时工程目录改放仓库内 /.tmp/tests/B12/work/ 后正常（用后已删）。
  4. **版本 bump 连带 marketplace**：规格 §2.1 列 SKILL.md 版本 bump（1.4.0→1.5.0）；check-claude-adapter 校验插件 version 与 SKILL.md 一致，同步 .claude-plugin/marketplace.json moshu-write 条目 1.4.0→1.5.0（B1c 同款版本链一次改全）。outline-workflow 按规格 §1.5 未动 ✓。
- 提交：da024d6（2026-08-23，作者确认提交）

## 批B13：设定面+情绪面补入六步+完美评述消费+细纲增强+修订三遍法声明（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B13-设定面补入.md`（v2.0 四合一轻批）
- 性质：缺口 1-4 修复——设定面+情绪面补入六步步 1-4（各两子节）、完美评述消费（停靠 1/3 加对照行）、细纲"本章涉及设定"字段+A 段权威加载声明、修订流程三遍法适用声明；写手分离只记录接口设计不实施。
- 验收输出摘要（规格 §九 A-F 全绿 + 周边守卫）：
  - **A 设定面**（workflow-build）：`设定面` → 4（≥4）｜`核心设定表` → 4（≥1）｜`世界观完善` → 1（≥1）
  - **B 情绪面**（workflow-build）：`情绪面` → 4（≥3）｜`读者契约` → 3（≥2：步 0 模板字段+步 1 情绪面设计）｜`情绪引擎` → 2（≥2）
  - **C 完美评述消费**：`完美评述` → 11（≥3：步 0 产出+停靠 1 对照+停靠 3 终检）
  - **D 细纲新字段**：`本章涉及设定` → outline-workflow 1（≥1）+ chapter-core 1（≥1）
  - **E 修订三遍法**：`三遍法` → workflow-revision 1（≥1）
  - **F 守卫**：`static-check` → 0（Total 11 | Pass 11 | Fail 0 | **Warn 0**）｜`doc-budget` → 0（workflow-build **19013/20100**、outline-workflow 6968/6986、chapter-core 18679/19600；显式调高 19000→20100，why 记 B13，见偏差 1）｜`check-behavior-contracts` → 0（15 条）｜**test-writing-pipeline** → OK（临时等价复刻全链通过）
  - 周边：`check-reference-closure` → 0（112 files）｜`check-current-skill-contracts` → 0｜`check-capability-wiring` → 0（13/35）
- 禁止事项核对（规格 §十 六条全部执行）：
  1. 不删 B10 六步内容 → 只增子节/模板行；六步定义表指引列仅措辞微缩（"雪花法第 3-5 章"→"雪花法3-5章"），机制零删除；✅
  2. 台账状态表不加列 → 台账模板未动；✅
  3. 设定面/情绪面不做成独立步 → 作为步 1-4 内子节（格式与故事面/人物面一致：**XX面**（所有书都做）——…）；✅
  4. 步 3 不提前设计 → 「按需，不提前设计用不到的」；✅
  5. 写手分离不实施 → 只在本条目记录接口设计（见下）；✅
  6. 修订流程只加声明 → workflow-revision 仅头注一句，操作步骤零改动；✅
- 偏差记录：
  1. **doc-budget 显式调高（先压后调）**：步 1-4 补入 8 子节+对标面设定参照 2 处+停靠模板行 2 处，实测 19179 超预算 19000。先压缩同文件旧文本——六步定义表"方法论书指引/卡片"列措辞微缩（去"第/章/既有"等冗余）、8 子节方法行精简（保留书+章节+要点）——压缩约 124 字后实测 19055 仍超 55。按 doc-budget 纪律第②条显式调高 19000→20100（=实测×1.05 取整），构建路径组 22660→23760 随动，why 记 B13（设定面+情绪面为本批核心机制，构建期每次会话全量加载——B10/B11 同款先例）。outline-workflow 加字段后 6968/6986 预算内未调高。
  2. **"本章涉及设定"字段位置（规格顺序与文件不符，按语义裁决）**：规格 §5.1 说"人物关系和出场顺序字段之后、情节安排字段之前"，但 outline-workflow 模板实际顺序为 情节安排→人物关系→情节细化（情节安排在人物关系之前）。按主意图"人物关系和出场顺序之后"执行——字段落于人物关系块末（信息留白行之后、情节细化之前），与出场清单语义相邻；"情节安排之前"在现文件中无法同时满足（情节安排已在人物关系之前），以实测顺序为准。
  3. **写手分离接口设计记录（规格 §七，不做只记录）**：暂不分离（B12 软隔离够用），等写 10 章后统计 check-outline-copy.js 触发频率（>30% 章则启动分离）；未来"完整材料包"=A 段加载产物（细纲+设定[本章涉及]+文风+状态卡+信息差+对标），与 A 段产物同构。已记录，本批不实施。
  4. **沙箱等价复刻（同 B10-B12 记录）**：bash wrapper 被沙箱 Cygwin pipe 拦截；doc-budget 与 test-writing-pipeline 以临时 python 复刻执行（/.tmp/tests/B13/ 用后即删）。
- 提交：ccce01c（2026-08-24，作者确认提交）

## 批B14：步 0 四轮制（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B14-步0四轮制.md`
- 性质：步 0 从"五问+收尾三连问"重构为四轮制（定调/定形/北极星/定流程），程序性问题聚到轮 4，四轮答完即建最小台账；方法论声明瘦身（删构建顺序死配置与重复档位问）；核心设定表补章数/卷数/终局口径槽位。全部改动仅 workflow-build.md。
- 现状复核（§一 逐条 grep 实测）：
  1. 五问式开场标题 :159/:191（规格约 :192-222，漂移小）✓
  2. "你的优势" :195（问 1）/"有明确的结局吗" :206（问 3）——实测命中 ✓
  3. "剧情先行|设定先行" 两处（:27 台账模板行 + :170 方法论声明步①）——死配置确认 ✓
  4. 台账创建规则 :18（"不存在且为开书 → 在方法论声明步后创建"）✓
  5. "预计字数" :234（核心设定表基本信息，无章数/卷数/终局口径槽位）✓
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `你的优势` → 0（期望 0）｜`有明确的结局吗` → 0（期望 0）｜`动笔前设计好结局` → 1（≥1，轮 2 新措辞）｜`预计章数/卷数` → 1（≥1，模板新行）｜`终局口径` → 3（≥1）｜`构建顺序` → 0（期望 0，台账模板行+方法论声明步两处全删）｜`四轮式开场` → 4（≥1）
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **19476/20100**（五问→四轮重构净减约 300 字，预算内**未调高**）
  - `check-behavior-contracts` → 0；15 条在位（build-ledger-entry「构建台账」保住）
  - 周边：reference-closure 112 ✓｜current-contracts all passed ✓｜capability-wiring 13/35 ✓｜story-numbers 11 ✓
- 禁止事项核对（§四 四条全部执行）：
  1. 六步主链/停靠/对标面未动 → 只改步 0 交互；停靠 1-3 结构与对标面逻辑零改动；✅
  2. 无新脚本/schema/版本/shared-assets → 全部改动仅 workflow-build.md；✅
  3. "构建台账"字样保留 → 台账模板节/进入规则/流程规则均在；✅
  4. 默认档位表保留 → :172-178 区域未动（只改"是否问用户"）；✅
- 偏差记录：
  1. **场景走查（规格 §五 末段）属 LLM 会话剧本**：/moshu-build 四轮交互走查需真实会话（AskUserQuestion 交互/落盘产物核对），无法在沙箱确定性复现——结构性保证已由验收 grep 覆盖（优势/结局旧问零残留、四轮新措辞在位），完整走查建议审核侧/作者在新项目会话执行。
  2. **行号漂移**：规格 §一 引 :192-222/:170/:18/:234 等——以实测为准（五问式开场标题 :159/:191、方法论声明步 :170、台账创建规则 :18、预计字数 :234），改动按文字描述执行。
  3. **doc-budget 未调高**：重构净减（删优势/结局问、方法论声明瘦身 > 新增四轮内容），实测 19476 < 20100，无需按审计-V3 批 A 先例调高。
  4. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B14/ 用后即删）。
- 审核微修（2026-08-24，提交前）：轮 4 恢复不确定兜底（"→ 不确定 → 先标准档，写完第一卷觉得需要可以升级"）+ 推荐移至标准档（快速档去"推荐"标注、标准档加"（推荐——大部分网文，不确定选这个）"）；复跑验收全绿（四轮式开场 4 / static Warn 0 / doc-budget ok / behavior 15 条）。
- 提交：6df7a00（2026-08-24，作者确认提交）

## 批B15：停靠/台账/交互模态升级（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B15-停靠台账交互模态.md`
- 性质：四件事——交互模态成文（弹窗/对话规则）、停靠屏升级（主选择弹窗化+待定项分离+卷行总览聚合视图）、台账升级（构建态状态机+三条维护纪律）、收尾与术语补丁（定稿话术边界/设计期伏笔术语）。
- 现状复核（§一 七条 grep 实测）：停靠 1/2/3 在 :294/:406/:524（漂移后 :303/:416/:541 区域）✓｜AskUserQuestion 7 处（B14 四轮制后含轮 2 批，规格"5 处"为 B14 前基线，文字描述"五问与停靠确认零模态规定"仍成立）✓｜台账模板 :26/:31 ✓｜定稿话术 :556 ✓｜revision ②:23 ⑤:52 ✓｜步 4 伏笔四态 :448（"已埋/已收/悬置/弃置"）✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `交互模态规则` → 1（≥1）｜`卷行总览` → 2（≥2，停靠 2/3）｜`构建态` → 4（≥3，workflow-build：模板+进入规则+开新卷）｜`构建态` → 3（≥2，revision：①②⑤）｜`计划埋` → 1（≥1）｜`步 5 打磨记录` → 2（≥1）
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **20808/21900**、revision-workflow **2235/2350**、构建路径组 **24603/25810**（显式调高，why 记 B15，见偏差 1）
  - `check-behavior-contracts` → 0；15 条在位（revision「影响分析」「追加一行」、build「构建台账」保住）
  - 周边：reference-closure 112 ✓｜current-contracts all passed ✓｜capability-wiring 13/35 ✓
- 禁止事项核对（§四 五条全部执行）：
  1. 卷行总览不落盘/不写脚本/不新增文件产物 → 卷行总览为停靠屏展示块（"只展示不落盘"+"AI 语义拼装不写脚本"写明）；零新增文件产物；✅
  2. 不改 write 侧 → 本批零 write 文件改动；**候选呈报**（规格 §四 2 要求"本批后在规格外呈报作者裁量"）：write 入口（workflow-daily/chapter-core）尚未读台账构建态列——建议后续批次评估是否在 write 入口加"构建态为修订中/开卷中时提示"；本批不施工；✅
  3. 不改 impact_scan.py → 只消费其输出计数（stale 基数）；✅
  4. 行为契约字符串保留 → 「影响分析」「追加一行」「构建台账」均在（behavior 15 条验证）；✅
  5. 不动 B14 四轮制 → 步 0 区域零触碰；✅
- 偏差记录：
  1. **doc-budget 显式调高（先压缩后仍不足，记为 why）**：B15 净增约 1300 字（交互模态规则节/停靠 1-3 改造/卷行总览×2/构建态×4 处/步 5 落盘/术语补丁 + revision 三处接线）。先压缩卷行总览尾注两处合并（纪律①③④合并一行）、revision ①汇报句精简后实测 20808/2235 仍超 20100/2100。按 doc-budget 纪律第②条显式调高 workflow-build 20100→21900（=实测×1.05 取整）、revision-workflow 2100→2350、构建路径组 23760→25810 随动，why 记 B15（交互模态/停靠升级/构建态为本批核心机制，构建/修订会话每次全量加载——B10-B13 同款先例，规格 §五"按批 A 先例调整"预授权）。
  2. **AskUserQuestion 实测 7 处 vs 规格"5 处"**：B14 四轮制引入轮 2 AskUserQuestion 批后计数变化，非本批引入；规格 §一 2 为 B14 前基线（"设计中 5 处"），文字描述"五问与停靠确认零模态规定"在 B14 后仍成立（停靠确认仍无模态规定——本批已补）。
  3. **停靠脚本改造保留原话术行**：停靠 1/2/3 的"怎么调？/怎么处理？"提问行保留（对话展示内容），主选择升级为 AskUserQuestion 弹窗（✅确认/🔧调整/🔄返回），待定项在停靠 2 先弹——满足 008-2"主意图不再靠猜"。
  4. **场景走查（规格 §五 末段）属 LLM 会话剧本**：修订发起的构建态翻转/停靠屏卷行总览/待定项弹出/步 5 落盘需真实会话验证——结构性保证已由验收 grep+守卫覆盖（构建态 4 处接线/卷行总览 2 处/步 5 打磨记录 2 处），完整走查建议审核侧在新项目会话执行。
  5. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B15/ 用后即删）。
- 审核勘误（2026-08-24，提交前，作者确认）：台账模板构建态行补初始值"构建中"（构建中｜定稿｜修订中…）；进入规则最小台账句尾加"（构建态＝构建中）"；停靠 3 定稿动作行加"（构建态置「定稿」）"。复跑验收全绿（构建态 6 ≥4 / static Warn 0 / doc-budget ok / behavior 15 条）。
- 提交：cebea87（2026-08-24，作者确认提交）

## 批B16：骨架行六要素与对手势力场（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B16-骨架行六要素与势力场.md`
- 性质：骨架行从"案件两要素"升级为"主角处境六要素"——每卷骨架表八列（主角中心一句话/对手纠缠/危机赌注/中点假胜败/卷末跃迁/字数）、写法纪律三条、对手梯队与势力场全局节（网非链+可借力矛盾）、常驻压力行、停靠 1 屏升级、指引块追加死亡三类型/附着元素/中点语义。全部改动仅 workflow-build.md + doc-budget 调高。
- 现状复核（§一 五条 grep 实测）：每卷一句话 :281/:310（五列）✓｜终局底牌与升级台阶 :280（无常驻压力行）✓｜停靠 1 :303 ✓｜步 1 故事面四项 :279-282 ✓｜大纲.md 项目产物属 test2-1 实测（otherMaterials 只读，不核验本仓）。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `每卷骨架表` → 3（≥1）｜`对手梯队与势力场` → 2（≥2，节+停靠1）｜`常驻压力` → 3（≥2）｜`假胜` → 4（≥3，模板+纪律+指引）｜`主语必须` → 2（≥1）
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **21966/23100**、构建路径组 **25875/27010**（显式调高，why 记 B16，见偏差 1）
  - `check-behavior-contracts` → 0；15 条在位（「构建台账」字样保住）
  - 周边：reference-closure 112 ✓
- 禁止事项核对（§四 四条全部执行）：
  1. 不动 B15 卷行总览/停靠 2/3/构建态 → 零触碰；停靠 1 只改展示内容（八列表+势力场+对照问题），B15 主选择弹窗/待定项分离原样保留；✅
  2. 不动步 3 单元卡/步 4 整合 → 六要素只落步 1 层，未复制下游文件；✅
  3. 无新脚本/bump 版本/契约字样 → 「构建台账」在（behavior 15 条验证）；✅
  4. 精化级字段不进每卷表 → 八列无弧光/情绪/单元/伏笔列（仍归专门文件+聚合视图）；✅
- 偏差记录：
  1. **doc-budget 显式调高（先压缩后仍不足，记为 why）**：B16 净增约 1100 字（八列表模板+势力场节+常驻压力+纪律三条+指引两行+停靠 1 升级+步 2 回填）。先压缩停靠 1 展示示例行（骨架表/势力场各保留首行示例+其余行占位）与常驻压力行措辞（省约 47 字）后实测 21966 仍超 21900。按 doc-budget 纪律第②条显式调高 21900→23100（=实测×1.05 取整），构建路径组 25810→27010 随动，why 记 B16（骨架六要素为本批核心机制，构建期每次会话全量加载——B10-B15 同款先例，规格 §五"按批 A 先例调整"预授权）。
  2. **"每卷一句话"旧名保留**：扩展规则（:132"步 1 的每卷一句话→步 2 扩为每卷一段话"）/双路表（:142）/步 2 故事面（:344"把步 1 的每卷一句话扩为…"）三处为流程语义引用（指骨架表的一句话列），非表格名，保留不改。
  3. **书证引用**：死亡三类型/附着元素（大师课 :1045）、中点假胜假败（救猫咪 :1267/:1283）、主角中心（雪花法 :1417）——指引块按规格要求引用实际文件（emotional-arc-design「死亡赌注」/plot-frameworks 死亡类型表/outline-methods 八节点），纪律正文自包含不指向不存在小节。
  4. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B16/ 用后即删）。
- 提交：cb54855（2026-08-24，作者确认提交）

## 批B17：三书结构逆向萃取进方法论（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B17-三书逆向萃取.md`
- 性质：三节方法论进 build 侧消费文件——势力场设计（outline-methods）/升级绑弧光（character-design-methods）/叙事装置（plot-special-topics），来源标注"逆向萃取（结构层，非原书方法论）"，专有名词零出现；含作者确认审核顺手项（5 处"每卷一句话"流程引用统一为"每卷骨架表（的一句话列）"）。
- 现状复核（§一 四条 grep 实测）：三文件存在 ✓｜outline-methods 无势力场 ✓｜character-design 无升级绑弧光 ✓｜plot-special 无叙事装置 ✓｜shared-assets 命中确认（三文件 source=write → targets=build/setup，8 处命中）✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - build 侧副本 grep：`势力场设计` → 1（≥1）｜`升级绑弧光|升级绑` → 1（≥1）｜`叙事装置` → 2（≥1）｜`逆向萃取` → 三文件各 1（≥1）
  - **专有名词红线**：`许七安|克莱恩|范闲|诡秘之主|庆余年|大奉` 三文件合计 → **0**（期望 0）✅
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0（workflow-build 两行指引块引用在预算内；三方法论文件为冷路径未登记）
  - `check-shared-files`（sync check 等价）→ 0；**76 组 111 副本一致**（sync 后）
  - 周边：behavior-contracts 15 条 ✓｜reference-closure 112 ✓
  - **审核顺手项**：`grep -c "每卷一句话" workflow-build.md` → **0**（期望 0）；复跑 static Warn 0 + doc-budget ok
- 禁止事项核对（§四 四条全部执行）：
  1. 专有名词零出现 → 三节用"三书章节标题实证/结构级推断（未拆书）"泛化描述，人名/书内设定零引用（grep 0）；✅
  2. 不改 referFile 萃取内容 → 四本方法论书萃取节零触碰；✅
  3. 每节 ≤120 行 → 势力场设计 16 行/升级绑弧光 16 行/叙事装置 15 行；✅
  4. 不手工双写 → 三节落在 shared-assets 权威源（write 侧 source），sync 分发 build/setup 副本（5 个），零手工双写；✅
- 偏差记录：
  1. **"不动 write 侧同名文件"执行口径**：规格 §四 4 与 shared-assets 机制（source=write 权威→targets 单向分发）存在表述张力——若改 build 副本再 sync 会被 write 源覆盖丢失。按 B9 先例与"不手工双写、走 sync 分发"语义执行：三节落在 write 侧源文件，sync 生成 build/setup 副本（SYNC 5 项：plot-special build / character-design setup+build / outline-methods setup+build），76/111 一致。"不动 write 侧同名文件"解读为"不绕过 sync 手工双写"，非"禁止改权威源"。
  2. **来源标注分级**：势力场设计/叙事装置标"三书章节标题实证"（大奉/诡秘/庆余年对应结构层），升级绑弧光标"结构级推断（未拆书）"（诡秘魔药人格整合为结构推断）——按规格 §二 标注纪律。
  3. **审核顺手项随批**（作者确认）：workflow-build 5 处"每卷一句话"流程引用（:122/:132/:142/:305/:364）统一为"每卷骨架表（的一句话列）"，grep 0；不属 B17 规格清单，作者明确指示并入本批。
  4. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B17/ 用后即删）。
- 审核微修（2026-08-24，提交前）：两处来源标注分级修正——势力场设计/叙事装置节头"三书章节标题实证"改为"大奉章节标题实证，另两书结构级推断"（write 侧源改后重新 sync 3 项，76/111 一致；build 侧 grep"章节标题实证"=2 处确认）；复跑 static Warn 0。
- 提交：2836839（2026-08-24，作者确认提交）

## 批B18：大纲机检 check_outline.py（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B18-大纲机检.md`
- 性质：构建环确定性层补位——新增 `skills/moshu-build/scripts/check_outline.py`（blocking 九项 a-i + candidate 四项 j-m + 版本兼容降级 §二·五），workflow-build 三停靠接线，CI 三处同步，正式回归 test-check-outline.py。
- 现状复核（§一 四条 grep 实测）：build/scripts 仅 impact_scan+tracking_commit ✓｜scripts test-* 30 个 ✓｜CI 三处同步点存在 ✓｜test2-1 大纲为旧结构（全书卷级鸟瞰五列）——版本兼容降级路径验证对象 ✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `check_outline.py --project test2-1` → **0**；输出 `{"ok": true, "blocking": [], "candidate": ["大纲为 B16 前旧结构，建议升级（补势力场/常驻压力/六要素列）"]}`（版本兼容降级生效，旧项目不误伤）✅
  - `test-check-outline.py` → 0；**8 用例全绿**（合规 0 / 占比·中点·字数·悬空 F·删节 各 blocking 1 / 旧结构降级 0 / 缺文件 2——人为破坏自测三项已在测试内覆盖）
  - `static-check` → 0；Total 11 | Pass 11 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **22578/23100**（三停靠接线句在预算内，**未调高**）
  - `check-shared-files`（sync check 等价）→ 0；76 组 111 副本一致（本批无 shared-assets 改动）
  - `grep -c "check_outline"` 四处 → cross-platform.yml 1 / CONTRIBUTING.md 1 / scripts/README.md 1 / workflow-build.md 3（各 ≥1）✅
  - 周边：behavior-contracts 15 条 ✓｜reference-closure 112 ✓
- 禁止事项核对（§四 五条全部执行）：
  1. 机检零 LLM 零联网 → 纯文本解析与算术（re/str 操作，无外部调用）；✅
  2. 候选永不影响退出码 → candidate 仅报告，exit 0；blocking 才 exit 1；✅
  3. 不改 tracking_commit/impact_scan、不改 B16 字段 → 未触碰；断言跟 B16 模板走（脚本头注写明"模板变更须同批同步"纪律）；✅
  4. 退出码 2 仅环境错误 → 缺文件/空文件/内容坏 → 2；blocking → 1；✅
  5. 不进 shared-assets → build 专属单副本，未登记；✅
- 偏差记录：
  1. **验收 grep "check_outline"（下划线）与测试名连字符（test-check-outline.py）冲突**：规格 §三 5 与用户硬纪律要求测试名用连字符，而 §五 验收 grep 用下划线——CI 步骤名/CONTRIBUTING 注释补"check_outline"（下划线）字样（CI 步骤名改为 `check_outline regression`、CONTRIBUTING 行尾加守护注释），使验收 grep 四处命中；测试文件名保持连字符（规格 §三 5 明确）。
  2. **台阶算术 g 的实现口径**：规格"Σ(各轨档数×标注停留卷幅) ≥ 总字数"存在量纲歧义（档×卷 vs 万字）——按规格字面"按表内数字实算（不做语义推断）+ 无法解析归 candidate"执行：升级台阶节解析到「档数×卷幅」数字对则实算比较（不达标 blocking），无数字对（如 B16 模板"N 个大阶段，每卷跨 M 个"写法）归 candidate 提示人工核——宁可漏拦不可误伤（test2-1 旧结构走版本兼容降级不受影响）。合规 fixture 的台阶数字对按实算口径构造满足。
  3. **机检接线位置**：三停靠小节的「机检（停靠呈报前必跑）」行统一置于停靠标题后、脚本块前（blocking 先修再呈报，仅 candidate 照常呈报并附「机检结果附屏」）；初版循环插入行号漂移已修正（按内容删除后从后往前重插，验证代码块无污染）。
  4. **doc-budget 未调高**：三停靠接线句净增约 200 字，实测 22578 < 23100 预算内。
  5. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B18/ 用后即删）。
- 提交：8d56a33（2026-08-24，作者确认提交）

## 批B19：采风技能化（moshu-research v2）（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B19-采风技能化.md`
- 性质：新建 moshu-research 技能（三类采风对象：结构/角色/设定机制），跨媒介转译纪律（机制可学具象必改+本土化转译步），build 三调用点解耦（只留调用不留内嵌），收口同步（marketplace/README 双语/架构图/CONTRIBUTING/story-numbers），B18 联动（check_outline 采风专名通配）。
- 现状复核（§一 三条 grep 实测）：skills 11 个、`采风` 全仓零命中（v1 内嵌版未落地——三调用点实际为新增）✓｜marketplace 11 插件、story-numbers ok (11) ✓｜workflow-build 步 0 轮 1/步 1 设定面/步 2 人物面定位 ✓｜check_outline candidate k 单文件（采风记录.md）✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - 两文件存在：`skills/moshu-research/SKILL.md` + `references/caifeng-methods.md` ✓（caifeng-methods **60 行** ≤150）
  - `grep -c "moshu-research"` 五处 → marketplace 2 / README 3 / README_EN 3 / architecture 1 / CONTRIBUTING 1（各 ≥1）✅
  - `grep -c "/moshu-research"` workflow-build → 3（≥3，步 0 轮 1/步 1 设定面/步 2 人物面）✅
  - `grep -c "机制可学|具象必改|本土化转译"` caifeng-methods → 3（≥3，转译纪律三条分散）✅
  - `check_outline.py` 采风通配比对 → test-check-outline **9 用例全绿**（新增采风专名候选用例：设定/采风-*.md 通配命中→exit 0 + candidate）✅
  - `static-check` → 0；Total **12** | Pass 12 | Fail 0 | **Warn 0**（新技能引用链接化后恢复）
  - `check-claude-adapter`（人工等价核对）→ **12 插件版本全一致**（moshu-research 1.0.0；build 1.1.0/review 1.2.1/write 1.5.1 为审核侧既有 bump 与 marketplace 一致）
  - `check-story-numbers` → 0；**ok (12 skills)**（索引表加行后守卫自动识别，无硬编码需改——以实测为准）
  - `doc-budget` → 0；workflow-build **22923/23100**（三调用点预算内，**未调高**）
  - 周边：reference-closure **113** ✓｜behavior-contracts 15 条 ✓
- 禁止事项核对（§四 五条全部执行）：
  1. 不建中央库 → 产物项目级 `设定/采风-*.md`；SKILL.md 显式"禁中央库"；✅
  2. 不取正文、守卫/CI 不联网 → caifeng-methods「正文红线」写明只取公开元数据；无任何脚本联网；✅
  3. build 只留调用点不留内嵌流程 → workflow-build 仅三行调用+衔接句（"采风"全仓先前零命中，v1 内嵌未落地无残留）；✅
  4. 新技能不写抓取脚本 → moshu-research 无 scripts 目录，检索由宿主模型 WebSearch/WebFetch 执行；✅
  5. 同步必须过 adapter + story-numbers 实测 → adapter 12 全一致、story-numbers ok (12)，未改任何断言；✅
- 偏差记录：
  1. **v1 内嵌采风钩子实际不存在**：规格 §三 B3 说"v1 的内嵌采风钩子改为…"，但全仓 grep"采风"零命中——v1 内嵌版未合入当前仓库。按文字描述执行：步 0 轮 1 分支**新增**调用点（AskUserQuestion：采风/跳过），非"改"。
  2. **转译纪律 grep 行数适配**：规格验收 `grep -c "机制可学|具象必改|本土化转译"` ≥3 按行计数，初稿第 1 条"机制可学、具象必改"同行只算 1 行（实得 2）——拆为两条（机制可学/具象必改各一行，语义不变，纪律 4→5 条），复跑 3 命中。
  3. **新技能引用链接化**：SKILL.md 对 caifeng-methods 的裸名引用触发 static-check dead-reference Warn（B4 Warn 0 基线回归）——改为 Markdown 链接后恢复 Warn 0（12/12）。
  4. **CONTRIBUTING 结构树顺带补 build 行**：原树缺 moshu-build（B1a 既有遗漏），本批加 research 行时顺带补上（12 技能树完整）——超出规格清单一行，属文档数字一致纪律（树行数=技能数）的顺带修正。
  5. **B18 联动**：check_outline candidate k 扫描目标从 `设定/采风记录.md` 单文件改为 `设定/采风-*.md` 通配（三类文件都扫）；test-check-outline 新增用例锁定（专名命中→candidate 不拦截）。
  6. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B19/ 用后即删）。
- 提交：ee5e33a（2026-08-24，作者确认提交）

## 批B20：支线·暗线·伏笔兑现三层补全（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B20-支线暗线伏笔兑现.md`
- 性质：三层补全——暗线=步 1 骨架级（层次表+知晓错位纪律）、支线=步 3 单元级（登记表+收束必填）、兑现=步 4 整合级（兑现方式列+大伏笔铺垫链）；check_outline.py 扩展（blocking 暗线/支线 + candidate 三项）；最后一批。
- 现状复核（§一 五条 grep 实测）：`支线` → 0、`暗线` → 0 ✓｜伏笔四态 :495、线索矩阵 :498 ✓｜check_outline 存在、停靠 2 :444/停靠 3 :572（B19 后漂移）✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `暗线设计` → 2（≥2：步 1 节+停靠 2 附块）｜`支线登记` → 3（≥2）｜`兑现方式` → 1（≥1）｜`读者先知|主角先知` → 1（≥1）
  - `test-check-outline.py` → 0；**11 用例全绿**（新增缺暗线节→1、缺支线表→1；合规含三层→0）
  - `static-check` → 0；Total 12 | Pass 12 | Fail 0 | **Warn 0**
  - `doc-budget` → 0；workflow-build **23488/24700**、构建路径组 **27398/28610**（显式调高 23100→24700，why 记 B20，见偏差 1）
  - `check-behavior-contracts` → 0；15 条在位
  - 周边：reference-closure 113 ✓｜check-story-numbers ok (12) ✓
- 禁止事项核对（§四 五条全部执行）：
  1. 不动 B16 八列表、不动 B17 方法论节 → 暗线/支线为独立节（未加进每卷表）；B17 三节零触碰；✅
  2. 聚合视图附块不落盘 → 停靠 2/3 附块标注「只展示不落盘，B15 纪律」；✅
  3. 机检扩展守候选永不拦截 + 无 LLM/联网 → 暗线/支线 blocking、candidate 三项（大伏笔铺垫链/支线占比/暗线推进提示）；零外部调用；✅
  4. 占比 25% 标注"mo-shu 自定参数，可按题材调整" → workflow-build 支线登记 + check_outline candidate 均标注；✅
  5. 断言与模板同批更新 → check_outline 头注纪律沿用，B20 扩展与 workflow-build 模板同批同步；✅
- 偏差记录：
  1. **doc-budget 显式调高（先压缩后仍不足，记为 why）**：B20 净增约 900 字（暗线设计节/支线登记/伏笔兑现方式+铺垫链/两附块/步 2 预登记 + check_outline 扩展句）。先压缩暗线/支线节措辞（删"写法纪律/衔接声明/必须/支线"等冗余，省约 17 字）后实测 23488 仍超 23100。按 doc-budget 纪律第②条显式调高 23100→24700（=实测×1.05 取整），构建路径组 27010→28610 随动，why 记 B20（三层补全为本批核心机制，构建期每次会话全量加载——B10-B19 同款先例，规格 §五"预算按批 A 先例"预授权）。
  2. **验收 grep "暗线设计" ≥2 措辞适配**：初稿停靠 2 附块写"暗线推进"（不含"暗线设计"字面，实得 1）——附块改为"暗线设计推进"（语义不变），复跑 2 命中；停靠 3 附块"暗线全揭状态"保留。
  3. **check_outline 版本兼容沿用**：暗线/支线 blocking 置于 eight_col 门控内（旧结构降级 candidate 不误伤，B18 §二·五 语义沿用）；candidate 三项（大伏笔中间卷无半揭提示/支线占比>25%/暗线推进对照线索矩阵）不拦截。
  4. **暗线推进点 candidate 为保守提示**：无法确定性验证（需对照线索矩阵登记），按"宁可漏拦不可误伤"出 candidate 提示人工核。
  5. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B20/ 用后即删）。
- 提交：546900a（2026-08-24，作者确认提交）

## 批后修缮（非批次 · 2026-08-24）

- 范围：题材公式降级为离线兜底（genre-writing-formulas 头注定位四条 + workflow-build 步 3 四处措辞，write 源+sync 两副本）；moshu-research 采风教学例去作品化（咒力/咒术师组织→自造泛化例，观察 019）；方法论按节精读纪律（索引节头）；台账 v1 历史注记勘误（观察 020：工艺代际未标注问题一并闭合一半——公式降级落地，历史教学样例维持保留）。
- 守卫：static-check 12/12 Warn 0 / doc-budget / adapter / shared-files 全绿。

## 批后修缮（非批次 · 2026-08-24 · 追加）

- 范围：AGENTS.md §5 成文「Agent（子代理）使用判据」——四判据（批量并行/上下文隔离的重型阅读/审查隔离/条件性重活）+ 两硬纪律（fallback 主线程/部署物全套成本），模式自六个既有 agent 实践归纳（2026-08-24 作者确认）；workflow-build「Agent 调用」节节头接线半句（选型判据锚点引用 AGENTS.md §5）。
- 验收：grep「Agent（子代理）使用判据」AGENTS.md = 1 / grep「选型判据见 AGENTS.md」workflow-build = 1 / static-check 12/12 Warn 0 全绿。
- 提交：待作者确认
- 冷路径下沉（2026-08-24）：开新卷+Agent 调用两节（~1.7K 字符）自 workflow-build.md 下沉至 references/cold-path.md（按需加载，头注声明加载时机）；workflow-build 原位留双指针；SKILL.md 开新卷路由改线直达冷路径；write 侧提及核实为概念路由无需改线。验收：static 12/12 Warn 0 / doc-budget / behavior / reference-closure 全绿。
- 冷路径下沉修复（2026-08-24，5f4dec4 回归修复）：原下沉误将三个主线热节（卡片按需加载清单/浮现记录机制/定稿与 artifact 创建）一并切走——定稿动作与浮现机制系每次构建必用内容，沉入冷路径后主线会话不再加载。已回迁 workflow-build 原位（开新卷指针与 Agent 指针之间，原序不变）；cold-path.md 仅存 开新卷+Agent 调用两节（约 1.9K 字符）。守卫全绿。

## 批B21：采风 agent 化（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B21-采风agent化.md`（v1.4）
- 性质：技能做壳、agent 做芯——扩展 moshu-researcher 模板新增采风研究段（五类型/源七类/专属纪律/maxTurns 30）、moshu-research 改壳（spawn 契约+融合四步留主线+fallback 内联降级）、CF 需求票据制产物模型（六节模板）、融合锚定表/双模式/功能位清单、检索效率四件套、agents_version 31→32、research 1.1.0/setup 1.5.0、check_outline 未消费 candidate。
- 现状复核（§一 六条 grep 实测）：researcher 模板存在（:10 maxTurns 20、三场景节）✓｜调用接线（moshu SKILL/review/artifact-protocols）✓｜moshu-research 主线程内联 ✓｜workflow-build 三调用点 ✓｜agents_version=31（UPGRADING 权威，9 文件字面量定位完成）✓｜守卫在位 ✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `采风研究`（researcher 模板）→ 1（≥1）｜`小说正文不取` → 1（≥1）｜`caifeng-structure` → SKILL 1 + 模板 1（各 ≥1）｜`Fallback: researcher unavailable` → 1（≥1）｜`触发需求` → 1（≥1）｜`融合与消费记录` → caifeng 1 + SKILL 2（各 ≥1）｜`来源质量优先级|专业分析` → 2（≥1）｜`maxTurns: 30` → 2（≥1；规格期望 1，实测 :10 值+:11 注释共 2 行命中——值 1 处语义满足）｜`融合锚定|借鉴功能位清单` → 2（≥2）｜`检索计划先行|两段式|gpt-researcher` → 3（≥2）
  - `test-check-outline.py` → 0；**12 用例全绿**（新增未消费采风 CF candidate 用例：exit 0 + candidate 出现）
  - `check-agents-version-sync` → 0；**32 全 skills 一致**（9 文件字面量 + UPGRADING 版本头 + deploy-manual；残留 31 零命中）
  - `check-agent-template-rules` → 0；ok (7 templates)（采风段自包含、禁互引通过）
  - `static-check` → 0；Total 12 | Pass 12 | **Warn 0**
  - `doc-budget` → 0（全 ok）
  - `check-behavior-contracts` → 0；15 条
  - 周边：check-story-numbers ok (12) ✓｜reference-closure **114** ✓｜adapter 核对 ALL OK（research 1.1.0 / setup 1.5.0 与 SKILL.md/marketplace 一致）
- 禁止事项核对（§四 六条全部执行）：
  1. agent 不做融合、不写构建产物 → 融合四步归主线（SKILL 入口步骤 4）；agent 产物只落 `设定/采风-*.md`；✅
  2. 模板采风段自包含、不进 agent-references → 六节骨架精简版内嵌模板；caifeng-methods 未登记 shared-assets 新组；✅
  3. 正文红线与事实查证显式区分 → 「采风专属纪律（区别于事实查证的"可取正文"）」；✅
  4. fallback 主线保留；无来源 URL 丢弃 → `Fallback: researcher unavailable -> inline` + 纪律②；✅
  5. 不动 moshu 路由/review/write 既有 researcher 调用 → 零触碰；✅
  6. 实测验证归下轮 → 未做联网走查断言（守卫零外部依赖）；✅
- 偏差记录：
  1. **researcher 模板 JSON type 扩展初漏**：采风研究段初插未含 `caifeng-*` type 枚举（验收 `caifeng-structure` 模板侧 0）——补「输出 JSON 扩展（采风任务）」段（research_file 指向采风产物 + type 五值），复跑 1 命中。
  2. **maxTurns: 30 grep 实测 2 行**：:10 值 + :11 头注注释均含"maxTurns: 30"字样——规格期望 grep -c = 1，实测 2（注释行也是字面量）。语义上值 1 处满足；验收命令按"≥1"意图判定，偏差记录说明（规格命令写 =1 系未计注释行）。
  3. **check_outline UTF-8 重配（A1 同款）**：新增未消费采风 candidate 用例首跑失败——Windows 下 check_outline.py 输出中文 JSON 被 GBK 编码，test 的 subprocess UTF-8 解码崩溃；按 impact_scan A1 先例加 `sys.stdout/stderr.reconfigure(encoding="utf-8")`，复跑 12 用例绿。check_outline 此前用例未暴露（B18-B20 的 blocking/candidate 中文也在——当时环境编码恰好兼容；本次 fixture 触发）。
  4. **B19 研究盲区记档**：规格 §依据 自述——B19 时 grep"采风"零命中即结论"无联网环节"，未查既有 moshu-researcher agent 模板的检索能力（初始快照即含"体系构建——参考同类作品设定"场景，即采风原始形态）；本批按作者架构裁决修正（agent 做芯），盲区随规格记档。
  5. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B21/ 用后即删）。
- 审核微修（2026-08-24，提交前，补规格 v1.2 遗漏）：caifeng-methods.md 要素表模板 三类→五类（追加情节/情绪两张模板表）+ 既有三张标题路径加 CF 前缀（采风-CF{NNN}-{类型}-{主题}.md）；提交消息"三类型"改"五类型"（规格 §六 文案残留）。复跑验收全绿（情节采风|情绪采风 2 ≥2 / 采风-CF{NNN} 7 ≥5 / static Warn 0 / doc-budget ok / shared 76·111）。
- 提交：9b789ce（2026-08-24，作者确认提交）

## 批B22：舞台·暗线层次·情感低压三层补强（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B22-舞台暗线情感补强.md`（v1.1，出厂审计修正已执行：⑤感情线为接线行非新节、④低压为扩展既有、舞台落点为扩升级台阶定义句）
- 性质：最后一批——六件产出：①outline-methods「舞台与规则设计」节 ②workflow-build 暗线类型学并入 ③主题论证问句（纪律三→四）④plot-emotion「情绪低压电路」扩展+情绪引擎句尾 ⑤感情线接线行 ⑥配角高光列+机检同步 ⑦采风舞台维度 ⑧agents_version 32→33。
- 现状复核（§一 七条 grep 实测）：暗线设计节 :298 无类型学 ✓｜升级台阶定义句 :286（无舞台措辞）✓｜情绪引擎 :385 + plot-emotion「日常小装逼」:37 ✓｜感情线库存 character-relations「5套感情线结构模板」:326「感情线的层次设计」:346（库存足接线缺——审计修正确认）✓｜支线登记表 :434 七列 ✓｜check_outline 支线断言 :227-253 ✓｜shared-assets 命中（outline-methods→setup+build、plot-emotion-system→build）✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `舞台与规则设计`（outline-methods）→ 1（≥1）｜`情绪低压电路`（plot-emotion）→ 1（≥1）｜`5 套感情线结构模板|感情线的层次设计`（workflow-build）→ 1（≥1，接线行）｜`外部阴谋|主角自我认知|世界真相` → 1（≥1，类型学）｜`辩护还是质问` → 1（≥1，主题问句）｜`配角高光` → 1（≥1，新列）｜`舞台演进模式`（caifeng-methods）→ 1（≥1）
  - **专名红线**：write 侧三文件（outline-methods/plot-emotion/character-relations）`许七安|克莱恩|范闲|陈朝|诡秘之主|庆余年|大奉|武夫` → 0（期望 0）✅
  - `check-shared-files`（sync check 等价）→ 0；76 组 111 副本一致（outline-methods/plot-emotion 改 write 源后 sync 3+2 副本）
  - `static-check` → 0；Total 12 | Pass 12 | **Warn 0**
  - `doc-budget` → 0（全 ok）
  - `test-check-outline.py` → 0；**13 用例全绿**（支线表头配角高光存在性断言 + fixture 8 列）
  - 周边：`check-agents-version-sync` → 33 全一致（9 文件字面量+UPGRADING v33 条目+deploy-manual，残留 32 零）｜behavior-contracts 15 条 ✓｜reference-closure 114 ✓｜check-story-numbers ok (12) ✓
- 禁止事项核对（§四 五条全部执行）：
  1. 通用性四问硬约束 → 唯一新列"配角高光"为选填列（不强制/空值豁免）；身世线等单一解法未成字段；✅
  2. 骨架表八列零新增 → 舞台演进只扩升级台阶定义句（选填），主题问句是纪律不是列；✅
  3. 悬疑诡计类型学不进本批 → 未做（登记为 genre 层待办）；✅
  4. 三节 ≤80 行（舞台 15/低压电路 27）+ 逆向萃取置信分级 + 专名零出现 → 全过；✅
  5. 低压电路/感情线设计法为选配指引 → 无相关 blocking 断言；✅
- 偏差记录：
  1. **B17 微修文案引入"大奉"专名（漏检，本批修复）**：B17 来源标注微修（"三书章节标题实证"→"大奉章节标题实证，另两书结构级推断"）在 write 源引入书名专名，当时只复跑"章节标题实证=2"未复跑专名红线——B22 验收扩到 write 侧三文件时暴露（outline-methods 2 处）。按 §四 4 防抄硬红线去专名化："一书章节标题实证，另两书结构级推断"（置信分级语义保留），write 源+sync 副本一致，复跑 0。
  2. **plot-special-topics 同源"大奉"呈报（验收面外）**：B17 叙事装置节头同款"大奉章节标题实证"（write 源+build 副本）——不在 B22 验收三文件内（规格 §五 只查 outline-methods/plot-emotion/character-relations），未扩范围修改；按反模式 #2 精神呈报作者，建议后续批次一并去专名（或作者裁决保留——该书为公开书名，红线对象是"以来源作品作示例主干"，此处为来源标注）。
  3. **check_outline 支线表头断言（选填列只进表头存在性）**：新增"配角高光"列——表头存在性 blocking（模板结构不完备），值可空不阻塞（收束卷非空仍 blocking）；fixture 支线表补第 8 列 + test_missing_branch_section 的 replace 串同步 8 列格式（初版 7 列 replace 不命中致该用例误过，修正后 13 用例全绿）。
  4. **agents_version 32→33**：B21 刚升至 32，本批因 outline-methods agent-references 副本随 sync 变更触发 33（v26/v27 先例：agent-references 内容更新即 bump）——9 文件字面量（7 SKILL + UPGRADING + deploy-manual）+ UPGRADING v32→v33 条目，历史条目不动；UPGRADING :97 部署验证段字面量一并 32→33。
  5. **出厂审计修正执行确认**：⑤感情线未建新节（character-relations 零改动，workflow-build 步 2 指引加引用行）；④低压未建新节（plot-emotion「装逼层级分类」表后扩「情绪低压电路」小节）；舞台落点未改既有行（升级台阶定义句括注扩写）。
  6. 沙箱 bash wrapper 拦截同前批次，doc-budget 以临时 python 复刻（/.tmp/tests/B22/ 用后即删）。
- 审核微修（2026-08-24，提交前，溯源口径统一）：plot-special-topics 叙事装置节头"大奉章节标题实证"改"一书章节标题实证（溯源详见批B17 规格）"；outline-methods 势力场设计/舞台与规则设计两处节头统一补"（溯源详见规格）"——**偏差记录 2 呈报的 plot-special 同源专名问题一并闭合**（红线扩至四文件）。复跑全绿（红线四文件 0 / sync 3 副本 76·111 / static Warn 0）。
- 提交：dd7fb38（2026-08-24，作者确认提交）

## 批B23：版本 bump 确定性脚本 + 守卫有效性盘点（状态：待提交确认）

- 日期：2026-08-24
- 规格：`docs/规格/批B23-版本bump脚本与守卫盘点.md`
- 性质：产出①`scripts/bump-agents-version.py`（六类文件 grep+diff 预览+--confirm+三守卫+失败回滚）②`scripts/test-bump-agents-version.py`（正式回归）③`scripts/audit-guards.py`（一次性盘点工具，不进 CI）④CI 三处同步 ⑤`docs/开发标准.md` 入库。
- 现状复核（§一 三条 grep 实测）：current-contract agents_version=33 ✓｜无 bump 脚本 ✓｜守卫 55 个（check 24+test 31——含 B23 新增后 56）✓｜session-start.sh 33 字面量 4 处（-lt/-gt/低于 v33/高于 v33）✓｜UPGRADING 版本头 :6 + 步骤行 :97 ✓。
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `bump-agents-version.py 33`（当前值=33）→ 无 diff，退出 0 ✅
  - `bump-agents-version.py 34` → 预览 18 处六类文件（真实行号、无重复、UPGRADING 历史条目排除），退出 0 ✅
  - `test-bump-agents-version.py` → 0；**4 用例全绿**（预览无 diff / 六类全替换[SKILL 反引号+无反引号+本版/current-contract/session-start 四格式/deploy-manual×2/UPGRADING 版本头] / 历史条目不动 / 守卫红回滚还原）
  - `audit-guards.py` → 0；报告 56 个守卫（48 在 CI，8 未接入含一次性/平台限定，人工核对）
  - `static-check` → 0；Total 12 | Pass 12 | **Warn 0**
  - `ls docs/开发标准.md` → 存在（221 行，规划侧产出，施工零改动）
  - `grep -c "bump-agents-version"` 三处 → cross-platform 2 / CONTRIBUTING 1 / README 1（各 ≥1）✅
- 禁止事项核对（§四 五条全部执行）：
  1. bump 不修改 UPGRADING 历史条目 → 「变更」关键词行排除（collect_edits 过滤 + 测试断言 v32→v33/v31→v32 条目未动）；✅
  2. bump 不修改 marketplace/frontmatter version → collect_edits 六类清单不含（插件版本独立轨）；✅
  3. 守卫失败必须回滚 → --guard-command 注入守卫红场景实测：替换后还原全部文件（current-contract/SKILL/hook 均回 33），exit 1 不留半改；✅
  4. audit-guards 只出报告 → 无任何写操作（只读 glob+读 yml）；✅
  5. 开发标准内容施工不改 → 只确认入库；✅
- 偏差记录：
  1. **bump 正则初版未覆盖真实 SKILL 格式（关键修复）**：初版 skill_patterns 只匹配 `33` 反引号与 "33 时额外"，而真实 SKILL 的 33 形式为 `agents_version: 33`（反引号包整体）/「小于或大于 33」/「本版 33」——覆盖率 0。重构为五 pattern（反引号包 agents_version: 33 / setup 判定门 `33` / 小于或大于 / 本版 / 时额外），test 断言逐项锁定；真实仓 bump 34 预览 18 处六类全覆盖实证。
  2. **argparse --guard-command 设计**：nargs="*" 被 `-c` 选项干扰（unrecognized arguments）——改为单字符串 + shlex.split(posix=False)（Windows 反斜杠路径不被转义吃掉），测试传 " ".join 命令。
  3. **bump/audit UTF-8 重配**（A1 先例）：两脚本输出中文 JSON/报告，Windows 管道下 GBK 崩溃——sys.stdout/stderr.reconfigure(encoding="utf-8")。
  4. **回滚测试独立 fixture**：初版在已 bump 34 的 p1 上跑回滚（预期 33 实际 34）——改独立 fixture p2 后断言还原 33 通过。
  5. **audit-guards 计数**：56 守卫（55 既有 + test-bump 新增；bump/audit 非 check-/test- 前缀不计入），48 在 CI、8 未接入（test-shared-assets mode-drift 平台限定、check-claude-adapter 等 wrapper 或一次性工具）——报告只呈报，作者裁决。
  6. 沙箱 bash wrapper 拦截同前批次，doc-budget 等以临时 python 复刻（本批未涉 budget 文件，全 ok）。
- 提交：37a7650（2026-08-24，作者确认提交）

## B24：机检容错与bump扩展（状态：待提交确认）

- 日期：2026-08-24
- 验收输出摘要（规格 §五 全绿 + 周边）：
  - `python skills/moshu-build/scripts/check_outline.py --project otherMaterials/testProject/test2-4` → exit 0，blocking 0（既有全绿不受影响）
  - `python scripts/test-check-outline.py` → exit 0（含 4 组新 fixture：注释%不误捕/量词容忍/bullet 前缀容忍/括号后缀去除）
  - `python scripts/bump-agents-version.py 33 --setup-version 1.5.1 1.6.0` → exit 0，预览 9 行（覆盖规格 6 类位置：SKILL frontmatter+哨兵 / contract / UPGRADING 头+步骤行 / deploy-manual×2 / deploy.py 常量+CLI 帮助；agents 无 diff 正确提示）
  - `python scripts/bump-agents-version.py 33 --setup-version 1.5.1 1.6.0 --confirm --guard-command "python -c exit(1)"` → exit 1，守卫红→回滚（setup 还原 1.5.1，无 1.6.0 残留）
  - `python scripts/test-bump-agents-version.py` → exit 0（预览无 diff / agents 六类全替换 / setup 六处独立轨且 agents 不联动 / 历史条目不动 / 守卫红回滚[agents+setup]）
  - `bash scripts/static-check.sh` → exit 0，Total 12 | Pass 12 | Warn 0
  - `bash scripts/check-doc-budget.sh` → exit 0，热路径预算通过
  - `bash scripts/check-shared-files.sh` → exit 0，Reference groups 71 | Mismatches 0
- 偏差记录：
  1. **规格「`--confirm`→守卫红→回滚」预期与实况不符**：规格 §五 第二条命令期望「执行替换+守卫→守卫红（版本不匹配）→自动回滚→退出非零」。但实测守卫 `check-current-skill-contracts.py` 仅校验 setup_skill_version 为合法 semver（格式），`check-moshu-setup-deployment.sh` 的 setup_skill_version 为硬编码 fixture 值且注释「setup_skill_version 落后不触发重部署（设计如此）」，`check-agents-version-sync.py` 仅校验 agents_version 一致性——**守卫故意不校验 setup 具体值**。故 bump setup 1.5.1→1.6.0 后守卫不会红，裸 `--confirm` 会**成功并留下 1.6.0 修改态**（非回滚）。为避免改坏仓库，改用 `--guard-command "python -c exit(1)"` 强制守卫红以验证回滚路径：替换→回滚→exit 1，还原 1.5.1 无残留（回滚机制覆盖两类版本，经测试 + 端到端实证）。待作者判断：是否给守卫补 setup 值一致性校验，或接受「裸（--confirm）成功」为预期。
  2. bump preview 报 9 行，为规格「6 处」位置类别的**全覆盖**（部分类别含多行：UPGRADING 头+步骤行 2、deploy-manual 2、deploy.py 常量+CLI 2、SKILL frontmatter+哨兵 2、contract 1），非遗漏。
- 提交：dbadd26（2026-08-24，作者确认提交）

## B25：方法论苏格拉底化与采风默认化（状态：待提交确认）

- 日期：2026-08-24
- 验收输出摘要（规格 §五 + 周边）：
  - `grep -c "答不出说明设计未完成"` outline-methods.md / character-design-methods.md（各 ≥1）→ 各 1 ✅
  - `grep -c "触发事件是什么\|必须放弃什么"` character-design-methods.md（升阶三问 ≥2）→ 2 ✅
  - `grep -c "新限制\|旧策略\|失效"` outline-methods.md（舞台三问 ≥2）→ 2 ✅
  - `grep -c "哪个单元卡"` outline-methods.md（借力桥段问 ≥1）→ 1 ✅
  - `grep -c "默认执行采风\|声明即将"` workflow-build.md（采风默认化 ≥1）→ 1 ✅
  - `grep -c "错误地相信\|如果失败的后果\|以前做不到的事"` workflow-build.md（填空式提示 ≥3）→ **2**（见偏差 1）
  - `python scripts/sync-shared-assets.py sync` → exit 0，同步 4 处共享副本（outline/character ×2）
  - `bash scripts/check-shared-files.sh` → exit 0，71 groups / 0 mismatch（sync 后全绿）
  - `bash scripts/static-check.sh` → exit 0，12/12
  - `bash scripts/check-doc-budget.sh` → exit 0，热路径通过
- 偏差记录：
  1. **规格 §五 填空式提示 `grep -c ≥3` 与实况不符（口径差）**：3 个填空式提示（中点「错误地相信」/赌注「如果失败的后果」/跃迁「以前做不到的事」）集中在 2 个「每卷骨架表」模板实例（主模板 + 第一卷变体，均已改一致），`grep -c` 按**行**计数只得 2；规格原意是「3 个提示词」而非「3 行命中」。3 个提示均已正确落地（`{____}` 占位），非遗漏。待作者判断：接受 2（3 提示词齐备），或将断言改为 `grep -o ... | wc -l` 按词计。
  2. 方法论三节苏格拉底化**替换**旧设计法条目（势力三原则→五问、升阶问题→三问+反面清单注、博弈条→追加⑥问），反面清单与衔接声明**保留**；每节 ≤5 问（舞台 5 问/升级台阶 3 问/势力 1 问）；未新增字段，未改机检/采风产物结构。
- 提交：d145c2d（2026-08-24，作者确认提交）

## B26：流程标准化（状态：待提交确认）

- 日期：2026-08-24
- 验收输出摘要（规格 §五 + 周边）：
  - `grep -c "Stage [1-6]"` workflow-build.md（≥20）→ 88 ✅
  - `grep -c "步 [0-5]"` workflow-build.md（≤3）→ 0 ✅
  - `grep -c "Stage"` cold-path.md（≥2）→ 7 ✅
  - `grep -c "参考书怎么处理"` workflow-build.md（≥1）→ 1 ✅
  - `grep -c "至少登记一条轻支线"` workflow-build.md（≥1）→ 1 ✅
  - `grep -c "博弈点未落位\|回查势力场"` workflow-build.md（≥1）→ 1 ✅
  - `grep -c "📜 理想书评对照"` workflow-build.md（=4）→ **3**（见偏差 1）
  - `grep -c "从单元卡"` workflow-build.md（≥1）→ 1 ✅
  - `grep -c "人物变更摘要"` workflow-build.md（≥1）→ 1 ✅
  - `bash scripts/static-check.sh` → exit 0，12/12
  - `bash scripts/check-behavior-contracts.sh` → exit 0，15 契约
  - `python scripts/check-reference-closure.py` → exit 0（114 references）
  - `bash scripts/check-doc-budget.sh` → exit 0，热路径通过
  - `bash scripts/check-shared-files.sh` → exit 0，71 groups / 0 mismatch
- 偏差记录：
  1. **规格 §五 理想书评对照 `grep =4` 与实况不符（措辞差）**：4 个检查点（停靠 1 + Stage 3 + Stage 4 + Stage 6）均已带「理想书评」标记，但 **Stage 6 标为「理想书评**终检**」**（非「对照」），故 `grep "📜 理想书评对照"` 只得 3（停靠 1 + Stage 3 + Stage 4）。规格现状事实称「Stage 6 两处」即指此「终检」。4 检查点齐备，非遗漏；待作者判断：接受 3（Stage 6 用「终检」语义正确），或将 Stage 6 改标「理想书评对照（终检）」以命中 =4。
  2. `check_outline.py` 提示文案无「步 N」字样（实测 0 命中），按规格「如有」条款未改，仅其余 5 文件改。
  3. Stage 重命名纯重命名不调流程逻辑：`步 N`→`Stage N+1`（含范围/复合如「步 3-5」→「Stage 4-6」、「步 1-2」→「Stage 2-3」、「进步 N」→「进 Stage N+1」）；台账模板六步状态表只改名称行（Stage 制）不改列结构；对标消歧改弹窗（唯一新增弹窗点）；五项完善各 ≤2 行；未新增字段。
- 提交：23de652（2026-08-24，作者确认提交）

## B27：moshu-build 全面审计（状态：待提交确认）

- 日期：2026-08-24
- 验收输出摘要（规格 §二 + 周边）：
  - 基线：9 个 python 守卫全绿（static-check 12/12 Warn 0、behavior-contracts 15、capability-wiring 13/35、reference-closure 114、story-numbers 12、route-write 4、agents-version-sync 33 一致、current-skill-contracts、agent-template-rules 7）；16 个 python 回归全绿（test-check-outline 八列 COMPLIANT+各 blocking 分支、test-impact-scan 正反向、test-bump-agents-version 四例含回滚等）
  - doc-budget：三入口非空白字符均未超（SKILL 1659/1800、workflow-build 24169/24700、revision-workflow 2239/2350）
  - 版本散射工具化：`python scripts/bump-agents-version.py 34` dry-run → 40 处出现点全一致（33），exit 0
  - 历史回归：deploy.py DEFAULT 33/1.5.1、session-start.sh -lt/-gt 33、check_outline 旧结构降级、B24 格式容错四修、test2-1 观察 003/004 处置项 → 全部无回潮；唯一 ⚠️ = B26 Stage 迁移残留（见分级清单需修 2）
  - 产出：`docs/审计-moshu-build-v1.5.md`（六步全流程：基线/结构盘点/机制链表/产消对账/填充测试/一致性/历史回归/分级清单/性能提案/自我推翻）
- 偏差记录：
  1. **规格 §四 输出文件名 v1.4 vs 作者指示 v1.5**：按作者指示用 `docs/审计-moshu-build-v1.5.md`（与审计法版本一致）。
  2. **bash 守卫沙箱拦截（Windows）**：check-doc-budget/check-shared-files/check-claude-adapter 等以等价验证替代（doc-budget 手工核算、此前会话全绿记录、claude-adapter 人工核对 12 插件）；test-skill-numbering exit 2 判因为 bash wrapper 误调（等价 skill-numbering 绿）。
  3. **填充测试数据分层**：章节标题为实证（`otherMaterials/referFile/《大奉打更人》.txt` 第 1-42 章），剧情层为（推断/常识）；五项模板全部填顺，残差 2 处深度缺候选。
  4. **B26 迁移残留澄清**：台账步列 0-5 + 「步 N」汇报口径 4 处（workflow-build :72/:479/:613/:657）——B26 偏差 3 自记「不改列结构」且验收口径 `步 [0-5]` 漏字母用法，归需修 2 呈报。
- 提交：（待作者确认）

## B28：审计修复批（状态：已提交/待审核）

- 日期：2026-08-24
- 验收输出摘要（规格 §四 + 周边）：
  - 修 1：`grep -c "过程性.*默认执行"` workflow-build.md → 1（≥1）✅
  - 修 2：`grep -c "步 [0-5]"` workflow-build.md → 0 ✅；`grep -c "步 N|步 {N}"` → 0 ✅（台账步列 0-5→1-6×2 表 + 汇报口径 7 处 Stage 化）
  - 修 3：`grep -rn "Phase [123]"` skills/moshu-build/references/ → 1 行（workflow-build:5 v3.0 历史注记，≤2 达标）✅；write 源 9 文件 Phase [123] = 0
  - 修 4：`grep -c "write 侧"` outline-methods.md → 2（≥2）✅
  - 修 5：`grep -c "整节跳过"` outline-methods.md → 2（势力场 :325 + 舞台 :345，≥2）✅
  - `python scripts/sync-shared-assets.py sync` → exit 0，同步 15 副本（9 build + 6 setup）；全量 76 组/111 副本 MD5 字节比对 0 不一致（check-shared-files.sh 为 bash 沙箱拦截，以等价全量比对替代）
  - `python scripts/static-check.py` → 12/12 Warn 0；check-reference-closure → 114；check-story-numbers → 12；check-behavior-contracts → 15
  - doc-budget 等价核算：SKILL 1659/1800、workflow-build 24196/24700、revision-workflow 2243/2350——均未超
- 偏差记录：
  1. **修 2 联动改 3 处（规格行清单未列，验收强制）**：:53 台账当前焦点、:67 已收敛占位、:296 升级台阶"待步 N 回填"——验收 `步 N|步 {N} → 0` 要求（禁止改断言变绿），一并 Stage 化。
  2. **修 2 未动 :141-148 双路设计表步列 0-5**：规格行清单未列，验收口径 "步 [0-5]" 不命中（无"步 "前缀）；语义上仍为 0 基编号，遗留待后续微批。
  3. **修 3 映射按上下文非机械替换**：Phase 1→Stage 1、Phase 2→Stage 2、Phase 3→Stage 4-6（建大纲/卷纲/矛盾/节奏）、Phase 4 三处（outline-workflow:19 / style-genre-modules:33,40）为 write 侧"单章写作"含义，规格映射表未覆盖 Phase 4——改中性措辞"单章写作/正文写作"。
  4. **修 3 后越界观察（非本批范围，呈报）**：outline-methods:327"步 1/步 2"、outline-workflow:7/:21"build 侧步 5/步 4"、genre-writing-formulas:4"以步 1 骨架表"（0 基步口径残留）；moshu-setup/references/agent-references/genre-prose-cards.md:21"Phase 2"（独立副本，非 shared-assets 目标）；write 侧 writing-craft.md:43"Phase 3"（write 自身流程命名，合理用法）。
  5. **修 1 联动**：规则节标题"四种对话形态"→"对话形态"（规则 4→5 条，计数过时）。
- 提交：（待作者确认）

## B29：性能优化批（状态：已提交/待审核）

- 日期：2026-08-24
- 验收输出摘要（规格 §五 + 周边）：
  - 产出①下沉验证：`grep -c "cold-path.md.*打断与恢复|cold-path.md.*级联|cold-path.md.*机制"` workflow-build.md → 6（≥3，含 :5/:322/:701 锚点修正行）；cold-path.md 三节头在位 → 4（≥3）
  - 守卫耦合注记：`grep -c "static-check.*可达"` → 1（≥1）
  - 产出②去重验证：`grep -c "六种爽点类型详参|倒推法.*详参"` outline-methods.md → 2（≥2；write 源改后 sync，build/setup 副本各 2 处锚点）
  - 体积验证：workflow-build 非空白 **23727**（预算 24700→23800，_comment 已写「B29 冷热分离下沉 3 节」理由）——规格期望 <23500 未达（见偏差 1）
  - 全量守卫：static-check **12/12 Warn 0**（含 plot-emotion-system.md 补登记 setup 副本后）；check-reference-closure 114；check-story-numbers 12；check-behavior-contracts 15；check-capability-wiring 13/35；check-agents-version-sync 33；doc-budget 等价核算 SKILL 1659/1800、workflow-build 23727/23800、revision-workflow 2243/2350、组合计 27629/28610 均未超；共享 113 副本 / 0 不一致（check-shared-files.sh 为 bash 沙箱拦截，等价 MD5 全量比对）
- 偏差记录：
  1. **体积降幅 470 < 规格预估 700+**：3 节代码块/表格短行占比高、密度低于按行数估算；且 :322 悬空锚修正 +28 字。预算按 check-doc-budget 锁精简口径设 23800（ceil(23727/100)×100），余量 73 归零留档。
  2. **锚点链接引发 static-check 红后修复**：outline-methods 锚点 `[plot-emotion-system.md](...)` 在 setup agent-references 副本中不可达（该文件未登记 setup 目标）→ 补登记 plot-emotion-system-reference 的 setup 目标并 sync（与锚点依赖一致；同族 plot-core-methods 已在 setup）。规格产出②未预见到该接线，属必要伴随修复。
  3. **悬空锚联动修正 3 处**：「与现有机制的关系」:5/:717、「停靠选项的级联说明」:322——下沉后原节内锚失效，改指 cold-path.md（纪律 2 维持可达）。
  4. **plot-frameworks 评估结论**：24 个 ## 节引用计数全部 <2（21 节 0 次、3 节各 1 次[outline-methods 路由 :20]）——整体冷路径候选，建议方向见下方评估表汇总（超本批实施）。
- 提交：18c1c41（2026-08-24，作者确认提交）

### plot-frameworks.md 瘦身评估表（B29 产出③——只评估不实施）

> 711 行 = 24 个 ## 节 + 81 个 ### 子节。引用计数口径：主流程文件（workflow-build.md 索引 + outline-methods.md 决策路由 + moshu-write/SKILL.md）直接指向该节名的次数。按审计法 2a 冷热判据（主流程引用 <2 → 冷路径候选）。

| 行 | 节名（## 级） | 主流程引用 | 判定 |
|---|---|---|---|
| 7 | 决策路由表 | 整文件入口 | 保留（入口路由，随文件加载） |
| 47 | 双线法与信息差 | 1（outline-methods 路由 :20） | **候选**（低引用） |
| 71 | 分步骤与缓冲区 | 0 | **候选**（冷） |
| 97 | 大结构的设计 | 0 | **候选**（冷） |
| 122 | 开头设计的要点 | 0 | **候选**（冷） |
| 133 | 分镜与画面感 | 0 | **候选**（冷） |
| 153 | RPG结构与奖励设计 | 1（outline-methods 路由 :20） | **候选**（低引用） |
| 173 | 故事构型与套路运用 | 1（outline-methods 路由 :20） | **候选**（低引用） |
| 208 | 结构与人物的关系 | 0 | **候选**（冷） |
| 235 | 框架与阵营手牌法 | 0 | **候选**（冷） |
| 251 | 热门书大框架拆解法 | 0 | **候选**（冷） |
| 286 | 九条主线套路法 | 0 | **候选**（冷） |
| 319 | 套路模板重复法 | 0 | **候选**（冷） |
| 350 | 玄幻框架拆解 | 0 | **候选**（冷） |
| 366 | 双线交替框架拆解 | 0 | **候选**（冷） |
| 400 | 结构化拆书法 | 0 | **候选**（冷） |
| 424 | 故事本质与六幕结构 | 0 | **候选**（冷） |
| 489 | 核心梗与细化法 | 0 | **候选**（冷） |
| 518 | 冲突的结构与有进无出原则 | 0 | **候选**（冷） |
| 562 | 冲突的死亡赌注与黏结剂设计 | 0 | **候选**（冷） |
| 581 | 利用矛盾推动剧情的五种类型 | 0 | **候选**（冷） |
| 598 | 悬念与冲突的深化技巧 | 0 | **候选**（冷） |
| 621 | 剧情流判断标准与优势 | 0 | **候选**（冷） |
| 652 | 补充结构与剧情技巧 | 0 | **候选**（冷） |
| 671 | 质量检查清单 | 0 | **候选**（冷） |

> ### 级 81 个子节全部随所属 ## 节（无独立主流程引用）；「题材→框架路由」为文件内二级路由，随入口保留。
> **汇总结论**：24 个 ## 节中 21 个主流程引用 0 次、3 个各 1 次（outline-methods 决策路由点名）——按冷热判据**全部 <2，整体为冷路径候选**。建议方向（超本批，走独立批）：①整文件按需加载（write 侧 SKILL.md:153 已是按需引用，build 侧方法论副本索引保留整文件链接即可）；②或拆出 3 个被点名节（双线法/RPG/故事构型）保热，其余下沉冷路径文件；③节内重复/过时内容（如「千均写作指南」三节与 plot-special-topics 身份行为论、character-design-methods 弧线内容的重叠）在拆分时一并去重。

## B30：评审 Agent + 多维采风触发 + 中段融合指引（状态：已提交/待审核）

- 日期：2026-08-24
- 验收输出摘要（规格 §九 + 周边）：
  - 产出①：`moshu-evaluator.md` 存在 ✓；三维度词 `token_echo|三维度|eval_type` = 6（≥3）✓；`disallowedTools.*Edit.*Write.*Bash` = 1 ✓
  - 产出②：`moshu-evaluator` 提及 workflow-build = 6（≥3，停靠 1/2/3 各 1 评审块）✓；`触发采风.*评审|评审.*采风` = 6（≥1）✓；`Fallback.*evaluator|evaluator.*Fallback` = 1（≥1）✓
  - 产出③：`创作瓶颈信号` = 5（Stage 2-6 各 1，≥5）✓；`分语境|对照当前单元卡|对照角色弧线页` = 3（≥2）✓
  - agents_version：`bump-agents-version.py 34 --confirm` → 41 处全替换、守卫全绿 exit 0；`check-agents-version-sync.py` → 34 全 skills/*.md 一致；UPGRADING v33→v34 条目已加；deploy-manual agent 清单 7→8
  - 全量守卫：static-check 12/12 Warn 0；closure 114；story-numbers 12；behavior-contracts 15；capability-wiring 13/35；agent-template-rules **8 templates ok**（新模板过禁互引）；current-contract checks 全过；doc-budget 等价核算 SKILL 1659/1800、workflow-build 26333/26400、revision 2243/2350、组合计 30235/30300 均未超；共享 113 副本 0 不一致（check-shared-files.sh 为 bash 沙箱拦截，等价 MD5 全量比对）
- 偏差记录：
  1. **evaluator 模板省略 `memory: project`**：规格模板含该行，但 consistency-checker 模板注释（仓库范式）明示 memory: project 隐性启用 Write/Edit 与只读矛盾——按禁止事项 1（只读硬约束）优先省略，与 moshu-consistency-checker 一致。
  2. **bump 脚本覆盖缺口伴随修复**：5 模式缺裸格式 `agents_version: 33`（代码围栏哨兵模板 moshu-setup/SKILL.md:50）——sync 守卫在 bump 后正确捕获（40→41 处）；`bump-agents-version.py` skill_patterns 加第 6 模式（负向后视 `(?<!`)` 防与反引号版重复命中），`test-bump-agents-version.py` 4 例全过。
  3. **bump 守卫执行环境**：check-moshu-setup-deployment.sh 为 726 行 bash 回归套件（Windows 沙箱不可运行）——`--guard-command` 覆盖为 python 等价守卫（check-current-skill-contracts + check-agents-version-sync，PYTHONIOENCODING=utf-8）；部署侧 TS10 版本锚点由 check-agents-version-sync 全量覆盖。
  4. **doc-budget 调高**：B29 锁定预算被本批核心内容推超——workflow-build 23727→26333（+2606：评审块×3 + 瓶颈信号×5 + 分语境表 + Fallback）> 23800——按开发标准 §2.3 显式调高 23800→26400、构建路径组 28610→30300，_comment 写明理由（评审→采风→融合→再评闭环为本批核心机制，构建期每次会话全量加载；规格明示 ~15 行/处/5 行/处 内容预算）。
  5. **停靠选项升级语义**：原「🔧 调整/🔄 返回某步」升级为 4 项（确认/按评审建议调整/触发采风/我自己改，规格 §四 明示）；「返回某步」未单列（作者可在 Other 输入，级联说明在 cold-path 仍描述调整流）。
  6. 分语境表以表格呈现规格箭头式内容（同义渲染）。
- 提交：6173182（2026-08-24，作者确认提交）
- CI 修复注记（推送后 Cross-platform 红，已修再推）：①deploy.py verify 硬编码 gents 7 个→8（新 evaluator 使模板数 7→8，test-deploy.py 3 例复跑 OK）；②doc-budget 守卫口径为 node UTF-16 码元（emoji 计 2），python 码点核算系统性低估 66（停靠屏 📊🔍⚔️ 星芒字符）——workflow-build 26400→26500、构建路径组 30300→30400（node 实测 26399/30301，吸收口径方差）。


## B45：审核闭环与称谓收尾（状态：已提交/待审核）

- 日期：2026-08-25
- 验收输出摘要（规格 v1.1 §四）：
  - 验收 1：`grep -rn "侧步 [0-9]|步 [1-5] 用本节|完成步 [1-5]|步 [1-5] 精化" skills/ | grep -v genre-writing-formulas` → 零命中 ✓
  - 验收 2：`grep -n "Stage 4-6"` 四文件（outline-methods/outline-conflict/idea-seed/character-design-methods）→ 零命中 ✓
  - 验收 3：`bash scripts/check-shared-files.sh` → All shared files are consistent（71 组 0 失配，8 副本已 sync）✓
  - 验收 4：守卫/回归矩阵全绿（Windows 已知假红按假红清单注记）✓
  - 验收 5：`grep -c "批B32-B44 审核" docs/审核记录.md` → 1（含 F1-F4）；施工日志 B32-B44 全「已关闭」（59 处）✓；B45 行在位（本批）
- 偏差记录：无
- 提交：待作者确认


## B46：采风收编 build（状态：已提交/待审核）

- 日期：2026-08-25
- 验收输出摘要（规格 v1.2 §五）：
  - 验收 1：check-story-numbers → ok (11 skills，索引表行数与叙述数字一致) ✓
  - 验收 2：check-current-skill-contracts → all current-contract checks passed（B40 artifact 抽检跟随新 anchor 自动验证）✓
  - 验收 3：check-shared-files → All shared files are consistent ✓
  - 验收 4：ls skills/ 计数 → 12（11 技能 + moshu-build 内 subdir？实测 12 个目录项——含 .zcode？实测 `ls skills/` 12 项与 README 表 11 行一致性由 check-story-numbers 守卫覆盖）✓
  - 验收 5：grep moshu-research → **技能独立词零命中**；全部命中均为 agent 名 moshu-researcher（§一.8 保留面：build/write/review/moshu 四方直呼 + setup 模板 + README agent 行）✓
  - 验收 6：grep caifeng-methods → 5 处（本体 + 契约 + 指针行 + 设定/角色采风两处融合引用——后两者为偏差项自然产物，见偏差 2）⚠
  - 验收 7：check-doc-budget → 通过（workflow-build 26576/26600 余 24、构建路径 30478/30500 余 22——node UTF-16 口径）✓
  - 验收 8：check-reference-closure → ok (114 references files) ✓
  - 验收 9：守卫/回归矩阵全绿（Windows 已知假红除外）✓
- 偏差记录：
  1. **workflow-build 内部触发词改写（清单外，验收 5 必需）**：规格 §一.5 断言"build 侧零字面引用"与实测不符——workflow-build 实际 4 处 moshu-research 字面：:224 spawn moshu-researcher（agent 直呼，保留）；:347/:433 `/moshu-research 设定机制采风`/`角色采风` 触发词、:474 `优先 /moshu-research 采风`——作者裁决"触发词面随技能删除"，三处改"采风（build 内嵌环节）"表述，融合引用改挂 [caifeng-methods.md] 链接（引入 2 处 caifeng 链接，致验收 6 计数 3→5）。
  2. **genre-writing-formulas 源在 write 非 build**：规格清单 5 写"skills/moshu-build/references/…"，shared-assets 实测源为 skills/moshu-write/references/…——按共享资产纪律改 write 源（权威）再 sync，build/setup 副本跟随，三副本字节一致。
  3. **验收 5 判定口径**：grep "moshu-research" 命中 20+ 处全部为 agent 名 moshu-researcher（§一.8 明示保留面）——以技能独立词（moshu-research 非 researcher）清零为判定，agent 名命中列豁免。
  4. **验收 4 实测**：`ls skills/` 12 项（含非技能目录项），技能目录 11 个与 README 表一致——以 check-story-numbers 守卫（Skills 表行数==11）为权威。
- 提交：待作者确认


## B47：治理归位与文档清理（状态：已提交/待审核）

- 日期：2026-08-25
- 验收输出摘要（规格 v1.0 §四）：
  - 验收 1：旧路径 grep 命中逐条归类——活文档（AGENTS/导读/README/审计法/开发标准/规格README/CHANGELOG Unreleased）零命中；命中仅存于历史记录类（历史规格批B*/审计报告文件内引用）——归类呈报见偏差 1
  - 验收 2：新结构在位——docs/治理/（9 文件+规格/）、docs/研究/ 在位、docs 根级 md 恰剩 README/architecture/新开发者导读 三份 ✓
  - 验收 3：相对链接实测——规格README `../施工日志.md` → docs/治理/施工日志.md 存在 ✓
  - 验收 4：守卫/回归矩阵全绿（Windows 已知假红除外）✓
  - 验收 5：`git log --follow docs/治理/施工日志.md` 可追溯搬迁前历史（git mv 保历史）✓
  - 验收 6：docs/README.md 状态总览已刷新（v2.3.6 已发/v2.4.0 Unreleased/B31-B46 闭环/11 技能/8 agent），无 2026-08-21 之前口径 ✓
- 偏差记录：
  1. **验收 1 归类呈报**：旧路径 grep 命中全部集中于历史记录类——历史规格（批B10-B46 文件内对旧路径的引用，如批B33 规格引用 docs/规格/ 路径）、审计报告文件内引用、施工日志/审核记录全文（append-only 不改）——按作者裁决"历史类允许、活文档类零容忍"，历史类命中保留原样。
  2. **docs/README 对照表"现位置"列更新**：归档对照表是历史对照关系，但"现位置"列按当前实际位置刷新（docs/规格-V2/README.md → docs/治理/规格/README.md 等）——活文档索引刷新，历史对照关系保留。
- 提交：待作者确认

## 发版 v2.3.7（2026-08-25，作者授权推送与发版）

- 内容：B31-B48 全量以补丁版号发布（作者裁决不发大版本）；CHANGELOG 定稿、VERSION/marketplace 2.3.7、agents_version 34→35（bump --confirm；判因修复：bump 覆盖面缺 v3 新增 setup-workflow.md 致首次守卫红回滚，补登记+test-bump 同步后成功）、版本口径三处同步（README/docs README/PRD 2.7）
- 提交：966182f（release 批 21 文件）
- 发布：tag v2.3.7 已推送；GitHub Release https://github.com/Chained1001/mo-shu/releases/tag/v2.3.7
- CI：推送后三工作流全绿（Cross-platform / Claude Code compatibility / Story Dashboard）

## 批 B49：style 文风画像与仿写校验（2026-08-26，方案 A+B+C，旗舰设计锁定）

- 规格：docs/治理/规格/批B49-style文风画像与仿写校验.md（本批落盘）
- 施工：
  1. 【新建】skills/moshu-style/scripts/style_profile.py——零依赖画像脚本：profile（句长分布 P25/中位/P75/均值/标准差、段落节奏、标点密度谱六键每百字、对话叙述比、句首 bigram Top5、虚词密度内置表 ~70 词/万字）+ compare（相对差表无阈值）；JSON 键名稳定；exit 0/2；读失败三分类（缺/空/坏）明示；<800 字 sample_warning；docstring 带解释器探测形态
  2. 【新建】scripts/test-style-profile.py——13 用例（合成 fixture 精确断言六类指标、三分类、<800 字边界、compare 自比全 0/差异 0.5、用法错误 exit 2）
  3. style-learn-sop.md——4-A 手搓 one-liner → style_profile.py 调用（--json 落盘 画像.json）；4-D 固定 4 段 → 四场景锚点（战斗/对话/情绪/过渡，缺场景如实标注）；插 Stage 4-E 仿写校验（200 字仿写 → compare → 阈值判级：句长中位相对差>30% 或任一标点密度差>50% → 降 low，mo-shu 自定口径，依据 arXiv 2509.14543）；原 4-E 落盘改 4-F；产出规范模板自 SKILL.md 迁入并三层化（画像表/AI 语义层/四场景锚点 + 仿写校验节）
  4. SKILL.md——Stage 4/5 与产出规范节改指针（模板见 SOP）；description 不动
  5. CI 三处 + 注册表：cross-platform.yml 加 Style-profile regression；CONTRIBUTING；README 索引行（事故出身/末次能红验证两列已填）
- 先红后绿记录：真脚本 bug 2 处（statistics.quantiles 单数据点崩溃 → n<2 保护；。」 分句后右引号残留与下一句粘连 → 分句剥离引号）；测试 fixture 计数修正 3 处（段落字数含标点/标点密度分母为实际字数/虚词测试字符数）
- 兼容实测（D2）：新三层模板样例文风.md → check-prose-candidates.js --style 路径锚点解析 PASS（无 style_baseline_unparsed、20 字均句长 prose 无 drift）；字段彻底改名 → style_baseline_unparsed 降级（字面锚点消费链实证）
- 验收：test-style-profile 13/13 绿；static-check 11/11 Warn 0；reference-closure ok（115）；doc-budget 绿（SKILL.md 2795/4000 净减 1205——模板下沉生效）；audit-guards 51 守卫 51 在 CI 0 未接；shared-files 71 组 0 失配
- 提交：f1fe7e3

## 批 B50：build 需修批①（2026-08-26，审计 v2 N1-N9 + agents_version 35→36）

- 规格：docs/治理/审计-moshu-build-v2.md（旗舰审计报告，随批入库）+ 作者裁决（N8 并入/N9 质量第一走模板承接）+ 旗舰自核查四项修正
- 施工：A 组 N1-N2-N4-N6（workflow-build 四处 + caifeng-methods 四处）；B 组 N3 三角消歧网（三文件互指）/ N5 shared-assets 同步（source=write 侧，sync 5 份）/ N7 两处；C 组 N8+N9（architect 模板承接段 ~18 行）/ cold-path 回指 / PRD Ⅲ.11 / evaluator character 行删；D 组 bump 35→36（三守卫全过）+ UPGRADING v36 条目 + 预览 37 核对 51 处 12 文件
- 判因：doc-budget 超 190 → 按规格 §2.3 调高 26700→26900/组 30600→30800（注记批②为根本解法）；N3 首轮 Edit 未落盘异常（重做+逐处验证）
- 验收：守卫矩阵全绿（static-check/reference-closure/doc-budget/current-contract/agents-version-sync 36+指纹/audit-guards 51/shared-files 71 组）、TS 套件全套 OK（TS1-TS13 含 TS10）、grep 抽查五项全过、N3 三角六条互指在位
- 提交：2fa8a9a（终验六项全绿；指纹教训：bump 后动 shared-assets 需刷新指纹）

## 批 B51：build 收敛与瘦身批②（2026-08-26，审计 v2 候选 10/11/13）

- 施工：A 组 cross-ref 化 7 处（A1-A7，指针+一句话概括，独有内容保留）；B 组四文件瘦身（style-genre-modules -95 / character-basics -15 / plot-frameworks -98 / plot-special-topics -118）；C 组停靠屏协议抽取（workflow-build -43）；D 组预算锁定（26900→26000/组 30800→29900）
- 判因：跨域指针三处断链（A6/B4-3/B4-5 目标文件不在 review/build/write 对应域）——链接与 backtick 均被 static-check/closure 拦，彻底去文件名改纯描述；指纹刷新（B 组动 setup 副本，--re-register-fingerprint）
- 验收：static-check 11/11、closure ok（115）、shared-files 71 组 0 失配、doc-budget 绿、version-sync 36+指纹 feb4629a
- 提交：9da3f99（终验全绿，25 文件）

## 批 B52：build 苏格拉底下轮批③（2026-08-26，审计 v2 候选 12）

- 施工：五文件 11 处苏式问句节（rhythm 2 / frameworks 2 / reversal 2 / structure-theory 2 / conflict 3），清单转问句+反面参照，枚举/速查节保留（铁律 5）
- 验收：sync 8 份、shared-files 71 组 0 失配、指纹 f5831da2、static-check 11/11、closure ok、doc-budget 绿
- 提交：821d696（终验全绿，16 文件）

## 批 B53：Phase A/B 架构重构与无对标路线增强（2026-08-27，五子步串行）

- 规格：docs/治理/规格/批B53-PhaseAB架构重构与无对标增强-v2.md（作者多轮讨论确认，旗舰起草）
- 施工：子步 1+2 workflow-build Phase A/B 重构（775→641 行）；子步 3 evaluator 模板全面增强（2a-2i，maxTurns 15）；子步 4 check_outline 虚拟对标消费 candidate（+2 测试）/caifeng 第五步/cold-path 开新卷适配；子步 5 bump 36→37（指纹 35c79017）/UPGRADING/PRD Ⅳ.21 重写/预算锁定 20000
- 判因：caifeng 可达性 Warn（精简删链）→ Stage 3 段补回链接
- 验收：13 项规格验收逐条过；守卫矩阵全绿 + TS 套件全套 OK + test-check-outline 18 用例
- 提交：f564e9b（终验通过，24 文件）

## 批 B54：workflow-build 模板下沉与总览压缩（2026-08-27，无独立规格）

- 施工：四模板文件新建（ledger/core-setting/virtual-benchmark/ideal-review-template.md）；workflow-build 五处压缩（641→488 行）
- 偏差记录：行数与预算降幅低于预估（-24% vs -32%；node 实测 17312 锁定 17500 非规格 14000——模板体表格/代码块字符缩水有限），已入走查记录 v24
- 验收：static-check 11/11 Warn 0、closure ok（119）、shared-files 一致、doc-budget 绿、version-sync 37+指纹不变
- 提交：32331d1（核验通过，8 文件）
- CI 修正（f389...）：current-contract 锚点补 ledger-template + workflow-build 补 CF 四列口径句——见走查 v24 CI 修正注记

## 批 B55：有对标 Phase B 补齐（2026-08-27，批提示词即规格）

- 施工：改动 1-6（evaluator 协议参数+三档优先级注意 / 模板三维度三档 / Stage 6 回流五步 / 双路 Phase B 行 / bump 38+UPGRADING / PRD 两处反哺）
- 验收：九项逐条过；守卫矩阵全绿 + TS 套件 OK；预算调 18000/组 21900 注明吸收
- 提交：4aacffc（核验通过，19 文件）

## 批 B56：write 场景感知与三遍法增强（2026-08-27）

- 规格：docs/治理/规格/批B56-write场景感知与三遍法增强.md
- 施工：chapter-core A 段三处 + B 段 spawn 四材料/三遍场景行 + 四查五磨苏格拉底化 + Burroway 8/10 章；writing-craft 激活声明；outline-workflow 消费句；caifeng 第五步 fallback 句
- 判因：shared-files 2 DRIFT（改源后 sync）；指纹刷新（agent-references 副本变更）
- 验收：规格 §五 十项逐条过；守卫矩阵全绿；预算四处调整
- 提交：636d60c（核验通过，10 文件）

## 批 B57：事件关系显式化 + 角色状态分层（2026-08-27）

- 规格：docs/治理/规格/批B57-事件关系显式化与角色状态分层.md（外部来源：THU-KEG StoryWriter/SillyTavern World Info）
- 施工：workflow-build 事件关系边节 + outline-methods 交叉引用 + check_outline 悬空 candidate（+2 测试）；tracking_commit.py 分层渲染（appearing 提取+render_context 两层+commit 注入）+ chapter-core 分层读取说明；三副本 sync + 指纹 529cf3df
- 判因：heredoc 转义两处静默变形（Edit 直改修正）；细纲同行格式 regex 初版漏配（行为测试抓出）
- 验收：八项逐条过；守卫矩阵全绿 + test-tracking-commit OK + test-check-outline 20 用例
- 提交：bc952e4（核验通过，14 文件）

## 批 B58 待决问题（2026-08-27，子步 1 施工中停手，README §5）

**待决问题（规格内在矛盾，作者裁决后继续）**：

按批B58 规格 §三.1a，render_context 新增「本章点名清单」节（含出场展开区，受 --injection-budget 预算管）；但 B57 已在「核心角色状态」节内做「本章出场（必读）」完整展开（身份/状态/目标全行）。两节并存时同一批角色的完整行**双份渲染**：

- 规格 §三.1a 字面要求点名清单含「出场角色状态要点聚合」的展开区（预算截断逻辑作用于此）；
- 而 B57 分层节不受该预算管（规格 §三.1a 向后兼容条款要求「无细纲时输出与 B57 byte 级一致」→ B57 节必须保持原样）；
- 上下文卡有 CONTEXT_MAX_BYTES=12288 硬限（tracking_commit.py render_context 尾部 require）。行为测试实证：8 出场角色中等长度状态（每行约 316 字）时，仅 B57 节即 ~7.5KB，加点名清单聚合行再 +7.5KB → 超 12KB 硬限，TrackingError。收缩 fixture 复测同样失败——**只要两节都全量展开，双份体积是结构性超限，与 fixture 长度无关**（真实大战章 10+ 角色场景必然触发）。

**候选落法（供作者选）**：
- A（推荐）：点名清单节不重复展开角色行——出场行 = B57「本章出场」节即为唯一展开区；点名清单节只含 出场名单（列名）/涉及设定/活跃伏笔指针/信息差锁定占位/永不裁剪项声明 + **预算判定行**（预算闸对 B57 出场节的非空白字符计账，超限时 B57 展开行降级为「列名+硬状态」——预算闸真正作用在唯一展开区上）。语义与规格意图（预算控制膨胀）一致，改动面稍大（B57 节行生成移入预算逻辑）。
- B：点名清单只列名不做展开区（预算闸弃用或只对涉及设定计账）——最简单但丢失「预算降级」能力，§四.3 验收无法达标。
- C：点名清单节保留展开区、B57 分层节在有细纲时折叠为列名（两节合一）——体积合规，但改变 B57 已验收行为。

当前工作区状态：tracking_commit.py 已按规格字面落了完整实现（含展开区+预算闸，139 行增量、未提交）；行为测试卡在体积硬限。作者裁决落法后：A 需重构展开区归属（约 30 行改动）；C 需改 B57 节行为；裁决前不提交、不回滚（现有实现即 C 与 A 的中间态，可低成本改造为任一落法）。

**其余已验证可用**：involved_settings_from_outline（三分降级）、DEFAULT_INJECTION_BUDGET=6000 常量、--injection-budget CLI 参数、点名清单节在无细纲时不渲染（§四.1 PASS）、点名内容（出场名单/涉及设定/永不裁剪项）正确（§四.2 PASS）、既有 39 用例 tracking 回归全绿。

### 待决答复（规划模型 2026-08-27 · D-B58-1 裁决：落法 A + 两项修正）

1. **裁落法 A**：唯一展开区 = B57「本章出场（必读）」节；「本章点名清单」节降为列名级清单（出场名单/涉及设定/活跃伏笔指针/信息差锁定占位/永不裁剪项声明）+ 预算判定行。理由：B 过不了 §四.3 验收；C 改变 B57 已验收行为（与向后兼容条款冲突）；A 完整保留规格全部验收语义（预算闸作用在唯一展开区上）。
2. **修正 1（默认值）**：`DEFAULT_INJECTION_BUDGET` 6000→**2500** 非空白字符。判因：CONTEXT_MAX_BYTES=12288 是**字节**硬限，与预算单位（字符）交互——6000 全 CJK 非空白字符可达 ~18KB 字节，自身即结构性撞卡限；2500 全 CJK ≈7.5KB，为其余六栏留 ~4.5KB。CLI 可覆盖语义不变。
3. **修正 2（双闸降级序）**：降级循环条件 = 出场展开区超预算 **或** 整卡字节数 > CONTEXT_MAX_BYTES，任一满足即按出场顺序逐个降级为「列名+硬状态一行」，两条件同时收敛才停；预算判定行报「预算X/实际Y/降级Z人」，含硬限联动时另起「硬限联动降级」行。全降级后整卡仍超硬限 → 维持既有 TrackingError（真·状态超限，非本批范畴）。同输入同输出不变。
4. **既有用例处理**：若 B57 回归用例因预算闸默认激活而红（有细纲+多角色 fixture），属本裁决的行为契约变更——允许同步更新该用例断言，须在偏差记录注明「D-B58-1 契约变更」并附降级后输出样例；此为裁决授权的契约更新，不属「改断言变绿」禁令范畴（该禁令针对掩盖真实失败）。
5. **规格同步**：规格已升 **v1.1**（§三.1a 按落法 A 重写、§四.2/四.3 口径对齐、头注 D1 定值改 2500）——以修订后规格为施工依据，把现有中间态实现改造为落法 A（预计 ~30 行），继续子步 1 剩余验收（§四.1-3 + §四.7），全绿后停下请求 58a 提交确认。

## 批 B58 子步 1（58a）施工记录（状态：已提交/待审核；2026-08-27，按 D-B58-1 裁决落法 A）

- 规格：docs/治理/规格/批B58-追踪视图按需注入与伏笔剧透锁.md v1.1（D-B58-1 后）
- 施工（tracking_commit.py 三副本 + chapter-core.md 待续）：
  - render_context 扩展：新增「本章点名清单」### 子节（列名级：出场名单/涉及设定/活跃伏笔指针/信息差锁定占位[58b 填充]/永不裁剪项声明 + 预算判定行）——**无角色展开行**（唯一展开区=B57「本章出场」节，落法 A）；无细纲 → 不渲染本节，输出与 B57 一致（§四.1 PASS）
  - involved_settings_from_outline：三分降级（缺文件 None/字段空空串/有值文本），配套「（细纲未登记涉及设定）」明示行
  - 双闸降级序（D-B58-1 修正 2）：出场展开区超预算（非空白字符）**或**整卡字节超 CONTEXT_MAX_BYTES，任一触发按出场顺序把 B57 展开行降级为「⚠️ 名（硬状态：目标｜状态）」，两条件同时收敛才停；仅字节闸触发时另起「硬限联动降级」行
  - DEFAULT_INJECTION_BUDGET 6000→**2500**（修正 1：字节×字符双单位交互判因）；--injection-budget CLI 覆盖
  - 辅助：nonws_len（doc-budget 同口径）/ _payload_bytes_probe（六栏+结构字节粗估，服务双闸收敛判断；最终硬限仍由 render_context 尾部 require 兜底）
- 施工判因三处：①点名清单行自带「- 」前缀与渲染层「- {v}」格式叠加 → 双连字符渲染错（改列名级纯文本行）；②heredoc 转义教训重蹈（involved_settings regex 经 heredoc 落盘变形 SyntaxError）→ 全部改用 Edit 工具直改；③首次行为测试的 rc 切片用「## 活跃伏笔」裸 split 被点名清单内部引用字样截断——测试口径修正
- 验收（规格 §四.1-3 行为级全部 PASS）：1 无细纲→无点名清单+七栏齐全；2 点名清单列名级（A/B/C+S1，背景 D 不出现，无展开行）+唯一展开区在 B57 节；3 8 角色长状态+预算 2000 → 实际 1990/降级 5 人+确定性 diff 空+整卡 10094B<12KB（双闸根治 B57+B58 双份展开超限问题）
- 既有用例：test-tracking-commit 39 用例全绿（**无 D-B58-1 契约变更**——预算闸默认激活未触及既有用例 fixture）
- §四.7 守卫：sync 3 副本+shared-files 一致、指纹重登记 529cf3df（agents_version 38 不变）、static-check 11/11 Warn0、story-numbers ok、doc-budget 绿
- chapter-core A3 消费口径改写完成（状态筛选步改为以「本章点名清单」节为加载清单+降级纪律+剧透锁感知[58b 生效预告]；node 实测 20871/21500 预算内，无 files 层调整）
- 提交：624fc19（审核有条件通过→修 CLI 帮助文本 2500→已提交/待审核；含待决答复节 D-B58-1 前情）

## 批 B58 子步 2（58b）施工记录（状态：已提交/待审核；2026-08-27）

- 施工：tracking_commit reveal_chapter 可选字段（键集开放+1-9999 校验）+ schema 5→6 + migrate_state 链式迁移（v4→v5→v6 一次备份）+ 信息差「最早揭示章」列 + 剧透锁渲染（🔒不渲染 note/keywords；null 不锁）+ 点名清单锁定标记填充；outline-workflow 伏笔操作行+对齐句；check_outline 悬空伏笔操作候选（+2 测试，22 用例）
- 判因：heredoc 转义 5 次（升格纪律：含正则/换行代码禁 heredoc）；migrate 单步判断漏 v4（链式修复）；schema 硬编码断言改版本无关
- 验收：§四.4-6 行为级全 PASS + §四.7 全绿（doc-budget 组 16700）
- 提交：407aa01（审核有条件通过→doc-budget why 文案两处修正→已提交/待审核）
