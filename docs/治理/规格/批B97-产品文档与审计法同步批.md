# 批B97：产品文档与审计法同步批（治理小批——审计波前置）

> **目的**：v2.6.0 三批（B94-96）后产品文档落后两个版本段、审计 SOP 缺三条实战教训——本批把 PRD+FSD 同步到 10 技能终态、固化 B94-96 教训进审计法与盲区清单，作为**审计波（审计-四期）与瘦身批的前置**。纯文档批：零代码、零技能文件、零 agent 模板。
>
> **作者裁定**（2026-09-01）：执行序列 B97（本批）→ 审计波（基线重扫+新规则首用）→ 瘦身批。
>
> **前置**：B96 已闭环（审核通过 2026-09-01）；推送由作者统一裁定，不阻塞本批。

## 现状事实（施工前逐条 grep 复核）

1. **PRD+FSD**（`docs/mo-shu项目-产品需求与详细设计文档（PRD+FSD）.md`，89,346 字节）：版本路线止于 v2.5.1（:147），B88-96 九批缺席；陈旧术语实测——理想书评 6 处/虚拟对标 12 处/deslop 5 处/路由 12 处/日更（作流程名用）12 处；2.3 能力全景含已退役能力；3.13 部署含 Dashboard/marketplace 12 技能态；3.11 Agent 规格 8 个（数量未变，仅 narrative-writer skills 依赖变更）。
2. **术语表**（docs/治理/术语表.md，39 条，格式=| 术语 | 定义 | 禁用的别称 |）：新词零覆盖（成品标尺/逐章写作/改稿两路/文字打磨路/轻量 A' 均为 0）。
3. **审计法**（docs/治理/审计法.md，v2.0，23,425 字节）：六步流程（第 2 步 2a-2e/第 3 步 3a-3b/第 4 步工具化/第 5 步实测回归）+纪律+产出格式；**无**第七查无条件原则/独立重跑 grep/运行时路径依赖三类。
4. **盲区清单**：#3 涟漪面域名清单现文=「含全部 .json」——缺 `.github/`、CLAUDE.md.tmpl、AGENTS.md 全文、.claude-plugin/；#13 无「规格自述不豁免 diff」句。
5. PRD 是否误述上游（webnovel-writer vs oh-story-claudecode）未核——施工时 grep「上游\|fork\|webnovel」确认，README:248（worldwonderer/oh-story-claudecode）为谱系权威。

## 文件级改动清单

### A. PRD+FSD v3（`docs/mo-shu项目-产品需求与详细设计文档（PRD+FSD）.md`）

**A1. 1.2 概念词典**：改写四条旧条目——理想书评/虚拟对标→**成品标尺**（`设定/题材定位.md` 标尺节；三组目标[节奏/情绪/结构]；无对标采风合成/有对标锚定指针；evaluator 打分锚点；B94 收口）；deslop→**文字打磨路**（write 改稿两路之一，B95 收编）；路由/moshu→条目改「已退役（B96），首装引导归 moshu-setup」。新增条目：逐章写作（K 参数统一车道）/改稿两路/轻量 A'（三个每章差异项）。
**A2. 2.3 能力全景**：技能数 12→10；打磨能力改「write 内部工序（文字打磨路）」；入口层改「10 技能各自入口+setup 首装引导（路由层 B96 退役）」；成品标尺替代理想书评/虚拟对标相关表述。
**A3. 2.4 用户旅程**：五条逐条核——「日更」旅程改「逐章写作」（K=1 默认/连写 2-3/批末核对+滚动补纲预告）；「审查闭环」改双环（write 改稿·问题修复路[工单处置+级联]↔review 出厂深检）；「大修」类旅程改「改稿两路」；开书旅程补三轮开场+成品标尺一次过目。
**A4. 2.7 版本路线**：新增三行（**历史行 :147-149 原文保留不动**）——
- `v2.6.0（待发，B94-B96）：成品标尺收口（理想书评+虚拟对标→核心设定标尺段，开书四轮→三轮、过目 2→1）；写作模式重构（单章+日更→逐章写作 K 参数统一车道、A 段分级[批中轻量 A'+三个每章差异项]、三遍法批内时序、滚动补纲预告、改稿两路[文字打磨+问题修复]）；deslop 收编+moshu 路由与 Dashboard 退役（12→10 技能）；agents_version 45→47`
- `v2.5.3（已发 2026-09-01，B93）：采风接线与竞态修复——源优先级表 v2（三引擎按序 fallback）/spawn 必含条款九条/轮 3 前置检查`
- `v2.5.2（已发 2026-09-01，B88-B92）：心流与原创纪律（参考三层合同/归因三问/构建时假想读者/热梗手法学/参考档案）/终审与告知闭环（大纲终审四检查面/定稿三查/收官产物地图/加载回执）/合同统一与采风运营（路径扁平/侧名纪律/CDP 预检）/网文作家工序萃取（微张力/节拍精化/刀片计划/第一章专项/灵感池/心流序列维）/挂载审计（五级挂载+守卫脚本）`
**A5. 2.8 产品边界**：与 AGENTS §6 不学清单对齐——dashboard 条目改「已于 B96 删除（非仅不扩）」语义。
**A6. 3.13 部署与分发**：marketplace 10 技能清单；Dashboard 段删；VERSION 位置改 `skills/moshu-setup/VERSION`；agents_version 47；首装引导（description 承接）一句。
**A7. 全文上游谱系核对**：grep「上游\|fork\|webnovel」——若误述上游为 webnovel-writer 等研究对象，改 worldwonderer/oh-story-claudecode（fork 源）；2.6 差异定位的竞品分析对象（webnovel/SparkArc 等）**保留**（市场对照语义，非谱系）。
**A8. 3.11 Agent 规格**：narrative-writer 条目 skills 依赖更新（deslop 清除，B95）；8 个数量核对。

### B. 术语表增量（docs/治理/术语表.md）

新增 5 条（沿用 | 术语 | 定义 | 禁用的别称 | 格式，禁用列承担退役消歧）：
- **成品标尺**｜`设定/题材定位.md` 标尺节的三组目标态（节奏/情绪/结构），B94 收口理想书评+虚拟对标而成；evaluator 打分锚点｜"理想书评""虚拟对标"（B94 退役词）
- **逐章写作**｜write 唯一正文车道（workflow-daily.md 承载），K 参数：默认 1/日更意图 2-3/点名 N→min(N,3)｜"单章模式""日更模式"当独立流程名（"日更/续写"保留为触发词）
- **改稿两路**｜write 改稿工作流：文字打磨路+问题修复路（mode=revision 级联）｜"大修"当流程名（"回炉/重写第X章"保留为触发词）
- **文字打磨路**｜原 /moshu-deslop 收编为 write 内部无状态工序（B95）｜"去AI味技能""deslop"
- **轻量 A'**｜日更批中章的写前准备分级：细纲+上一章结尾+点名清单增量+三个每章差异项｜"批中全量准备"

### C. 审计法三条固化（docs/治理/审计法.md，版本 v2.0→v2.1 内注）

**C1. 第七查无条件原则（B95 假零教训）**——第七查条目内追加：「`templates/agents/` 是否有 diff 以 **git 实测为准**，规格/施工侧声明的『零 agent 模板变更』不构成豁免——有 diff 即必须 bump（盲区 #13）。B95 实证：规格 A2 列 5 模板改造而盲区 #13 声明零变更，自相矛盾未被发现直至审核。」
**C2. 独立重跑 grep 纪律（B94-96 三批连续涟漪漏项教训）**——纪律节追加：「审核/审计侧对规格的涟漪面结论（『零引用/零残留/零消费』）**必须独立重跑全域名 grep**，不采信规格自述；全域名=skills/ + scripts/ + .github/ + .claude-plugin/ + CLAUDE.md.tmpl + README×2 + AGENTS.md + 全部 .json 契约。B94②/B95①③/B96①③ 三批实证：规格涟漪清单合计漏 8 处，全部由验收 grep 网兜住。」
**C3. 运行时路径依赖类（B96 hook→VERSION 教训）**——第 2 步 2a 追加一类：「**运行时路径依赖**：引用面 grep 之外，另核①hook 读文件（session-start.sh→VERSION）②脚本读常量/路径字面量（deploy.py）③模板路径参数——删除/迁移任何文件前先核这三类消费者；此类依赖 grep 引用面抓不到（B96：hook 版本源靠施工发现）。」

### D. 盲区清单两处补强（docs/治理/盲区清单.md）

**D1. #3 涟漪面**：域名清单补「`.github/` workflows、CLAUDE.md.tmpl、AGENTS.md 全文、.claude-plugin/（B96 实证三处漏项的两类来源）」；追加「审核侧独立重跑，不采信规格自述（B94-96）」。
**D2. #13 部署面联动**：追加「agent 模板 diff 以 git 实测为准，规格自述不豁免（B95 假零实证）」。

## 禁止事项

1. 零代码/零技能文件/零 agent 模板改动（第七查应真零 diff）。
2. PRD 历史版本行（:147-149 及更早）**原文保留**——版本路线只增不改（append-only 语义）。
3. 施工日志/审核记录零触碰（append-only）。
4. 审计法/盲区改动**仅限本规格列明的五处**（C1-C3+D1-D2），不得顺手改其他条款——宪法级 SOP 的改动须逐条可追溯。
5. 术语表只增不删不改旧条目（39 条原样）。
6. 禁止 git add -A。

## 验收命令（Windows；零网络）

```bash
# 1. PRD 同步核验（各节陈旧术语清零——历史版本行 :147-149 与 2.7 旧行除外）
grep -n "理想书评\|虚拟对标\|moshu-deslop" "docs/mo-shu项目-产品需求与详细设计文档（PRD+FSD）.md" | grep -v "v2\.5\|v2\.4\|v2\.6\.0（待发\|收口\|退役"
grep -c "成品标尺\|逐章写作\|改稿两路\|文字打磨路" "docs/mo-shu项目-产品需求与详细设计文档（PRD+FSD）.md"   # ≥8
grep -c "10 个技能\|10 技能\|agents_version 47" "docs/mo-shu项目-产品需求与详细设计文档（PRD+FSD）.md"        # ≥2

# 2. 术语表增量
grep -c "成品标尺\|逐章写作\|改稿两路\|文字打磨路\|轻量 A'" docs/治理/术语表.md   # ≥5

# 3. 审计法三条固化
grep -c "无条件原则\|不构成豁免" docs/治理/审计法.md        # ≥1
grep -c "独立重跑" docs/治理/审计法.md                      # ≥1
grep -c "运行时路径依赖" docs/治理/审计法.md                # ≥1

# 4. 盲区两处
grep -c "\.github\|CLAUDE.md.tmpl" docs/治理/盲区清单.md    # 在 #3 行内命中
grep -c "不豁免\|git 实测为准" docs/治理/盲区清单.md        # ≥1

# 5. 范围核对：仅 4 文件 diff（PRD/术语表/审计法/盲区清单）+ 施工日志（append）
git status --short | grep -v "^??" | grep -v "docs/治理/施工日志\|docs/治理/审核记录\|docs/研究"

# 6. 第七查：templates/agents/ 真零 diff
git diff --stat -- skills/moshu-setup/references/templates/agents/

# 7. 固定守卫（应全绿——零代码改动）
bash scripts/check-story-numbers.sh && bash scripts/check-doc-budget.sh
```

## 提交规范

单提交：`b97: 产品文档与审计法同步（审计波前置）——PRD+FSD v3（成品标尺/逐章写作/改稿两路术语同步+版本路线补 v2.5.2/2.5.3/2.6.0+10 技能终态+上游谱系核）+术语表 +5 条（含退役词消歧）+审计法 v2.1 三条固化（第七查无条件/独立重跑 grep/运行时路径依赖）+盲区 #3 补域/#13 补不豁免句`

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（审计波前置同步，作者裁定序列） |
| 2 | 预算余量实测 | 通过——PRD/术语表/审计法/盲区均不在 doc-budget 在册（治理文档冷路径）；PRD 净变化 flash 实测报数 |
| 3 | 涟漪面（含 .json/互引） | 通过——纯治理文档批零代码涟漪；PRD 内部互引自洽（词典↔旅程↔全景用词一致）由验收 1 grep 保证 |
| 4 | 共享副本声明 | 通过——四文件均非 shared-assets 组 |
| 5 | 跨技能接口 | 通过——零技能文件改动；PRD 与实现的一致性即审计法 3c 双基线本义（本批后审计-四期以新版为基线） |
| 6 | 存量兼容 | 通过——无存量影响（纯文档） |
| 7 | 端到端走查 | 通过——验收 1/3/4 即产品↔规则落点走查 |
| 8 | 输出幂等性 | 通过——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫，Windows 零网络 |
| 10 | 热路径字数报告 | 通过——零热路径触碰 |
| 11 | token 成本核算 | 通过——零 spawn/零脚本变更 |
| 12 | 决策树过闸 | 通过——零新文件（4 文件原地改）零新机制（三条规则进既有 SOP 条目） |
| 13 | 部署面改动联动 | 通过——零 agent 模板变更（第七查真零 diff——B97 自身适用 C1 新规：以 git 实测为准） |
| 14 | 概念分离后术语考古 | 通过——本批即术语考古的执行体（退役词消歧 3 条+新词 5 条）；PRD 全文陈旧术语清零（历史行除外） |
