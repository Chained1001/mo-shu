# 规格 · 批 B76.5：指纹 LF 归一 + 状态总表回填（小治理批）

- 版本：v1.0（2026-08-29 旗舰起草，作者立项）。
- **目的**：根治 B76 推送 CI 首红事故的复发地雷——部署物指纹按工作区**原始字节**哈希，Windows 工具链（本机 `core.autocrlf=true` + 编辑器写 CRLF）会让本地指纹与 CI（LF 检出）分叉，形成「本地假绿 → CI 红 → 两周期判因」的循环；顺手清状态总表 28 批断档欠账与盲区教训固化。

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 目的声明 | 见版本行 | 通过 |
| 2 预算实测 | 不涉及 doc-budget 文件 | 不适用 |
| 3 涟漪面 | 指纹计算被 check-agents-version-sync.sh（CI static-guards 步）与 bump-agents-version.py --re-register-fingerprint 两处消费——两处须同口径，改动清单已列 | 通过 |
| 4 共享副本 | 两脚本均在 scripts/ 单份，非共享副本 | 不适用 |
| 5 跨技能接口 | 无 | 不适用 |
| 6 存量兼容 | 归一后指纹值不变（204fdb7366e3，工作区已全 LF，归一为空操作）——零存量影响，验收①证明 | 通过 |
| 7 端到端走查 | 验收②③即走查（CRLF 免疫构造证明 + 守卫矩阵） | 通过 |
| 8 幂等性 | 重跑指纹守卫结果确定 | 通过 |
| 9 验收可复现 | 全本地命令；**本批即是字节级等价教训的根治件** | 通过 |
| 10 热路径字数 | 盲区清单 +~60 单位（治理文件非热路径）；AGENTS.md 零改动 | 通过 |
| 11 token 成本 | 无 agent/spawn 变化 | 不适用 |
| 12 决策树 | 改既有脚本、挂既有守卫（check-agents-version-sync.sh 已在 CI 三处同步内）、加正式回归用例——无新文件无新机制 | 通过 |

## 一、改动清单

### 1. 指纹计算 LF 归一（scripts/check-agents-version-sync.py）

- 主指纹聚合处（`_fingerprint`/`:51-72` 区域，实测行号以当前文件为准）：对每个输入文件改用 `read_bytes().replace(b"\r\n", b"\n")` 后哈希——与该文件 `:159` 判因辅助函数的既有归一口径**统一**（现在是权威口径脆、辅助口径稳的内部不一致）；
- 函数 docstring 补一句判因注记：B76 推送 CI 首红（18 文件 CRLF、本地假绿），2026-08-29 B76.5 根治。

### 2. 重登记口径同步（scripts/bump-agents-version.py）

- `--re-register-fingerprint` 的指纹计算若为独立实现，同步改归一口径；若 import 同一函数则零改动（施工时核实，偏差记录写明哪种）。

### 3. 正式回归用例（防「再次坏掉」）

- 核实 check-agents-version-sync 是否已有测试文件（scripts/ 下 grep）；**有**则在其中追加一条用例：构造临时 CRLF 副本输入 → 归一算法下指纹与 LF 版一致（免疫证明）；**无**则新建 `test-agents-version-sync.py`（自执行模式，顶部「守护对象」声明），并按 §2.5 纪律进 scripts/README.md 测试清单 + CI 三处同步（cross-platform.yml、CONTRIBUTING.md）。
- 临时 fixture 放 `/.tmp/tests/B76.5/` 用完删。

### 4. 状态总表回填（docs/治理/施工日志.md）

- B48-B75 共 28 批逐行补入状态总表（从各批日志条目提取：批名/哈希/审核结论；无审核记录的填「—」，**不得凭记忆编造**——逐条对照日志节）。

### 5. 盲区清单 §1-9 扩充（docs/治理/盲区清单.md，作者已批）

- 「验收可复现」行的怎么做追加：「字节级等价：指纹/哈希类验收与登记须 LF 归一后计算（Windows 工具链 CRLF 假绿）」；出处追加「B76 推送 CI 首红、B76.5 根治」。

## 二、验收

1. `bash scripts/check-agents-version-sync.sh` 绿且指纹值仍为 **204fdb7366e3**（归一空操作、零扰动证明）；
2. CRLF 免疫证明：临时把任一输入文件转 CRLF → 复跑守卫**仍绿**（值不变）→ 还原；记录进日志；
3. 状态总表 B48-B75 连续 28 行，哈希与各批日志条目一致（抽查 5 行写进日志）；
4. 盲区清单含新句（grep「字节级等价」命中）；
5. 守卫矩阵全绿（static-check 12/12 Warn0 + doc-budget + shared-files + 新/旧回归用例）。

## 三、禁止事项

1. agents_version 保持 41 不变（不 bump、不重登记——归一后值不变即无需）；
2. 不重构指纹算法其它部分（最小改动：一处归一+一句注记）；
3. 临时 fixture 不入库；状态总表回填不得编造无日志依据的哈希或结论。

## 四、提交规范

`fix(治理): B76.5 指纹 LF 归一根治 CRLF 假绿（与判因辅助口径统一，值不变 204fdb73）+ CRLF 免疫回归用例 + 状态总表 B48-B75 回填 28 行 + 盲区清单 §1-9 字节级等价补录`
