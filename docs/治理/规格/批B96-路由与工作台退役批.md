# 批B96：路由与工作台退役批（v2.6.0 第二批——删 moshu 路由技能与 Dashboard）

> **目的**：落实 C19 作者裁定（2026-09-01）——B89 入口导览+CLAUDE.md 告知三则+各 skill 开场白+write 裸调用停靠已覆盖引导，路由技能价值被架空；Dashboard 无需求。删除 `skills/moshu/` 整技能（**11 技能→10**，v2.6.0 累计 -2 技能）。
>
> **前置**：严格串行——B95 提交+审核闭环后才开工本批。
>
> **七决策点落位**（C19 登记）：next_step.py 删除（消费面两处改内联）；首装引导并入 setup description；AGENTS 两处更新；治理涟漪；版本检查随删（README 留手动指引）；多书切换随删（可接受）；session-start hook 依赖（**施工新发现**：hook 读 moshu/VERSION——VERSION 迁 moshu-setup）。

## 现状事实（施工前逐条 grep 复核；行号为起草时实测，以 grep 为准）

1. **moshu 技能构成**（8 文件）：SKILL.md（1.4.3）+ VERSION（2.5.3）+ scripts/next_step.py + scripts/dashboard-server.mjs + references/dashboard-guide.md + assets/{app.js,index.html,styles.css}。
2. **next_step.py 消费面（技能外）**：write workflow-daily D4「完结判定可跑 next_step.py（FINALIZE 为建议非拦截）」——**内联判定已并存**（"若最新定稿章=卷纲末章提示卷复盘"）；volume cold-path.md 1 处引用；capability-wiring.json:288 挂载；check-current-skill-contracts.py 校验引用；**scripts/test-next-step.py（正式回归测试）**；AGENTS.md 头部 V2 落地说明点名；scripts/README.md 索引。
3. **session-start.sh:159-160** 读 `$ROOT/.claude/skills/moshu/VERSION`（回退 `$HOME/...`）作版本注入源——**删除 moshu 技能即断**。
4. **setup-workflow.md** 读 `skills/moshu/VERSION`（setup 展示版本号的来源）。
5. **dashboard CI**：.github/workflows/dashboard.yml 专属 workflow；scripts/README.md/CONTRIBUTING.md 无索引（dashboard-server.mjs 为 skill 内脚本非 scripts/ 域）。
6. **契约/清单挂载**：marketplace.json:12-27（name/description/路径）；spawn-contracts.json:300/:396（SKILL.md 引用）；doc-budget.json:70；README.md:97/:119+README_EN.md:98/:115（技能表行+Dashboard 使用段）。
7. **setup SKILL description 现状**：无「首次使用/npx 装完」引导语（该引导现挂 moshu description）。
8. CLAUDE.md.tmpl 无 bare-moshu 行（B95 已处理 deslop 行；施工时再 grep 确认）。

## 文件级改动清单

### A. 技能删除与 VERSION 迁移

**A1. `skills/moshu-setup/VERSION` 新建**（内容=现 2.5.3，随发版流程走）；**A2. 删除 `skills/moshu/` 整目录**（8 文件）。
**A3. session-start.sh:159-160** 两行路径改 `$ROOT/.claude/skills/moshu-setup/VERSION`（回退行同改）。
**A4. setup-workflow.md** 版本读取路径改 `skills/moshu-setup/VERSION`；setup SKILL description 吸收首装引导：「**首次使用（刚 npx skills add 安装完）**：请新开一个 Claude Code 会话，然后运行 /moshu-setup 部署写作环境」+触发词「刚安装完怎么用」。

### B. next_step.py 退役

**B1. write workflow-daily D4**：删「完结判定可跑 next_step.py」句（内联判定"最新定稿章=卷纲末章→卷复盘"已在同段，语义零损失）。
**B2. volume cold-path.md**：next_step 引用改内联文件证据判定（按上下文该处语义改写，施工日志记原文与改法）。
**B3. capability-wiring.json:288** 删 next_step 挂载条目；**check-current-skill-contracts.py** 同步删校验引用。
**B4. 删 scripts/test-next-step.py**——正式回归删除，施工日志声明守护对象退役（AGENTS §3.5 精神：测试文件头「守护对象」已不存在）。
**B5. scripts/README.md** 删 next_step 索引行；AGENTS.md 头部 V2 落地说明改写：「moshu 路由技能已于 B96 退役（S0-S6 判定语义由各 skill 进度入口按文件证据承担）」。

### C. Dashboard 退役

**C1. 删 `.github/workflows/dashboard.yml`**（专属 workflow 随技能退役；CI 三处同步规则不适用——该脚本从未入 scripts/ 域索引）。
**C2. AGENTS.md §6** 不学清单「Dashboard 常驻服务化/产品化——v6 建了 v7 删；mo-shu 现有 dashboard-server.mjs 保持轻量本地浏览定位，不扩」→ 改「……mo-shu 的 dashboard-server.mjs 已于 B96 删除（作者裁定无需求）」。

### D. 治理与清单涟漪

**D1. marketplace.json** 删 moshu 条目（12-27 行段）。
**D2. spawn-contracts.json:300/:396** 删/改 moshu SKILL.md 引用（按上下文——若为 spawn 材料清单则删行）。
**D3. doc-budget.json** 删 skills/moshu/SKILL.md 条目+组随动。
**D4. README.md/README_EN.md**：技能表删 moshu 行（技能数 11→10）；删「运行 /moshu dashboard 打开本地写作工作台」段；技能数全量核对（B95 后 11→本批后 10）；版本检查功能随删处补一句「更新看 [Releases](…)/CHANGELOG」。
**D5. check-story-numbers / static-check / capability-wiring / behavior-contracts / shared-files / check-current-skill-contracts** 全套跑绿；CLAUDE.md.tmpl 施工时 grep「/moshu[^-]」确认零引用。
**D6. 施工日志条目**（含 next_step 消费引用处置清单/测试退役声明/偏差记录）；CHANGELOG v2.6.0 条目补本批。

## 禁止事项

1. **VERSION 不得丢**：A1 先建 A2 后删（同 B95 D3→D4 顺序铁律精神——hook 依赖不断链）。
2. 不动 write/review/outline/volume 等 10 个技能的任何流程文件（B1/B2 是删引用不是改流程）。
3. 不动 agent 模板（预计零 diff，第七查）；hooks/CLAUDE.tmpl 若改→指纹重登记，无 agents_version bump（B71/B78 先例：非 agents 部署物内容改→仅指纹）。
4. next_step 删除不得留悬空引用：验收 2 grep 全仓零残留（含 .github/）。
5. 老书兼容：已部署项目 `.claude/skills/moshu/` 残留目录无害（无入口指向）；session-start hook 新路径部署后生效（老项目重跑 /moshu-setup 更新 hook——UPGRADING 不需新条目，无 agents_version 变化）。
6. 禁止 git add -A；禁止改断言变绿（B4 是守护对象退役声明，非弱化）。

## 验收命令（Windows 探测链；零网络）

```bash
# 1. 删除确认
test ! -d skills/moshu && test ! -f .github/workflows/dashboard.yml && test ! -f scripts/test-next-step.py && test -f skills/moshu-setup/VERSION && echo OK-DEL

# 2. 残留清点（产出=逐行核对：仅允许 AGENTS 历史句/施工日志/规格/CHANGELOG/研究档案）
grep -rn "skills/moshu/" skills/ scripts/ .github/ README.md README_EN.md AGENTS.md | grep -v "B96\|历史\|已退役\|已删除"
grep -rn "next_step\|dashboard-server\|/moshu dashboard" skills/ scripts/ .github/ README.md README_EN.md AGENTS.md | grep -v "B96\|历史\|已退役"

# 3. 首装引导落位
grep -c "首次使用\|刚安装完" skills/moshu-setup/SKILL.md

# 4. 固定守卫全套
bash scripts/check-story-numbers.sh && bash scripts/check-doc-budget.sh && bash scripts/check-shared-files.sh
python scripts/check-capability-wiring.py 2>/dev/null || bash scripts/check-capability-wiring.sh
python scripts/check-current-skill-contracts.py 2>/dev/null || true
python scripts/check-methodology-wiring.py && bash scripts/static-check.sh 2>/dev/null || true

# 5. 第七查：templates/agents/ 零 diff
git diff --stat -- skills/moshu-setup/references/templates/agents/

# 6. 指纹重登记后一致
python scripts/check-design-fingerprints.py 2>/dev/null || ls scripts/ | grep -i fingerprint

# 7. 端到端纸面走查三景
#   ①新装用户：npx 装完→新会话→「刚安装完怎么用」触发 setup（description 引导在位）→部署→开书
#   ②老书会话启动：session-start hook 从 moshu-setup/VERSION 读版本（新路径）→近况注入正常
#   ③写作流不受影响：write 裸调用停靠/卷末判定（内联）/cold-path 判定（内联）全链无 next_step 依赖
```

## 提交规范

单提交：`b96: 路由与工作台退役——删 moshu 路由技能与 Dashboard（11→10 技能）：VERSION 迁 setup 保 hook 版本源+首装引导并入 setup description+next_step 退役（消费引用内联化+测试退役声明+capability-wiring 清挂载）+dashboard CI workflow 删除+marketplace/README/契约/doc-budget 涟漪+AGENTS 两处更新`

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（C19 裁定+七决策点落位+施工新发现 hook 依赖） |
| 2 | 预算余量实测 | 通过——净删批次：doc-budget 删 1 条目+组随动；setup SKILL description +1 行（不在册或小余量，flash 实测回填） |
| 3 | 涟漪面（含 .json/互引） | 通过——.json 实测：capability-wiring:288/spawn-contracts×2/doc-budget:70；.github/dashboard.yml；AGENTS×2；hook:159-160——全在清单内 |
| 4 | 共享副本声明 | 通过——moshu 技能 8 文件均非 shared-assets 组（dashboard 资产独立） |
| 5 | 跨技能接口 | 通过——write/volume 各 1 处 next_step 引用改内联（B1/B2）；消费方走查在验收 7③ |
| 6 | 存量兼容 | 通过——老项目 .claude/skills/moshu/ 残留无害；hook 新路径重跑 setup 后生效；无 agents_version 变化 |
| 7 | 端到端走查 | 通过——验收 7 三景（新装首触/老书钩子/写作流无依赖） |
| 8 | 输出幂等性 | 通过——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫，Windows 零网络；static-check 按其调用形态 |
| 10 | 热路径字数报告 | 通过——零热路径文件改动（10 技能流程文件零触碰——禁事项 2） |
| 11 | token 成本核算 | 通过——每次会话少一个路由层判定；首装引导+1 行 description；净减 |
| 12 | 决策树过闸 | 通过——净删 10 文件（技能 8+workflow+测试）+净增 1（VERSION 迁移）；零新机制 |
| 13 | 部署面改动联动 | 通过——零 agent 模板变更（第七查）；hooks 路径改+指纹重登记（无 bump，B71/B78 先例） |
| 14 | 概念分离后术语考古 | 通过——「moshu 路由/Dashboard/工作台/next_step」全仓 grep 三分类（历史保留/误用零容忍）；「工具箱主入口」概念随批退役 |
