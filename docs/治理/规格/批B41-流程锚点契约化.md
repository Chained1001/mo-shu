# 规格 · 批 B41：流程锚点契约化 + 测试断言从契约读

- 版本：v1.0（2026-08-25）
- 依据：架构建议 #3——B34 CI 红教训（test-tracking-workflow-contracts 硬编码节标题，称谓改名后 CI 红且无人同步）；命名迁移类改动需单点传导
- 性质：契约扩展 + 守卫断言 + 测试改造，零行为变更

## 一、现状事实

- test-tracking-workflow-contracts.py:261 硬编码 `## Stage 4-4：批末收尾`（B34 已改一次，再改名仍会红）
- 流程节锚点（批末收尾/质量修复）散落文档与测试双份维护

## 二、设计

current-contract.json 新增 `flow_anchors` 域（流程节锚点唯一真源）：
- `daily_batch_finalize` → {doc: workflow-daily.md, section: "Stage 4-4：批末收尾"}
- `daily_quality_repair` → {doc: workflow-daily.md, section: "若本步修文改变了会影响后续的事实"}

守卫 `flow_anchor_findings()`：契约锚点 section 必须在 doc 中存在（缺失 → `flow-anchor-missing` FAIL）。
test-tracking-workflow-contracts 从契约读锚点（命名迁移时改契约+文档单点，测试自动跟随）。

## 三、文件级改动清单

1. `scripts/current-contract.json`：+ flow_anchors 域
2. `scripts/check-current-skill-contracts.py`：EXPECTED_KEYS + ContractManifest 字段 + load_manifest 校验 + flow_anchor_findings() + main 接入
3. `scripts/test-tracking-workflow-contracts.py`：test_daily_quality_repairs... 从契约读锚点
4. `scripts/test-current-skill-contracts.py`：补两断言（结构违规红 / 锚点缺失红）

## 四、禁止事项

- 不改 workflow-daily.md 内容（契约迁就文档现状）
- 新增锚点须 section 在 doc 中实际存在

## 五、验收命令

1. `python scripts/check-current-skill-contracts.py` → 全 PASS
2. `python scripts/test-tracking-workflow-contracts.py` → 14 tests 绿
3. `python scripts/test-current-skill-contracts.py` → 绿
4. 违规验证：契约锚点改不存在节 → flow-anchor-missing 红（复现后还原）

## 六、提交规范

消息：`feat(contract): 流程锚点契约化——current-contract 增 flow_anchors 域（daily 批末收尾/质量修复节锚点真源）；守卫断言锚点在文档存在；test-tracking-workflow-contracts 从契约读锚点（B34 CI 红类根治：命名迁移单点传导）+ 规格批B41 入库`

施工日志追加 B41 行。
