# 规格 · 批 B84：v2.5.1 打磨批——实测一期措辞/规则/轻模板 7 项

- 版本：v1.0（2026-08-31 旗舰起草，作者立项——实测一期产出 A 组修正项集中收口）。
- **目的**：实测 #001-#023 诊断出的措辞级/规则级/轻模板级问题一次性修完——快速收口、清理欠账，为批次 2（模板批）和批次 3（萃取批）扫清文件面。
- 性质：纯文本/规则/轻模板，零 schema 零 agent 零新脚本（除 P5 可能加一句日志提示）。**单批单提交**，子步验收清单固定含 doc-budget。

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 目的声明 | 见版本行 | 通过 |
| 2 预算实测 | 涉及热路径文件：workflow-outline（不在 doc-budget）/volume SKILL（1315/1320 余 5）——P1 前置块要压缩对冲；其余均为治理文件或不在预算的 references | 通过 |
| 3 涟漪面 | P9 动 AGENTS.md（须报字数）；P3 改名涉及 shared-assets 若干组名+PRD+doc-budget 条目名 | 通过 |
| 4 共享副本 | P3 改名如果涉及 ledger-template.md（volume 自有）——无共享；P7 涉及 caifeng-methods（volume 自有）——无共享 | 通过 |
| 5 跨技能接口 | P3 改名后 current-contract.json 的 artifact 路径须同步 | 通过 |
| 6 存量兼容 | P3 改名：test2-5 已有 设定/构建台账.md——保留原文件不改（测试项目已封存，新文件夹用新名） | 通过 |
| 7 端到端走查 | P6/P8 改流程步骤——走查两分支（预填/正常采集） | 通过 |
| 8 幂等 | 纯文本 | 不适用 |
| 9 验收可复现 | 全 grep/守卫 | 通过 |
| 10 热路径字数 | AGENTS.md 改后 node 实测报日志 | 通过 |
| 11 token 成本 | 全部减或平 | 通过 |
| 12 决策树 | P5 可能加一句 stdout 提示（非新脚本） | 通过 |

## 一、改动清单

### P2 文风样张→写法参照（实测 #013 术语考古）

`skills/moshu-outline/SKILL.md:14`：「题材卡**文风样张**按需跨技能只读」→「题材卡**写法参照**按需跨技能只读」。一处措辞，消除与 moshu-style（纯文风另立）的概念混淆。

### P3 台账→创作进度改名（作者拍板）

全量替换「构建台账」→「创作进度」：
- **文件名**：`skills/moshu-volume/references/ledger-template.md` → `progress-template.md`（volume 自有，无共享）
- **产物路径**：`设定/构建台账.md` → `设定/创作进度.md`（流程文件中所有引用）
- **涟漪面**（grep 实测为准，起草时已知）：volume-workflow（27 处）/cold-path（10）/workflow-outline（8）/revision-workflow（3）/ledger-template（3）/volume SKILL（3）/idea-seed（write+outline 各 2）/caifeng-methods（2）/write SKILL（1）/outline SKILL（1）/PRD（23）/开发标准（2）——逐文件替换，注意词形（「构建台账」「台账」「台账进入规则」等），保持语义连贯
- **current-contract.json**：artifact 路径如有 `构建台账` 字样须同步
- **doc-budget.json**：如 ledger-template 有条目须改路径
- **不迁 test2-5**（测试项目已封存）

### P5 产物落盘告知（实测 #007 模式提炼）

**开发标准 §2.6 加第 11 条**：

> | 11 | **产物落盘告知**：任何步骤把产物写入磁盘后，完成汇报必须含产物路径一行（带编号的照 CF 式报编号+路径）；后台任务（agent spawn）完成时同样适用，且不得把后台完成通知当作用户输入。管增量；存量四个点位（D4 批末/打磨环每轮/工单落盘/final-report）随本批补一句。 | B75 产物落盘告知模式（实测 #007 提炼） |

**存量四处补告知句**（每处一行，找现有汇报句加路径）：
- workflow-daily D4：「口头汇报本批完成情况」处加「含产物路径（正文/第N*.md 与 追踪/ 更新摘要）」
- volume-workflow Phase B 打磨环每轮汇报处
- review-workflow 工单落盘后汇报处
- workflow-daily 完结链 final-report 后

### P6 轮 3 预填确认（实测 #008）

`skills/moshu-outline/references/workflow-outline.md` 轮 3 节：固化子问题后加一段：

> **预填路径（作者可用）**：如果作者说「说不太准/你按已有信息拟一版我来改」→ 跳过子问题采集，直接按已收集信号（轮 1 参考偏好+轮 2 定形+采风要素）生成理想书评草稿，进入「草稿→作者改定」流程。两问保留为可选深化，不作必答。

### P8 轮 1 参考作品速写确认（实测 #011）

`skills/moshu-outline/references/workflow-outline.md` 轮 1 节：「参考偏好快问」后、采风 spawn 前插入：

> **参考作品速写确认**：AI 对每部参考作品给三行速写（底色内核+模式占比｜开局手法 vs 全文主体｜主角弧阶段转变），AskUserQuestion 弹窗（每部一屏或合屏）请作者确认/纠正。确认后才组采风 query——防止 AI 误权重（如把大奉的探案开头当成全书底色）污染 query 框架。

### P9 「例证≠需求」宪法补句

`AGENTS.md` §9 功能模仿原则段末追加一句：

> 作者举例是原理解释不是内容需求——借原理不搬素材，选项生成须覆盖原理空间而非锚定单一示例（采风转译纪律的对话层泛化，实测 #012）。

### P10 档位表执行归属标注

`skills/moshu-outline/references/workflow-outline.md` 默认档位表后加一行：

> 三域深度为全书构建声明（记入创作进度）：大纲/设定由 outline（Stage 2-3）执行；卷纲由 volume（开书接力 Stage 4-6）执行——此处声明、彼处消费。

### P13 交接措辞校正

`skills/moshu-volume/references/volume-workflow.md` 及 `skills/moshu-outline/references/workflow-outline.md` 交接处：
- 「转 /moshu-volume」→「**建议你使用** /moshu-volume」（技能不调用技能，作者是调度者——宪法 §1.3）
- 「Stage 4-6」在 volume 侧语境改业务名（「单元构建→整合检验→定稿」）或 volume 侧自编号 Step 1-3——跨技能编号不留残留

### P7 采风模板 v2（实测 #007/#011 产出）

`skills/moshu-volume/references/caifeng-methods.md` 六节模板升级：

1. **头部加第 0 节「核心发现」（≤5 条+一句转译方向）**——产物自带要点表，展示=直接引用，重读免消化
2. **要素表加「建议消费去向」列**——每要素产出时声明喂哪个设计文档哪个节
3. **五类要素表前加「要素 0：底色配方」**——模式占比+开局手法判定（治 query 框架污染）
4. **融合节角色澄清**——「agent 可预拟融合建议，标『agent 预拟·待主线确认』」（消除越位灰区）
5. **来源清单按来源组分组**（组内多 URL 作交叉印证注记）

同步：check_outline 的采风专名比对（candidate k）如读新字段须兼容（grep 核实——大概率读通配不读节名，无需改）。

## 二、验收

1. `grep -rn "文风样张" skills/` 零命中（P2）
2. `grep -rn "构建台账\|ledger-template" skills/ scripts/ --include="*.md" --include="*.json"` 零命中（P3，test2-5 排除）；`设定/创作进度.md` 在流程文件中引用一致
3. 开发标准 §2.6 含第 11 条（P5）；四个存量点位 grep「产物路径」或等价表述各 ≥1
4. workflow-outline 轮 3 含「预填路径」段（P6）；轮 1 含「速写确认」段（P8）
5. AGENTS.md 功能模仿原则含「例证≠需求」句（P9）；node 实测裸长报日志
6. 档位表后有执行归属声明行（P10）
7. `grep -rn "转 /moshu-" skills/` 零命中（P13）；`grep -rn "Stage 4-6\|Stage 4" skills/moshu-volume/` 零命中（改业务名）
8. caifeng-methods 含「核心发现」「消费去向」「底色配方」「agent 预拟」四关键词（P7）
9. 守卫矩阵全绿（最终态复跑+Result 行整段引用含退出码）

## 三、禁止事项

1. P3 改名**不触碰 test2-5 测试项目**（已封存，用旧名不迁）
2. P9 宪法补句后 AGENTS.md 裸长必须 <11000（node 实测报日志）
3. P7 采风模板改后 **check_outline 采风专名比对必须仍绿**（不破坏既有机检）
4. 本批零 schema/零 agent 模板/零新脚本；禁止顺手改批次 2/3 的内容（P11 大纲模板/C14 萃取——不在本批）

## 四、提交规范

`fix(打磨): B84 v2.5.1 打磨批——实测一期 7 项修正：P2 文风样张→写法参照/P3 台账→创作进度全量改名/P5 产物落盘告知规则+存量四点/P6 轮 3 预填路径/P8 轮 1 参考速写确认/P9 例证≠需求宪法补句/P10 档位归属/P13 交接措辞/P7 采风模板 v2 五项升级`
