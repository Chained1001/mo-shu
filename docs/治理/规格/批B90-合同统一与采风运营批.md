# 批B90：合同统一与采风运营批

> **目的**：路径合同扁平化（作者裁定一文件夹一书——治五层组件三种口径的分叉）＋跨技能引用侧名纪律＋采风协议运营强化（回合预算/临时目录/Windows 环境节/缺点规避入模板/国内源优先级表+CDP 预检）＋令牌工单适配——清掉实测二期捞到的全部工程级残项。
>
> 规划模型：旗舰｜依据：实测二期 #026/#029/#031/#032/#034/#039/#049、§三c 二-1/3/4/5/6/8/9/15｜施工顺序：B88、B89 之后（严格串行末批）。

## 现状事实（施工前逐条复核；行号为起草时实测，以 grep 为准）

1. **路径三种口径（实测 #029/#030/#031）**：`skills/moshu-setup/references/templates/CLAUDE.md.tmpl:22-27` 文件结构节声明 `{书名}/正文/` 等嵌套；`skills/moshu-setup/references/setup-workflow.md:13` 状态四查 `ls -d */追踪`（嵌套式）；`skills/moshu/SKILL.md:50` 路由表「无书名目录（无含 `正文/`、`大纲/`、`追踪/` 任一的项目目录）」＋`:104-108` 多书切换节；而 `skills/moshu/scripts/next_step.py:131` `outline_dir = project / "大纲"`（扁平）＋三个 workflow 全用根级相对路径；`skills/moshu-setup/references/templates/hooks/lib/common.sh:30` 双形态容错（保留不动）。作者已裁定（2026-08-31）：**一文件夹一书，开新书换新文件夹**。
2. `skills/moshu-setup/scripts/deploy.py:374` `--book` 参数（缺省=项目名，`:389`）——扁平世界无落点；`setup-workflow.md:27` 位置确认弹窗三问（目录/项目名/书名）。
3. `skills/moshu-outline/references/ideal-review-template.md:19` 裸相对链接 `[reader-psychology.md](reader-psychology.md)`——文件实在 moshu-write 侧（实测 #034：B86 承诺从未被加载）；同技能正确先例「moshu-volume 侧 caifeng-methods」（workflow-outline:111 等）。
4. `skills/moshu-outline/references/workflow-outline.md` 入口「先查选题决策」：「缺失时自动找（项目根起向下 3 层按名搜）」——无「不向上」限定（实测 #026：向上遍历 2/2 复现，曾触及兄弟项目）。
5. `skills/moshu-volume/references/caifeng-methods.md`：检索策略节（`:5`）无回合预算/临时目录/Windows 环境提示/CDP 预检；来源质量优先级节（`:20`）是可信度分级、非可达性优先级；六节模板（`:75`）无「缺点与规避」正式节（实测 #031：作者指令被自发执行但无规格）。
6. `skills/moshu-volume/references/volume-workflow.md:241` evaluator 协议含「review_tickets.py verify-token 校验令牌」——moshu-evaluator 只读（Read/Glob/Grep）不写工单，脚本路径未走通（实测 #049：退化为人工比对）。
7. 实测数据：采风 agent 3/3 撞 30 回合上限（#031/#044，骨架先行后仍需续跑）；临时文件 `_tmp_*.html` 污染项目根+`.research_tmp/` 残留（Git Bash /tmp 与原生 Python 不互通）；外网探测全灭浪费 6-8 回合；有效源栈=搜狗发现→豆瓣书评+萌娘+百科API+平台问答（#032 审计）。

## 文件级改动清单

### 1. `skills/moshu-setup/references/templates/CLAUDE.md.tmpl`（路径扁平化）
文件结构节（`:22-27`）改为：
```
- `拆文库/` — 拆文分析结果存放目录
- `正文/` — 长篇小说正文章节
- `设定/` — 角色设定、世界设定
- `大纲/` — 卷纲、细纲
- `追踪/` — _tracking-state.json 唯一结构化权威……（其余文字不变）
- `对标/` — 对标作品分析
```
（`{书名}/` 前缀全部去除；Compact 节 `:53` `{书名}/追踪/上下文.md` 同步改 `追踪/上下文.md`。）

### 2. `skills/moshu-setup/references/setup-workflow.md`
1. 状态四查（`:13`）：第 2 查改「项目根存在 `追踪/` 或 `设定/` 或 `大纲/` 任一目录 → 识别为长篇项目」（根级直查，兼容旧嵌套可加 `*/追踪` 备位一句——**主口径扁平**）；示例命令同步。
2. 位置确认弹窗（`:27`）：三问改两问（部署目录＋项目名）——「书名」问删除，附注一行「书名在开书流程提案（目录扁平：一文件夹一书）」。

### 3. `skills/moshu/SKILL.md`
1. 路由表行 2（`:50`）改：「无 `正文/`、`大纲/`、`追踪/` 任一目录（项目根直查）→ 开书引导」。
2. 多书切换节（`:104-108`）保留功能，节首加注一行：「主流用法=一文件夹一书（作者裁定 2026-08-31）；多书切换为兼容保留」。项目状态感知节（`:99`）措辞同步扁平口径。

### 4. `skills/moshu-setup/scripts/deploy.py`
**零改动**——`--book` 参数保留（缺省=项目名，无害兼容），不在本批删除（避免部署面 bump 涟漪）；弹窗侧已不问，参数自然休眠。注释级标注可选不加。

### 5. `skills/moshu-outline/references/ideal-review-template.md`
`:19` 改：「（设计参照 **moshu-write 侧** [reader-psychology.md]——开篇心理/爽点动力学/期待管理，B86）」——侧名式（不带裸相对链接目标）。

### 6. 全仓跨技能裸相对链接同型排查（施工项）
flash 执行：对 `skills/*/references/*.md` 逐文件提取 `](xxx.md)` 链接，目标不在本技能 references/ 目录内的即为同型嫌疑——逐条改侧名式（「moshu-X 侧 xxx.md」）或修正路径；**排查结果列表写进施工日志**（命中数+逐条处置；预期量少——侧名先例为主）。`docs/治理/开发标准.md` 加纪律一行：「跨技能引用必须带侧名（moshu-X 侧 文件名），禁裸相对链接——两段互引时 LLM 看不到对方（§4 反模式 #3 同源）」。

### 7. `skills/moshu-outline/references/workflow-outline.md`
入口「先查选题决策」句改：「缺失时自动找（**项目根起向下** 3 层按名搜；**不向上、不吃兄弟项目**）」。

### 8. `skills/moshu-volume/references/caifeng-methods.md`（采风运营四件）
1. 检索策略节（`:5`）内加「**效率纪律（回合预算）**」段：检索 ≤20 回合；**先写产物骨架文件再增量补内容**（待补占位）；超限即以已有材料+模型知识收尾并在元数据头标降级——「宁可少两 个场面也要确保落盘」。
2. 检索策略节内加「**环境节（Windows/Git Bash）**」段：临时文件统一进项目内 `设定/.caifeng-tmp/`（禁项目根散落、禁依赖 /tmp——Git Bash /tmp 与原生 Python 不互通）；收尾清理；三坑提示（路径映射用项目内绝对路径／python 输出加 PYTHONIOENCODING=utf-8／搜狐系 GBK 显式解码）。
3. 检索策略节内加「**CDP 预检**」段：主会话 spawn researcher 前先跑 `node {moshu-cdp skill 根}/scripts/setup-cdp-chrome.js 9222 --detect-only`——`ready` 则 spawn prompt 注明可用 agent-browser（解锁豆瓣 subject/知乎/起点详情/JS 页）；`needs-setup` 则**征询作者**是否起调试 Chrome（启动会杀当前 Chrome，须作者同意——不得自动启动）；拒绝则走 curl 栈。
4. 来源质量优先级节（`:20`）扩「**国内可达源优先级表**（curl 模式）」：搜狗搜索（发现层主力）→豆瓣读书书评页/萌娘百科/百度百科开放 API（BaikeLemmaCardApi）/平台问答页（潇湘等，只取元数据）；已知反爬名单（起点/知乎 API/百度搜索页/豆瓣搜索页/bilibili 限流——白探即弃）；分词坑注记（cn.bing 对中文书名分词失效）；**外网默认排除**（google/wikipedia/duckduckgo 国内环境不可达，禁探测浪费回合）；CDP 模式解锁层另列。表为优先级引导非硬白名单（防站点过期）。
5. 六节模板（`:75`）加正式节「**缺点与规避清单**」：每条缺点带来源+一句规避方向（实测 #031 话术固化——「优缺点双看，缺点必须单独标注规避，不能明知缺点还学习」作者指令 2026-08-30）。

### 9. `skills/moshu-volume/references/volume-workflow.md`
evaluator 全局评审协议（`:241`）令牌行改：「主会话收到评审报告后，代写工单（`review_tickets.py write --input <临时 JSON 文件>`，AI 产出先落文件）再 `verify-token --ticket --token` 校验——脚本收口保留；evaluator 本体只读不写工单」。

## 禁止事项

1. **deploy.py / merge-claude-settings.py / templates/agents/ / agent-references 零改动**（零 agent 变更；第七查 diff 对照）——`--book` 保留不删。
2. **next_step.py 与 hooks 零改动**（前者已扁平；后者双形态容错保留兜底）。
3. **老项目不迁移不删除**——test2-5/test1-1 等嵌套项目即弃即弃（测试产物无存量价值）；兼容声明写「嵌套老项目在新合同下 next_step 判未开书——已知且接受」。
4. 优先级表**不是硬白名单**（防站点过期）；**不做代理池/IP 轮换/cookie 注入**（作者 2026-08-31 裁定：对抗性绕过越界，低频元数据检索无需）。
5. CDP 预检**不自动启动 Chrome**（杀进程须作者同意——moshu-cdp 技能既有纪律）。
6. 多书切换功能**保留不删**（只加主流用法注记——去留作者过目本规格时可改）。
7. 不改 check_outline.py / review_tickets.py（后者只改调用方协议表述）。

## 验收命令（Windows 探测链；零网络）

```bash
# 1. 路径扁平
grep -c "{书名}" skills/moshu-setup/references/templates/CLAUDE.md.tmpl   # 期望 0
grep -n "正文/" skills/moshu-setup/references/templates/CLAUDE.md.tmpl | head -2   # 根级形态
grep -n "项目根直查\|不向上" skills/moshu/SKILL.md skills/moshu-setup/references/setup-workflow.md skills/moshu-outline/references/workflow-outline.md
grep -n "一文件夹一书" skills/moshu/SKILL.md
# 2. 侧名与链接排查
grep -n "moshu-write 侧" skills/moshu-outline/references/ideal-review-template.md
# flash 自跑排查脚本（临时放 /.tmp/tests/B90/）：提取全部 references/*.md 的 ](xxx.md) 链接对照本技能目录，命中列表进日志，用完即删
grep -n "跨技能引用必须带侧名" docs/治理/开发标准.md
# 3. 采风四件
grep -n "回合预算\|先写产物骨架\|≤20 回合" skills/moshu-volume/references/caifeng-methods.md
grep -n "caifeng-tmp\|PYTHONIOENCODING" skills/moshu-volume/references/caifeng-methods.md
grep -n "detect-only\|CDP 预检" skills/moshu-volume/references/caifeng-methods.md
grep -n "国内可达源优先级\|外网默认排除" skills/moshu-volume/references/caifeng-methods.md
grep -n "缺点与规避清单" skills/moshu-volume/references/caifeng-methods.md
# 4. 令牌
grep -n "代写工单\|write --input" skills/moshu-volume/references/volume-workflow.md
# 5. 固定守卫
bash scripts/check-doc-budget.sh
# 6. shared 副本核查（CLAUDE.md.tmpl/workflow-outline/caifeng-methods/volume-workflow/setup-workflow/moshu SKILL 若在组 → sync+指纹+check-shared-files.sh）
# 7. 部署面零变更对照（第七查）：git diff 空 于 templates/agents/、agent-references/、deploy.py、merge-claude-settings.py
```

全部 grep 命中、守卫退出码 0、排查列表入日志方算完成。

## 提交规范

一条提交：`fix(B90): 合同统一与采风运营批——路径扁平化（CLAUDE.md 模板/setup 四查/moshu 路由，一文件夹一书作者裁定）、跨技能侧名（ideal-review-template+全仓同型排查+开发标准纪律）、采风运营四件（回合预算/临时目录+Win 环境节/CDP 预检/国内源优先级表+缺点规避入模板）、令牌工单代写协议`。只含本批清单文件。

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（清工程残项，主刀路径合同） |
| 2 | 预算余量实测 | 通过——被改文件全在 doc-budget 登记内，跑绿为准；flash 回填前后值 |
| 3 | 涟漪面（含 .json/互引） | 通过——`{书名}` 全仓 grep（CLAUDE.md.tmpl/setup-workflow/moshu SKILL 三处为预期命中面；其他命中=意外残留随批清）；next_step.py:140 的「大纲/大纲.md」措辞本就扁平无需动；不涉 .json |
| 4 | 共享副本声明 | 条件——六份被改文件 flash 复核 shared-assets.json，在组则 sync+指纹 |
| 5 | 跨技能接口 | 通过——路径口径统一后 next_step/workflows 消费面一致；caifeng-methods 改动被 outline 跨技能消费（侧名引用），验收含走查 |
| 6 | 存量兼容 | **重点声明**——嵌套老项目（test2-5/test1-1）在新合同下 next_step S1 判「未开书」：已知且接受（测试产物无存量价值，不迁移不删除）；真实用户无存量（v2.5.1 前部署皆扁平用法为主流） |
| 7 | 端到端走查 | 条件——路径口径变更涉多组件，flash 施工后以「新项目 mkdir 正文/设定 → next_step 判 S1 有书」纸面推演进日志（不跑 LLM） |
| 8 | 输出幂等性 | 不适用——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫+临时排查脚本（/.tmp/tests/B90/ 用完即删），Windows 零网络 |
| 10 | 热路径字数报告 | 通过——CLAUDE.md.tmpl/moshu SKILL/setup-workflow 均部署热路径，flash 回填前后值 |
| 11 | token 成本核算 | 通过——零 agent/spawn 模板改动；CDP 预检为 +1 条 bash 命令/采风前（可忽略） |
| 12 | 决策树过闸 | 通过——零新文件/新脚本/新 agent（排查脚本是临时验证，/.tmp/tests/ 用完即删，符合红线 4） |
| 13 | 部署面改动联动 | 通过——零 agent 模板/agent-references/deploy.py/merge 变更（禁止事项 1；--book 保留正是为避免 deploy.py 涟漪——第七查 diff 对照） |
| 14 | 概念分离后术语考古 | 通过——「书名目录」旧概念 grep 全仓（moshu SKILL/setup-workflow 为预期命中随批改）；「多书切换」降级为兼容注记非删除 |
