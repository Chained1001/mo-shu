# 批B95：写作模式重构批（v2.6.0 首批——车道统一+deslop 收编+内核去重）

> **目的**：落实 #058/#059 作者裁定与候选 H/I/J——写作调度收敛为一条流（产纲/顺序写/改稿两路），单章+日更合并为 K 参数化统一车道，deslop 技能收编删除（**12 技能→11，主版本级**），chapter-core 四处重复收口。数据源层（对标/无对标双路）**不动**。
>
> **作者裁定链**：两意图模型（顺序写 K/指定章三落点，2026-09-01）→ 合二留一（#058）→ A 段分级含每章差异项（对标质询补）→ 三遍法批内时序定死 → 滚动补纲预告 → 改稿两路+deslop 收编+两契约（#059 补记）。**不做**：发布水位线/向前修路/N 轮通用打磨/C18 书证化（另批）。
>
> **实测空窗声明**：写作流至今未经实测（一/二期止于卷纲，三期停在 B94 验证）——本批收益（尤其 A 段分级省 token）为纸面推演，施工后实测三期正文流验证。

## 现状事实（施工前逐条 grep 复核；行号为起草时实测，以 grep 为准）

1. **车道文件三件**：workflow-chapter.md（~3.2K 薄壳：13 步指针+单章特有补充[标题预检/质检/写前契约指针/第一张专项一行]）；workflow-daily.md（~19.7K：D1-D4+旧信息梯子+细纲缺失补建流程+适用条件）；workflow-revision.md（~8.6K：R1-R5+工单处置+重听批评）。
2. **workflow-chapter/workflow-daily 引用面（全仓实测）**：write SKILL.md、chapter-core（头注三车道声明）、write outline-workflow、meme-craft、recovery-protocol、volume-review、writing-craft（路由表"workflow-chapter.md Stage 4-C10"）、volume 侧 outline-workflow、agent-references/writing-craft、**5 个 agent 模板**（architect/explorer/consistency-checker/character-designer/narrative-writer）、4 个 json 契约（behavior-contracts/capability-wiring/spawn-contracts/current-contract）+doc-budget。
3. **deslop 构成**（8 文件）：SKILL.md（1.1.1）+ references/deslop-workflow.md（独有：外科手术纪律——章级两轮/保留清单 grep 在位/事实保真/字数系数 B60）+ references/{anti-ai-writing,banned-words}.md（副本）+ scripts/×4（check-ai-patterns/check-outline-copy/check-degeneration/normalize-punctuation）。
4. **deslop 是 shared-assets 6 组的 source**（scripts 4 组+references 2 组，targets=write/review 副本）——**收编必须先把 source 迁至 `skills/moshu-write/`**（内容与副本同步一致），再删目录。
5. **deslop 零专属 agent 模板**（templates/agents/ 8 个：architect/chapter-extractor/character-designer/consistency-checker/evaluator/explorer/narrative-writer/researcher）→ **零 agent 模板变更，无 agents_version bump**。
6. **marketplace**：.claude-plugin/marketplace.json:128-144 moshu-deslop 条目（name/deslop 触发词/skills 路径）。
7. **CLAUDE.md.tmpl:10** 命令表含 `/moshu-deslop`、`/去AI味` 行。
8. **review 侧 4 处出口**：review SKILL 流程衔接表「发现 AI 味需清理→moshu-deslop」；review-workflow.md:140（自动修复转 deslop）/:173（整改遵守 deslop-workflow 外科手术纪律）/:218（改写走 deslop Gate F）。
9. **write SKILL 流程衔接表**「写完，去 AI 味→moshu-deslop」行。
10. **chapter-core 重复点（B94 后行号以 grep 为准）**：题材卡 fallback 三副本（A2:12/A3(c)/写前准备契约 6）；三遍法每遍「方法论书」头注+步骤 1 双写；字数验收双写（第 3 遍 step4 vs「字数验收权威」节）；D4「按 workflow-daily 构造」core→shell 反转；A1 内联细纲字段清单已改指模板（B94 C1）。
11. **预算现状**：chapter-core 21800/workflow-chapter 1650（B94 后）；workflow-daily/anti-ai-writing 在册值施工时守卫口径实测；删除文件（deslop 组/workflow-chapter）预算项同步清除。
12. check-story-numbers 技能数动态校验（无硬编码 12）——改 README/CHANGELOG 数字即可。

## 设计决策（作者已裁定）

| # | 决策 | 出处 |
|---|---|---|
| D1 | 两意图模型：顺序写（K 默认 1/日更意图 2-3/点名 N→min(N,3)）；指定写某章三落点（N=下一章→顺序写 K=1；N≤已写→改稿；跳章→引导回顺序写） | #059 |
| D2 | 车道合二留一：workflow-chapter 并入 workflow-daily 成统一「逐章写作」车道；**workflow-daily.md 文件名保留**（30+ 引链兼容，语义改在标题与内容——设计说明 #15 详述）；workflow-revision.md 文件名保留、内容重构为改稿两路 | #058 |
| D3 | A 段分级：批首全量 A/批中轻量 A'/批末核对；A'=细纲+上一章结尾+点名清单增量+**三个每章差异项**（按章选情绪模块/挑匹配章 K/场景卡路由） | H②+对标质询 |
| D4 | 三遍法批内时序：C 段以遍 1 落盘后为准（追踪与毒句式不隔夜）；第 3 遍后只补候选机检+字数复验；Sanderson 编排可选不默认 | H③ |
| D5 | 滚动补纲预告：批末探测下一单元细纲未建→汇报带「现在补（推荐）/下次再说」 | H④ |
| D6 | 改稿两路：文字打磨路（write 内部工序，deslop 方法论并入）+问题修复路（工单驱动+mode=revision 级联） | #059 补记 |
| D7 | deslop 收编：技能删除 12→11；**方法论零丢失**（外科手术纪律→改稿路；改味三条原则→anti-ai-writing） | #059 补记 |
| D8 | 两契约：write 拥有全部稿件写权（文字打磨为内部无状态工序）；review 永不改文只出工单 | #059 |

## 文件级改动清单

### A 组·车道统一与调度（H①③④）

**A1. `workflow-daily.md` 重构为统一车道**（文件名保留）：标题改「逐章写作工作流（K 参数化——单章与日更统一，B95）」；头注声明三事（本文件=唯一正文车道/文件名沿用 daily 为引链兼容/单章差异=停靠语义并入 K）；「适用条件」改「进入条件」（K 判定规则：无数量意图默认 1+防失控停靠语义保留/日更续写意图 2-3/点名 N→min(N,3)）；吸收 workflow-chapter 的「单章特有补充」节（标题预检指针/质检索引/第一张专项——专项行随迁移展开为 2-3 行块）；D2 逐章执行改按「分级 A」（见 B1）。
**A2. 删除 `workflow-chapter.md`**——13 步指针并入 A1 车道首的「每章执行序」（一行一步指 core 对应段）；全仓引用改指：write SKILL 路由、chapter-core 头注（三车道→两车道声明）、writing-craft 路由表行、5 个 agent 模板、meme-craft/recovery-protocol/volume-review/两 outline-workflow、4 个 json 契约+doc-budget 中的 workflow-chapter 引用——逐处 grep 改指 workflow-daily.md 或 chapter-core 对应段（施工日志记引用改造清单）。
**A3. `SKILL.md` 路由重写**：场景表 5→4（开书转出/补纲产纲/逐章写作[含指定章三落点语义+K 规则]/改稿）；匹配优先级段简化（大修→改稿；写指定章并入逐章写作条目）；裸调用停靠保留；description 更新（version 1.7.1→1.8.0，「日更续写」改「逐章写作（K 参数）」+「改稿」）；流程衔接表 deslop 行改「深度去 AI 味→本技能改稿·文字打磨路」。
**A4. 滚动补纲预告**：统一车道 D4 批末汇报节加一项——`ls`/读卷纲探测下一批细纲覆盖（当前单元剩余 ≤2 章或下单元未建）→汇报选项「现在补下一单元细纲（推荐）/下次再说」，选补即走 outline-workflow 补纲小流程后收尾。
**A5. 三遍法批内时序**：chapter-core B 段头注补一段「批内时序（B95）」：C 段以遍 1 落盘后为准（含修复预算；追踪事务 D 段照旧每章）；第 3 遍后仅补 check-prose-candidates+字数复验；Sanderson 连续快写可选、默认逐章三遍。

### B 组·A 段分级（H②）

**B1. `chapter-core.md` A 段头部加「分级声明（B95）」**：批首章全量 A（现状 A1-A4）；**批中章轻量 A'**=①本章细纲②上一章结尾段③点名清单增量（新出场/久别角色状态）④**三个每章差异项**——按本章情绪从已加载情绪模块选 selected_emotion_module、按本章基调挑匹配章 K、按本章定位路由场景卡；文件级加载（情绪模块/节奏/文风/题材卡/对标产物）批级复用不重读；批末 K>1 跑跨章核对（车道 D3）。单章车道（K=1）即批首=批末，天然全量。
**B2. 统一车道 D2 逐章执行**改为引用分级声明（第 2 章起标「轻量 A'」），删除与 core 重复的加载表（见 C5）。

### C 组·chapter-core 去重与归位（I①-⑤）

**C1.** 题材卡 fallback 三副本收口：A3(c) 为唯一完整版；A2:12 与写前准备契约 6 改一行指针。
**C2.** 三遍法每遍「方法论书」双写合并：保留头注块（帽子/书/输入/产出/禁止），删步骤 1 的重复书单行（三遍共删 3 行，步骤重编号）。
**C3.** 字数验收合一：保留「字数验收权威」节为唯一规则源；第 3 遍 step4 压成两行（90%/×1.1 区间判定+「规则见『字数验收权威』」）。
**C4.** core→shell 归位：D4 删「按 workflow-daily 构造」改为 core 自含（9 细则已在 core，仅删指向）；「细纲缺失补建流程」5 步自 workflow-daily 迁入 core A1 节后（统一车道留一行指针）。
**C5.** 统一车道 D1 加载表压缩：6 行表改「加载项与缺失处理见 core A2/A3（13 项+召回 (a)-(h)）」+回执位保留（B92）+审查记录/explorer 快捷路径两项车道特有内容保留。
**C6.** 第 3 遍与文字打磨路的内部分界（I⑥，deslop 收编后为内部边界）：第 3 遍管「写得对不对味」（文风偏差/句式节奏/章首尾钩/对话动力+修剪，现状五磨不动）；改稿·文字打磨路管「像不像 AI」的深度清理（D6 执行体）。

### D 组·改稿两路与 deslop 收编（J）

**D1. `workflow-revision.md` 重构为「改稿工作流（两路，B95）」**（文件名保留）：入口路由（文字打磨路：存稿章表达层不满/问题修复路：工单或自检发现）；**文字打磨路**新写——deslop-workflow.md 独有内容迁移（外科手术纪律四条：章级两轮/保留清单 grep 在位/事实保真/字数系数；改味优先别当改错；改最少效果最大；过度去 AI 味保护[不得整段删/删除前确认伏笔钩子角色特征情节推进情绪承接因果锚点]；删除优先判断链）；**问题修复路**=原 R1-R5 保留精简（定位/加载/备份与范围确认/三遍法修订/级联事务）+工单处置+重听批评节不动；头部加两契约声明（D8）。
**D2. `anti-ai-writing.md` 增「文字打磨纪律（B95，自 deslop 收编）」节**：改味三条原则+保留清单红线（preserve 只有作者与 [需复核] 转正两个来源——自 review-workflow:172 迁移语义）。
**D3. shared-assets source 迁移**：6 组的 `source: skills/moshu-deslop/...` 改 `skills/moshu-write/...`（scripts 4 组→write scripts 现有文件；references 2 组→write references 现有文件）；sync 全部 target 校验一致后执行 D4。
**D4. 删除 `skills/moshu-deslop/` 整目录**（8 文件；deslop-workflow.md 独有内容迁移核对清单在施工日志记录，防丢）。
**D5. review 侧 4 处出口改指**：SKILL 衔接表行/workflow:140→「转 /moshu-write 改稿·文字打磨路」/:173 整改纪律→「改稿·文字打磨路『外科手术纪律』」/:218 Gate F 引用同步。
**D6. 治理与部署涟漪**：.claude-plugin/marketplace.json 删 moshu-deslop 条目；CLAUDE.md.tmpl:10 行改「深度去 AI 味→/moshu-write 改稿·文字打磨路」+**指纹重登记**（无 agent 模板变更→无 agents_version bump）；README/README_EN 技能清单与数字（12→11）；CHANGELOG 预留 v2.6.0 条目；check-story-numbers 跑绿；capability-wiring/behavior-contracts/spawn-contracts/current-contract 中 deslop/workflow-chapter 引用逐处核对改指；**moshu 路由技能两处**（路由表「去AI味→/moshu-deslop」行改指 /moshu-write 改稿·文字打磨路；状态机判定表序 5「日更/写下一章」措辞对齐「逐章写作」——next_step.py 的 DTO 语义不变只核 wording，2026-09-01 补）。

### E 组·章纲与打磨补强（设计讨论产出，作者过目规格时可整组裁掉）

**E1.** write 侧 outline-workflow 批末细纲评审节加两问：「本批最让你想写的一场戏是哪场？读者翻这章的理由是什么？」——答不出=章纲无兴奋点回炉（苏式问句三要素合规）。
**E2.** 探索稿升格：B79 扩展版探索稿适用条件从「高难单元/卡文批次」扩为「关键章默认」（高潮章/反转章/单元首章默认备选并陈 a/b/c；普通章直出不变）。
**E3.** 关键章加磨：chapter-core 第 3 遍后条件聚焦环（细纲功能标签含高潮/反转时）——只磨本章高潮场面段（问句：这个场面值不值得读者截图？慢镜头够不够？）；普通章不加。

### F 组·预算与收尾

**F1.** doc-budget：删 workflow-chapter/deslop 条目；workflow-daily（统一车道重构后）/anti-ai-writing（增节）/workflow-revision（重构后）实测回填；组预算随动。
**F2.** 施工日志条目（含引用改造清单/deslop 迁移核对清单/偏差记录）；CHANGELOG。

## 禁止事项

1. 不动追踪 schema/tracking_commit 字段与语义（事务构造只归位不改协议）。
2. 不动 review 审查逻辑与工单 schema（只改 4 处出口引用）。
3. 不动 B94 刚落的成品标尺/三轮开场/第一张专项接线。
4. candidate 永不拦截语义不变；三遍法三帽结构不变（A5 是时序澄清非重设计）。
5. **deslop 方法论零丢失**：外科手术纪律/改味三原则/保留清单红线必须落进 D1/D2，删除前施工日志列迁移核对清单。
6. 数据源双路不动：A 段 (a)-(f) 对标分支/成品标尺分支原样（B1 分级不得合并按章差异项）。
7. 不做 C18 书证化/瘦身批 A-G 项（另批）；E 组若作者裁掉则整组跳过。
8. 老书兼容：已部署项目的 .story-deployed 不受影响（零 agent 变更）；书内旧 CLAUDE.md 的 deslop 提示属可接受漂移（模板已更新）。
9. 禁止改断言变绿；禁止 git add -A。

## 验收命令（Windows 探测链 python3→python→py；零网络）

```bash
# 1. 文件删除
test ! -f skills/moshu-write/references/workflow-chapter.md && test ! -d skills/moshu-deslop && echo OK-DEL

# 2. 残留清点（产出=逐行核对：仅允许 UPGRADING/CHANGELOG/规格/施工日志/审核记录/研究档案历史）
grep -rn "moshu-deslop\|deslop-workflow" skills/ scripts/ .claude-plugin/ README.md README_EN.md | grep -v "B95\|收编\|历史"
grep -rn "workflow-chapter" skills/ scripts/ | grep -v "B95\|历史"

# 3. 路由与分级语义核验
grep -c "K 参数\|逐章写作" skills/moshu-write/SKILL.md skills/moshu-write/references/workflow-daily.md
grep -c "三个每章差异项\|轻量 A'" skills/moshu-write/references/chapter-core.md

# 4. 固定守卫全套
python scripts/test-check-outline.py && python scripts/test_methodology_wiring.py && python scripts/check-methodology-wiring.py
bash scripts/check-shared-files.sh && bash scripts/check-story-numbers.sh && bash scripts/check-doc-budget.sh
python scripts/check-capability-wiring.py 2>/dev/null || bash scripts/check-capability-wiring.sh

# 5. 第七查：templates/agents/ 零 diff（零 agent 模板变更声明验证）
git diff --stat -- skills/moshu-setup/references/templates/agents/

# 6. shared-assets source 迁移后一致性（6 组 targets 与新 source 逐字一致）
python scripts/sync-shared-assets.py && bash scripts/check-shared-files.sh

# 7. 端到端纸面走查（盲区 #7，AI 按清单四景）
#   ①无对标新书：逐章写作 K=1（首章全量 A+第一张专项）→ K=3（批中轻量 A'+批末核对+滚动补纲预告）
#   ②有对标书：A' 三个每章差异项不漏（按章选模块/挑匹配章/场景卡路由）；对标铺章/evaluator 两档不受车道合并影响
#   ③改稿两路：文字打磨路（deslop 纪律在位）/问题修复路（工单→级联→复审闭环）
#   ④老书续写：旧 CLAUDE.md 提 deslop 不炸（技能入口消失但文件内引用仅提示性）
```

## 提交规范

单提交：`b95: 写作模式重构——单章+日更并统一逐章写作车道（K 参数/删 workflow-chapter）+A 段分级（批中轻量 A'+三个每章差异项）+三遍法批内时序+滚动补纲预告+改稿两路（文字打磨[deslop 收编方法论并入]/问题修复）+删 moshu-deslop 技能 12→11（shared-assets source 迁 write+marketplace/CLAUDE.tmpl/review 四出口涟漪）+chapter-core 四处去重归位+E 组章纲打磨补强`

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（#058/#059 裁定链+实测空窗声明） |
| 2 | 预算余量实测 | 条件——workflow-daily 重构（吸收 chapter 特有补充+分级改写，预估净 -800~-1500：删 D1 加载表/补建流程迁出）；anti-ai-writing 增节（+400~600）；workflow-revision 重构（+600~900：文字打磨路迁入）；chapter-core 净减（-300~-500：双写合并/归位收口 vs 分级声明+批内时序+E3）；删 3 文件预算项；flash 守卫口径前后值回填 |
| 3 | 涟漪面（含 .json/互引） | 通过——.json 实测命中 4 契约+doc-budget+shared-assets（source 迁移是本批最大暗礁，D3 专项）；agent 模板 5 处引用改造；互引新增 workflow-revision↔anti-ai-writing |
| 4 | 共享副本声明 | 条件——deslop 为 6 组 source：先迁 source 至 write 再删目录；sync+指纹；CLAUDE.md.tmpl 改指纹重登记 |
| 5 | 跨技能接口 | 通过——review 4 处出口/volume outline-workflow 引用改造；验收 7 四景含跨技能走查 |
| 6 | 存量兼容 | 通过——零 agent 变更无 bump；.story-deployed 不受影响；书内旧 CLAUDE.md 漂移可接受；追踪协议零改动 |
| 7 | 端到端走查 | 通过——验收 7 四景（含**有对标书专项**——作者质询产出的双路兼容验收） |
| 8 | 输出幂等性 | 通过——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫+回归，Windows 零网络 |
| 10 | 热路径字数报告 | 条件——workflow-daily/chapter-core/anti-ai-writing/workflow-revision 前后值 flash 回填 |
| 11 | token 成本核算 | 通过——批中章 A' 省文件重读（情绪模块/节奏/文风/对标产物 4-6 文件/章）；删 1 workflow 文件+1 技能的注意力面；改稿文字打磨路复用 write 上下文（省 deslop 独立会话冷启动）；净收益正（纸面推演，实测三期验证） |
| 12 | 决策树过闸 | 通过——净删 9 文件（workflow-chapter+deslop 8）零新增；两路改稿挂既有 revision 文件；E 组零新文件 |
| 13 | 部署面改动联动 | 通过——零 agent 模板变更（第七查 diff 对照）；CLAUDE.md.tmpl 行改+指纹重登记（无 bump，B88-90 先例）；marketplace.json 条目删除 |
| 14 | 概念分离后术语考古 | 条件——「日更/单章/大修」旧词全仓 grep 三分类（统一车道内合法保留[触发词照旧]/历史/误用零容忍）；新词「逐章写作/改稿两路/文字打磨路/轻量 A'」随批登记 |
| 15 | 本批特设：文件名沿用决策 | workflow-daily.md/workflow-revision.md 文件名保留、语义改内容——30+ 引链（含 4 契约+5 agent 模板）的改名成本 vs 纯语义收益，判定不值得；标题与头注显式声明新语义，防后续混淆 |
