# 规格 · 批 B71：题材库系统集成（catalog 接线 + 防撞共性源 + scan 覆盖 + 数据降档）

- 版本：v1.1（2026-08-28 补第六件「采风前置查库」——作者质询「素材库和采风会不会重复」引出：共性 query 出网检索会取回题材卡已有内容，加前置库匹配收窄检索范围。v1.0 来源：作者裁定立项「按推荐方案做 catalog 接线」。**目的：把上游题材库从"被动消费"升级为全系统集成**——catalog 去孤儿化、防撞获最强共性信号、scan 获覆盖检查、上游数据按研究纪律降档）
- 性质：五处接线/声明（跨 build/analyze/scan/cold-path/索引×3 副本 sync）+ B69 规格补一行。单批单提交，零 schema 零 agent 变更。

## 一、设计依据（五个缺口的实证，grep 可复核）

| # | 缺口 | 证据 |
|---|---|---|
| 1 | **catalog 孤儿化**：自述"拆文和写大纲时对照使用"，analyze/build 双侧零引用 | build Stage 1 只用 core-mechanics+索引（workflow-build:162）；analyze-workflow 无 catalog 引用 |
| 2 | **防撞共性判定缺题材卡信号**：cold-path:104 "≥2 参照源同现→共性"——参照源仅拆文库/采风 | 题材卡=「该题材所有书共有套路」知识库，是最强共性证据源 |
| 3 | **scan 无覆盖检查**：genre-trends:61 有"新题材观察项"概念但从不对照库 | 32 卡覆盖不到的新梗永远无人发现 |
| 4 | **上游数据未降档**：32 卡证据摘要（"1837 本样本"）系上游快照不可复现 | 研究纪律 §7：不可复现数据=【文档宣称】，须显式声明 |
| 5 | **confidence 无维护**：上游标定置信度会过期 | 挂 B69 settings 型复核（B69 未施工正好赶上） |
| 6 | **采风出网前不查库**（v1.1）：query 直接出网，共性问题检索回题材卡已有内容——功能边界是「库管共性底线/采风管特性参照」，但缺前置分流实现这个边界 | caifeng-methods 检索流程零前置匹配步骤 |

## 二、现状事实（flash 开工前复核）

1. `skills/moshu-build/references/workflow-build.md` 实测 **18793/18800 贴顶**（审计候选 2）——本批 #1 增量 ~45 字：**先压缩既有内容 ≥45 对冲（净零），压缩不足才调 18800→18900**（why 记审计候选 2 联动判因）。Stage 1 轮 1 定调段约 :22 区（「轮 1 定调+参考偏好采集」:79）。
2. `skills/moshu-analyze/references/analyze-workflow.md`（预算 15500）：Stage 1「确认拆解对象+进入管道」约 :5。
3. `skills/moshu-build/references/cold-path.md` 判定规则行约 :104（未登预算，按需冷路径）；「防撞对照协议」节。
4. `skills/moshu-scan/references/topic-decision.md`（选题决策.md 输出模板，未登预算）+ `genre-trends.md:61` 新题材行；scan SKILL 3466/3600。
5. `skills/moshu-write/references/genre-prose-cards.md`（索引 3076，**shared-assets 三副本**——write 源改后 sync）：头注区+高置信/中低置信卡表（含别名匹配词列——scan 覆盖检查的数据源）。
6. B69 规格（已入库未施工）settings 型准则节。

## 三、文件级改动清单

**1. catalog 接线两处（作者裁定：接线不精简）**：
- **build Stage 1 轮 1**（定调段加 ~45 字）：「题材初判走 [genre-catalog.md](genre-catalog.md) 路由表——按故事核心快速定位题材框架（如'重生复仇/追妻火葬场'），框架结论写入题材定位.md 头部`题材框架`行，供单元卡与对标参照分流」；
- **analyze Stage 1**（确认拆解对象时加 ~50 字）：「对照 genre-catalog 路由表初判本书题材框架，标注进拆文产物元数据头（`题材框架：{框架名}`）——供 build 选对标与防撞多源判定消费」。

**2. 防撞共性第三信号源**（cold-path 判定规则节加 ~40 字）：「元素记载于题材对应卡（genre-prose-cards 匹配卡）→ **题材共性**，降噪音注记（题材套路非渗透——与 ≥2 参照源同现的共性判定并列的第三信号源）」。

**3. scan 覆盖检查候选**（topic-decision.md 输出模板加一行 + genre-trends 新题材行补半句）：选题决策.md 模板加 `| 题材库覆盖 | 已命中卡{名} / 未命中（库外题材——可考虑补卡，作者裁决） |`——对照 genre-prose-cards.md 索引别名表匹配榜单/选题题材词；未命中只呈报候选，**不自动生成卡**。

**4. 索引头注降档声明**（write 源 + sync 三副本，~70 字）：「**证据摘要口径（B71）**：各卡尾部样本数据（可用本数/抽样/占比）系上游项目快照，语料不可复现——按【文档宣称】使用，作参考量级不作精确依据；置信度为上游标定，可由 settings 评审复核（B69）」。

**5. B69 规格补一行（v1.1）**：settings 型作者维度加「题材卡置信度复核——本书主题材卡的置信度标注与实测写作体验是否相符（偏高/偏低/相当，呈报）」。

**6. 采风前置查库（v1.1 第六件）**：caifeng-methods.md 检索流程节（query 组装后、spawn researcher 前）加规则（~60 字）：「**前置查库**：query 先对 [genre-catalog.md](genre-catalog.md) 路由与 [genre-prose-cards.md](genre-prose-cards.md) 索引别名表匹配——命中共性内容则收窄检索范围为特性层（库已给共性，query 改写为本书特殊需求）；未命中才全量检索」——分流规则与「库管共性/采风管特性」的功能边界显式对齐。

## 四、验收命令（fixture 放 `/.tmp/tests/B71/` 用完即删）

1. 接线 grep：workflow-build 轮 1 含 genre-catalog 引用+题材框架行说明；analyze-workflow Stage 1 含 catalog 对照+产物标注；catalog 头部「用途」自述与实际消费点一致化（若自述措辞需微调随批）。
2. 防撞：cold-path 判定规则含「题材共性」第三信号源；既有两信号源文字不动。
3. scan：topic-decision 模板含「题材库覆盖」行；用索引别名表做两例匹配演练（「都市系统」→命中都市脑洞卡；虚构「规则怪谈」→未命中出候选）留证走查记录。
4. 降档：索引头注声明 grep；`bash scripts/check-shared-files.sh` 三副本绿（sync 后）。
5. B69 spec settings 含置信度复核句。
5b. 采风前置查库：caifeng-methods 检索流程含前置匹配规则；匹配演练两例留证走查（共性问题「都市脑洞常见爽点」→命中卡收窄；特性问题「冷门题材的代价设计」→未命中全量）。
6. 预算：workflow-build **先压缩对冲**（净零优先），不足则 18800→18900（why 记审计候选 2 联动）；analyze-workflow ≤15500；scan SKILL ≤3600；守卫矩阵全绿。

## 五、禁止事项

1. **32 张卡零改动**（降档用索引全局声明，不逐卡标注）；补卡不自动（scan 只呈报，新卡生成走作者裁决+独立批）。
2. catalog 单源不复制（接线用链接引用）；防撞既有两信号源判定文字不动（只增第三源）。
3. 置信度只复核不改动（settings 型呈报，改标注走作者裁决）。
4. 零 schema / 零 agent / agents_version 不动。

## 六、预算耦合

workflow-build 18793+45 → **压缩对冲净零优先**（贴顶纪律+审计候选 2），不可行则 18800→18900（why：B71 catalog 接线为批核心+审计候选 2 联动处理）；其余界内（analyze 15500 / scan SKILL 3600）；索引与冷路径未登（按需）。

## 七、提交规范

`feat(多域): B71 题材库系统集成——catalog 去孤儿化（build 轮1 框架路由+analyze 拆文标注）+ 防撞第三共性信号源（题材卡）+ scan 库外题材覆盖候选 + 上游证据数据降档声明（索引三副本）+ B69 补置信度复核 + 采风前置查库（共性 query 收窄特性层）`

## 八、风险与回滚

风险 1：catalog 框架与卡/单元卡的层次混淆——规格已显式三层（catalog 管题材框架路由/卡管题材味/单元卡管本书切分），接线措辞含分工说明。
风险 2：scan 误报库外题材（别名表未覆盖但实际近亲）——候选语义+作者裁决兜底；实测校准别名表。
回滚：五处独立接线，revert 单提交即恢复；索引声明随 sync 回滚。
