# 批B94：标尺合并与接线修复批

> **目的**：治实测三期作者确认的结构性冗余——「目标态声明三源并行」（理想书评↔核心设定读者偏好↔虚拟对标，节奏/情绪目标各存在 2-3 处副本）与三处流程接线缺陷（B91 第一张专项悬空、daily 悬空指针、D3 审查定位缺失）。**理想书评+虚拟对标收口为核心设定「成品标尺」段**，四轮式开场简化为三轮，作者过目 2 次并 1 次；净删 2 个模板文件，零新增。
>
> **作者裁定（2026-08-31/09-01）**：①理想书评并入核心设定（摒弃独立产物）；②同类流程冗余一并清；③D3 加定位声明；④本段定名「成品标尺」（弃「理想书评」名——实测反馈名不达意、与其他产物混淆）。
>
> **不学清单对照**：不触发（零新机制、零新管线、零数据库/快照/框架——纯收口批）。

## 现状事实（施工前逐条 grep 复核；行号为起草时实测，以 grep 为准）

1. **三源冗余**：节奏目标存于 ideal-review-template（三维度②）、virtual-benchmark-template（三类①）、有对标时拆文 节奏.md 三处；情绪目标存于 ideal-review（③）、core-setting-template「读者偏好」段、virtual-benchmark（②）三处。
2. **同一问题问三遍**：workflow-outline.md:107（轮 1 开场"给读者什么感觉"）、:137（轮 2 问 D）、:149（轮 3 子问题①）。
3. **归因三问写三遍**：workflow-outline.md:127（轮 1 step5 完整版）、:152（轮 3 注记）、:165（轮 4 注记）——:152/:165 为逐字复制短版。
4. **第一张专项三向悬空**：workflow-daily.md:9-14 存块且自注「日更续写跳过」（:11）；声称入口 outline-workflow.md:19 停靠无回指；workflow-chapter.md 全文未提及。单章车道（写第 1 章的实际车道）加载不到该块。
5. **悬空指针**：workflow-daily.md:120「按 SKILL.md Stage 3 的新版细纲模板」——SKILL.md Stage 3 已是墓碑节，模板实际在 write 侧 outline-workflow.md:33-84。chapter-core.md:11（A1）内联细纲字段清单，已滞后于模板新增字段（伏笔操作/时间锚点/契约风险/本章涉及设定未列）。
6. **D3 审查隔离张力**：chapter-core.md:230 narrative-writer 既写正文（B 段 spawn）又做去 AI 味审查（D3），无定位声明。
7. **消费面实测（grep 全仓）**：`理想书评` 命中 outline（SKILL:13,23 / workflow:多处 / idea-seed:41-47 / skeleton:46）、volume（volume-workflow:85,112,134,260,270 / progress-template:11,12,31）、setup（evaluator 模板:68,87,139,152 / UPGRADING:26 历史）、write（idea-seed:41 / chapter-core 经虚拟对标间接）、scripts（spawn-contracts:186 / test-check-outline:67）；`虚拟对标` 命中 outline（workflow:7,59,167-172,191,201,207,217）、volume（volume-workflow:9,52,101,130,140,261,314,325 / caifeng-methods:214-226 / cold-path:53 / progress-template / virtual-benchmark-template）、write（chapter-core:39-40）、scripts（check_outline:342-354 / test-check-outline:239-257 / doc-budget why 历史注记）、docs/治理/作家心流模型.md:11。
8. **预算现状（check-doc-budget 守卫口径）**：workflow-outline 10100/实测 10030（余 70）、volume-workflow 14800/14721（余 79）、chapter-core 21600/21563（余 37）——三处近零；core-setting-template / caifeng-methods 不在册（冷路径按需加载）。
9. capability-wiring.json 零命中（无 benchmark 概念登记）；CLAUDE.md.tmpl 不含两产物名（零改动，指纹随 bump 重登记）；check-methodology-wiring.py 不引用两模板（删除不红）。
10. shared-assets.json:341 idea-seed-reference 组：write 源 → outline 副本（改后须 sync+指纹）。
11. agents_version 现值 45（UPGRADING.md 头部）；evaluator 模板为 B55 三档参照（①benchmark_book_paths ②virtual_benchmark_path ③benchmark_path）。

## 文件级改动清单

### A. 大纲侧（合并主体）

**A1. `skills/moshu-outline/references/core-setting-template.md`**——新增「成品标尺」段（第 8 段）：
- 段头注：B94 合并段——原理想书评三维度+虚拟对标三类收口；落盘进 `设定/题材定位.md`「成品标尺」节。
- 撰写口吻（方法论迁移自《小说的骨架》第 2 章理想书评萃取）：以「完全读懂成品的评论家」视角推演目标态再落字段——具体可测（拒绝「好看/流畅」级空词），每条能对照产物打分。
- 三组字段：**节奏目标**（期待密度/释放频率/爆发密度/爽点循环/追读锚点+更新适配一句，与轮 2 问 E 联动）；**情绪基准**（核心情绪/首卷感受/交替模式/低压容忍[连续 N 章不爽可接受线]/禁区[轮 2 问 F 雷点在此登记]）；**结构要点**（结构自评 1-10+一句依据/中点安排/对手升级节奏/伏笔密度）。
- **填充两路**：无对标——轮 3 由多源采风产物合成（定向参考优先，同题材泛化补充）；有对标——登记锚定指针「对标锚定：{书名}，权威数据见 `对标/{书}/剧情/节奏.md` 与 `情绪模块.md`」+三句关键锚点简述（不复制全量）；两路皆可标「未消费采风·按信号推演」（作者跳过采风时）。
- **消费方**一行：大纲终审/卷 Phase B evaluator 打分锚点（benchmark_path 即本文件）/write A 段无对标 fallback/骨架表「读者承诺」列承诺来源。

**A2. `skills/moshu-outline/references/workflow-outline.md`**——三轮化+去重：
- 头注 :7 虚拟对标块 → 一句「成品标尺（B94）：目标态三源收口为核心设定标尺段——节奏/情绪/结构；无对标由采风合成，有对标锚定拆文产物」。
- 开场白表 :17 Stage 1 行：产出改「核心设定三文件（含成品标尺）」、作用列删「理想书评」字样；:22 交互点数 ~8-12 → ~7-10。
- 方法论索引 :38-55：删 ideal-review-template 行；idea-seed 行括注改「灵感种子收敛」（理想书评口吻方法论已迁 core-setting-template 标尺段）；索引引言加半句「Stage 2/3 必读方法论（outline-causality/character-construction/skeleton-template）在对应 Stage 节内联链接」——补全改口，不另列三行。
- Stage 1 故事面编号清单（:71-89）：压成五行纯导航（选题决策/灵感种子→轮 1 定调采风→轮 2 定形→轮 3 定流程+落盘→对标面），细则只留轮块；「执行归属」注记保留。
- **前置检查（:76）**：绑定点从「进理想书评」改「轮 3 标尺段织入前」，语义不变（ls 采风-CF / 等 / 跳过标注）。
- **删原轮 3 块（:146-155）与轮 4 块（:157-165），合成新轮 3**（定流程+落盘）：档位问（原轮 4 一问）→ 前置检查 → 参考三吸引点（可选深化；预填路径 B84 保留：作者说「拟一版我来改」→跳过采集，按轮 1 参考+轮 2 定形+采风要素起草）→ AI 按模板起草核心设定表（成品标尺段以评论家口吻推演）→ **成稿展示作者过目（一次替代原两次）**→ 确认后落盘三文件+创建最小创作进度+方法论声明补全（档位一行+三域默认档位表）→ 无对标时标尺段即采风合成落点（CF 票据标「已消费·合成成品标尺」；有对标登记锚定指针）。
- 轮 1 :116「三吸引点在轮 3 理想书评采集」→「在轮 3 标尺段采集（可选深化）」。
- 轮 2 问 D（:137）：加预填确认——轮 1 已捕捉明确感觉时选项预填「（推荐）」标注该基调。
- **归因三问单点化**：保留 :127 完整版；删 :152、:165 两处逐字复制。
- **删轮 4 后虚拟对标合成块（:167-170）与 :172 模板指注**（并入轮 3）。
- Stage 2 :191 与 Stage 3 :201「虚拟对标参照」→「题材定位·成品标尺参照」。
- 大纲终审 :207：benchmark=理想书评 → benchmark=`设定/题材定位.md`（成品标尺节）；删 virtual_benchmark 参数。
- 收官产物地图 :209-219：五行并四行（删理想书评/虚拟对标两行；核心设定行注「含成品标尺」）。

**A3. 删除 `skills/moshu-outline/references/ideal-review-template.md`**（要义浓缩进 A1 标尺段引言）。

**A4. `skills/moshu-outline/SKILL.md`**——产出清单（:13）删理想书评、题材定位注「含成品标尺」；模板指引（:23）删 ideal-review 行。

**A5. `skills/moshu-outline/references/skeleton-template.md`**——「理想书评承诺」列（:46）→「读者承诺」列（列语义不变：兑现章数+兑现方式；承诺来源=成品标尺）。

### B. 卷侧涟漪

**B1. `skills/moshu-volume/references/volume-workflow.md`**——:9 头注块、:52 开新卷重配置、:85 Phase A 概览、:101 适配层、:112/:134 六步表（删「虚拟对标合成」字样→「标尺合成」并入核心设定）、:140 双路表（虚拟对标列→成品标尺）、:260 benchmark_path 改 `设定/题材定位.md`（含成品标尺节）、:261 删 virtual_benchmark_path 行、:270 两档说明改一档（对标拆文最高/成品标尺次之）、:308-316 打磨维度表锚点（虚拟对标→题材定位标尺段）。

**B2. `skills/moshu-volume/references/caifeng-methods.md`**——「第五步：虚拟对标合成」（:214-226）→「第五步：成品标尺合成（B94；无对标时在轮 3 执行，有对标登记锚定指针跳过）」：落点改 `设定/题材定位.md` 成品标尺节；CF 票据标「已消费·合成成品标尺」；write 侧消费注记改「标尺节供 write A 段 (a)(b) 无对标 fallback（chapter-core 消费）」。

**B3. `skills/moshu-volume/references/progress-template.md`**——产物清单 :11-12 与产物版本表 :31：理想书评+虚拟对标两行并一行「题材定位（含成品标尺节）｜v2.0」。

**B4. `skills/moshu-volume/references/cold-path.md`**——:53 开新卷重配置行同步（核心设定标尺保留）。

**B5. 删除 `skills/moshu-volume/references/virtual-benchmark-template.md`**。

### C. write 侧涟漪与接线修复

**C1. `skills/moshu-write/references/chapter-core.md`**：
- A3(a)（:39）/A3(b)（:40）无对标 fallback：`设定/虚拟对标.md` → `设定/题材定位.md`「成品标尺」节（情绪基准/节奏目标对应取）；`selected_emotion_module`/`rhythm_reference` 记「成品标尺」；**老书回退**：标尺节缺失且存在旧 `设定/理想书评.md`/`设定/虚拟对标.md`（B94 前产物）时读旧文件并注明来源世代（读失败三分类显式声明）；皆无则按现行「从本书细纲/卷纲/题材定位取，记『无』」不变。
- A1（:11）：删内联字段清单，改一句「补齐 write 侧 outline-workflow『细纲（第 N 章）』模板全部必需字段」。
- D3 头部加定位声明：「**定位声明（B94）**：本步是 narrative-writer 的工艺自检（7 Gate+删除优先），不是独立评审——创作者与审查者同源时深度有限；跨产物独立评审走 `/moshu-review`（四 reviewer+审稿令牌）。轻量自检与深度评审不互相替代。」

**C2. `skills/moshu-write/references/workflow-daily.md`**：删第一章专项块（:9-14，整体移 C3）；:120 悬空指针改「按 [outline-workflow.md](outline-workflow.md)『细纲（第 N 章）』模板补建」。

**C3. `skills/moshu-write/references/workflow-chapter.md`**——「单章特有补充」节新增「第一章专项（B91→B94 接线修复）」块：多版本开头（2-3 版 300 字级，悬念/画面/对话切入，作者选一版续写；「直接写」跳过）+ 前 300 字专项打磨（三问句）；触发条件=本章为第 1 章首写；日更续写跳过。

**C4. `skills/moshu-write/references/outline-workflow.md`**——:19 停靠报告句加一行「写第 1 章时走 workflow-chapter『第一章专项』（多版本开头+前 300 字打磨）」。

**C5. `skills/moshu-write/references/idea-seed.md`（shared 副本，源）+ `skills/moshu-outline/references/idea-seed.md`（副本）**——:41-47 理想书评注记块改写：方法论保留（评论家口吻/具体/全面/满足溢美三要求转为字段纪律），落点改「核心设定表『成品标尺』段（core-setting-template v2，B94）」；三问（准入）调用点改「轮 3 核心设定起草时」。sync 双副本+指纹。

### D. 治理与脚本

**D1. `skills/moshu-setup/references/templates/agents/moshu-evaluator.md`**——B55 三档参照改两档：①benchmark_book_paths（不变，最高）②benchmark_path=`设定/题材定位.md`（成品标尺节——节奏/情绪/结构三组目标；节缺失时降级泛化判断并在 research_needed 标注）；删 virtual_benchmark_path 参数及三个维度块（责编/作者/读者）中对应档位行；:152 协议行与 score 注记（:139）「理想书评」改「题材定位·成品标尺」→ **agents_version 45→46（bump-agents-version.py --confirm）+ UPGRADING.md v45→v46 条目 + 指纹重登记**。

**D2. `skills/moshu-outline/scripts/check_outline.py`**——B53 candidate（:342-354）锚点改：`设定/虚拟对标.md` → `设定/题材定位.md` 含标尺锚点关键词（「节奏目标/爆发密度/爽点循环/低压容忍」任一命中）且 `大纲/大纲.md` 零引用时出 candidate「题材定位成品标尺未被大纲引用」；candidate 语义与退出码不变。行为注记：B94 前老书（无标尺节）检查自然跳过；旧 虚拟对标.md 不再被探测（advisory，无消费方依赖）。

**D3. `scripts/test-check-outline.py`**——fixture 同步：:67 骨架表「理想书评承诺」→「读者承诺」；:239-257 两用例改 题材定位.md 标尺锚点 fixture（零引用→candidate exit 0；大纲显式引用锚点→不出 candidate）。

**D4. `scripts/spawn-contracts.json`**——:186 evaluator notes「对照理想书评打分」→「对照题材定位·成品标尺打分」。

**D5. `docs/治理/作家心流模型.md`**——:11 十环表①脑内读者行「理想书评」→「题材定位·成品标尺」。

**D6. `scripts/doc-budget.json`**——施工后按守卫口径回填三热路径实测值与预算变更（本批方向为减，见盲区 #2）。

## 禁止事项

1. 不动 tracking schema、追踪文件、moshu-review 技能。
2. 不删书项目内旧产物（skill 只是不再产出；读取侧回退链保老书）。
3. candidate 永不拦截语义不变（check_outline 改锚不改档位）。
4. **不做瘦身批四项**（chapter-core 内部三副本去重/每遍方法论书双写/字数验收双写/D1 表压缩/core→daily 依赖归位——留 v2.6.0，同一文件不两遍施工）。
5. 不新增脚本/文件（净删 2 个 references 模板）；三遍法 A-D 段结构不动（D3 只加声明）。
6. templates/agents/ 只动 moshu-evaluator.md 一个（第七查 git diff 对照）；CLAUDE.md.tmpl 零改动。
7. 禁止改断言变绿：test-check-outline fixture 改动只跟随产品语义（锚点迁移），不弱化断言。

## 验收命令（Windows 探测链 python3→python→py；零网络）

```bash
# 1. 文件删除确认
test ! -f skills/moshu-outline/references/ideal-review-template.md && test ! -f skills/moshu-volume/references/virtual-benchmark-template.md && echo OK-DEL

# 2. 残留清点（产出=逐行核对：仅允许 ①chapter-core/evaluator 老书回退链 ②UPGRADING/CHANGELOG/规格/施工日志/审核记录历史 ③doc-budget why 历史注记）
grep -rn "理想书评" skills/ scripts/ | grep -v "回退\|老书\|B94"
grep -rn "虚拟对标" skills/ scripts/ | grep -v "回退\|老书\|B94"

# 3. 回归
python scripts/test-check-outline.py          # OK 尾行
python scripts/test_methodology_wiring.py     # 全绿
python scripts/check-methodology-wiring.py    # 按 README 口径

# 4. 共享副本（idea-seed 组 sync 后）
bash scripts/check-shared-files.sh

# 5. 数字守卫（agents_version 46 一致性）
bash scripts/check-story-numbers.sh

# 6. 预算
bash scripts/check-doc-budget.sh

# 7. 第七查：templates/agents/ 仅 evaluator 一文件 diff
git diff --stat -- skills/moshu-setup/references/templates/agents/

# 8. bump（45→46 + UPGRADING 条目 + 指纹重登记）
python scripts/bump-agents-version.py --confirm

# 9. 端到端纸面走查（盲区 #7，AI 按清单）
#   ①无对标新书：轮1(采风 spawn)→轮2→轮3(前置检查等待→标尺织入→一次过目→落盘三文件+创作进度) 产物齐全无断链
#   ②有对标新书：轮3 标尺段=锚定指针；大纲终审 benchmark=题材定位
#   ③老书续写：题材定位无标尺节→chapter-core 回退读旧 理想书评/虚拟对标 并注明世代
```

## 提交规范

单提交：`b94: 标尺合并与接线修复——理想书评+虚拟对标收口为核心设定「成品标尺」段（四轮→三轮/过目2→1/归因三问单点化/情绪基调问收敛/前置检查平移）+第一张专项搬家接线+daily 悬空指针+D3 审查定位声明+evaluator 两档参照 agents_version 46+check_outline 锚点改题材定位+读者承诺列名+老书回退链`

## 设计说明（盲区对照）

| # | 项 | 结果 |
|---|---|---|
| 1 | 目的声明 | 通过——头注（三源冗余收口+接线修复，作者四项裁定） |
| 2 | 预算余量实测 | 条件——三热路径余量 70/79/37 近零；本批方向为减：workflow-outline 删轮 3/4 合并+删复制+索引改口（预估守卫口径 -600~-900）；volume-workflow 多处单行改（-150~-250）；chapter-core A1 删清单 vs D3 加声明（±0~-100）；core-setting-template/caifeng-methods 不在册；flash 施工前后守卫口径实测回填 doc-budget，超限压缩对冲 |
| 3 | 涟漪面（含 .json/互引） | 通过——.json 实测命中 spawn-contracts:186 与 test-check-outline fixture（capability-wiring 零命中）；删 2 模板后引用方全在改动清单内；互引新增 workflow-chapter↔outline-workflow（同技能内） |
| 4 | 共享副本声明 | 条件——idea-seed.md 双副本（write 源/outline 副本）：sync+指纹重登记 |
| 5 | 跨技能接口 | 通过——题材定位.md 格式变化，消费方 outline/volume/write/evaluator 四走查（验收 9 三景覆盖） |
| 6 | 存量兼容 | 通过——老书两产物不删；chapter-core/evaluator 回退链显式；check_outline 无标尺节自然跳过；标尺段全可选；兼容四原则全过 |
| 7 | 端到端走查 | 通过——验收 9 纸面三景（无对标/有对标/老书续写） |
| 8 | 输出幂等性 | 通过——无时间戳工件 |
| 9 | 验收可复现 | 通过——grep+守卫+回归，Windows 探测链零网络 |
| 10 | 热路径字数报告 | 条件——workflow-outline/volume-workflow/chapter-core 前后值由 flash 回填（本批预期净减） |
| 11 | token 成本核算 | 通过——evaluator spawn -1 参数（virtual_benchmark_path）+模板净减 ~200 字/次；开书交互 -1 轮；作者过目 -1 次；净收益正 |
| 12 | 决策树过闸 | 通过——净删 2 文件、零新脚本/零新 agent/零新机制（收口进既有模板段） |
| 13 | 部署面改动联动 | 条件——evaluator 模板改动 → bump 45→46+UPGRADING 条目+指纹重登记（bump --confirm）；其余 templates/agents 零 diff（第七查）；CLAUDE.md.tmpl 零改动 |
| 14 | 概念分离后术语考古 | 通过——「理想书评/虚拟对标」全仓 grep 三分类处置（回退链保留/历史保留/误用零容忍）；新词「成品标尺」随批登记；「读者承诺」列名替换骨架表旧列名 |
