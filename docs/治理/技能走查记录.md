# 技能走查记录（append-only，每技能一节）

> 依据《产品需求与详细设计文档》Ⅲ.19 标准（12 维+五步式迹线，v2 标准）；双产物之一（另一为产品文档修订）。问题分级：阻断/需修/候选/亮点。

## moshu-setup（2026-08-25 · 首查）

- **执行迹线**：触发→版本展示（读 skills/moshu/VERSION+agents_version）→参考包自检（一条 ls）→状态四查（sentinel/书名目录/settings/.active-book）→版本三分支→AskUserQuestion 部署位置→deploy.py deploy（内部 8 步，子依赖 find_python 探测与 merge-claude-settings.py 原子合并）→verify 八项→安装报告（重启提示+路由推荐）。详见对话走查。
- **11 维结论**：①触发四面齐（命令/中文/自然语言/README 引导）②三 Stage 结构清晰，交互模态合规（封闭问弹窗×2、CONFLICT 处置弹窗）③依赖链=SKILL.md→deploy.py→{templates 25 文件,merge-claude-settings.py,agent-references 33+32}，冷路径 deploy-manual 108 行④产=hooks/rules/agents/CLAUDE.md/settings/sentinel，消费=Claude Code 运行时+全 skill 入口门+session-start⑤三层分工典范（部署确定性归脚本；AI 只做探测/弹窗/报告）⑥分支闭合：降级门禁 fatal/CONFLICT 不碰用户文件/脚本不可用走冷路径——T1-T5 行为实测背书⑦哲学符合（文件即真相 sentinel；不覆盖用户配置铁律实测）⑧标准符合（B32 Stage 化后）⑨实付低：SKILL.md 98 行薄热路径，合理⑩优化空间：重部署口径在 SKILL.md 与 deploy-manual 双源（低风险）⑪问题清单见下。
- **问题清单**：候选×1——「重新部署」节 SKILL.md:74-76 与 deploy-manual.md「重新部署」双份维护（漂移风险低，改一处需记得另一处）；亮点×3——同路径检测防符号链接自复制、CONFLICT 用户文件零接触（实测）、verify 失败非零退出（审计-V3 PM1 整改成果）。
- **反哺**：产品文档 Ⅲ.13/Ⅳ.19 经走查核对无失真，无需修订；本记录即沉淀。
- **v2 补查（五步式重走）**：候选 +1——交互内容规格化不均：①部署位置确认（SKILL.md 仅「使用 AskUserQuestion 确认部署位置」，选项与项目名/书名收集方式未规格化，AI 即席）②CLAUDE.md CONFLICT 处置（deploy-manual:100 仅「AskUserQuestion 让用户选择保留哪个版本」，选项粒度 AI 即席）——低风险（AI 即席内容语义明确），但与「版本三分支提示语逐字写死」的规格化深度不一致，记录为文档粒度改进候选。结构注记：setup 无独立 workflow 文件属有意变体（确定性占比极高，主流程即 deploy.py；热路径内联 SKILL.md+冷路径 deploy-manual）。

- **v3 结构统一（作者裁决，2026-08-25）**：setup 补建 references/setup-workflow.md（流程权威），SKILL.md 瘦身为索引+部署锚点（64 行）——与其他技能「薄入口+workflow」形态统一。断言锚点按 deploy-manual:175 既有惯例保留在 SKILL.md（递归复制注记/sentinel 字段块/三分支摘要含「小于·大于 `34`」/「8 个 agents」字面），TS 套件零改动（本地 python3 假红停于 :188 为已知项，TS10 三正则手动复验通过）。附带修复：契约「8 个 agents」断言字面随迁补回。走查判据注记：三条拆分信号（超预算/lane 分叉/语义密度反转）当时均未命中，本次统一为作者一致性裁决——判据价值不变，统一性收益（消除例外形态的认知成本）优先。

- **v4 触发面收缩（作者裁决，2026-08-25）**：技能触发固定两枚——/moshu-setup、「部署墨枢写作环境」（原四自然语言触发移除）；模糊意图由 moshu 路由表兜底（路由意图词追加规范短语、原模糊词保留——两层分工：路由管模糊、技能管精确）。同步面：SKILL.md description / README 触发列 / moshu 路由表；marketplace 与 README_EN 无触发词未动。注记：「墨枢」为项目正式中文名，品牌口径全产品统一另行裁决。

- **v5 概念澄清入册（2026-08-25，作者问答驱动）**：产品文档新增 Ⅲ.13「版本地图」（三实体图/四层表/两轴正交/Android versionCode 类比/装修类比/双向实例/四乱点挂账）；词典重写 agents_version 词条（纠正「三轨」误导性并列）、新增 SKILL.md 与触发两层分工词条。乱点四项按作者指示暂挂待裁决，未施工。

- **v6 独立复核走查（2026-08-25，flash 侧对照 PRD+FSD Ⅲ.12/13+Ⅳ.19 全量重走）**：12 维逐维重过 + 实测核数（git ls-files=97 与附录 A 一致；agent-references 33+32、hooks 12、agents 8、rules 4 全对上；verify 八项、sentinel 6 字段、settings-hooks 8 注册与 MANAGED_HOOK_SCRIPTS 8 对账全一致；check-agent-template-rules/check-agents-version-sync/check-claude-adapter/check-hook-regex-sync/check-hook-locale-safety 全 PASS）。**新发现 5 项（候选级，不阻断）**：
  1. **session-end 默认禁用未文档化**：实现为 `STORY_SESSION_LOG=1` 显式启用才写一行时间戳到已有追踪目录（session-end.sh:14-22）；README :147 与 PRD Ⅲ.12 均描述为「记录会话日志到 追踪/session-log.txt」，漏「默认禁用+显式启用」语义——文档-实现差异。
  2. **PRD Ⅲ.13「bump 脚本覆盖 7 类文件 48 处」数字过期**：bump-agents-version.py 自述「六类文件」；实测 34→35 预览 46 行替换（UPGRADING 历史条目排除后）——7 类/48 处均不符（清单类断言过期，仓库自有教训）。
  3. **detect-story-gaps 功能表不完整**：实现 6 项缺口（正文-设定失衡/伏笔异常/大纲缺失/拆文未完成/连续性 staleness+标题去重，detect-story-gaps.sh:22-123），README :148 与 PRD Ⅲ.12 仅列 3 项示意。
  4. **check-python-invocation 对 __pycache__/*.pyc 二进制误报**：本地跑过 deploy.py 后 pyc 在工作区，守卫 grep -r 扫 skills/ 命中二进制 → 本地恒红（CI 干净环境不红）——守卫应排除二进制/__pycache__（误报面，非平台问题）。
  5. **PRD Ⅲ.11 architect 行「契约摘要随 spawn 附带」在 agent 模板无对应**（模板「被调用协议」仅任务描述/文件路径/上下文摘要）——待 build 走查时核 spawn 侧实践再定性。
  另：check-moshu-setup-deployment.sh:188 裸调 python3 为 Windows 本地已知假红（v3 已注记），本次复核确认仍在；templates/.gitkeep 未跟踪且目录非空、占位符无功能（超小候选）。
  **反哺**：Ⅲ.12 session-start/end 行、Ⅲ.13 版本管理行、Ⅲ.12 机检/缺口描述、README :147-148 功能表共 4 处文档修订候选，攒批处理；architect 行待 build 走查验证。

- **v6 修复注记（2026-08-25，作者授权直改）**：CLAUDE.md.tmpl 路由表 2 处触发词与 description 对齐——`/moshu-import`、`/导入` → `/moshu-import`、「导入小说」（:12）；`/moshu-setup`、`/准备写书` → `/moshu-setup`、「部署墨枢写作环境」（:14）。判据：路由表命令非 description 触发词子串即不命中（v4 收缩同步面漏 CLAUDE.md.tmpl）。不 bump agents_version——先例 B32（4d47468）改 CLAUDE.md.tmpl 未 bump；CLAUDE.md.tmpl 不在 bump 六类管理面；版本地图「文档变更零误报」口径。验证：全仓 `/准备写书`、`/导入` 命令形零残留（moshu 路由表 :21「准备写书」为设计内模糊意图词保留）；check-claude-adapter/check-current-skill-contracts PASS；check-moshu-setup-deployment 仍为 :188 python3 已知假红（与本次无关）。守卫盲区注记：路由表命令 ∈ description 触发词无守卫断言（候选）。

## 旗舰侧裁决（2026-08-25，回应施工侧 4 项请示与 setup v6 五候选）

- **勘误注记（19e49dc）**：该提交的 SKILL.md 实际包含旗舰侧未提交的 v3（结构瘦身：内联三 Stage→薄壳+setup-workflow 引用）与 v4（触发词收缩 4→2）改动——同一文件无法拆分，整体入库，提交消息未涵盖 v3/v4 属记录性缺陷，以本注记补齐溯源（v3/v4 详情见本文件 v3/v4 条）。接受现状，不重写历史。
- **裁决 1（19e49dc 处置）**：接受现状（同上）。撤回重提需要 reset/rebase 改写已入库历史，破坏 append-only 精神且零收益。
- **裁决 2（旗舰批提交）**：由旗舰侧提交剩余 7 项（PRD+FSD v3.0 全书化+更名、architecture.md 吸收删除、README/导读/docs README/CHANGELOG/moshu 路由同步、走查记录建档 v1-v6）。等作者「提交」口令。
- **裁决 3（五候选处置）**：①session-end 默认禁用——**改文档不改行为**（默认关是合理设计，避免每会话写文件噪音；文档如实描述 opt-in：`STORY_SESSION_LOG=1` 才写）→ 修 PRD Ⅲ.12 与 README:147；②bump 覆盖数过期——**改软口径**（「散布全仓 40+ 处，以 bump 预览实测为准」，不写死数字——story-numbers 精神）→ 修 PRD Ⅲ.13/词典/版本地图三处；③detect-story-gaps 6 项缺口——**文档列全或标「等 6 项」** → 修 PRD Ⅲ.12 与 README:148；④check-python-invocation 排除 `__pycache__/*.pyc` 二进制——**真守卫缺陷需修**（本地恒红诱因），随走查修复批落；⑤architect「契约摘要」——**挂起至 build 走查核 spawn 侧实践再定性**（同意施工侧建议）。①②③+README 两行与④组为「setup 走查修复批」，施工侧按本裁决执行，验收含守卫矩阵绿。
- **裁决 4（守卫盲区候选）**：**纳入守卫化评估**——「CLAUDE.md.tmpl 路由表命令 ∈ 对应 SKILL.md description 触发词」断言缺失是本次残留根因，与「本地守卫矩阵对齐」「部署物变更未 bump 即红」同列守卫化 backlog，走 B42 式评估流程。
- **致注**：施工侧以 Ⅲ.19 标准独立完成 setup 12 维走查并抓出旗舰 v4 触发收缩的扫描面遗漏（CLAUDE.md.tmpl 未同步）——双侧走查交叉验证机制首次实战生效。

- **v8 修复批闭环（2026-08-25，旗舰复核通过后提交）**：四项修复一次入库 `4071c78`——①session-end opt-in 如实描述（PRD Ⅲ.12/README/README_EN 三处 `STORY_SESSION_LOG=1` 口径）；②bump 覆盖数改软口径「40+ 处以实测为准」（Ⅲ.13 版本管理行/版本地图表/乱点①③、Ⅰ.2 词典共 5 处）；③detect-story-gaps 六项列全（中英双语，英文行以 duplicate titles 等表述）；④check-python-invocation 排除 `__pycache__/*.pyc` 二进制误报（`-I`+`--exclude-dir`+`--exclude`，注释含原因，本地恒红根治）。验收：守卫矩阵 11 全绿、「48 处」活区零残留（仅历史记录豁免）、「40+ 处」恰 4 处。旗舰复核：四项全部通过（③英文行先前零命中系 grep 词语言不匹配非漏改）。⑤architect「契约摘要」维持挂起至 build 走查。setup 走查线全部收口（12 维走查→结构统一→触发收缩→版本地图→五候选处置→修复批）。

- **v9 Stage 2 深挖修复批（2026-08-25，旗舰裁决六项全修）**：A 空 CLAUDE.md 走生成（三分支重构：空文件覆盖/含节合并/纯自定义 CONFLICT）；B deploy-manual 合并策略写明头部替换行为（第 5 条）；C render 占位符残留 DeployFatal（`\{[^}]*\}` 校验）；D 删 deploy-manual marker 残留（:95 句 + 清单表 merge 模式改 section merge）；E 四 managed 目录 rmtree+重拷真 replace（same_path 跳过；UPGRADING 所有权表补清空重建警示；CHANGELOG 行为变更一条）；F verify agents 计数动态化（源目标一致）+ 守卫「templates/agents 计数 == manifest.agents_count」断言 + PRD Ⅲ.18 五问收口补 manifest 同步义务。回归：TS13（空 CLAUDE.md 生成 / 残留被清 / 占位符 fatal），本地 python 替代 python3 全绿（TS1-TS13）。
  **验收两发现**：①TS10「先自检参考目录」断言锚点漂移——v3 结构统一把自检段移入 setup-workflow.md 而断言仍指 SKILL.md，本地被 :188 假红挡在 TS10 前 + 提交未推送 CI 未跑， unflagged 至今，本地验收抓出并修复（锚点改指 setup-workflow.md）；②A 首修逻辑错误——`existing.strip() and SECTION_RE.search(existing)` 让空文件仍落 CONFLICT，TS13 抓出后改三分支重构。两处均证明「本地验收价值」：CI 未覆盖的路径必须本地跑通。
  评审注记：`check-moshu-setup-deployment.sh` 本地仍以 :188 python3 假红 FAIL（已知项，CI Linux 绿；TS13 同面）。
- **v9 回填（2026-08-25）**：Stage 2 深挖修复批提交 hash `4c880c7`（7 文件，95+/19-；旗舰复核通过含行为实测——空文件覆盖生成/残留清除/verify 全过）。Stage 2 深挖线闭环。

- **v10 TS 补测批（2026-08-25，旗舰六项指令 P1-P5）**：P1 same_path 灾难分支——test-deploy.py 新增 `test_same_path_skips_copy_without_rmtree`（monkeypatch AGENT_REFS=目标自身，断言不 rmtree 源+日志「同路径跳过复制」）+ TS13 补 agent-references stale 子卡残留清除断言；P2 模板命令 hook 名集合 == MANAGED_HOOK_SCRIPTS 集合双向差集断言（TS9 区）；P3 TS3 引用模式扩中文 + genre-prose-cards 子卡全集==索引所列断言；P4 TS12 负向（无标记不得确认注册）；P5 TS8b 四项缺口补测（正文-设定失衡/伏笔异常/大纲缺失/连续性 staleness+标题去重各正负 fixture）。验收：test-deploy.py 6 测全绿；TS1-TS13+TS8b 本地 python 替代 17 OK；守卫矩阵全绿。**红灯验证记录**：P2 独立红（MANAGED 加假条目→差集断言红，模板侧违规先被 TS2 幂等抓——双保险实证）；P3 红（移走子卡→断言红）；恢复后全绿。零生产行为变更（纯测试面）。注：工作区另有旗舰侧审计法 v1.8 升级（本批三段实践入 SOP：2e 存量能红验证/锚点漂移扫描/覆盖缺口三分类；纪律「假红遮蔽须 shim 跑完全套」即本批 TS10 发现的方法论化）——不在本批提交范围。
- **v10 回填（2026-08-25）**：TS 补测批提交 hash `c70ecfb`（3 文件，126+/1-；旗舰复核通过含亲跑 test-deploy 6 测 OK）。TS 补测批闭环。

- **v11 B48 守卫生命周期与去重（2026-08-25，规格批B48）**：Step 0 判因实证——8 个「未接 CI」实为 8 个 .sh 包装守卫的 .py 实现文件（同名 .sh 全部 exec 调用），非独立守卫；旗舰裁决口径修正（audit-guards 归并单条目，48 守卫口径）。施工：①audit-guards.py 重写——.py 有同名 .sh 时归并（旧口径 8 个误报「候选下线」文案风险随改）+ 注册表校验扩展（每个守卫 README 有行且事故出身/末次能红验证两列非空，缺即红，exit 1）；②README 守卫注册表扩两列（事故出身：按 B 系列/审计来源填，未知「—（未登记）」；末次能红验证：TS 补测批红绿验证过的 2 个填 2026-08-25，其余「未登记-待体检」），48 数据行，双文件实现保留「+ .py」标注；③开发标准 v1.3→v1.4（3.2 准入三条件→四条件：防线数第四问「≥2 道需作者特批」；4.2 补守卫退役规则——守护场景下线守卫必须同步退役，反模式 #2 的守卫版，不接 CI 即走豁免标注或退役裁决）；④审计法 v1.8 纪律节 2e 加元递归终点声明（2e 封顶，不再叠守卫/审计的元守卫）；⑤规格勘误 v1.1（Step 0 发现 + 「候选下线」误导文案风险）。验收：audit-guards exit 0（48 全在 CI、注册表两列全非空）；守卫矩阵全绿；TS13 并入 test-deploy 待作者终裁未动。零新增守卫（减法与治理批）。
- **v11 终裁补记（2026-08-25，作者+旗舰一致）**：TS13 并入 test-deploy.py **终裁=不并**——TS 套件 12 节 hook 行为测试保持内聚，deploy 行为测试单点化收益不足以拆散套件（§二.5 撤销，提交消息去 TS13 段）。
  **候选节（不施工）**：:188 假红根治候选——TS 套件内 python 调用（TS2/TS9/TS13 等）改 `python3→python→py` 探测形态（同 deploy.py find_python 模式，一行类改动），Windows 本地守卫即可全绿、不再依赖 shim 替换；列入候选待作者裁决。
- **v11 回填（2026-08-25）**：B48 提交 hash `931eb93`（6 文件，122+/74-；旗舰复核通过含补做能红验证——清空数据格→红→恢复→绿）。B48 守卫生命周期与去重批闭环。

- **v12 发版 v2.3.7（2026-08-25，作者授权）**：B31-B48 全量以补丁版号发布——CHANGELOG 定稿、VERSION/marketplace 2.3.7、agents_version 35、版本口径三处同步（README/docs README/PRD 2.7）、bump 覆盖面补 setup-workflow（v2.3.7 发版判因：v3 新增流程权威未登记，bump 首次守卫红回滚→补登记+test-bump fixture/断言同步→成功）。发布：966182f + tag v2.3.7 + release（三工作流全绿）。

- **v13 外部审核整改批（2026-08-25，审核结论：有条件通过——F1 阻断+同轮修复 F2/F3）**：F1 部署物指纹跨平台不收敛（阻断）——登记值绑定本机 CRLF 字节，CI Linux（LF 检出）必红（审核 blob 树实测复现：登记 03a824b8=CRLF 口径 vs LF 树 4bed2c2c）；修复=compute_deployment_fingerprint 换行归一化（CRLF→LF）+ 登记值改 LF 口径 + CRLF/LF 回归（同一内容两形态指纹相等）；F2 裸「等于 N」模式无回归锁定（fixture 带反引号命中既有模式）→ fixture 补裸形态+断言；F3 弹窗规格「--name 未指定用目录名」与 deploy.py required=True 不一致 → 措辞改「必填，弹窗留空由 AI 用目录名代入」（零部署物改动）；F4 verify 空模板假绿（all() 空迭代 True）→ bool(tpl_sh) 非空断言；F5 「∈ description」实为全文匹配 → 仅解析 frontmatter description 行；F6 路由表 0 行摆设绿 + 边界②反向断言（技能全集必须全进路由表，防新技能漏登记——build 漏行同族事故）→ 差集断言一并落地；F7 「等于 N」无词边界 → (?!\d)；F8 .gitkeep 豁免死代码注记（不改行为）；F9 test-bump 回滚未断言指纹还原 → 补断言。验收：三回归绿、守卫矩阵 8 全绿、反向断言红验证（删 build 行→红→恢复绿）、CRLF/LF 收敛验证。**判定注记**：整改批改 deploy.py（F4）触发指纹红（守卫正确工作）——按审核边界①口径（补漏修复不 bump + 状态自洽）重新登记整改后指纹（4a15883d），不 bump；若作者认为该 bump 可另行指示。
- **v13 补充（2026-08-25）**：指纹登记路径口径漂移候选**闭环**——bump 新增 `--re-register-fingerprint`（无版本变化的确认性重登记：预览提示→--confirm 执行→守卫红回滚还原，语义与版本 bump 一致；test-bump 回归：预览不改动指纹、确认后更新+守卫绿）。审核侧候选注记转闭环，不再留「手工改 contract」尾巴（docstring「由 bump --confirm 登记」口径随家族命令成立）。
- **v13 补充二（2026-08-25）**：指纹跨平台缺陷第二层根因——**Python 3.13+ pathlib 默认排序按 parts 比较（3.12 按字符串）**，CI（3.14/Linux）与本地（3.12/Windows）的 `sorted(set(files))` 聚合顺序不同 → 指纹不同（推送后 CI 红；判因过程：逐文件哈希全同、文件集全同（93=93）、行尾已归一化，仅聚合顺序异——经 parts 排序模拟复现 CI 值 574310bb）。修复：compute_deployment_fingerprint 全部排序显式 `key=relative_to(root).as_posix()`（跨版本/跨平台确定），重新登记 9e7061ef。指纹不一致时的逐文件 sha256 输出保留为正式差异定位辅助（判因过程证明其价值）。

## moshu-scan（2026-08-26 · 首查，flash 侧 12 维）

- **执行迹线**：触发（/moshu-scan 等 4 枚）→ Stage 1 平台方向问答 → Stage 2 采集（scraper 脚本/CDP 或用户提供或内置知识三模式）→ Stage 3 scan-analyze 确定性提取 → Stage 4 扫榜报告 → Stage 5 选题决策（四步+可行性三档）→ 选题决策.md（下游 build 3 层可达消费）。
- **12 维结论**：①触发 4 枚（2 命令+2 中文模糊）但形态与 setup 的 v4 收缩不一致（模糊词仍留 description——触发面逐技能收缩为整体候选，scan 是实例）②5 Stage 清晰，每 Stage 有 references 路由；③依赖链=SKILL.md→{collection-guide,analysis-guide,topic-decision,scan-output-format}；脚本链 scraper→cdp-utils→moshu-cdp；④产消：榜单 md→scan-analyze→报告→选题决策.md（build 消费）；⑤确定性边界：采集+提取脚本化（禁手写内联解析），分析/选题语义归 AI，决策归作者；⑥降级完备（采集三模式/CDP 不可用/内置知识标「未实时校验」/缺目录 exit 2 明示）；⑦哲学符合（确定性优先/候选不拦截/工具不审问）；⑧标准符合（test-scan-analyze/test-scan-runtime 在 CI、退出码三分类）；⑨热路径 SKILL.md 101 行+references 按需；⑩可优化：触发面收缩（整体议题）；⑪问题清单见下；⑫交互：Stage 1 开放采集→对话、Stage 5 收敛成单步决策（「只问一个决策」+不问计划字数——交互收敛的好设计）。
- **问题清单（候选 3 项，不阻断）**：①触发面形态不统一（模糊词留 description vs setup v4 收缩——整体议题实例）；②scan-analyze detectPlatform 识别失败默认「起点」（自定义文件头可能套错适配器——有警告兜底，影响仅分析维度）；③题材分布分类表写死 15 类（新题材归「其他」——可扩展或标注口径）。
- **亮点**：字段缺失警告逐文件标注「对应分析维度可能不可信」（替代一刀切）；--dup 的 title+author 联合键防同名误报；采集质量四步「发现问题当场修复不留给分析」；选题四步「能爆的原因先当假设，待拆文验证」（苏格拉底化+验证闭环）；PRD Ⅳ.20.1 与实现逐条一致零过时。
- **反哺**：Ⅳ.20.1 经对照无失真，无需修订；3 候选攒批待裁决（触发面收缩并入整体议题）。

- **scan 反哺 build 衔接修复（2026-08-26，旗舰审核通过附 P2/P3 观察）**：选题决策「能爆的原因」待拆文验证假设在「扫榜→跳过拆文→开书」场景无提示（build 侧此前只有扫榜日期提示）——workflow-build:183 消费段补提示「假设未拆文验证，可选先 /moshu-analyze 拆一本验证，或直接开书（不拦开书）」（与日期提示同构；P2 审核调「坐实」→「验证」——analyze 侧「只拆一本不算坐实」口径对齐）；doc-budget 调高（26700/30600，先压缩后调高序，余量 59/57 健康）。**P3 观察记入 build 走查待办**：workflow-build 预算 26500→26600→26700 三连调趋势——build 走查维度 9/10 应正式评估冷热下沉（B27 审计 P1 的冷热分离候选仍在 backlog）。

## moshu-analyze（2026-08-26 · 首查，flash 侧 12 维）

- **执行迹线**：触发（约 9 枚）→ Stage 1 确认拆解对象（统一入口无快速/深度分叉）→ Stage 2 管道 2-1 章节边界（chapter_boundary.py 落 _progress.md 唯一切片真值）→ 2-2 黄金三章→停靠（快速预览+两问一次）→ 2-3 逐章摘要（chapter-extractor 并行 5-8/批+检查点+check_chapter_summary 质量门+merge 拼接）→ 2-4 剧情聚合（节奏.md/情绪模块.md 权威）→ 2-5 设定+角色（4a/4b/4c）→ 2-6 汇总报告→2-7 技法总结（学习材料）。
- **12 维结论**：①触发约 9 枚（2 命令+6 中文模糊+文本路径）——模糊词留 description，「触发面逐技能收缩」整体议题第 3 实例（setup/scan 已收缩）②薄壳+workflow 形态（与 setup 同型：SKILL.md 64 行+analyze-workflow 300 行流程权威+专题 references）③依赖链：SKILL→analyze-workflow→{pipeline-ops, material-decomposition, output-templates, deconstruction-notes, technique-summary-sop}；脚本链 chapter_boundary→check_chapter_summary→merge→agent ④产消：原文→边界表→摘要→汇总→剧情（权威）→设定/角色→报告→技法；下游 write 消费节奏/情绪模块（missing_primary_contract）、build 对标、import 保留⑤确定性边界：解析/质检/拼接脚本化（禁手写），提取/聚合 AI+agent，停靠/冲突作者⑥降级完备：agent 未部署→串行、部分失败容忍（completed_with_errors）、大书子代理降维→串行、恢复 schema_version:2+断点、停靠跳过情形⑦哲学符合（文件即真相=边界表唯一真值防物理分章漂移；确定性优先=禁手写解析脚本）⑧标准符合（退出码三分类、A1/A2 路径预检、AC2 dry-run 非零、枚举单一权威=脚本为权威防跨文档漂移、test-merge-summaries 在 CI）⑨性能：analyze-workflow 300 行热路径 doc-budget 登记；大书子代理并行主线程不读原始语料；检查点间隔按窗口科学依据（1M→50 章/200K→30 章）⑩可优化：触发面收缩（整体议题）⑪问题清单见下⑫交互：Stage 1 问句写死+停靠两问一次（AskUserQuestion 一次两个）+检查点停靠三选项（继续/重新选间隔/暂停）——交互收敛设计优秀。
- **问题清单（候选 1 项）**：触发面形态（9 枚含 6 中文模糊词，未收缩——整体议题实例，与 scan 同处理路径）。
- **亮点（大量）**：边界表唯一切片真值（禁物理分章——第二套切片漂移风险+914 章重复存储的双重教训）；合成阶段事实保真（硬事实可溯源+缺失写「原文未明确」+跨指代不串——强模型漂移最大来源）；质量三比值显式声明「AI 自检口径非机检门限」（口径防误用）；检查点机制（防无限拆解+窗口科学依据+间隔不持久化防假定沿用）；停靠两问一次（交互收敛）；大书子代理降维聚合（主线程只处理 ≤8K tokens 聚合，不触碰原始语料）；选题决策回填（只回填待拆文验证的+invalid_topic_decision_contract 报告+重复不覆盖）。
- **反哺**：Ⅳ.20.2 与实现逐条一致零过时；触发面收缩并入整体议题攒批。
- **v14 触发词收缩闭环（2026-08-26，旗舰审核通过）**：analyze 触发词 9→2 枚（/moshu-analyze、「网文拆书」）+ 长篇修饰清理 9 文件（SKILL/deconstruction-notes/README 双语/路由表/意图行/import 顺带）+ 指纹 --re-register-fingerprint 重登记。旗舰确认：「拆书」为既有正文高频术语+PRD Ⅳ.20.2 节标题本名（非新造词）；「拆书=用户向/拆文=功能泛称」为 PRD 自身分工；文本路径入口双重保留；import 顺带改动合理；指纹三重验证（预览只读/工作区/模拟提交树=2945e5e4）。「触发面逐技能收缩」整体议题：setup/scan/analyze 三技能已收缩，余 style/write/build/review/import/deslop/cdp/moshu 待后续逐技能裁决。

- **v15 审计修复批（2026-08-26，依据 docs/治理/审计-moshu-analyze-v1.md 旗舰审计报告，随批入库）**：**A 组 N1-N3**：①material-decomposition「跨会话恢复」节（退化拷贝，照做绕过 schema 门槛）→ 一行指针到 pipeline-ops「恢复机制操作步骤」（schema 校验/paused 特判/概要核对三保护不重复）；②SKILL.md 加载指引「Stage 2-5」→「Stage 2-3~2-6 按节加载（语料读取/阈值门禁/分块策略多点位引用）」；③doc-budget 登记 analyze-workflow 新建「长篇拆解」组 budget=15500（实测 14684 非空白，余量 816；管道会话触发即全量加载，对齐 workflow-build 先例；此前 C1-C8 增量处于无守卫状态）。**B 组五小项**：C-b 散落兜底 output-templates 七步说明→六步指针+落盘骨架+定位声明（定位声明为模板侧独有，保留）；C-c 分块策略总起句列举（2-1/2-2/2-4/2-5/2-6）对齐正文实际覆盖→「2-4~2-6（2-1/2-2 原文量小直接整体处理）」；C-d 三处双份指针化（拆文报告清单→output-templates「Stage 2-6 汇总报告」、抽象对抗路由→output-templates「爽点分析」、confidence 档位→technique-summary-sop「Stage 2-7-4」权威声明）；C-e 桥段词表路径 references/deconstruction-notes.md→同目录相对路径；C-g 阶段 1 加脚本化定位注记。**C 组 P-A 销案（存疑 11）**：check_chapter_summary.py 实测为**单遍 Python 正则实现**——check_file 以 read_text 一次读入全文，P_LINE/WHITE_DESC/TONE_VAL/TAG_LINE 四个正则遍历计数，无任何 subprocess/grep 子进程调用（`grep -n subprocess` 零命中）；Windows 千章场景无「数千次子进程」开销，无需改造，销案不改。**偏差记录（两处，明示）**：①C-d② 抽象对抗指针目标——批次写 material-decomposition，grep 实证该内容（完整路由字段）只在 output-templates.md:170（material-decomposition 无抽象对抗节），指针指向实际权威，防断链；②批次称「走查记录 v12」——v12 已占用（发版 v2.3.7），按现有编号续为 v15。**验收**：doc-budget 新条目绿（check-doc-budget 通过）、三回归绿（test-chapter-summary 10/test-chapter-boundary 10/test-merge-summaries 5）、static-check 11/11 Warn 0（指针化零断链）、reference-closure ok（115 references）、shared-files 71 组 0 失配、防断链 grep 抽查五个指针目标节全部命中。**提交：b5dde94**（旗舰核验通过；偏差①经旗舰勘误入审计报告 C-d② 与自我推翻第 3 条）。

- **v16 收敛批（2026-08-26，C-a 四份收敛 + C-f 触发面固定，作者两项裁决：权威位=agent 模板、触发词=/moshu-analyze+「进行拆书」）**：**C-a P 行格式四份收敛**——①agent 模板 moshu-chapter-extractor.md 为权威位，**内容零改动**（check-agents-version-sync 绿=35 一致+指纹一致，零改动证明）；②output-templates Stage 2-3 段骨架化（删与 agent 模板逐字重复的说明文本/白描铁律全文/P7 龙血针样例约 70 行，保留可填骨架+粒度说明，原位加双权威声明：格式权威=agent 模板、枚举权威=check_chapter_summary.py）；③analyze-workflow spawn prompt 压缩版加「格式快照注记」（**结构性必需消不掉**：spawn 时 prompt 必须内嵌格式约束以不依赖项目已部署的 agent 版本，老项目不重跑 setup 也能拿到约束——注记声明漂移以权威为准）；④material-decomposition 密度公式不动（**语境不同**：方法论语境 150-200 字/个密度计算 vs agent 模板硬约束语境 10-40 上下限，非逐字复制，不收敛）。**C-f 触发面固定**——SKILL.md description 重写：触发方式固定两枚 `/moshu-analyze`+「进行拆书」（作者裁决替换「网文拆书」触发词）；操作细节（管道概览）移入正文 Stage 2 节；「提供小说文本文件路径同样进入管道」句保留——**这是触发模态能力非触发词**（作者知悉注记），保留在 description；同步面：moshu 路由表意图词追加「进行拆书」（原模糊词保留，两层分工先例）、README.md 触发列改『/moshu-analyze 『进行拆书』』、marketplace keywords 无旧触发词不改（check-claude-adapter 验证双向一致）、README_EN 无中文触发列（既有格式惯例）不改、PRD Ⅳ.20.2 无触发词不改。**判因记录**：C-f 后 SKILL.md 2423/2422 超 1 字（description 细节移正文净增）——按 doc-budget 纪律先压缩管道概览措辞（阶段明细以 analyze-workflow 管道表为准），2391 余量 31 恢复绿。**验收**：static-check 11/11 Warn 0、reference-closure ok（115）、claude-adapter OK（路由表 11 行双向一致）、story-numbers ok（11）、agents-version-sync 绿（35+指纹 2945e5e4）、doc-budget 通过、grep 抽查（权威声明在位/「龙血针」output-templates 零残留+agent 模板 2 处仍唯一/「进行拆书」三同步面命中）。**守卫化 backlog 销案注记**：『路由表语义守卫』经核实已由 check-claude-adapter:91 既有断言承担（v4 触发收缩时落地），backlog 该项销案。**提交：c458fbc**（旗舰审查通过）。

- **v17 审计终态收尾（2026-08-26，作者裁决）**：**A-2 销案**——方法论/运维拆分经评估否决：pipeline-ops 职责是进度文件运维，分块策略是内容处理方法论，搬迁属职责错位；方法论复用无第二消费者（YAGNI）；10+ 引用点搬迁风险 vs 零现实收益。终态=销案。**S 瘦身终态结算**——S-1 已由 C-a 完成（-54 行）；S-2 大头随 N1 删除，剩余「冷节」定性为功能性内容（>500 章分块/小体量特例）保留；S-3 定位注记本批落（deconstruction-notes.md 文件头 B6 式定位声明：经典作品拆解/影视拉片/抽象拆书法/多线叙事/素材转化等节无硬消费点，保留不删）。**analyze 审计 18 项发现全部终态：修 12 / 销案 3（P-A/A-2/backlog 路由守卫）/ 定位保留 3，零挂账**（口径：修 12=N1-3+C-a/b/c/e/f/g 六项+C-d①②③按子项计 3；S-1 并入 C-a 不单计；保留 3=S-2 功能性冷节+S-3 知识库+C-a 时判定的密度公式；销案 3=P-A/A-2/backlog 路由守卫）。

## moshu-style（2026-08-26 · B49 文风画像与仿写校验批，flash 侧）

- **v18 B49 三案一批**（依据旗舰设计锁定 + 作者批准；规格 docs/治理/规格/批B49）：走查预习四短板 → 方案 A 确定性画像 / B 场景分型锚点 / C 仿写校验。
- **D1-D5 落地**：零依赖脚本（style_profile.py 纯 stdlib+正则，内置虚词表 ~70 词 mo-shu 自定口径）；兼容铁律（「平均句长」「文风可用：是」逐字保留——双向实测：新三层模板 → check-prose-candidates 锚点解析 PASS 无降级；字段彻底改名 → style_baseline_unparsed 降级，字面锚点消费链实证）；三层分工（计算归脚本/AI 语义与阈值判级/品味归作者）；write 侧零改动（锚点分场景靠标注引导取用）；产出模板 SKILL → SOP 下沉（SKILL 净减 1205 字，预算 4000 余量充足）。
- **先红后绿记录**：真脚本 bug 2（quantiles 单数据点崩溃 → n<2 保护；。」分句引号残留粘连 → 分句剥离引号）；测试 fixture 计数修正 3（段落字数含标点、标点密度分母为实际字数、虚词测试字符数）。
- **方案 C 落地**：仿写校验步（200 字仿写 → profile → compare → 阈值判级：句长中位相对差>30% 或任一标点密度差>50% → 降 low，mo-shu 自定标注；依据 arXiv 2509.14543 分布锚可靠性）——confidence 从声明变度量。
- **验收**：test-style-profile 13/13 绿；static-check 11/11；reference-closure ok；doc-budget 绿；audit-guards 51 守卫 51 在 CI（新行两列已填）；shared-files 71 组。
- **backlog**：check-prose-candidates 分布带升级留候选（消费端跟进）；URL 抓取源 future 注记保留。
- **提交：f1fe7e3**（旗舰审查通过，10 文件一次入库）。
- **v18 备注（旗舰审查通过后补记）**：①**正面发现**——compare 的人类可读表 + sample_warning 判级联动提示为超出规格的加分设计（`--compare` 输出表格化相对差、任一画像带 sample_warning 时 stderr 提示「判级应降 low」），记录在案；②**对话叙述比字段名结论**——与 check-prose-candidates 可选对话锚点（「对话行占比」）不同名不冲突已行为级双向实证（正向：新模板无降级；反向：字段彻底改名触发 style_baseline_unparsed），消费端跟进（分布带升级）仍留候选。
