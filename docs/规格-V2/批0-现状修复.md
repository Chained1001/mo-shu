# 规格 · 批 0：现状修复

- 版本：v1.0（2026-08-20）
- **前置依赖**：无（首批）。
- 依据：《执行总纲V2》§五批 0；`docs/研究-v3/04-mo-shu-现状盘点.md` 缺陷 #1/#2/#3/#4/#6/#9（全部实测）。

## 1. 目标

一句话：修正 6 处已核实的文档数字/版本/卫生不一致，零逻辑改动，跑通"规格→施工→守卫绿→提交"节奏。

## 2. 现状事实（本次实测）

| # | 事实 | 证据 |
|---|---|---|
| F1 | `scripts/README.md:21` 写"Claude marketplace 与 **9 个 skill** 的一一映射"，实际 skills/ 下 10 个目录 | `ls skills`；static-check Total: 10 |
| F2 | `docs/architecture.md:86` 分层图节点写"Skills[**9 个 Skill 入口**]" | 同上 |
| F3 | 版本三轨漂移：`CHANGELOG.md:5` 顶条目 `## v1.1.2（2026-08-18，未发布）`；`skills/moshu/VERSION` 内容 `1.1.1`；`README.md:19` "> **最近更新（v1.1.1）**…" | 三文件实测（批 0 验收 grep A 当前命中 F1/F2 两处） |
| F4 | `README.md:5` 首段"覆盖扫榜、拆文、写作、去AI味的全流程"，未提审查/导入/文风 | README.md:5 |
| F5 | check-doc-budget 输出 **4 条**松弛提示（实测 2026-08-20）：moshu-review/SKILL.md（低 1101 字，建议 budget→2900）、moshu-import/SKILL.md（低 991，→2600）、moshu-deslop/SKILL.md（低 1229，→3300）、moshu-analyze/SKILL.md（低 1227，→2300） | `bash scripts/check-doc-budget.sh` |
| F6 | ~~`.gitignore` 无 `__pycache__` 条目~~ **勘误（2026-08-20，据施工复核 P1）**：`.gitignore:13-14` 已含 `__pycache__/` 与 `*.pyc`（基线即有）；pyc 文件存在且 git 未跟踪属实（`find skills -name '*.pyc'`；`git ls-files \| grep -c pycache` → 0）——**本项无需任何改动** | 施工复核实测 |
| F7 | `scripts/check-shared-files.sh:13-22` IGNORE_NAMES 中 `AGENTS.md.tmpl`、`hooks.json`、`genre-writing-techniques.md` 三项自述为 no-op（对应文件已不存在） | 源码注释 |

## 3. 文件级改动清单

| 文件 | 改什么 | 注意点 |
|---|---|---|
| `scripts/README.md` | :21 "9 个 skill" → "全部 skill" | 用非数字表述，避免再过期（批 2 守卫兜底） |
| `docs/architecture.md` | 分层图 "9 个 Skill 入口" → "全部 Skill 入口" | 只改这一个词，不动图结构 |
| `skills/moshu/VERSION` | `1.1.1` → `1.1.2` | 对齐 CHANGELOG 已合入变更，即发布 v1.1.2 |
| `README.md` | :7 "最近更新（v1.1.1）" → "（v1.1.2）"；:5 首段补"审查、导入、文风"能力词 | 首段一句内补齐，不扩段 |
| `README_EN.md`（P3 增补，2026-08-20） | :21 "Latest update (v1.1.1)" → "(v1.1.2)" | 与中文版同步，同类漂移一并修 |
| `CHANGELOG.md` | 顶条目去掉"未发布"标注 | 历史条目一字不动 |
| `scripts/doc-budget.json` | 按 F5 **4 条**提示收紧：直接采纳脚本建议值（review→2900、import→2600、deslop→3300、analyze→2300），`_comment` 追加"批0 按松弛提示收紧"理由 | 只动这 4 条；以当次脚本输出为准 |
| `scripts/check-shared-files.sh` | IGNORE_NAMES 删除 F7 三个 no-op 项；**并同步删除 :22/:24-25 两段专述这三项的注释行**（P4 增补，防无主残留；其余注释行保留） | 其余白名单项不动；若删后 check-shared-files 报新 mismatch 立即停手报告 |

## 4. 新文件设计

无新文件。

## 5. 验收命令（零外部依赖，退出码判定）

```bash
# A. skill 计数残留清零（期望：无输出）
grep -rn "9 个" scripts/README.md docs/architecture.md README.md README_EN.md CONTRIBUTING.md
# B. 版本一致（期望：四处均含 1.1.2——CHANGELOG/README/README_EN/VERSION）
grep -n "1.1.2" CHANGELOG.md README.md README_EN.md skills/moshu/VERSION
# C. CHANGELOG 无"未发布"残留于顶条目（期望：无输出）
head -5 CHANGELOG.md | grep "未发布"
# D. 守卫全绿
bash scripts/static-check.sh
bash scripts/check-doc-budget.sh
bash scripts/check-shared-files.sh
python3 scripts/skill-numbering.py check
```

## 6. 守卫与 CI

不新增守卫；全部既有守卫必须保持绿（CI 清单见 CONTRIBUTING.md「CI 检查」）。

## 7. 回滚点

单提交，`git revert` 即回滚；无数据/契约影响。

## 8. 禁止事项

1. 禁止改任何 skill 的 SKILL.md/references/scripts 正文逻辑。
2. 禁止动 skill frontmatter 各自的 version（它们由各自功能变更驱动，本批无 skill 逻辑变更）。
3. 禁止修改 CHANGELOG 历史条目。
4. 禁止删除/修改 `docs/归档/`（旧规划封存区：实施总纲、规格/、研究-v2/ 等 6 项）。
5. 禁止删除工作区 .pyc 文件或 .tmp 之外的任何清理动作。

## 9. 提交规范

```
fix(docs): 批0 现状修复——skill 计数 2 处改全量口径、版本四轨对齐发布 v1.1.2（含 README_EN）、README 首段补能力词、doc-budget 收紧 4 条、白名单 no-op 清理及注释同步（.gitignore 勘误：基线已含条目，无改动）
```
