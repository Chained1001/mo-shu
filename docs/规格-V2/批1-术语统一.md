# 规格 · 批 1：术语统一

- 版本：v1.0（2026-08-20）
- **前置依赖**：批 0 已合入。
- 依据：《执行总纲V2》§三术语表；`docs/研究-v3/04-mo-shu-现状盘点.md` 缺陷 #8。

## 1. 目标

一句话：全仓机制命名收敛到总纲 §三术语表，消灭双名称机制。

## 2. 现状事实（本次实测）

| # | 事实 | 证据 |
|---|---|---|
| F1 | 同一物三种称呼："续写状态卡 / `追踪/上下文.md` / 当前语义检查点"在 `skills/moshu-write/references/workflow-daily.md:44-46` 三段内并用 | sed 实测 |
| F2 | "主对标 / 对标书 / 对标"混用指唯一登记对标 | workflow-daily.md:9 |
| F3 | `scripts/behavior-contracts.json` 用 must_contain 锁定部分文档措辞（9 条约束），改措辞可能触发守卫 | 盘点 §3.5 |
| F4 | 术语表唯一权威=《执行总纲V2》§三（含"禁用的别称"列） | docs/执行总纲V2.md |

## 3. 文件级改动清单

| 文件 | 改什么 | 注意点 |
|---|---|---|
| `skills/moshu-write/references/workflow-daily.md` | 首次出现处保留定义句"续写状态卡（`追踪/上下文.md`，当前语义检查点）"，其余场合一律"续写状态卡"；指唯一登记对标处一律"主对标" | 不改目录名/文件名（用户可见路径不动） |
| 其余出现点 | `grep -rn "当前语义检查点\|检查点卡" skills/ scripts/ README.md docs/architecture.md` 逐个评估：属机制指代的改"续写状态卡"；属定义句的保留 1 处 | docs/研究-v3/、docs/规格-V2/、docs/执行总纲V2.md 自身不改（研究档案引用原文措辞） |
| `skills/moshu-write/references/tracking-transaction.md` | 核对其中机制称呼与术语表对齐 | 该文件是 schema 契约文档，只改叙述不改字段名/JSON 键 |
| `scripts/behavior-contracts.json` | 若锁定了被改措辞：同一提交内更新契约 must_contain 串 | 先跑 check-behavior-contracts 确认哪些条目受影响 |

## 4. 新文件设计

无新文件。术语表已存在于总纲 §三，不复制第二份（防双源漂移）。

## 5. 验收命令

```bash
# A. 旧称呼残留（期望：仅剩各定义句 1 处，其余零命中；人工核对输出）
grep -rn "检查点卡" skills/ scripts/ README.md docs/architecture.md
grep -rn "当前语义检查点" skills/ scripts/ README.md docs/architecture.md
# B. 守卫全绿
bash scripts/static-check.sh
bash scripts/check-behavior-contracts.sh
bash scripts/check-doc-budget.sh
bash scripts/check-shared-files.sh
python3 scripts/skill-numbering.py check
```

## 6. 守卫与 CI

不新增守卫；check-behavior-contracts 必须绿（契约与文档同提交变更）。

## 7. 回滚点

单提交 revert；纯文本改动，无契约风险（behavior-contracts 同步变更）。

## 8. 禁止事项

1. 禁止改用户可见的目录/文件名（追踪/上下文.md 等路径保持）。
2. 禁止改 JSON 字段名、脚本标识符、frontmatter 键（只统一**叙述称呼**）。
3. 禁止为"顺带优化"重写句子——只做称呼替换。
4. 禁止动 docs/研究-v3/、docs/执行总纲V2.md、docs/规格-V2/。
5. 禁止使 doc-budget 超支（等量替换原则）。

## 9. 提交规范

```
docs(terms): 批1 术语统一——续写状态卡/主对标按总纲 §三术语表全仓对齐，behavior-contracts 同步
```
