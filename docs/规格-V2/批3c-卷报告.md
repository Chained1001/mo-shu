# 规格 · 批 3c：追踪 schema v5——卷报告子命令

- 版本：v1.0（2026-08-20）
- **前置依赖**：批 3b 已合入。
- 依据：《执行总纲V2》§五批 3；04 盘点空白 #4。

## 1. 目标

一句话：新增 `volume-report` 子命令，卷末伏笔清账/悬置/信息差未兑现由脚本确定性出表，卷复盘 AI 只做判读。

## 2. 现状事实（本次实测）

| # | 事实 | 证据 |
|---|---|---|
| F1 | state 无卷元数据（顶层键见批 3a F2）→ 卷界由调用方传起止章，脚本不猜卷 | sed 实测 |
| F2 | 命令仅 init/commit/check（:1102-1110） | 同上 |
| F3 | 派生视图强校验集（render_views :1085-1101）会要求视图每次 commit 后一致——卷报告是**按需查询产物**，不进该校验集 | 同上 |
| F4 | 卷复盘流程 `skills/moshu-write/references/volume-review.md` 四步全 AI；产物 `大纲/卷复盘_第X卷.md` | 文件实测 |

## 3. 文件级改动清单

| 文件 | 改什么 | 注意点 |
|---|---|---|
| `skills/moshu-write/scripts/tracking_commit.py` | 新子命令 `volume-report --project P --from-chapter A --to-chapter B [--json-out PATH]`：只读 state，输出①伏笔四态清账（按 updated_chapter 落 [A,B] 过滤，计数+id 清单）②悬置超阈值清单（复用 3b）③信息差未兑现清单（status=="登记"）④分层摘要行；写 `追踪/卷报告_第A-B章.md`（+可选 JSON） | **不进派生视图强校验集**（F3 边界：按需产物非每章派生） |
| `skills/moshu-write/references/volume-review.md` | 四步的第一步改为"跑 volume-report 拿表，AI 只做判读"；产物引用 `追踪/卷报告_第A-B章.md` | 冷路径定位不变 |
| `scripts/test-tracking-commit.py` | 增 2 用例：①fixture 卷范围内容断言（计数/清单/MD 非空）；②同 state 重跑输出逐字节一致（确定性重放） | |
| 副本 | sync-shared-assets sync | |

## 4. 新文件设计

无独立新文件（产物 `追踪/卷报告_第A-B章.md` 由子命令生成，非仓库文件）。

## 5. 验收命令

```bash
python3 scripts/test-tracking-commit.py
bash scripts/check-shared-files.sh
bash scripts/check-current-skill-contracts.sh
bash scripts/static-check.sh && bash scripts/check-doc-budget.sh
```

## 6. 守卫与 CI

既有守卫；无 yml 变更。

## 7. 回滚点

独立提交 revert；按需产物，删除命令即回滚。

## 8. 禁止事项

1. 禁止解析卷纲/猜测卷界（显式起止章）。
2. 禁止把卷报告纳入 check 的派生视图强校验集。
3. 禁止在卷报告中修改 state（只读）。

## 9. 提交规范

```
feat(tracking): 批3c 卷报告——volume-report 子命令（伏笔清账/悬置/信息差未兑现），卷复盘改脚本出表 AI 判读
```
