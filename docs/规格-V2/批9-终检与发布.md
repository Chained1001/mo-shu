# 规格 · 批 9：终检与发布收尾

- 版本：v1.0（2026-08-20）
- **前置依赖**：批 0-8 全部合入。
- 依据：《执行总纲V2》§五批 9；陷阱清单 #11（悬空引用）；04 盘点缺陷 #2 收口。

## 1. 目标

一句话：全仓一致性终检 + 版本三轨终对齐 + v1.2.0/v1.3.0 CHANGELOG 汇总，V2 收口。

## 2. 现状事实（本次实测）

| # | 事实 | 证据 |
|---|---|---|
| F1 | 版本轨道：skills/moshu/VERSION、README"最近更新"、CHANGELOG 三处需终对齐 | 盘点缺陷 #2（批 0 已建立对齐习惯） |
| F2 | CI 强制清单以 `.github/workflows/cross-platform.yml` 为准；新守卫已三处同步（批 2/4/5/6/7/8） | scripts/README.md:5 |
| F3 | hook 语义红线（总纲附录 B 第 6 条）：PreToolUse 只 ask 不 deny——现有 `skills/moshu-setup/references/templates/hooks/` 的 guard-*.sh 语义需核对 | 总纲附录 B（存疑项转本批核验） |
| F4 | doc-budget 历史上有超支救火记录 | 盘点 §1.6 |

## 3. 文件级改动清单（= 终检核对表，逐项执行并记录）

| # | 检查/改动 | 命令或判据 |
|---|---|---|
| C1 | 悬空引用清零 | `bash scripts/static-check.sh`（md 链接）+ 手工核对 scripts/README.md、CONTRIBUTING、各 SKILL.md 中提及的脚本文件全部存在（`ls` 逐个验证） |
| C2 | 叙述数字 | `bash scripts/check-story-numbers.sh` 绿 |
| C3 | 全守卫复跑 | CI 清单全量（CONTRIBUTING.md「CI 检查」节）+ 本轮新增（总纲 §七 7 类、共 11 个新脚本文件：check-story-numbers.py/.sh、test-story-numbers.py、test-next-step.py、test-prose-candidates.js、test-review-tickets.py、check-agent-template-rules.py/.sh、test-agent-template-rules.py、check-eval-scenarios.sh、test-writing-pipeline.sh）逐条跑绿 |
| C4 | 版本三轨终对齐（v1.3.0） | VERSION、README"最近更新"、CHANGELOG 顶条目三者一致；CHANGELOG 增 v1.2.0/v1.3.0 汇总条目（按批 3-8 实际内容撰写，不夸大） |
| C5 | agents_version 链核对 | UPGRADING.md 含 27→28（批 6）、28→29（批 7）两段；check-agents-version-sync 绿 |
| C6 | schema 口径三方一致 | tracking_commit.py（INPUT=2/TRACKING=5）、tracking-transaction.md 示例、scripts/current-contract.json（progress_schema_version=3） |
| C7 | hook 语义核对（只记录不改码） | grep `skills/moshu-setup/references/templates/hooks/` 各脚本退出码语义：若存在以非零阻断 Write/Edit 的路径，**只写进汇报不改代码**（是否整改由作者决定——红线项） |
| C8 | doc-budget 全量 | `bash scripts/check-doc-budget.sh` 绿；本轮新增文档（规格-V2 除外——非热路径）不超支 |
| C9 | 归档目录零改动 | `git status` 确认 `docs/归档/`（实施总纲.md、规格/、研究-v2/、reference-review.md、moshu-style-plan.md、scan-analyze-v2-plan.md 共 6 项）与归档时（2026-08-20）完全一致，施工全程零触碰 |
| C10 | 批次依赖双向核对 | 总纲 §4.2 表与 12 份规格（批 0-2、3a/3b/3c、4-9）头部"前置依赖"互相一致（人工核对后在本规格提交说明记录"已核对"） |
| C11 | 术语终检 | 批 1 验收命令复跑零残留 |

## 4. 新文件设计

无新文件（纯核对与文档收口；C4 的 CHANGELOG 条目是唯一内容性写作）。

## 5. 验收命令

```bash
# 终检一把梭（期望全部退出 0）
bash scripts/static-check.sh
python3 scripts/skill-numbering.py check
bash scripts/check-current-skill-contracts.sh
bash scripts/check-doc-budget.sh
bash scripts/check-shared-files.sh
bash scripts/check-behavior-contracts.sh
bash scripts/check-agents-version-sync.sh
bash scripts/check-story-numbers.sh
bash scripts/check-agent-template-rules.sh
bash scripts/check-eval-scenarios.sh
bash scripts/test-writing-pipeline.sh
python3 scripts/test-tracking-commit.py
python3 scripts/test-next-step.py    # 批 4 被跳过时剔除本行（总纲 §4.3）
python3 scripts/test-review-tickets.py
# 版本一致（期望三处同版本号）
grep -n "1\.3\.0" CHANGELOG.md README.md skills/moshu/VERSION
```

## 6. 守卫与 CI

无新增；本批就是守卫的全量复演。

## 7. 回滚点

单提交（CHANGELOG+版本号+可能的文档级修正）；C7 若发现 hook 越界，整改另立批次（不混入本批）。

## 8. 禁止事项

1. 禁止为过检改断言/改数字（发现不一致先判因）。
2. C7 发现 hook 越界时禁止顺手改 hooks 代码（记录待作者决策）。
3. 禁止在 CHANGELOG 写未实际发生的事（逐批对照提交记录）。
4. 禁止动旧规划目录（C9 是核对不是清理）。

## 9. 提交规范

```
chore(release): 批9 终检与发布——全守卫复演绿、版本三轨对齐 v1.3.0、CHANGELOG 汇总 v1.2.0/v1.3.0、悬空引用/术语/依赖链终检清零
```
