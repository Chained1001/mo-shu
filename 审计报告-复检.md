# oh-story-claudecode 逐技能审计报告（独立复检 · v0.7.6）

- **审计对象**：`oh-story-claudecode` v0.7.6（agents_version 25）全部 13 个 skill + 仓库元层基础设施
- **审计方式**：14 个并行审计代理（13 个 skill + 1 个元层）逐文件通读（SKILL.md + references + scripts 全量读取；JS 跑 `node --check`、Python 跑 `py_compile`、共享资产做 SHA-256 比对）+ 主线程对全部 medium 及以上发现二次复核证据
- **审计日期**：2026-08（快照：`oh-story-claudecode-main`，非 git 仓库）

## 〇、总体结论

**14 个审计单元全部通过仓库静态守卫基线，未发现 high（必然触发的功能级错误）。** 问题集中在文档/实现漂移、失效引用、脚本错误处理三类。评级分布：**C × 6、B × 8**（含元层）。共检出 **medium × 8、low × 36、info 若干**，其中 4 条 medium 为既有 `审计报告.md` 之外的**新发现**。

| 评级 | 含义 | Skill |
|---|---|---|
| **C** | 有 medium 未修 | `story`、`story-long-analyze`、`story-long-scan`、`story-short-write`、`story-short-analyze`、`story-import` |
| **B** | 只有 low/info | `story-setup`、`story-long-write`、`story-short-scan`、`story-deslop`、`story-review`、`story-cover`、`browser-cdp`、元层 |

## 一、中危发现（medium · 8 条，均人工复核属实）

> ✅ **修复状态：8 条 medium 已全部修复**（本次会话）。修复位置、文件与验证见 [§五 修复进度追踪](#五修复进度追踪)。

### 1. story — Dashboard 打开 GBK 文件后保存会静默损坏编码
- **位置**：`skills/story/scripts/dashboard-server.mjs:597`
- **问题**：`readWorkspaceFile` 以 `utf8` 读取，GBK 等编码被有损替换为 U+FFFD；后续基于有损内容的 sha256 版本校验照常通过，`replaceFileAtomically` 再以 utf8 写回——打开 GBK 的 `.md/.txt` 后直接保存会**永久重编码原文件且全程无警告**。网文作者 GBK 旧稿是现实触发场景。
- **建议**：读取时检测 BOM / U+FFFD 或字节异常，命中返回 4xx 提示先转码；保存路径拒绝含 U+FFFD 的内容。

### 2. story-long-analyze — 失效的「章节正则」引用
- **位置**：`skills/story-long-analyze/SKILL.md:130`（另见 :322）
- **问题**：Stage 0 声称「用 style-profile-generator.md Step 4 的章节正则（含千/两）grep 章节行号」，但该文件 Step 4 现为「原文采样」，且全包已无此正则定义（#333 后改为「只读 `_progress.md` 边界表、不重新搜标题」）。执行 agent 按引用查找会扑空。
- **建议**：把规范章节正则内联到 Stage 0，或删除该引用并注明「边界表落盘后即唯一切片真值」。

### 3. story-long-scan — 「文件头质量状态」硬要求缺实现
- **位置**：`skills/story-long-scan/SKILL.md:151-159`（对照 `scripts/qidian-rank-scraper.js`）
- **问题**：SKILL.md 强制每个采集文件头部含 `数据质量 / 有效条目 / 问题摘要`，但**起点脚本三者全缺**（grep 实测 0 匹配）、番茄/晋江仅部分实现且枚举值不同，仅七猫完整落地；`[数据稀疏]` 标注也只有七猫输出。
- **建议**：给起点 `renderMarkdown` 补三个必填头部（并对 <15 条输出 `[数据稀疏]`），统一各平台枚举值，或把检查表改为逐平台说明。

### 4. story-short-write — 细纲照搬检测器 fail-open
- **位置**：`skills/story-short-write/scripts/check-outline-copy.js:237-240`
- **问题**：顶层 `try { process.exit(main()) } catch { process.exit(0) }` 把包括内部 bug 在内的任意异常吞成「干净」，是本 skill 唯一的 fail-open 门禁，与仓库 #265「质量门禁不掩盖破坏性失败」原则相悖。且该脚本在短篇流程**完全未接线**（Phase 3/4 从不运行）。
- **建议**：catch 分支改 stderr 报错 + 退出码 2；并在 Phase 3/4 复扫接入该脚本，或移除该休眠资产。

### 5. story-short-analyze — 悬空 + 越界引用 writing-craft.md
- **位置**：`skills/story-short-analyze/references/anti-ai-writing.md:293`
- **问题**：「见 writing-craft.md『视角姿态：深度限知』」——该文件在本 skill 内不存在（仅存在于 `story-long-write` / `story-setup`），同时违反引用完整性与跨 skill 自包含红线。此问题同源影响 `story-deslop`、`story-review` 两份共享副本（5 份副本中 3 份悬空）。
- **建议**：改写为自包含表述，或把 writing-craft.md 纳入共享同步；需同步全部 5 份字节副本。

### 6. story-import — 正文格式规范悬空 + 越界引用（新发现）
- **位置**：`skills/story-import/references/format-and-structure.md:122,133-134`
- **问题**：引用 `genre-writing-formulas.md`（122 行）与 `writing-craft.md`（133、134 行），两文件在本 skill 内均不存在（仅存在于其他 skill）。这是**既有审计报告未覆盖的新 medium**——static-check 的路径扫描不捕获裸文件名，CI 因此漏检。
- **建议**：把所引用的片段内联进本 skill，或删除指引句，保持自包含。

### 7. 元层 — README 长篇 demo 目录树与实际不符（新发现）
- **位置**：`README.md:240` vs `demo/长篇/…/追踪/`
- **问题**：README 画的 `追踪/` 含 `逐章记录/`，实测 demo 的 `追踪/` 只有 5 项，**`逐章记录/` 目录不存在**（Test-Path=False）。
- **建议**：补 demo 的 `逐章记录/`，或修正 README 目录树。

### 8. 元层 — README「100+ 份方法论」口径失真
- **位置**：`README.md:262` vs `agent-references/`
- **问题**：部署包 `agent-references/` 实为 58 个 `.md`（26 顶层 + 32 题材卡），不足「100+」。若指全仓 references（259 个 .md）则数字成立但措辞与 agent 实际加载的部署包不符。
- **建议**：校准为 58，或改写为「全仓 references 200+ 份」并注明部署包子集。

## 二、低危发现汇总（low · 36 条）

| Skill | 位置 | 问题 | 建议 |
|---|---|---|---|
| story | SKILL.md:64 | 文档称「修改时间」防外部更新，实际是 sha256 版本 | 改文案对齐实现 |
| story | dashboard-server.mjs:643 | 写串行化锁 key 未 realpath，symlink 可绕过队列 | 锁 key 取 realpath |
| story-setup | merge-*.py:99/108 | 未捕获 UnicodeDecodeError，GBK 配置裸 traceback | except 扩为含 UnicodeError |
| story-long-write | workflow-daily.md:40 | 把 444 行文件写成「1500+ 行」 | 改实际值 |
| story-long-write | check-outline-copy.js:237 | 不可读/异常静默退 0（fail-open） | 报错并 exit 2 |
| story-long-analyze | pipeline-ops.md:60 | 跨 skill 引用 outline-structure-theory.md | 只提 skill 名不带路径 |
| story-long-analyze | README.md:174 | 「深度模式」旧术语（现为单管道） | 改「完整拆解管道」 |
| story-long-analyze | README.md:194 | 盘龙 demo 称「五个剧情单元」实为 4 个 | 改「四个单元」 |
| story-long-analyze | style-profile-generator.md:73 | 原文备份无 GBK 转码指引 | 加转码/提示 |
| story-long-scan | SKILL.md:91 | 番茄「评分」字段与 reference 矛盾 | 删「评分」 |
| story-long-scan | ciweimao-*.js:125 | `--type` 不校验，报泛化错误 | 加白名单校验 |
| story-long-scan | test-scan-runtime.js | fanqie 解码/截断函数无单测 | 补纯函数断言 |
| story-short-write | output-contract.md:114 | 跨 skill 引用 material-decomposition.md | 标注仅 analyze 有效 |
| story-short-write | README.md:324-333 | 短篇结构图与 SKILL.md 不符 | 按 SKILL.md 重画 |
| story-short-write | short-format.md:36-63 | 模板空行与「禁空行」硬约束矛盾 | 加注「仅示意」 |
| story-short-write | SKILL.md:333 | check-outline-copy.js 未接线 | 接入或移除 |
| story-short-write | check-outline-copy.js:141 | `--outline` 缺值静默回退 | 报用法错误 |
| story-short-analyze | banned-words.md 等 7 处 | 引用本 skill 无副本的 check-ai-patterns.js | 补副本或改通用表述 |
| story-short-analyze | README.md:222 | 「Phase 7 门控」旧术语 | 改「验收门控」 |
| story-short-analyze | genre-writing-formulas.md:3 | 「21 大公式」实为 19 个（缺公式五/十一） | 补齐或改 19 |
| story-short-scan | dz-browse-scraper.js:228 | 多频道部分失败仍 exit 0 | 返回 structured outcome |
| story-short-scan | dz/heiyan 报告头 | 抓取时间用 UTC、文件名用本地日 | 统一本地时间 |
| story-short-scan | dz-browse-scraper.js:112 | `--channel/--port` 无校验 | 加白名单/整数校验 |
| story-short-scan | test-scan-runtime.js | dz 主流程无 e2e（替身已备） | 补 runScraper e2e |
| story-deslop | anti-ai-writing.md:293 | 悬空 writing-craft.md（同 medium #5） | 改写/共享 |
| story-deslop | check-outline-copy.js:237/156 | fail-open + 不可读静默退 0 | 报错 exit 2 |
| story-deslop | SKILL.md:419-425 | check-outline-copy.js 未接线 | 接线或移除 |
| story-review | anti-ai-writing.md:293 | 悬空 writing-craft.md（同 #5） | 同 #5 |
| story-review | SKILL.md:295 | 「8 种 AI 模式」vs reference「10 种」 | 统一计数 |
| story-cover | SKILL.md:193/255 | `file` 未守卫/未声明 bins | `command -v file && …` |
| story-cover | SKILL.md:187/249 | `base64 --decode` 为 GNU 语法（BSD 需 -D） | 探测式兼容 |
| story-import | SKILL.md:58 | init「整体移入存档」vs 固定清单归档不符 | 整目录搬移或收紧措辞 |
| browser-cdp | SKILL.md:121 | Windows 清理用已弃用 wmic | 改 PowerShell 形式 |
| meta | marketplace.json | story-setup/短篇拆文版本与 SKILL.md 脱节 | 统一版本同步 |
| meta | .agents/skills | 退化为 9 字节文本文件（非 symlink） | README 已注明，可加静态检测 |

## 三、info / 观察级（要点）

- **story-setup**：session-start 读 `story/VERSION` 属跨 skill 耦合；`narrative-writer` frontmatter 声明 `skills:[story-deslop]`；character-designer「项目内搜索」措辞与禁搜规则冲突；SKILL.md 把 hook matcher 简写为 `Write/Edit`（实际含 MultiEdit）。
- **story-import**：`format-and-structure.md` 与 long-write 副本字节一致但未登记进 shared-assets.json。
- **story-cover**：`curl -f` 吞掉非 200 错误详情；中文输出目录未注明 UTF-8 环境；CI 无内联 bash 语法守卫。
- **browser-cdp**：`--profile` 分隔符未校验；`CHROME_INSTALLED=no`、`-y` 别名未文档化；`--remote-allow-origins=*` 无安全提示。
- **story-short-scan**：提示文案列「番茄短篇」但本 skill 无番茄采集资产。
- **meta**：`story` 与 `browser-cdp` 的 frontmatter 缺 version 字段（其余 11 个有）；demo 角色状态缺 `李林.md`（设定有 6 角色、快照只有 5）。

## 四、与既有 `审计报告.md` 的差异

本次为独立复检，结论与既有报告**高度一致**（同为 6 个 C 级、无 high），但有几处值得注意：

1. **story-import 的中危变了**：既有报告的中危是「分批导入『重跑完整导入』会被 init 覆盖保护拒绝」（`character-state-reverse.md:73` vs `tracking_commit.py`），本次检出的是「format-and-structure.md 悬空引用」。**两条都成立、都应处理**。
2. **story-long-scan、story-short-write 由 B 升为 C**：既有报告把「文件头质量字段缺失」「check-outline-copy fail-open」降为 low/info，本次按「文档硬要求未实现」「质量门禁 fail-open」判为 medium，更严格。
3. **新增 4 条 medium**：story-import 悬空引用（#6）、README demo 树缺失（#7）、100+ 份口径（#8），以及 story-short-write 的 fail-open（#4）在既有报告中仅列为 info/low。

## 五、修复进度追踪

### ✅ 已修复（本次会话）

**P0 · 中危（5 条 + 1 条连带）**
1. ✅ #1 `story` Dashboard GBK 有损写回 — `dashboard-server.mjs` 读/写两侧加 U+FFFD 检测（读 415 / 写 400）
2. ✅ #2 `story-long-analyze` 失效章节正则引用 — `SKILL.md:130` 内联正则 `第[一二三四五六七八九十百千零两0-9]+章`、`:322` 删旧描述
3. ✅ #5 `anti-ai-writing.md` 悬空 `writing-craft.md` — 5 副本（short-analyze/deslop/review/long-write/setup）改自包含表述
4. ✅ #6 `story-import` `format-and-structure.md` 悬空引用 — 3 副本（含 setup/agent-references）删跨 skill 文件名引用
5. ✅ `story-import` 分批导入「重跑完整导入」矛盾 — `character-state-reverse.md` 改 append 事务

**P1 · 代码**
6. ✅ #3 `story-long-scan` 文件头质量字段 — qidian/fanqie/jjwxc 统一输出 `数据质量/有效条目/问题摘要`；同步 `test-scan-runtime.js` 断言 + `scan-output-format.md` 模板 + SKILL.md
7. ✅ #4 `check-outline-copy.js` fail-open — 源 + 3 副本：不可读文件/异常/`--outline` 缺值均 exit 2；接入 `story-short-write` Phase 3/4 与 `story-deslop` Phase 4

**P1 · 文档/元层**
8. ✅ README 导入 demo 树删除不存在的 `逐章记录/`
9. ✅ README「100+ 份」→「部署包 agent-references 58 份 / 全仓 200+ 份」
10. ✅ README 短篇结构图（项目根 `拆文库/` + `对标/`）对齐 SKILL.md
11. ✅ README「深度模式」「五个剧情单元」「2故事线」「Phase 7」等口径
12. ✅ `marketplace.json` 版本同步（story-setup 1.0.0→1.2.7、story-short-analyze 1.0.0→3.0.0）
13. ✅ `story`/`browser-cdp` frontmatter 补齐 `version: 1.0.0`

**P1 · low 清理**
14. ✅ 代码健壮性 — story 写锁 key 改 realpath；story-setup merge 脚本捕获 UnicodeDecodeError；ciweimao `--type` 校验；dz `--channel` 校验 + 部分失败退出码 + 本地时间戳（cdp-utils `localTimestamp`，dz/heiyan UTC→本地）；browser-cdp `--profile` 分隔符校验
15. ✅ 文档措辞 — story「修改时间」→sha256；workflow-daily「1500+」→约 440 行；pipeline-ops 跨 skill 软引用；story-long-analyze 原文 GBK 转码；story-long-scan 番茄「评分」；output-contract「拆文管道适用」标注；short-format 空行说明；「21→19」公式（genre-writing-formulas ×4 + SKILL.md + README）；output-templates 断锚；deslop Gate 映射；review「8→10 种」；cover file/base64；browser-cdp wmic→PowerShell

**P1 · 测试/收尾**
16. ✅ 测试覆盖 — `test-scan-runtime.js` 新增 fanqie 纯函数单测（cleanDesc 模板剥离/100 字截断、fmtReads/fmtWords 万格式化、fmtStatus 映射）+ dz e2e（探活→提取→质量门→写盘 + `--channel all` 部分失败 exit 2）；补 ciweimao `--type` 参数校验用例
17. ✅ story-import 归档措辞 — 「按原样整体移入」→「按受管清单移入」（SKILL.md ×2 + tracking-transaction.md ×3 + workflow-daily + UPGRADING，7 处全链一致）
18. ✅ browser-cdp 文档 — `-y` 别名、`CHROME_INSTALLED=no` 探测输出补注
19. ✅ demo 完整性 — 补 `追踪/角色状态/李林.md`（设定 6 角色与状态快照 6 份一一对应）

**验证**：`static-check.py` 13/13 PASS（0 fail 0 warn）；所有改动脚本 `node --check` 通过；共享副本（anti-ai-writing ×5、format-and-structure ×3、check-outline-copy ×3、cdp-utils ×2、output-contract ×2、genre-writing-formulas ×3、tracking-transaction ×3）SHA-256 字节一致；fanqie 纯函数断言已实跑通过。

### ✅ 剩余待处理（复检后第二轮已处理 3/4，仅剩 1 项）

| Skill | 位置 | 问题 | 状态 |
|---|---|---|---|
| story-short-analyze | banned-words.md 等 7 处 | check-ai-patterns.js 无副本引用（low） | ✅ 已修：banned-words ×6 + anti-ai-writing ×5 共 7 处/副本改「确定性 AI 句式检测」通用表述，规则名保留，全副本字节一致 |
| story-cover | SKILL.md | `curl -f` 吞 4xx/5xx 错误详情（info） | ⏳ 保留：需真实 API 验证，避免破坏 fail-fast 语义 |
| story-short-write | shared-assets.json | banned-words/output-contract 未登记（meta） | ✅ 已修：登记 2 组 → 10 组/24 份，sync check 与 check-shared-files 全过 |
| meta | .agents/skills | 退化为文本文件，建议加静态检测（info） | ✅ 核实为已实现：check-codex-adapter.sh:112-114 已有 symlink 检测并接入 CI，本机实跑验证拦截生效 |

---

*报告生成：oh-story-claudecode v0.7.6 快照 · 14 个审计单元（13 skill + 元层）· 8 条 medium 已人工复核证据并全部修复 · 修复进度与剩余项见 §五*
