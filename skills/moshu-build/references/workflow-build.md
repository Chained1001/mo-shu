# workflow-build.md：书驱构建（雪花法六步·开书构建与开新卷）

> **来源声明**：本文件源自 moshu-write 侧开书工作流的构建段拆分（V2.1 批 B1a 改写，非逐字拷贝；B1b 起旧文件删除、细纲职责由 write 侧 outline-workflow 承接）；**细纲部分（细纲模板/安全七检/分批建纲/中途补纲/细纲后设定补全）归 write 侧**。构建环只做设定/大纲/卷纲的计划层工作。
>
> **v3.0 重构（批 B10）**：本文件由 v2.0（Phase 1 选题 / Phase 2 核心设定 / Phase 3 卷级大纲）重构为**六步书驱构建**——基底流程直接来自雪花法 10 步（压缩为 6 步网文适配版），网文适配层来自五步法/八节点/多卷连载，对标参照层来自既有对标机制；每步含 **故事面 / 人物面 / 对标面** 三子节（对标面标注"有主对标时"），每步开头挂**方法论书指引**（先读后操作）。本步的"定稿"=构建完成的标记（v1.0），与 write 侧"锁定"（有正文章节即保护锁）共存不冲突——定稿在前（构建完成），锁定在后（正文产生），见「与现有机制的关系」。
>
> **对标书路径查找**（本书引用规则）：优先 `{项目}/对标/{书名}/`，不存在则回退 `拆文库/{书名}/`；排除同名或来源指向当前正文的 `对标/{当前书}/`。
>
> **消费侧只读**：本工作流读 `追踪/`（last_committed_chapter、连贯性风险、信息差冲突）作输入，不写任何追踪文件——唯一例外是开书末尾的 `tracking_commit.py init`（事实层第 0 章基线，见「定稿与 artifact 创建」）。

---

## 构建台账与进入规则（三种入口统一）

构建环三种入口（开书构建 / 修订 / 开新卷）的第一步均先读 `设定/构建台账.md`（一册一份，作为构建期对谈的锚——构建态与方法论单真源）：

- **存在** → 读取它，向作者汇报「当前焦点 + 待定 N 项 + 方法论档位」，确认后再继续后续对谈。
- **不存在且为开书** → 在「方法论声明步」后创建台账（空台账，先填方法论声明）。
- **不存在且为修订/开新卷** → 提示先创建空台账（`设定/构建台账.md`）再继续。

### 台账模板（书项目产物 `设定/构建台账.md`）

```markdown
# 构建台账（{书名}）

## 方法论声明（开书定，可修订——修订走变更日志）
- 构建顺序：剧情先行（默认）｜设定先行
- 规划深度：大纲=全局骨架（默认）｜前半细化｜仅阶段一；卷纲=首卷细（默认）｜前两卷细；设定=核心+终局相关（默认）｜全量前置
- 注：无论何档，终局与升级台阶全书规划（契约体系根基）

## 当前焦点
{正在打磨什么 + 所属域（题材/设定/大纲/卷纲）+ 六步位置（步 N·名称）——螺旋跳转的锚点}

## 待定项（未收敛决策）
| # | 决策点 | 所属域 | 候选 | 当前倾向 | 轮次 |
|---|---|---|---|---|---|
| 1 | {} | {} | {A / B / C} | {偏 X} | {N} |

## 已收敛 → 变更日志
- {决策一行摘要} → 详见 大纲/变更日志.md（第 N 行/对应日期）
```

### 更新规则

- **决策收敛必写**：待定项 → 已收敛，并在 `大纲/变更日志.md` 追加一行（变更同步，不重复变更日志职能）。
- **会话收尾必结**：收尾时定格当前焦点与待定项（台账是构建期对谈的快照锚，不是机读状态机）；自动步被打断时同样先落台账再停。
- **浮现随手记**：步 3 填卡时涌现的灵感随手记入台账或卷纲附注（见「浮现记录机制」），步 4 正式化后再收敛。

### 方法论副本索引（经 outline-methods 路由表按需加载）

- [outline-structure-theory.md](outline-structure-theory.md)
- [outline-rhythm.md](outline-rhythm.md)
- [outline-conflict.md](outline-conflict.md)
- [character-design-methods.md](character-design-methods.md)
- [character-basics.md](character-basics.md)
- [plot-frameworks.md](plot-frameworks.md)
- [plot-core-methods.md](plot-core-methods.md)
- [opening-design.md](opening-design.md)
- [genre-writing-formulas.md](genre-writing-formulas.md)
- [naming-cards.md](naming-cards.md)
- [genre-readers.md](genre-readers.md)
- [plot-special-topics.md](plot-special-topics.md)
- [plot-emotion-system.md](plot-emotion-system.md)
- [outline-workflow.md](outline-workflow.md)

---

## 六步构建流程（书驱构建 v3.0）

### 流程总览

#### 三层架构

```
┌─────────────────────────────────────────────────────┐
│  基底流程（雪花法 10 步 → 压缩为 6 步网文适配版）        │
│  一句话 → 一段话 → 角色 → 扩展 → 单元 → 场景/整合      │
├─────────────────────────────────────────────────────┤
│  网文适配层（五步法 + 八节点 + 多卷连载）                │
│  高潮优先 → 单元剧 → 八线 → 章位分配 → 对标回流         │
├─────────────────────────────────────────────────────┤
│  对标参照层（有主对标时激活，无对标时跳过）               │
│  概要参照 → 节奏校准 → 单元参照 → 密度校准 → 坐标回流   │
└─────────────────────────────────────────────────────┘
```

#### 六步流程定义（雪花法基底 + 网文适配）

| 步 | 名称 | 密度 | 故事面（所有书都做） | 人物面（所有书都做） | 对标面（有主对标时） | 方法论书指引 | 卡片按需加载 |
|---|---|---|---|---|---|---|---|
| 0 | 信息采集 | — | 选题方向+完美评述+方法论声明 | — | **对标发现**：扫拆文库→推荐→登记主对标 | 《骨架》第 2 章（完美评述） | — |
| 1 | 骨架步 | 句子 | 每卷一句话+高潮定死+终局底牌+升级台阶+全书四阶段 | 每个核心角色**四件套**：目标/抱负/价值观冲突/顿悟 | 读对标**概要.md**：参考每卷功能定位 | 雪花法第 3-5 章 + 五步法第 1 步 | naming-cards（按需） |
| 2 | 人物步 | 段落 | 每卷一段话（三幕+灾难性事件）+阶段边界微调 | 每个角色一页：背景故事+**弧线六阶段**+大纲微调 | 读对标**剧情/节奏.md** 一级结构：灾难事件级别校准 | 雪花法第 6-8 章 + 大师课第 9 章 | 人物主题卡（8 张） |
| 3 | 单元步 | 单元 | 逐卷**剧情单元卡**：单元 ID/章节范围/承诺/风险/对标剧情参照+章功能分配 | 每卷角色弧线段（本卷起终点+变化节点） | 读对标**剧情/{标题}.md**：参考同功能单元结构分布+情节点密度 | 五步法第 2 步 + 雪花法第 7/9-11 章 + 题材公式 | beat-cards + genre-writing-formulas |
| 4 | 整合步 | 精修 | 单元卡精修：伏笔四态+反转类型+跨卷呼应+**线索矩阵**（八线分线→合并交叉） | **动机链核验**+删主角测试+删题材核心测试 | 读对标**剧情/情绪模块.md**：伏笔/反转密度校准 | 雪花法第 12-14 章 + 《小说写作》第 6 章 + 大师课 LOCK | hooks-suspense（按需） |
| 5 | 打磨步 | 定稿 | 对标回流（坐标表+锚点）+字数/章位配比+八节点占比+LOCK 四查+**卷纲定稿** | **因果断裂修复验证**（步 4 发现的问题已解决） | **对标回流**：读节奏.md→换素材排坐标表→选段以对标单元为单位→写锚点 | 大师课第 1-2 章（LOCK）+ 既有对标回流机制 | — |

#### 扩展规则（雪花法一手原则）

**每一步都消费上一步的产出再扩展**——不是重写：

- 步 1 的每卷一句话 → 步 2 扩为每卷一段话（五句=三幕+两次灾难）
- 步 2 的每段 → 步 3 拆为单元卡（每段拆成 2-5 个单元）
- 步 3 的单元卡 → 步 4 精修（加伏笔/反转/因果标注）
- **结构保持不变，密度加深**

#### 双路设计（同流程+可选参照——不是两套流程）

| 步 | 无对标路径 | 有对标路径 |
|---|---|---|
| 0 | 选题+完美评述+方法论声明 | 同左 + **对标发现** |
| 1 | 雪花法→八节点定结构→每卷一句话 | 同左 + **读概要参考卷功能** |
| 2 | 雪花法→弧线六阶段→角色一页 | 同左 + **读节奏校准灾难级别** |
| 3 | 五步法→题材公式→单元卡 | 同左 + **读对标单元参考功能/密度** |
| 4 | 伏笔/线索/因果+可证伪 | 同左 + **读情绪模块校准密度** |
| 5 | 八节点占比+LOCK+定稿 | **对标回流**+LOCK+定稿 |

#### 停靠与档位

| 停 | 位置 | 问什么 |
|---|---|---|
| 🔴 1 | 步 1 后 | "骨架+角色四件套如下…确认方向？" |
| 🔴 2 | 步 3 后 | "单元卡+角色弧线段如下…确认内容？" |
| 🔴 3 | 步 5 后 | "整合+对标+LOCK 完成…定稿并转 write？" |
| ⚪ | 步 2、4 后 | 自动 |

| 档位 | 步骤序列 | 停 | 适用 |
|---|---|---|---|
| 轻量 | 0 → [1+2] → [3+4+5] | 2 | 短篇感/试水 |
| 标准 | 0 → 1 → [2+3] → [4+5] | 3 | 大部分网文 |
| 完整 | 0 → 1 → 2 → 3 → 4 → 5 | 3 | 重结构文 |

> 档位在步 0 由作者选定（方法论声明同步记入台账）；不确定先标准档，写完第一卷可升级。**不强制完整档**。

---

### 步 0·信息采集（五问式开场）

> **方法论书指引**（先读后操作）：**《小说的骨架》第 2 章（完美评述）**——理想评论家口吻的成品书评，作目标态锚；操作已萃取进 [idea-seed.md](idea-seed.md)「完美评述」节，直接用，不另读原文。

**故事面**（所有书都做）——五问开场，按回答落地：

1. **先查选题决策**：如果项目根存在 `选题决策.md`（moshu-scan Phase 5 产出），读取它——取排在最前（可行性最高）的推荐选题作为开书起点，向用户确认；看 `扫榜日期`：距今较久则提示"市场数据可能过期，建议复扫"。缺失时先自动找一遍（从项目根及其上一级目录起、向下最多 3 层按文件名搜，跳过隐藏目录，按 mtime 由新到旧取最新 2 份，AskUserQuestion 列候选）——见下方「五问式开场」问 1。
2. **模糊灵感**（画面/脑洞/感觉，说不清题材卖点）：先做**灵感种子收敛**（[idea-seed.md](idea-seed.md)，产物落 `{项目根}/灵感种子.md`），Logline 作后续方向与设定的一致性尺度。
3. **规模与结局**：问 2/问 3（体量/卷数；完结或无限流终局口径），不确定按 200 万字/5 卷规划。
4. **完美评述**：LLM 生成理想书评草稿 → 作者改定 → 落 `设定/`，作北极星尺子。
5. **核心设定落盘**：按下方「核心设定表」模板产出并创建 `设定/题材定位.md`（题材核心梗三分法+对标分析，参考 [genre-core-mechanics.md](genre-core-mechanics.md)「核心梗解析」；含**终局底牌与升级台阶**登记）、`设定/关系.md`（角色关系映射，参考 [character-relations.md](character-relations.md)「四种关系类型」）、`设定/题材正文提示卡.md`（题材边界/核心逻辑/读者期待/核心爽点，参考 genre-prose-cards 索引与 [style-genre-modules.md](style-genre-modules.md)）。
6. **方法论声明步（步 0 收尾）**：用 AskUserQuestion 确认三项并写入台账「方法论声明」节——①构建顺序：剧情先行（默认）｜设定先行；②三域规划深度档位（见下）；③构建精细度档位（轻量/标准/完整）。

> **默认档位表**：

| 域 | 默认 | 可选档 | 不可裁剪项 |
|---|---|---|---|
| 大纲 | 全局骨架 | 前半细化 / 仅阶段一 | 终局与升级台阶（全书） |
| 卷纲 | 首卷细 | 前两卷细 | 首卷（开书必须） |
| 设定 | 核心+终局相关 | 全量前置 | 主角/金手指/核心冲突 |

**对标面**（有主对标时做）——**对标发现**（先于反应式加载，主动扫拆文库推荐，不被动等用户开口）：

1. `ls 拆文库/`（数据源）与项目 `对标/`（引用视图）列已有书目；先按当前项目目录名、`.active-book` 和 `设定/题材定位.md` 中的本书信息识别当前作品，**排除同名或来源指向当前正文的 `拆文库/{当前书}/`**。moshu-import 为重建工程生成的本书拆文分析不是对标候选，也不得复制回 `对标/`。排除后都为空 → 跳到第 4 条。
2. 逐本读题材（读 `拆文库/{书}/` 的 `概要.md` 头部或 `拆文报告.md`「基本信息」的「题材」），与本书题材/方向比对，标 同题材 / 弱相关 / 不相关。
3. 有同题材或弱相关候选 → 用 AskUserQuestion 推荐（列候选书 +「都不用」+「另拆一本」），选定主对标 1 本（走情绪/节奏/正文；文风独立由文风库提供）。选定后写入 `设定/题材定位.md`「对标登记」的 `主对标书`，并按「首次引用对标书」规则把选中书从 `拆文库/{书}/` 复制到 `对标/{书}/`（复制清单不含 文风.md——文风独立由 `文风库/文风.md` 提供）。登记后若文风库尚无该书文风，询问用户是否现在用 /moshu-style 学习（可跳过，写第一章时缺失会再次提醒）。
4. 无候选 → 无对标继续（骨架按八节点自排，见步 1）；用户想对标可先 `/moshu-analyze` 拆一本。

**对标上下文加载**（用户提到对标书或工作目录已有 `对标/` 时）：按上方第 1 条口径识别当前作品并排除自对标；随后检查 `剧情/情绪模块.md` 与 `剧情/节奏.md`——任一主产物缺失时必须停止，设置 `missing_primary_contract: true` 并给出 `repair_action`（重跑 `/moshu-analyze` Stage 3+ 或重新 `/moshu-import`），不得以 `拆文报告.md`、章节摘要或故事线代替；两个主产物都存在时先读情绪模块的读者需求/情绪引擎与可复现模块，再读节奏.md 的关键信息推进、情绪触动点和爆发节奏；拆文报告只作人类可读概览。角色/普通剧情单元/设定子目录存在时按需召回（消费方是 write，本工作流只记录引用关系）。

> 开篇的"第一个冲突/机遇"定义可参考**激发事件四问**（见 [opening-design.md](opening-design.md) :87 起——什么是/什么不是/在何处/四特质），在步 1 定全书四阶段时使用。

#### 五问式开场（步 0 交互）

```
问 1（方向）：
"你想写什么类型的故事？有没有特别想对标的书？你的优势是什么（脑洞/文笔/节奏感/生活经验）？"
→ 有方向 → 继续问 2
→ 没方向 → "要不要先扫一下最近的榜单？或者说说你想让读者有什么感觉？"
→ 有对标意向 → 执行对标发现（扫拆文库推荐）→ 作者选定主对标

问 2（规模）：
"这本书大概什么体量？预计总字数/总章数/分几卷？"
→ 知道 → 记录
→ 不确定 → "先按 200 万字/5 卷规划，写完第一卷可以调"

问 3（结局）：
"有明确的结局吗？"
→ 完结 → 记录"终局底牌待步 1 设计"
→ 无限流 → 记录"无固定终局，升级台阶驱动"

问 4（精度）：
"构建精细度选哪档？"
  🏃 快速（2 停，适合试水/脑洞文）
  🚶 标准（3 停，大部分网文）
  🧗 完整（3 停，多线/强反转/群像）
→ 不确定 → "先标准档，写完第一卷觉得需要可以升级"

问 5（完美评述）：
"在你确定写什么之前，我做一份'完美评述'——想象一个完全读懂你这本书的评论家，
为它写一篇溢美的书评。你说说你希望这本书给读者什么感觉？"
→ [作者描述感受]
→ AI 生成理想书评草稿 → 作者改定 → 落盘设定/
→ "好，这是你的北极星——后面每一步都拿它当尺子。"
```

#### 核心设定表（步 0 落盘模板，步 1-5 的原料）

```
## 核心设定表

### 基本信息
- 书名：{暂定名}
- 题材/类型：{主类型 + 副类型}
- 目标平台：{起点/番茄/晋江/其他}
- 预计字数：{X} 万字
- 目标读者：{画像}
- 读者契约：{本书承诺的核心阅读快感；参 [reader-contract-and-progression.md](reader-contract-and-progression.md)}
- 主角高光/代理权：{主角不可替代的判断、选择或贡献}
- 核心期待债：{开篇必须偿还的承诺}

### 一句话梗概
{主角 + 目标 + 阻碍 + 反转，一句话概括全书}

### 主角设定
- 姓名：{} ｜ 年龄：{} ｜ 核心特质：{2-3 个关键词}
- 金手指/核心能力：{} ｜ 弱点/缺陷：{}
- 核心动机：{他为什么要做这件事}

### 世界观骨架（只记录长期有效的世界事实；章节内临时状态/开放线索归 `追踪/`）
- 时代/背景：{} ｜ 核心设定：{} ｜ 力量体系：{} ｜ 社会结构：{}
- 可选：信息生态/货币与经济/规则与禁忌/资源与限制/术语表

### 核心冲突
- 主线矛盾：{} ｜ 终极 Boss/终极阻碍：{}
```

---

### 步 1·骨架步（密度：句子）→ 🔴 停靠 1

> **方法论书指引**（先读后操作，约 5-10 分钟——不是"参考一下"，是照着走）：
> - **雪花法第 3 章（一句话概括）**：一句话 ≤25 词、不泄露结局——每卷一句话的尺度
> - **雪花法第 5 章（灾难的重要性）**：三次灾难=三幕骨架——高潮定死与卷内灾难的出处
> - **大师课第 2 章（结构）**：LOCK 四要素初查（主角/目标/冲突/结尾）
> - **五步法第 1 步**（[outline-methods.md](outline-methods.md)「五步大纲创建法」）：确定高潮剧情——高潮有绝对优先级（最大冲突/最多人物/最强情绪）

**故事面**（所有书都做）——基于步 0 核心设定，先写全书体量与阶段总览，再逐卷一句话：

- **全书体量与阶段总览**（卷纲前置，共同约束）：全书总章节数/目标字数/情绪曲线；四阶段划分（开篇期参考 10%-15% / 发展期 50%-60% / 高潮期 20%-25% / 收尾期 5%-10%），每阶段列核心任务/情绪基调/阶段结束时读者应建立什么；各阶段节奏公式（核心任务/情绪基调/爽点密度/伏笔策略/冲突层级/阶段钩子/禁止提前释放）；关键节点与钩子链（小/中/大高潮章位+阶段钩子链）。比例只作默认参考，按题材/目标字数/对标节奏调整。
- **终局底牌与升级台阶**（无论何档全书规划，期待债/禁止提前释放的约束根基）：定死终局底牌（登记进 `设定/题材定位.md`）与升级台阶（N 个大阶段，每卷跨 M 个）；声明裁剪只作用于中间卷粗细。
- **每卷一句话**：每卷一行「一句话 + 高潮定死」——高潮事件明确到可检验（"第 2 卷高潮=主角当众揭穿家主用邪功续命"）。
- **八节点定结构**（无对标时按八节点自排卷结构；[outline-methods.md](outline-methods.md)「八节点故事结构」：开篇/发展/转折一/转折二/高潮/矛盾结果/转折三/结局，按占比铺卷）。

**人物面**（所有书都做）——**核心角色四件套**（每个核心角色，不展开背景）：

```
👥 核心角色四件套：
  主角·{名}：目标={具体的}｜抱负={抽象的}｜价值观冲突={A} vs {B}（≥2 样"没什么比…更重要"）｜顿悟={结尾领悟}
  反派·{名}：目标={...}｜抱负={...}｜价值观={...}｜顿悟=无（反派不悟）
  导师·{名}：...
```

- 四件套与每卷一句话同步产出；骨架调整时先查四件套是否受影响，受影响则同步更新（级联，见「停靠选项的级联说明」）。

**对标面**（有主对标时做）——读主对标 `对标/{书}/概要.md`，参考**每卷功能定位**（"对标第 2 卷=升级期"→校准我们的卷功能），只取功能位不照搬情节。

**卡片**：起名（书名/卷名）按需加载 [naming-cards.md](naming-cards.md)（冷路径）。

#### 停靠 1（步 1 后，🔴）

```
[AI 完成骨架+角色四件套]

"骨架和人物核心如下：

📖 每卷一句话：
  第一卷：「{一句话}」高潮={事件}    ← 每卷一行
  第二卷：「{一句话}」高潮={事件}
  ...

🎯 终局底牌：{一句话}
📊 升级台阶：{N} 个大阶段，每卷跨 {M} 个
⏱️ 全书四阶段：开篇{A}-{B}章/发展{B}-{C}章/高潮{C}-{D}章/收尾{D}-{E}章

👥 核心角色四件套：
  主角·{名}：目标={具体的}｜抱负={抽象的}｜价值观冲突={A} vs {B}｜顿悟={结尾领悟}
  反派·{名}：目标={...}｜抱负={...}｜价值观={...}｜顿悟=无（反派不悟）
  导师·{名}：...

怎么调？"
→ ✅ 确认方向，进步 2
→ ✏️ 调整某卷 → "哪卷？怎么调？" → 修改后重新展示本屏
→ 🔄 返回步 0 → "好的，回到选题重来"
→ ❓ "某卷不确定" → 记入台账待定项，进步 2（后续可回改）
```

---

### 步 2·人物步（密度：段落）→ ⚪ 自动

> **方法论书指引**（先读后操作）：
> - **雪花法第 6 章（没什么比人物更重要）**：四件套字段清单+操作细节——本步人物一页的骨架来源
> - **大师课第 9 章（角色弧线）**：弧线六阶段——起始点 → 不情愿的门 → 影响内在层次的事件 → 加深影响的扰乱事件 → 转变瞬间（顿悟）→ 后果

**故事面**（所有书都做）——把步 1 的每卷一句话**扩为每卷一段话**（五句=三幕+两次灾难）：每卷一段话含 三幕骨架 + 两次灾难性事件（雪花法：灾难的重要性——灾难事件是卷推进的引擎）；同时做阶段边界微调（四阶段分界章随段落细化微调）。

**人物面**（所有书都做）——**每个角色一页**（每个核心角色）：

- **背景故事**：角色过去的关键经历（用**人物探索三件套**深挖，见 [character-design-methods.md](character-design-methods.md) :340 起——采访挖现在/幕后故事挖过去/轨迹表规划未来；按角色分量决定挖多深）。
- **弧线六阶段**：起始点（出场内在层次）→ 不情愿的门（必须穿过的门）→ 影响（影响内在层次的事件）→ 扰乱（加深影响的扰乱事件）→ 转变（转变瞬间，可经顿悟）→ 后果。
- **关系原型选卡**：角色关系设计时从**人物主题卡** 8 张选卡定关系原型（镜像宿敌/伤口回避/价值自洽反派/导师旧债/守护底线/利益盟友/关系信息差/身份冲突，见 [character-design-methods.md](character-design-methods.md)「人物主题卡」）——选卡定关系原型，不逐张套用。
- **大纲微调**：人物一页与每卷一段话互相校验，冲突处以人物一致性优先回改骨架（记台账变更日志一行）。

**对标面**（有主对标时做）——读主对标 `对标/{书}/剧情/节奏.md` 一级结构：灾难事件级别校准（"对标在 1/4 处灾难=退婚"→我们的灾难量级匹配吗）——只校准量级不照搬事件。

**卡片**：人物主题卡（8 张，[character-design-methods.md](character-design-methods.md)）。

#### 自动交互（步 2 后台运行，不打扰作者）

```
[AI 在后台完成角色深化]

不打扰作者，但实时更新台账：
  "✅ 主角弧线六阶段完成"
  "✅ 反派背景故事完成"
  "🔄 正在整合角色关系网..."

→ 自动进步 3

[例外] 发现需要作者决策的问题（如"主角的师父应该是敌是友？"）：
  → 记入台账待定项（不打断流程）
  → 下次停靠（步 3 后）一并呈报
```

---

### 步 3·单元步（密度：单元）→ 🔴 停靠 2

> **方法论书指引**（先读后操作）：
> - **五步法第 2 步**（[outline-methods.md](outline-methods.md)「五步大纲创建法」）：单元剧方法——每个单元剧展示金手指不同用法/相邻单元禁止相同金手指逻辑/单元剧之间必须建立因果关系
> - **雪花法第 7 章（一页纸）+ 第 11 章（完成大纲）**：扩展规则——每句→段→页，一页纸的骨架密度
> - **题材公式**：[genre-writing-formulas.md](genre-writing-formulas.md) 按题材选公式（都市系统/玄幻仙侠/年代重生/宫闱宅斗/现代悬疑/架空历史）

**故事面**（所有书都做）——把步 2 的每卷一段话**拆为剧情单元卡**（每段拆成 2-5 个单元）：

- 逐卷产出**剧情单元卡**，字段模板（内联进卷纲代码块，不另建文件）：

```
### 剧情单元 {L卷号-序号}
- 单元ID/位置：{L卷号-序号}（write 细纲按此锚定——细纲_第N章.md 头部必填）
- 章节范围：{A}-{B} 章
- 单元承诺：{本单元兑现的读者快感/期待债}
- 风险等级：{低/中/高}
- 单元节拍/章功能分配：{按 beat-cards 节拍卡 BC-ID 分配各章功能}
- 对标剧情参照：{同功能对标剧情单元，≤3 个}
- 结构分布/情节点索引：{来自拆文剧情单元}
```

- **细纲消费口径**：write 侧 outline-workflow 每章细纲必填「单元ID/位置」，workflow-daily/chapter-core 写前按 卷契约 → 当前剧情单元（单元ID/位置）→ 章节细纲 顺序消费，故单元ID 必须可锚定（缺则 write 补纲时按 `{L卷号-序号}` 口径回填，但构建时先产出避免悬空）。

**人物面**（所有书都做）——每卷**角色弧线段**：本卷起终点（主角从 {A} 变成 {B}）+ 变化节点（弧线六阶段中本卷覆盖哪几段）——与剧情单元卡一一对应，单元调整时同步。

**对标面**（有主对标时做）——读主对标 `对标/{书}/剧情/{标题}.md`（拆文侧剧情单元）：参考**同功能单元的结构分布+情节点密度**——功能位思维，参考不照搬（禁止对标面变成复制）。

**卡片**：beat-cards（章功能 BC-ID）+ genre-writing-formulas（题材公式）。

**浮现记录**（步 3 开始生效）：填卡时**自然浮现**的线索/伏笔/反转灵感**随手记不展开**（见「浮现记录机制」表格，记入台账附注或卷纲附注）。

#### 停靠 2（步 3 后，🔴）

```
[AI 完成单元卡+角色弧线段+浮现记录]

"第一卷的内容规划：

📋 剧情单元（{N} 个）：
  L1-01 {标题}（第 {A}-{B} 章）：{一句话}
  L1-02 {标题}（第 {C}-{D} 章）：{一句话}
  ...

👥 本卷角色弧线：
  主角：从{起点}到{终点}，关键变化={描述}
  {其他角色}...

📝 浮现记录（填卡时涌现的灵感）：
  💡 线索：{描述}
  💡 伏笔：{描述}
  💡 反转：{描述}

📋 待定项（步 2 中发现的问题）：
  ⚠️ {问题}（需要你决定）

怎么处理？"
→ ✅ 确认内容，进步 4
→ ✏️ 调整某单元 → "哪个单元？怎么调？"
→ ✏️ 处理待定项 → 逐条问 "你倾向哪个方案？"（AskUserQuestion）
→ 🔄 返回步 1 改骨架 → "好的，回到骨架层"
```

---

### 步 4·整合步（密度：精修）→ ⚪ 自动

> **方法论书指引**（先读后操作）：
> - **雪花法第 14 章（场景清单）**：每个场景必须有冲突——单元卡精修的最小单位检查
> - **《小说写作》第 6 章（塔和网）**：福斯特因果定义（故事=在根本上有因果关联的诸多事件的集合）+ 冲突三层
> - **大师课（LOCK）**：目标/冲突/结尾在单元层复核

**故事面**（所有书都做）——单元卡精修：

- **伏笔四态**：登记每卷单元卡的伏笔（埋/收配对），标四态（已埋/已收/悬置/弃置）。
- **反转类型**：检查反转覆盖类型（[reversal-toolkit.md](reversal-toolkit.md)「反转类型」），避免同类型连续。
- **跨卷呼应**：标注跨卷呼应的伏笔/线索/情感线。
- **线索矩阵**（五步法第 3 步八条线分线→合并交叉）：八条故事线（地图线/阵营线/人物线/金手指线/世界观线/矛盾线/收集线/感情线，见 [outline-methods.md](outline-methods.md)「第3步：故事线组织与信息点预埋」）逐卷分线登记推进点，标合并/交叉点——产出落进卷纲（write 侧批次定位消费）。

**人物面**（所有书都做）——**动机链核验**：

- "角色的目标驱动了这些事件吗？"——逐单元核验事件由角色目标/选择驱动，非巧合堆叠。
- **删主角测试**：删除主角后情绪兑现是否仍基本不变？是 → 主角缺代理权，先修引擎。
- **删题材核心测试**：删除题材核心的对象/关系/问题后是否退化为通用职业升级或换皮情节？是 → 先修引擎。
- 以上可证伪降级检查承接既有大纲安全审查（[emotional-methods.md](emotional-methods.md) 正向情绪发动机 + 主角代理权/所有权/成长负向风险护栏：因果权+结算权、关键节点四问与期待所有权、核心资产交换、机构/势力边界、换书债和推进线；契约破坏 必须修纲，需补强 必须补交换/铺垫/成本）。

**对标面**（有主对标时做）——读主对标 `对标/{书}/剧情/情绪模块.md`：伏笔/反转密度校准 + 跨卷呼应模式参考——只校准密度不复制模块。

**卡片**：hooks-suspense（跨卷悬念，按需——待评估，见「卡片按需加载清单」）。

**浮现记录正式化**：步 3 随手记的浮现逐条处理（落实到单元卡/转待定/放弃），处理后清表。

#### 自动交互（步 4 后台运行）

```
[AI 在后台完成伏笔/反转/线索/因果]

进度更新：
  "✅ 伏笔登记完成（{N} 条，埋/收配对）"
  "✅ 反转类型检查（{N} 处，覆盖 {M} 种类型）"
  "✅ 线索矩阵：八线 {N} 条交叉点 {M} 个"
  "🔄 因果核验中..."
  "✅ 删主角测试：通过"
  "✅ 删题材核心测试：通过"

→ 自动进步 5

[例外] 因果核验发现断裂：
  → ⚠️ "发现问题：{描述}。已标记，打磨步呈报处理方案。"
  → 不打断，继续整合其他部分
```

---

### 步 5·打磨步（密度：定稿）→ 🔴 停靠 3

> **方法论书指引**（先读后操作）：
> - **大师课第 1-2 章（LOCK）**：LOCK 四查收尾——主角（Lead）有辨识度/目标（Objective）具体/冲突（Confrontation）够强/结尾（Knockout）有冲击
> - **既有对标回流机制**：坐标表+锚点（本文件「对标结构坐标」模板）

**故事面**（所有书都做）——打磨与定稿：

1. **卷级五问**（卷纲定稿前按 [reader-contract-and-progression.md](reader-contract-and-progression.md) 答一遍）：① 本卷交付什么情绪？什么剧情模式能可靠交付？② 本卷核心冲突是什么？③ 卷节奏（起承转合）哪段加速哪段减速？④ 本卷需要新埋设的伏笔有哪些？上一卷待回收的伏笔如何处理？⑤ 章节定位分布是否有高低层次（不是全程高压）、低压+过场是否克制？——**卷级五问在构建环答**；⑥⑦（契约安全/终局储备逐章口径）与细纲批次级全量检查归 write。
2. **字数/章位配比**：开篇/发展/高潮/收尾四阶段配比核对（对照步 1 阶段总览的参考比例与目标字数）。
3. **八节点占比**：八节点各占比例核对（[outline-methods.md](outline-methods.md)「八节点故事结构」节点速查表），偏差标候选呈报。
4. **LOCK 四查**：L=主角有辨识度 / O=目标具体 / C=冲突够强 / K=结尾有冲击。
5. **对标回流**（有主对标时做，核心内容）：读主对标 `对标/{书}/剧情/节奏.md` → 关键点换素材排坐标表 → 选段以主对标剧情单元为单位（按「类型/桥段标签」圈同类）→ 写锚点——见「对标结构坐标」模板。
6. **卷纲定稿**：作者确认后写入定稿标记（卷纲头部加 `定稿：v1.0` + 日期），执行「定稿与 artifact 创建」。

**人物面**（所有书都做）——**因果断裂修复验证**：步 4 标记的断裂问题逐一验证是否已解决（目标→事件→兑现闭环）；未解决的在停靠 3 呈报方案。

**对标面**（有主对标时做）——**对标回流执行**：读节奏.md 一级结构关键点（1/4·中点·3/4）换素材排进「对标结构坐标」；选段以主对标剧情单元为单位，选中的剧情单元写入剧情单元卡「对标剧情参照」。若尚未登记主对标书但 `拆文库/` 有同题材候选，先回步 0「对标发现」登记再排节奏；确无对标书则按八节点占比自排（无对标时对标章位写"—"——八节点自排也有坐标，对照仍可做）。

#### 对标结构坐标（固化模板，回流必填）

卷纲的「### 对标结构坐标」按下表固化填写：锚点取本卷计划事件中的 2-4 个专名/动作词，顿号分隔，命中任一即算落点。

```markdown
### 对标结构坐标
| 点位 | 对标章位 | 本卷计划章位 | 事件锚点（关键词） |
|---|---|---|---|
| 1/4 点 | 对标第{X}章 | 本卷第{Y}章 | {词1、词2} |
| 中点 | 对标第{X}章 | 本卷第{Y}章 | {词1、词2} |
| 3/4 点 | 对标第{X}章 | 本卷第{Y}章 | {词1、词2} |
```

#### 停靠 3（步 5 后，🔴）

```
[AI 完成对标回流+LOCK+配比]

"打磨完成，准备定稿：

📊 质量检查：
  ✅ 伏笔 {N} 条（{X} 埋 {Y} 收，配对完整）
  ✅ 反转 {N} 处（类型覆盖：{列举}）
  ✅ 因果核验通过
  ✅ 动机链无断裂
  ✅ 删主角/删题材核心测试通过

📍 对标坐标（{有对标时显示}）：
  1/4 点=第 {X} 章｜中点=第 {Y} 章｜3/4 点=第 {Z} 章
  锚点 {N} 个已填

🔒 LOCK 四查：
  L=主角有辨识度 ✅  O=目标具体 ✅  C=冲突够强 ✅  K=结尾有冲击 ✅

📏 字数/章位配比：
  开篇 {A}% 发展 {B}% 高潮 {C}% 收尾 {D}%

⚠️ 因果核验发现的问题（如有）：
  {问题} → 建议方案：{方案}
  → 需要你决定怎么处理

定稿并转 /moshu-write？"
→ 🔒 定稿 → 写入定稿标记+执行 tracking init → "卷纲已定稿，可以 /moshu-write 开始写第一章了"
→ 🔧 继续调整 → "哪部分？" → 回到对应步
→ 🔄 返回某步 → "回到哪步？" → 台账更新
```

---

### 自动步的打断与恢复

```
作者在步 2/4 运行中说"等一下"或 "/moshu-build"：
  → AI 读台账："你在步 {N}·{名称}，故事面{状态}，人物面{状态}。
     待定 {N} 项。继续还是调整？"
  → 台账的「当前焦点」实时记录进度（含六步位置）
```

### 停靠选项的级联说明

```
停靠 1 选"调整某卷"：
  → 骨架变了 → AI 检查角色四件套是否受影响 → 受影响则提示同步更新 → 更新后重新展示本屏

停靠 2 选"调整某单元"：
  → 单元变了 → AI 检查浮现记录和角色弧线段是否受影响 → 受影响则同步 → 重新展示

停靠 3 选"继续调整"：
  → "哪部分？" → 指向对应步 → 台账更新 → 从该步重新开始
```

### 开新卷（第 2 卷及后续）

卷末复盘（write 侧流程）产出 `大纲/卷复盘_第X卷.md` 后，本工作流负责下卷规划。**步 0-2 已在开书时完成（全书级），不重做**；开新卷从步 3 起增量执行。

```
"上一卷写完了，开始第 {N} 卷的构建。

📋 上卷复盘要点：
  {卷报告摘要——伏笔回收/悬置/信息差/对标偏差}

🎯 上卷定下的方向：
  {卷复盘_第{N-1}卷.md 中的下卷方向候选}

按这个方向直接进单元步？（或先调整方向）"
→ ✅ 直接进步 3 → 正常流程（停靠 2 → 步 4 → 停靠 3）
→ ✏️ 先调整 → 回到步 1 快速改骨架（只改第 N 卷的部分）
```

1. **读候选**：读 `大纲/卷复盘_第X卷.md` 的「下卷方向候选」（1-3 个，含各候选的主推线/战果线、与终局储备的衔接）；无复盘文件 → 询问作者直接进入常规开卷（或回 `/moshu-write` 续写）。
2. **作者确认**：AskUserQuestion 列候选（+「自定」），作者选定后进入增量流程。
3. **增量构建**：步 3-5 增量执行——新卷引入新角色/势力/设定 → 先补 `设定/` 档案（增量补充、不覆盖既有），再补 `大纲/卷纲_第X卷.md`（只增不改既有卷；新剧情单元卡追加到卷尾，卷级五问与契约检查照常）；纯延续卷 → 直接补新卷卷纲。
4. **边界**：新卷的首批细纲由 write 侧产出（B1b outline-workflow）；本工作流不建细纲。卷纲锁定定义沿用 write 侧口径（某卷一旦已有正文章节，该卷已写区间对应的既有剧情单元卡与对标结构坐标即视为锁定——未经用户确认不改既有内容，但允许在卷尾**追加**新剧情单元卡）。

> 开新卷同样先读 `设定/构建台账.md`（存在时，见「进入规则」）——汇报当前焦点/待定项/方法论档位后再规划。

---

## 卡片按需加载清单

| 卡片 | 加载步 | 用途 | 处置 |
|---|---|---|---|
| beat-cards（BC-001~012） | 步 3·单元步 | 单元卡"章功能分配"字段引用节拍卡 ID | ✅ 纳入 |
| 人物主题卡（8 张） | 步 2·人物步 | 角色关系设计时选卡（镜像宿敌/价值自洽反派等） | ✅ 纳入 |
| genre-writing-formulas | 步 1+3 | 骨架定方向+单元按公式填 | ✅ 纳入 |
| naming-cards | 步 1（按需） | 起名 | ✅ 保留冷路径 |
| hooks-suspense | 步 4（按需） | 跨卷悬念设计 | ⚠️ 待评估 |
| genre-prose-cards | write 侧 | — | ❌ 不进 build |
| hooks-chapter | write 侧 | — | ❌ 不进 build |
| scene-cards | write 侧 | — | ❌ 不进 build |
| craft-cards | write 侧 | — | ❌ 不进 build |

---

## 浮现记录机制

步 3（单元步）开始生效——填单元卡时**自然浮现**的线索/伏笔/反转灵感，**随手记不展开**（记入台账附注或卷纲附注）：

```markdown
## 浮现记录
| 类型 | 内容 | 处理 |
|---|---|---|
| 线索 | 地图线第 3 卷有交叉点 | 待步 4 |
| 伏笔 | F005 灵感：主角的剑有秘密 | 待步 4 |
| 反转 | 第 2 卷末可以做一个身份反转 | 待步 4 |
```

步 4（整合步）**正式化**：逐条处理（落实到单元卡/转待定/放弃），处理后清表。

---

## 定稿与 artifact 创建

作者在停靠 3 确认定稿后：

- **写入定稿标记**：卷纲头部加 `定稿：v1.0（YYYY-MM-DD，构建环）` 行——这是构建完成的标记（v1.0），write 侧据此放行细纲/正文；不构成"锁定"（锁定=已有正文章节，见「与现有机制的关系」）。
- **大纲/大纲.md**：全书卷级鸟瞰（卷名+字数+章数+核心事件+状态变化，一段式汇总）。
- **大纲/卷纲_第X卷.md**：每卷的剧情单元+情绪弧线（含章节定位）+人物弧线+伏笔+反转+线索矩阵+对标结构坐标（参考 [outline-methods.md](outline-methods.md)「大纲三层结构法」 + [emotional-arc-design.md](emotional-arc-design.md)「六种弧线速查」 + [reversal-toolkit.md](reversal-toolkit.md)「反转类型」）。
- **追踪事务初始状态**：按 `references/tracking-transaction.md`（write 侧共享副本）构造第 0 章初始化 JSON，执行 `scripts/tracking_commit.py init`（build 侧共享副本），生成检查点（续写状态卡，固定 7 栏）、空的伏笔/时间线视图与目录；核心角色动态快照可在第一次正文事务中创建（write 侧）。

> 追踪 init 后，**事实层归 write**：build 不再写 `追踪/`，只读（修订工作流的边界判定输入）。

---

## Agent 调用（moshu-architect / moshu-character-designer）

- **moshu-architect（步 0 题材定位）**：轻量题材定位优先由主会话完成；只有涉及复杂世界观、多线结构、强反转工程或用户明确要求时，才调用。已部署时（检查 `.claude/agents/moshu-architect.md` 是否存在）可 spawn `Agent(subagent_type: "moshu-architect", prompt: "项目目录：{dir}\n任务类型：题材定位\n查询参数：{用户选择的方向+对标信息}")`。如 agent 不可用，由主线程直接执行。
- **moshu-architect + moshu-character-designer（步 1-2 骨架与人物）**：结构复杂/反转链多/角色关系密时辅助骨架与角色设定（任务类型：核心设定/角色设定）。spawn prompt 必须原样附带「moshu-architect 契约摘要」。
- **moshu-architect（步 3-5 卷级）**：只有结构复杂、反转链多或主会话方案不稳时，才调用辅助卷级结构（卷级大纲、钩子/反转/情绪弧线设计）。**首批细纲的产出不在本工作流**（归 write，B1b）。

> **moshu-architect 契约摘要（spawn 时必须原样附带）**：部署的 moshu-architect agent 不认识本 skill 的 `references/reader-contract-and-progression.md`，只能靠 spawn prompt 里带的这段摘要对齐 schema。摘要内容（终局储备边界/透支两问/剧情单元/细纲层字段/读者契约与主角代理权/关键节点四问）与 write 侧同源——从 `references/reader-contract-and-progression.md` 摘取（本文件不复制全文，引用即挂载）。

---

## 与现有机制的关系

| 现有机制 | 处置 |
|---|---|
| 五步法 | **不删除，融入**——第 1 步（定高潮）→ 步 1、第 2 步（单元剧）→ 步 3、第 3 步（八条线/信息点预埋）→ 步 3 浮现 + 步 4 线索矩阵 |
| 八节点 | 不删除——步 1 定结构 + 步 5 占比校验 |
| 对标回流 | 不删除——步 5 核心内容（坐标表+锚点） |
| 大纲安全审查 | 拆分——可证伪降级（删主角/删题材核心）→ 步 4、卷级五问 → 步 5 |
| 构建修订工作流 | 不动——定稿后走修订（[revision-workflow.md](revision-workflow.md)） |
| 台账 v1 | B11 升级为 v2（迭代状态表）；本批沿用 v1 |
| **write 的"锁定"定义** | **不冲突声明**：本步"定稿"=构建完成的标记（v1.0）；write 侧 outline-workflow 的"锁定"=有正文章节即视为保护锁——**两者共存，互不替代**：定稿在前（构建完成），锁定在后（正文产生） |
